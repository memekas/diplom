---
phase: 12-court
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/nav.tsx
  - src/components/tournament-status-badge.tsx
autonomous: false
requirements: [UI-01, UI-10]
tags: [css, theme, tokens, nav, badge, responsive, container-queries]

must_haves:
  truths:
    - "Глобальная шапка Nav отрисована в Court (граница --border, CTA Регистрация = .btn .btn-primary, ссылки в токен-цвете), структура навигации/ссылок неизменна"
    - "tournament-status-badge маппит статус → .badge-* (registration→badge-reg, in_progress→badge-prog, finished→badge-fin), наследует форму/тип/uppercase из .badge"
    - "Reg-бейдж получает пульсирующую точку из .badge-reg::before бесплатно (без изменения разметки)"
    - "Базовая оболочка (body + Nav) корректна при сужении до ~375px без непреднамеренного горизонтального скролла"
    - "В nav.tsx и tournament-status-badge.tsx нет захардкоженных hex/Tailwind-цветовых литералов"
  artifacts:
    - path: "src/components/nav.tsx"
      provides: "Nav restyled на Court токены/классы; структура ссылок/сессии неизменна"
      contains: "btn-primary"
    - path: "src/components/tournament-status-badge.tsx"
      provides: "статус → .badge-* class map; type-guard isKnownStatus сохранён"
      contains: "badge badge-reg"
  key_links:
    - from: "src/components/tournament-status-badge.tsx"
      to: "globals.css .badge-reg/-prog/-fin"
      via: "STATUS_CLASSES → ported _base classes"
      pattern: "badge badge-(reg|prog|fin)"
    - from: "src/components/nav.tsx"
      to: "globals.css .btn .btn-primary"
      via: "Регистрация CTA className"
      pattern: "btn btn-primary"
---

<objective>
Перевести глобальную хрому (переиспользуемые фундаментные компоненты) на дизайн-язык Court, потребляя токены и классы из Plan 01: минимальный токен-рестайл `Nav` (без изменения структуры навигации/ссылок) и перевод `tournament-status-badge` на `.badge-*` маппинг из компонентного слоя. Затем верифицировать базовую корректность оболочки на ~375px без непреднамеренного горизонтального скролла.

Покрывает UI-01 (компонентный слой применён на уже отрисованных поверхностях — шапка, бейдж) и верификационную часть UI-10 (≤375px без поломок/непреднамеренного скролла на базовой оболочке).

Purpose: `Nav` и `tournament-status-badge` — глобальные/переиспользуемые компоненты-фундамент; они должны жить в Court с самого начала, чтобы каждая страница фаз 13/14 уже имела корректную шапку и бейджи. Это доказывает, что компонентный слой Plan 01 реально работает на живых поверхностях.

Output: шапка и статус-бейдж в Court; подтверждённая адаптивная корректность оболочки на телефоне.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-court/12-CONTEXT.md
@.planning/phases/12-court/12-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/foundation.md
@.planning/phases/12-court/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Перевести tournament-status-badge на .badge-* маппинг</name>
  <files>src/components/tournament-status-badge.tsx</files>
  <read_first>
    - src/components/tournament-status-badge.tsx (текущий: STATUS_LABELS, STATUS_CLASSES с хардкоженными Tailwind-литералами `bg-green-900/40`/`bg-amber-900/40`/`bg-white/10`, type-guard isKnownStatus, обёртка `inline-block rounded-full px-3 py-1 text-xs font-medium`)
    - .planning/phases/12-court/12-PATTERNS.md (раздел `src/components/tournament-status-badge.tsx`, строки 103-118 — точный целевой class-map, что убрать)
    - .claude/skills/sketch-findings-diplom/references/foundation.md (строки 237-259 — status mapping таблица + live-dot)
    - .planning/phases/12-court/12-01-SUMMARY.md (подтвердить наличие классов .badge/.badge-reg/-prog/-fin из Plan 01)
  </read_first>
  <action>
    Сохранить структуру `Record<TournamentStatus,…>` + type-guard `isKnownStatus` (чистая, оставить). Поменять ТОЛЬКО class-map: `STATUS_CLASSES` → `{ registration: "badge badge-reg", in_progress: "badge badge-prog", finished: "badge badge-fin" }` (маппинг из foundation.md строки 241-245 и компонентного слоя Plan 01).

    Убрать литеральную обёртку `inline-block rounded-full px-3 py-1 text-xs font-medium` — `.badge` уже даёт форму/тип/padding/uppercase/радиус-pill. Рендерить `<span className={className}>{label}</span>`, где className = маппинг (для известного статуса) или `"badge"` (fallback для неизвестного, вместо `bg-white/10 text-foreground/70`).

    `STATUS_LABELS` НЕ менять (поведение/тексты v2.0 сохранить): registration "Регистрация открыта", in_progress "Идёт", finished "Завершён" — class-map это load-bearing требование, не текст метки. Пульсирующая точка для reg приходит бесплатно из `.badge-reg::before` (Plan 01), разметку не трогаем.

    Хардкоженных Tailwind-цветовых литералов (`bg-*`, `text-*-NNN`) не оставлять — цвет полностью из `.badge-*` токенов.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n 'badge badge-reg\|badge badge-prog\|badge badge-fin' src/components/tournament-status-badge.tsx && ! grep -nE 'bg-(green|amber|white)|rounded-full|inline-block' src/components/tournament-status-badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` без ошибок
    - STATUS_CLASSES = `{ registration: "badge badge-reg", in_progress: "badge badge-prog", finished: "badge badge-fin" }`
    - рендер — `<span className={className}>{label}</span>`, без `inline-block rounded-full px-3 py-1 text-xs font-medium`
    - fallback для неизвестного статуса использует `"badge"` (не `bg-white/10`)
    - type-guard `isKnownStatus` и `STATUS_LABELS` сохранены
    - grep НЕ находит `bg-green`/`bg-amber`/`bg-white`/`rounded-full`/`inline-block` в файле
  </acceptance_criteria>
  <done>Бейдж статуса использует ported `.badge-*` классы (имена 1:1), наследует пульсирующую точку из Plan 01, без хардкоженных Tailwind-цветов; tsc чистый. Поведение/метки v2.0 неизменны.</done>
</task>

<task type="auto">
  <name>Task 2: Минимальный токен-рестайл Nav под Court</name>
  <files>src/components/nav.tsx</files>
  <read_first>
    - src/components/nav.tsx (текущий: RSC с getOptionalSession, инлайн SVG-логотип `fill="currentColor"`, монохром: `border-current/15` строка 16, `hover:opacity-80`, CTA `bg-foreground px-3 py-1 text-background` строки 58-63)
    - .planning/phases/12-court/12-PATTERNS.md (раздел `src/components/nav.tsx`, строки 90-101 — точный целевой рестайл, что менять/что оставить)
    - .claude/skills/sketch-findings-diplom/references/foundation.md (строки 328-342 — анти-паттерн хардкоженных цветов; .muted token color)
    - .planning/phases/12-court/12-01-SUMMARY.md (подтвердить наличие .btn/.btn-primary/.muted из Plan 01)
  </read_first>
  <action>
    ТОЛЬКО токен-рестайл (CONTEXT строка 38: без структурных изменений, без изменения навигации/ссылок/сессионной логики). Сохранить getOptionalSession, ветвление admin/session, все `<Link>` и их href, LogoutButton.

    Граница `<nav>`: `border-b border-current/15` → `border-b border-[var(--border)]`. Фон может остаться прозрачным поверх `--page`.

    CTA Регистрация (строки 58-63): заменить `rounded-md bg-foreground px-3 py-1 text-background hover:opacity-80` на классы компонентного слоя `btn btn-primary` (из Plan 01) — это даёт Court ball-green кнопку с токен-тенью; убрать монохром-утилиты.

    Обычные ссылки (`Турниры`, `Прошедшие турниры`, `Создать турнир`, `Личный кабинет`, `Войти`): держать тонкий hover; для muted-текста использовать класс `.muted` или токен-цвет `text-[var(--text-muted)]`, hover остаётся ненавязчивым. Имя пользователя (`<span className="opacity-70">`) — допустимо `.muted` / `.faint`.

    Логотип SVG `fill="currentColor"`/`stroke="currentColor"` уже токен-дружелюбен — оставить; убедиться, что окружающий текст wordmark наследует `--text` (через body) и при желании `--font-display`.

    Анти-паттерн (foundation.md строки 330-331): никаких хардкоженных hex и Tailwind-цветовых литералов (`bg-foreground`/`text-background`/конкретные цвета) — всё через токены `var(--…)` или ported-классы.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n 'btn btn-primary\|var(--border)' src/components/nav.tsx && ! grep -nE 'bg-foreground|text-background|border-current' src/components/nav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` без ошибок
    - `<nav>` использует `border-b border-[var(--border)]` (нет `border-current/15`)
    - CTA Регистрация использует `btn btn-primary` (нет `bg-foreground`/`text-background`)
    - grep НЕ находит `bg-foreground`/`text-background`/`border-current` в файле
    - сессионная логика (getOptionalSession, ветвление admin/user/guest), все `<Link href>`, LogoutButton — неизменны (структура навигации сохранена)
  </acceptance_criteria>
  <done>Nav отрисован в Court (токен-граница, ball-green CTA через `.btn .btn-primary`, muted-ссылки через токены), структура/ссылки/сессия v2.0 неизменны, без хардкоженных цветов; tsc чистый.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Верификация оболочки Court (визуал + адаптив ≤375px, UI-10)</name>
  <what-built>
    Plan 01 перенёс токены/шрифты/компонентный слой Court в globals.css + layout.tsx; этот план перевёл Nav и tournament-status-badge на Court. Базовая оболочка (тёмное court-поле, Court-шапка, шрифты Oswald/Inter, статус-бейджи с pulse-точкой) теперь рендерится. `next build` и `tsc` зелёные (автоматически проверено в задачах выше).
  </what-built>
  <how-to-verify>
    1. Запустить dev-сервер: `npm run dev` (или подтвердить, что он уже запущен).
    2. Открыть http://localhost:3000 — главную. Ожидаемо: тёмное court-поле (teal-градиент `--page`, НЕ монохром-чёрный), текст светлый (`--text` #eafff2), шрифт тела — Inter, заголовки/логотип-wordmark — Oswald (condensed).
    3. Шапка (Nav): граница снизу тонкая токен-зелёная; кнопка «Регистрация» (как гость) — ball-green заливка с мягкой тенью (`.btn .btn-primary`); ссылки читаемы, hover ненавязчивый. Структура и состав ссылок — как было в v2.0.
    4. Статус-бейдж: открыть любую страницу/список с турнирами (`/tournaments`); бейдж «Регистрация открыта» — pill в Court cyan-токене с пульсирующей точкой слева; «Идёт» — ball-green; «Завершён» — нейтральный. Все uppercase, Oswald.
    5. **Адаптив ~375px (UI-10):** DevTools → device toolbar → ширина 375px (iPhone SE). Проверить главную и `/tournaments`: НЕТ непреднамеренного горизонтального скролла (страница не уезжает вбок); шапка переносится корректно (`flex-wrap` уже есть); контент читаем. Намеренный скролл сетки/широких таблиц здесь не проверяется (фаза 14).
  </how-to-verify>
  <resume-signal>Напишите "approved" если оболочка Court корректна на десктопе и ≤375px без непреднамеренного скролла; иначе опишите, что сломалось (цвет/шрифт/скролл/бейдж).</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
Новых переиспользуемых символов этот план НЕ создаёт — он применяет токены и классы из Plan 01 на существующих компонентах.

Изменённое поведение/презентация (для справки фаз 13/14):
- `Nav` — глобальная Court-шапка (токен-граница, `.btn .btn-primary` CTA, muted-ссылки). Структура ссылок/сессии прежняя.
- `TournamentStatusBadge` — рендерит `.badge .badge-{reg|prog|fin}` (вместо хардкоженных Tailwind-литералов), с бесплатной pulse-точкой для reg. API компонента (`{ status }`) и метки прежние.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

Новых границ доверия нет. Restyle-only: presentational JSX/className на двух компонентах. `Nav` — Server Component, читает сессию через существующий `getOptionalSession()` (логика не изменена); `TournamentStatusBadge` — чистый presentational компонент. Не затрагивает auth-логику, данные, server actions, пользовательский ввод.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | (n/a) | Nav className / TournamentStatusBadge className | accept | No new attack surface — restyle-only: меняются только CSS-классы/токены на presentational-разметке. Сессионная логика Nav (`getOptionalSession`, admin-ветвление) не тронута — потоки данных и auth-границы неизменны. Нет user input, нет изменения server actions, новых пакетов нет. Угроз STRIDE по природе нет. |

Пакеты не устанавливаются. Package Legitimacy Gate неприменим.
</threat_model>

<verification>
- `npx tsc --noEmit` — без ошибок типов (оба компонента)
- `grep` подтверждает `.badge-*` маппинг в badge и `.btn .btn-primary` + `var(--border)` в nav; отсутствие хардкоженных Tailwind-цветов в обоих файлах
- Human-verify checkpoint: визуальная корректность Court-оболочки (поле/шрифты/шапка/бейджи) + ≤375px без непреднамеренного скролла (UI-10)
</verification>

<success_criteria>
- `Nav` в Court: токен-граница, `.btn .btn-primary` CTA, muted-ссылки через токены; структура/ссылки/сессия v2.0 неизменны
- `TournamentStatusBadge` маппит статус → `.badge .badge-{reg|prog|fin}`, наследует pulse-точку; type-guard и метки сохранены
- Нет хардкоженных hex/Tailwind-цветовых литералов в обоих компонентах
- Оболочка корректна на ~375px без непреднамеренного горизонтального скролла (UI-10 подтверждён на базе)
- tsc зелёный; human-verify approved
</success_criteria>

<output>
Create `.planning/phases/12-court/12-02-SUMMARY.md` when done.
</output>

---
phase: 12-court
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements: [UI-01, UI-10]
tags: [css, theme, tokens, tailwind, fonts, container-queries]

must_haves:
  truths:
    - "Загрузка приложения показывает оболочку Court (тёмное court-поле --page, текст --text, шрифты Oswald/Inter/JetBrains Mono) вместо монохрома"
    - "Все токены Court (surfaces/text/brand/status/type/shape) определены в globals.css на :root и доступны всем экранам"
    - "Компонентный слой _base (.card/.btn/.field/.badge/.meta/.progress/etc) доступен как @layer components с именами классов 1:1 из sources/_base.css"
    - "Шрифты Oswald/Inter/JetBrains Mono загружены через next/font/google с subset cyrillic и проброшены как --font-display/-body/-mono"
    - "Конвенция container-query (container-type: inline-size + @container, не @media) установлена для пер-экранных обёрток фаз 13/14"
    - "В коде нет захардкоженных hex — все цвета через var(--token)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "next/font Oswald/Inter/JetBrains Mono с cyrillic subset, var-bridge на <html>"
      contains: "next/font/google"
    - path: "src/app/globals.css"
      provides: "Court token :root block + @layer components (_base) + body shell + container-query utility + liveDot keyframes"
      min_lines: 200
  key_links:
    - from: "src/app/globals.css"
      to: "src/app/layout.tsx"
      via: "--font-display/-body/-mono consume --next-oswald/-inter/-jetbrains"
      pattern: "var\\(--next-(oswald|inter|jetbrains)\\)"
    - from: "src/app/globals.css body"
      to: "Court --page token"
      via: "background: var(--page)"
      pattern: "background:\\s*var\\(--page\\)"
---

<objective>
Перенести дизайн-фундамент Court в реальный Next 16 + Tailwind 4: загрузить шрифты Oswald/Inter/JetBrains Mono через `next/font/google` (subset cyrillic) и пробросить их CSS-переменными, перенести полный токен-контракт Court из `sources/themes/court.css` и компонентный слой `_base` из `sources/_base.css` в `src/app/globals.css`, перевести `body` на Court court-поле, установить конвенцию container-query (UI-10) и live-dot для reg-бейджа.

Покрывает UI-01 (тема + токены + компонентный слой в Tailwind 4 `@theme`/`globals.css`, доступны всем экранам) и фундаментную часть UI-10 (container-query как адаптивная конвенция, не media-queries).

Purpose: фазы 13/14 переносят разметку из `sources/NNN-*/index.html` без переименований — поэтому имена токенов и классов должны быть 1:1 со скетчами. Без этого фундамента ни один пер-экранный рестайл не работает.

Output: рабочая оболочка Court (тёмное поле, шрифты, компонентные классы), готовая к потреблению пер-экранными фазами.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-court/12-CONTEXT.md
@.planning/phases/12-court/12-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/foundation.md
@.claude/skills/sketch-findings-diplom/sources/themes/court.css
@.claude/skills/sketch-findings-diplom/sources/_base.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Загрузить шрифты Court через next/font и пробросить CSS-переменные</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (текущий root shell: `<html lang="ru" className="h-full antialiased">`, `<body className="min-h-full flex flex-col">`, без шрифтов — system-ui)
    - .planning/phases/12-court/12-PATTERNS.md (раздел `src/app/layout.tsx`, строки 68-87 — точная идиома next/font, имена переменных, веса, cyrillic)
    - .claude/skills/sketch-findings-diplom/sources/themes/court.css (строка 6 — Oswald wght 500;600;700)
    - .claude/skills/sketch-findings-diplom/sources/_base.css (строка 24 — Inter wght 400;500;600;700 + JetBrains Mono wght 500;700)
  </read_first>
  <action>
    Импортировать `Oswald`, `Inter`, `JetBrains_Mono` из `next/font/google`. Создать три загрузчика на уровне модуля (вне компонента): Oswald с `subsets: ["latin","cyrillic"]`, `weight: ["500","600","700"]`, `variable: "--next-oswald"`, `display: "swap"`; Inter с `subsets: ["latin","cyrillic"]`, `variable: "--next-inter"`, `display: "swap"` (весовой диапазон Inter переменный — weight не указывать); JetBrains_Mono с `subsets: ["latin","cyrillic"]`, `weight: ["500","700"]`, `variable: "--next-jetbrains"`, `display: "swap"`. Веса зеркалят `@import` скетчей. Subset `cyrillic` ОБЯЗАТЕЛЕН (RU-интерфейс).

    Пробросить три `.variable` на `<html>`: `className={\`${oswald.variable} ${inter.variable} ${jetbrains.variable} h-full antialiased\`}` (сохранить `h-full antialiased`). Структуру `<body className="min-h-full flex flex-col"><Nav/>{children}</body>` НЕ менять — структурных изменений в scope нет.

    Имена `--next-oswald/--next-inter/--next-jetbrains` — единый источник истины: ровно эти имена потребляет Task 2 в globals.css. Не расходиться в написании.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` проходит без ошибок
    - layout.tsx импортирует `Oswald, Inter, JetBrains_Mono` из `next/font/google`
    - все три загрузчика имеют `subsets` с `"cyrillic"` и `variable` = `--next-oswald` / `--next-inter` / `--next-jetbrains`
    - `<html>` className содержит все три `.variable` плюс `h-full antialiased`
    - `<body className="min-h-full flex flex-col">` и `<Nav/>{children}` неизменны
  </acceptance_criteria>
  <done>Шрифты Court загружаются через next/font (без FOUT, без внешнего @import), их CSS-переменные доступны на `<html>` для globals.css. tsc чистый.</done>
</task>

<task type="auto">
  <name>Task 2: Перенести токен-контракт Court + компонентный слой _base + Court body shell в globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (текущий: `@import "tailwindcss"` + монохром `:root` `--background/--foreground` + `@theme inline` mapping + body на system-ui)
    - .planning/phases/12-court/12-PATTERNS.md (раздел `src/app/globals.css` строки 21-64 — порядок блоков, что НЕ переносить, container-query, live-dot)
    - .claude/skills/sketch-findings-diplom/sources/themes/court.css (строки 8-62 — полный token :root, единственное место hex; строку 6 `@import Oswald` НЕ переносить)
    - .claude/skills/sketch-findings-diplom/sources/_base.css (строки 26-219 — компонентный слой для порта; строку 24 `@import Inter/JetBrains`, строки 43-49 `.app` preview-frame hack, строку 40 `padding-top:52px`, строки 221-266 sketch-chrome — НЕ переносить)
    - .claude/skills/sketch-findings-diplom/references/foundation.md (строки 215-235 container-queries, 237-259 status-mapping + live-dot, 261-276 порядок слоёв, 328-342 анти-паттерны)
  </read_first>
  <action>
    Порядок блоков в globals.css (foundation.md строки 261-276): `@import "tailwindcss"` → `:root` токен-блок → `@theme inline` mapping → `@layer components { … }` → глобальный shell (`*` box-sizing, `body`, container-query утилита, keyframes). Компоненты читают токены, поэтому токены идут первыми.

    `:root` токен-блок: перенести ВЕРБАТИМ значения Court из court.css строк 9-62 — все группы: surfaces (`--page --bg --surface --surface-2 --surface-input --border --border-strong --ring --card-blur`), text (`--text --text-muted --text-faint --on-primary --on-accent`), brand (`--primary --primary-2 --primary-soft --accent --accent-2 --danger --danger-soft --success --success-soft --winner-bg --winner-text --btn-primary-bg --btn-shadow`), status (`--badge-reg-bg --badge-reg-fg --badge-prog-bg --badge-prog-fg --badge-fin-bg --badge-fin-fg`), type weights/tracking (`--fw-strong --fw-display --tracking-display --tracking-eyebrow`), shape (`--radius --radius-lg --radius-pill --shadow --shadow-lg`). Сохранить существующую пару `--background: #0a0a0a; --foreground: #ededed;` как решено в CONTEXT (часть набора). Hex живёт ТОЛЬКО здесь.

    Шрифтовые токены: переопределить три семейства так, чтобы они потребляли next/font-переменные из Task 1, а не хардкоженный `'Oswald'`: `--font-display: var(--next-oswald), system-ui, sans-serif;` `--font-body: var(--next-inter), system-ui, sans-serif;` `--font-mono: var(--next-jetbrains), monospace;`. Имена `--next-*` точно как в layout.tsx.

    `@theme inline`: оставить существующий mapping `--color-background`/`--color-foreground`. Расширять Tailwind utility-цветами не требуется — семантический цвет идёт через токены/классы, не через Tailwind-литералы (foundation.md строки 39-41).

    `@layer components { … }`: перенести ВЕРБАТИМ компонентные правила _base.css строк 51-219 — типографика (`h1/h2/h3`, `.eyebrow`, `.muted`, `.faint`, `.mono`, `a`), `.card/.card-pad/.surface-2`, `.btn/.btn-primary/.btn-ghost/.btn-block` + `:active/:hover/:disabled`, `.field/.label/.input/.hint/.error` (включая `.input:focus` color-mix ring), `.badge` + `.badge::before` + `.badge-reg/-prog/-fin`, `.pill/.pill-accent`, `.avatar`, `.meta/.meta-row/.meta-key/.meta-val`, `.progress` + `.progress > span`, `.empty`. Имена классов 1:1 — фазы 13/14 от этого зависят. НЕ переносить: `_base.css` строку 24 (@import шрифтов — идут через next/font), строки 43-49 (`.app` preview-frame `max-width`/`--app-max` toolbar hack), строки 221-266 (`#variant-nav`/`#sketch-tools`/`.variant` sketch-chrome).

    Глобальный shell: `*, *::before, *::after { box-sizing: border-box; }` и `body` из _base.css строк 30-41, но БЕЗ `padding-top: 52px` (это hack под фиксированный sketch-tab-bar): `body { margin:0; min-height:100vh; background: var(--page); color: var(--text); font-family: var(--font-body); font-size:15px; line-height:1.5; -webkit-font-smoothing:antialiased; text-rendering:optimizeLegibility; }`. Это заменяет текущее монохромное body-правило.

    Container-query конвенция (foundation.md строки 215-235; CONTEXT строка 34): добавить именованную утилиту-обёртку с `container-type: inline-size` (например класс `.app` или `.cq`, имя на усмотрение — НЕ форсить единый layout-контейнер в root). Корневой layout её не применяет; фазы 13/14 навешивают её на пер-экранные обёртки и используют `@container (max-width: …)`, НЕ `@media`. Утилита-обёртка предоставляется здесь как конвенция фундамента.

    Live-dot (foundation.md строки 249-252): добавить `.badge-reg::before { animation: liveDot 1.8s ease-in-out infinite; }` и `@keyframes liveDot { 0%,100% { opacity:1; transform:scale(1); } 50% { opacity:.35; transform:scale(.7); } }`. Анимирует существующую точку `.badge::before`, разметку не меняет.

    Анти-паттерн (foundation.md строки 330-342): никаких хардкоженных hex/цветовых литералов вне `:root` токен-блока; цвет всегда `var(--token)` или color-mix от токена.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20 && grep -n -- '--page\|--primary\|--badge-reg-fg\|@layer components\|container-type: inline-size\|liveDot\|var(--next-oswald)' src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` завершается успешно (CSS компилируется Tailwind 4)
    - `:root` в globals.css содержит токены `--page`, `--surface`, `--surface-2`, `--border`, `--ring`, `--text`, `--text-muted`, `--on-primary`, `--primary`, `--accent`, `--danger`, `--success`, `--winner-bg`, `--btn-primary-bg`, `--btn-shadow`, `--badge-reg-fg`, `--badge-prog-fg`, `--badge-fin-fg`, `--radius`, `--radius-lg`, `--radius-pill`, `--shadow`, `--shadow-lg`, `--fw-strong`, `--tracking-eyebrow` (полный контракт court.css)
    - `--font-display` = `var(--next-oswald), …`, `--font-body` = `var(--next-inter), …`, `--font-mono` = `var(--next-jetbrains), …`
    - присутствует `@layer components { … }` с классами `.card`, `.card-pad`, `.surface-2`, `.btn`, `.btn-primary`, `.btn-ghost`, `.btn-block`, `.field`, `.label`, `.input`, `.hint`, `.error`, `.badge`, `.badge-reg`, `.badge-prog`, `.badge-fin`, `.pill`, `.pill-accent`, `.avatar`, `.meta`, `.meta-row`, `.meta-key`, `.meta-val`, `.progress`, `.empty`, `.eyebrow`, `.muted`, `.faint`, `.mono` (имена 1:1 из _base.css)
    - `body` правило использует `background: var(--page)` и `color: var(--text)` и `font-family: var(--font-body)`, без `padding-top: 52px`
    - присутствует утилита с `container-type: inline-size` и есть `@keyframes liveDot` + `.badge-reg::before` с `animation`
    - НЕ перенесены: `#variant-nav`, `#sketch-tools`, `.variant`, `--app-max`/`.app` preview-frame max-width hack, `@import` шрифтов googleapis
    - grep по globals.css вне `:root` токен-блока не находит сырых hex-литералов `#[0-9a-fA-F]` в компонентных правилах (цвет только через `var(--…)`/`color-mix`)
  </acceptance_criteria>
  <done>Токен-контракт Court, компонентный слой _base (имена 1:1), Court body-shell, container-query утилита и live-dot живут в globals.css; шрифтовые токены потребляют next/font-переменные; next build зелёный; хардкоженных hex вне токен-блока нет.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Этот план создаёт следующие новые символы (потребляются фазами 13/14, имена менять нельзя):

**CSS custom properties (дизайн-токены) на `:root` в globals.css** (значения 1:1 из court.css):
- surfaces: `--page --bg --surface --surface-2 --surface-input --border --border-strong --ring --card-blur`
- text: `--text --text-muted --text-faint --on-primary --on-accent`
- brand: `--primary --primary-2 --primary-soft --accent --accent-2 --danger --danger-soft --success --success-soft --winner-bg --winner-text --btn-primary-bg --btn-shadow`
- status: `--badge-reg-bg --badge-reg-fg --badge-prog-bg --badge-prog-fg --badge-fin-bg --badge-fin-fg`
- type: `--font-display --font-body --font-mono --fw-strong --fw-display --tracking-display --tracking-eyebrow`
- shape: `--radius --radius-lg --radius-pill --shadow --shadow-lg`
- сохранены: `--background --foreground`

**`--font-*` bridge vars (на `<html>` через next/font в layout.tsx):**
- `--next-oswald` (Oswald display), `--next-inter` (Inter body), `--next-jetbrains` (JetBrains Mono numerals)

**Компонентные классы (`@layer components` в globals.css), имена 1:1 из _base.css:**
- `.card .card-pad .surface-2`
- `.btn .btn-primary .btn-ghost .btn-block`
- `.field .label .input .hint .error`
- `.badge .badge-reg .badge-prog .badge-fin`
- `.pill .pill-accent`
- `.avatar`
- `.meta .meta-row .meta-key .meta-val`
- `.progress`
- `.empty`
- type helpers: `.eyebrow .muted .faint .mono`

**Утилиты/конвенции:**
- container-query wrapper utility (`container-type: inline-size`) — имя на усмотрение исполнителя, документируется в SUMMARY для фаз 13/14
- `@keyframes liveDot` + `.badge-reg::before` animation

**Новых файлов нет** (всё в существующих globals.css + layout.tsx).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

Новых границ доверия нет. Это restyle-only фаза: CSS-токены, `@layer components`, `next/font`, проброс CSS-переменных на `<html>`. Не затрагивает auth, данные, server actions, пользовательский ввод, сетевые границы.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | (n/a) | CSS tokens / @layer components / next/font / `<html>` var bridge | accept | No new attack surface — restyle-only: CSS custom properties, component classes, шрифты через next/font/google (без runtime-зависимостей, без новых npm-пакетов), без потоков данных, без auth, без user input, без изменения server actions. Угроз STRIDE по природе нет. |

Пакеты не устанавливаются (`next`/`react` уже в package.json; `next/font` — встроенный модуль Next 16). Package Legitimacy Gate неприменим.
</threat_model>

<verification>
- `npx tsc --noEmit` — без ошибок типов (layout.tsx)
- `npx next build` — успешная сборка (globals.css компилируется Tailwind 4, шрифты резолвятся)
- `grep` подтверждает наличие токенов, font-bridge, `@layer components`, container-query утилиты, liveDot в globals.css
- Визуально (проверяется в Plan 02): `body` показывает тёмное court-поле `--page`, текст `--text`, шрифт Inter
</verification>

<success_criteria>
- Полный токен-контракт Court (surfaces/text/brand/status/type/shape) на `:root` в globals.css, значения 1:1 из court.css
- Компонентный слой _base (`.card/.btn/.field/.badge/.meta/.progress/...`) в `@layer components`, имена классов 1:1 из _base.css
- Шрифты Oswald/Inter/JetBrains Mono загружены через next/font/google с subset cyrillic, проброшены как `--font-display/-body/-mono`
- `body` переведён на Court (`--page` фон, `--text` цвет, `--font-body` шрифт)
- Конвенция container-query (`container-type: inline-size` + `@container`, не `@media`) установлена
- live-dot для `.badge-reg` добавлен
- Нет хардкоженных hex вне `:root` токен-блока
- tsc и next build зелёные
</success_criteria>

<output>
Create `.planning/phases/12-court/12-01-SUMMARY.md` when done.
В SUMMARY обязательно зафиксировать **точное имя container-query утилиты-обёртки** (`.app` / `.cq` / иное) — фазы 13/14 будут навешивать именно её на пер-экранные обёртки.
</output>

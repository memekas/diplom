---
phase: 07-multiformat-data-model
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - prisma/seed.ts
  - scripts/seed-test-users.ts
  - prisma/migrations/
autonomous: true
requirements: [DATA-06, DATA-01]
must_haves:
  truths:
    - "Миграция multiformat_data_model применяется чисто; prisma generate проходит (DATA-06)"
    - "migrate reset --force пересоздаёт БД и запускает seed-хук без ошибок (DATA-06)"
    - "Оба сида создают каждого юзера с валидным skillLevel (5 латинских) + опц. birthDate; идемпотентны; ник @@unique сохранён (DATA-06/01)"
    - "Существующая playoff-структура (Match/Pair/SetScore) воссоздаётся сидами; 4 playoff-теста + 3 validation-теста зелёные (DATA-05 — без регрессии)"
    - "@prisma/client после generate имеет делегаты tournamentPlayer/round/roundMatch/playerMatchScore (DATA-03/04/05)"
  artifacts:
    - path: "prisma/migrations/"
      provides: "8-я миграция multiformat_data_model (rebuild user-таблицы для NOT NULL skillLevel)"
      contains: "multiformat_data_model"
    - path: "prisma/seed.ts"
      provides: "admin-seed с валидным skillLevel"
      contains: "skillLevel"
    - path: "scripts/seed-test-users.ts"
      provides: "тестовые игроки с 5-значным SKILL_LEVELS + опц. birthDate"
      contains: "progressing"
  key_links:
    - from: "scripts/seed-test-users.ts SKILL_LEVELS"
      to: "src/lib/validation/auth.ts skillLevels (5 значений)"
      via: "round-robin (i-1)%5 покрывает progressing"
      pattern: "progressing"
    - from: "prisma migrate reset"
      to: "prisma/seed.ts + seed:test-users"
      via: "seed-хук (prisma.config.ts) + npm run seed:test-users"
      pattern: "migrate reset"
---

<objective>
Применить мультиформатную схему (из плана 07-01) к БД и обновить сиды: создать миграцию `multiformat_data_model`, выполнить `migrate reset` + reseed (sanctioned для обязательного skillLevel — избегает backfill), и привести оба сида (`prisma/seed.ts` админ, `scripts/seed-test-users.ts` игроки) к 5-значному skillLevel + опциональному birthDate с сохранением идемпотентности и уникальности ника. Завершить фазу прогоном всех playoff- и validation-тестов (инвариант: playoff не сломан) и проверкой делегатов нового клиента.

Purpose: Сделать новую модель данных реально применённой (миграция + клиент) и наполненной (сиды) — закрыть DATA-06 и зафиксировать инвариант «playoff цел».
Output: 8-я миграция, обновлённые сиды, перегенерированный @prisma/client, зелёные тесты.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multiformat-data-model/07-CONTEXT.md
@.planning/phases/07-multiformat-data-model/07-RESEARCH.md
@.planning/phases/07-multiformat-data-model/07-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- Миграция `prisma/migrations/<timestamp>_multiformat_data_model/` (8-я; перестраивает user-таблицу из-за NOT NULL skillLevel — генерируется Prisma автоматически, НЕ редактировать вручную).
- Обновлённый `prisma/seed.ts`: admin получает валидный `skillLevel` (например `"pro"`); идемпотентность по email сохранена.
- Обновлённый `scripts/seed-test-users.ts`: `SKILL_LEVELS` → 5 латинских значений (покрывает progressing); опц. детерминированный `birthDate`; идемпотентность + try/catch WR-01 сохранены.
- Перегенерированный `@prisma/client` с делегатами tournamentPlayer/round/roundMatch/playerMatchScore.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Обновить оба сида под обязательный skillLevel(5) + опц. birthDate (DATA-06)</name>
  <files>prisma/seed.ts, scripts/seed-test-users.ts</files>
  <read_first>
    - prisma/seed.ts (идемпотентный admin-seed; signUpEmail body на ~строке 33-35; post-update role).
    - scripts/seed-test-users.ts (SKILL_LEVELS массив строка 34; round-robin (i-1)%len; signUpEmail body строка 68-70; post-update courtSide строка 71; try/catch WR-01).
    - src/lib/validation/auth.ts (после плана 07-01 — 5 латинских значений skillLevels; источник истины для SKILL_LEVELS массива).
    - .planning/phases/07-multiformat-data-model/07-RESEARCH.md §"Seed updates" (точные изменения), §"Common Pitfalls" Pitfall 5 (идемпотентность/ник).
  </read_first>
  <action>
    (A) scripts/seed-test-users.ts: расширить `SKILL_LEVELS` массив с 4 до 5 латинских значений — `["beginner", "progressing", "intermediate", "advanced", "pro"] as const` (совпадает со skillLevels из validation/auth.ts; round-robin `(i-1) % SKILL_LEVELS.length` теперь покрывает progressing). signUpEmail body уже передаёт skillLevel (строка ~69) — ничего не ломать. Опционально: добавить детерминированный `birthDate` в post-create `prisma.user.update` рядом с courtSide (строка ~71), вычисляемый из i (например new Date по году 1990 + (i % 20)). Сохранить email-guard идемпотентность и try/catch (WR-01).

    (B) prisma/seed.ts: добавить валидный `skillLevel` в signUpEmail body (например значение "pro") — admin теперь обязан иметь уровень (Better Auth required:true из плана 07-01). Опционально birthDate через существующий post-create prisma.user.update (рядом с role). Email-guard идемпотентность — НЕ трогать.

    НЕ менять: пароли/хеширование (Better Auth signUpEmail), nickname-схему, env-чтение.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep находит "progressing" в scripts/seed-test-users.ts — SKILL_LEVELS содержит 5-й тир.
    - grep находит "skillLevel" в prisma/seed.ts — admin-seed передаёт уровень.
    - Идемпотентность сохранена: оба сида по-прежнему содержат findUnique-guard по email; seed-test-users сохраняет try/catch вокруг signUpEmail (WR-01).
    - npx tsc --noEmit проходит (сиды компилируются с обновлённым типом signUp).
  </acceptance_criteria>
  <done>Оба сида создают пользователей с валидным 5-значным skillLevel (+ опц. birthDate), идемпотентны, ник-уникальность не нарушена.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Создать миграцию, выполнить reset+reseed, перегенерировать клиент (DATA-06)</name>
  <files>prisma/migrations/</files>
  <read_first>
    - prisma/schema.prisma (применяемая схема — из плана 07-01).
    - prisma/migrations/ (7 применённых; последняя 20260606180646_add_user_nickname — новая будет 8-й).
    - prisma.config.ts (seed-хук = tsx prisma/seed.ts).
    - .planning/phases/07-multiformat-data-model/07-RESEARCH.md §"Migration approach" (точная последовательность), §"Common Pitfalls" Pitfall 1 (nullable→required на SQLite).
  </read_first>
  <action>
    [BLOCKING] Применить схему к БД. Неинтерактивно (yolo; thesis DB одноразовая — потеря данных допустима, CONTEXT D6).

    Порядок (RESEARCH §Migration approach):
    1. npx prisma validate — убедиться, что схема валидна перед миграцией.
    2. npx prisma migrate dev --name multiformat_data_model — создаёт 8-ю миграцию И регенерирует клиент. Поскольку skillLevel nullable→required, Prisma перестроит user-таблицу (create-new/copy/drop/rename) — это нормально, миграцию НЕ редактировать вручную. Если CLI запросит подтверждение data-loss и зависнет на промпте — перейти к шагу 3 (reset создаёт и применяет миграцию неинтерактивно).
    3. npx prisma migrate reset --force — гарантированно чистое состояние: пересоздаёт БД, применяет все миграции, запускает seed-хук (tsx prisma/seed.ts → admin). На пустой/пересозданной БД NOT NULL skillLevel не конфликтует (нет NULL-строк).
    4. npm run seed:test-users — наполнить тестовыми игроками (20 по умолчанию).
    5. npx prisma generate — гарантировать актуальный клиент (migrate dev/reset уже генерирует, но явный вызов фиксирует).

    Приоритет — чистое неинтерактивное применение без зависших промптов; используй флаг --force везде, где CLI может запросить подтверждение.
  </action>
  <verify>
    <automated>npx prisma migrate reset --force && npm run seed:test-users && node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();['tournamentPlayer','round','roundMatch','playerMatchScore'].forEach(d=>{if(!p[d])throw new Error('missing delegate '+d)});console.log('delegates OK');p.\$disconnect()"</automated>
  </verify>
  <acceptance_criteria>
    - Каталог prisma/migrations/ содержит новую папку с суффиксом _multiformat_data_model.
    - npx prisma migrate reset --force завершается без ошибок и запускает seed-хук (создаётся admin).
    - npm run seed:test-users создаёт игроков (включая хотя бы одного с уровнем progressing) без abort.
    - node -e require('@prisma/client') показывает делегаты tournamentPlayer/round/roundMatch/playerMatchScore (вывод "delegates OK", exit 0).
    - npx prisma validate exits 0.
  </acceptance_criteria>
  <done>Миграция multiformat_data_model применена, БД пересоздана и наполнена сидами, @prisma/client регенерирован с делегатами 4 новых моделей.</done>
</task>

<task type="auto">
  <name>Task 3: Зафиксировать инвариант «playoff цел» — прогон playoff + validation тестов (DATA-05)</name>
  <files></files>
  <read_first>
    - src/lib/services/bracket.test.ts, result.test.ts, registration.test.ts, tournament-status.test.ts (4 playoff-теста — self-contained tsx, header "Run:").
    - src/lib/validation/profile.test.ts, registration.test.ts, tournament.test.ts (3 validation-теста).
    - .planning/phases/07-multiformat-data-model/07-RESEARCH.md §"Keeping playoff intact" (карта тестов — почему остаются зелёными).
  </read_first>
  <action>
    Прогнать все существующие self-contained тест-скрипты через tsx, чтобы зафиксировать главный инвариант фазы: playoff (Match/Pair/SetScore) и валидация не сломаны аддитивными изменениями схемы + расширением skillLevels. Тесты НЕ модифицировать (RESEARCH: остаются зелёными by construction — playoff-модели не тронуты; profile.test проверяет невалидный "legend", не количество уровней). Только запустить и убедиться, что все проходят.

    Если какой-либо validation-тест упадёт из-за расширения skillLevels (маловероятно — RESEARCH §"Keeping playoff intact": тесты не ассертят count уровней) — НЕ менять тест; зафиксировать падение в SUMMARY как регрессию для разбора (это сигнал реального конфликта, а не повод ослаблять тест).
  </action>
  <verify>
    <automated>for f in src/lib/services/bracket.test.ts src/lib/services/result.test.ts src/lib/services/registration.test.ts src/lib/services/tournament-status.test.ts src/lib/validation/profile.test.ts src/lib/validation/registration.test.ts src/lib/validation/tournament.test.ts; do echo "== $f =="; npx tsx "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Все 4 playoff-теста (bracket/result/registration/tournament-status) проходят (exit 0).
    - Все 3 validation-теста (profile/registration/tournament) проходят (exit 0).
    - Цикл verify завершается с кодом 0 (ни один тест не упал).
  </acceptance_criteria>
  <done>Все 7 существующих тест-скриптов зелёные — playoff-инвариант и валидация подтверждены неизменными после мультиформатной схемы.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed-скрипты → DB | Создают пользователей через Better Auth signUpEmail (scrypt-хеш в Account); креды только из env |
| migrate reset → dev.db | Полностью пересоздаёт локальную thesis-БД (одноразовая) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | креды в сидах | accept (mitigated by pattern) | ADMIN_EMAIL/ADMIN_PASSWORD/TEST_USER_PASSWORD — только из env; никогда не коммитятся. Пароли хешируются Better Auth (scrypt), не хранятся в открытом виде. Существующий паттерн — не меняется. |
| T-07-05 | Denial of Service / Integrity | migrate reset стирает данные | accept | Thesis-БД одноразовая (CONTEXT D6 явно санкционирует reset вместо backfill). Реальных данных нет; reset пересоздаёт из миграций + сидов детерминированно. |
| T-07-06 | Tampering | nickname @@unique коллизии при reseed | mitigate | Сиды сохраняют email-guard идемпотентность + try/catch WR-01 вокруг signUpEmail; reset даёт пустую таблицу (нет коллизий). Дубль ника логируется-и-пропускается, не падает loop. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | Фаза НЕ ставит пакетов (RESEARCH §Package Legitimacy Audit: zero installs). Нет supply-chain surface. |
</threat_model>

<verification>
- npx prisma validate exits 0.
- npx prisma migrate reset --force + npm run seed:test-users успешны (admin + игроки созданы).
- @prisma/client имеет делегаты tournamentPlayer/round/roundMatch/playerMatchScore.
- Все 4 playoff-теста + 3 validation-теста зелёные.
- Prisma Studio (ручная опц. проверка): новые таблицы tournament_player/round/round_match/player_match_score присутствуют и user/tournament показывают новые поля — `npx prisma studio` (не блокирует автоматику; ROADMAP success criterion 5).
</verification>

<success_criteria>
- Миграция multiformat_data_model применяется чисто; prisma generate проходит; playoff-структура (Match/SetScore) сохранена (ROADMAP SC1).
- Сиды создают пользователей с обязательным 5-значным skillLevel (+ опц. birthDate), идемпотентны, ник-уникальность цела (ROADMAP SC2).
- Делегаты 4 новых моделей доступны в клиенте; запросы по ним работают (ROADMAP SC4/SC5).
- Все существующие тесты зелёные — playoff не сломан (ROADMAP SC1, DATA-05).
</success_criteria>

<output>
Create `.planning/phases/07-multiformat-data-model/07-02-SUMMARY.md` when done
</output>

---
phase: 07-multiformat-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/validation/auth.ts
  - src/lib/auth.ts
  - src/app/(public)/tournaments/[id]/page.tsx
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-07]
must_haves:
  truths:
    - "prisma/schema.prisma defines models TournamentPlayer, Round, RoundMatch, PlayerMatchScore (DATA-03/04/05)"
    - "User.skillLevel is required (no `?`) and skillLevels constant has 5 latin values incl. progressing (DATA-01)"
    - "User.birthDate DateTime? exists (DATA-01)"
    - "Tournament gains format/participantMode/level/price/scoringMode/targetPoints/totalRounds (DATA-02/07)"
    - "Better Auth additionalFields skillLevel is required:true so seed signUp must carry it (DATA-01)"
    - "npx prisma validate exits 0 (all 6 new User back-relations wired)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Multiformat data model — 4 new models + extended User/Tournament"
      contains: "model RoundMatch"
    - path: "src/lib/validation/auth.ts"
      provides: "5-value skillLevels constant + skillLevelLabels RU map"
      contains: "progressing"
    - path: "src/lib/auth.ts"
      provides: "skillLevel required:true additionalField"
      contains: "required: true"
    - path: "src/app/(public)/tournaments/[id]/page.tsx"
      provides: "RU label for progressing tier"
      contains: "прогрессирующий"
  key_links:
    - from: "src/lib/auth.ts (skillLevel required:true)"
      to: "prisma User.skillLevel (NOT NULL)"
      via: "Better Auth signUp writes skillLevel; column rejects null"
      pattern: "required:\\s*true"
    - from: "prisma RoundMatch (4 User FK)"
      to: "prisma User back-relations"
      via: "named @relation RMTeamA1/A2/B1/B2"
      pattern: "@relation\\(\"RMTeam"
---

<objective>
Перевести Prisma-схему и app-слой уровней на мультиформатную модель данных v2.0: добавить четыре новые модели для round-based/индивидуальных форматов (TournamentPlayer, Round, RoundMatch, PlayerMatchScore), расширить Tournament семью полями конфигурации формата/подсчёта, сделать User.skillLevel обязательным с пятью латинскими значениями (+ новый тир `progressing`) и опциональным birthDate. Playoff-стек (Match/Pair/SetScore) НЕ трогается (locked D4) — изменения строго аддитивны.

Все правки этого плана — статические (файлы исходников), без обращения к БД. Миграция, reset и сиды идут в плане 07-02.

Purpose: Фундамент данных Слоя 1 v2.0 — на нём строится весь бэкенд (Фазы 8–9) без новых миграций.
Output: Изменённые prisma/schema.prisma + три app-файла (skillLevels-константа, Better Auth required-флаг, RU-лейбл progressing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multiformat-data-model/07-CONTEXT.md
@.planning/phases/07-multiformat-data-model/07-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Новые символы (этот план):
- **Модели Prisma:** `TournamentPlayer`, `Round`, `RoundMatch`, `PlayerMatchScore` (map: `tournament_player`, `round`, `round_match`, `player_match_score`).
- **Новые поля User:** `birthDate DateTime?`; `skillLevel` меняет тип `String?` → `String` (required). 6 новых back-relations: `tournamentPlayers`, `roundMatchesAsA1`, `roundMatchesAsA2`, `roundMatchesAsB1`, `roundMatchesAsB2`, `playerMatchScores`.
- **Новые поля Tournament:** `format`, `participantMode`, `level`, `price`, `scoringMode`, `targetPoints`, `totalRounds`; back-relations `tournamentPlayers`, `rounds`.
- **app:** `skillLevels` расширена 4→5 (добавлен `progressing`); новый экспорт `skillLevelLabels` (RU-map) в `src/lib/validation/auth.ts`. `src/lib/auth.ts` skillLevel `required:true`. RU-лейбл `progressing` в `tournaments/[id]/page.tsx`.
- Имя миграции (создаётся в 07-02): `multiformat_data_model`.
</artifacts_this_phase_produces>

<level_key_decision>
**Решение по ключам уровня (зафиксировано):** хранить ЛАТИНСКИЕ slug-ключи `beginner|progressing|intermediate|advanced|pro` (расширение существующих 4 значений; новый тир `progressing` вставляется МЕЖДУ beginner и intermediate). RU-строки — только на UI-слое (label-map). Обоснование: совпадает с текущей 4-значной константой, не кладёт кириллицу в БД/FormData, формы register/profile рендерят значения из константы автоматически. (RESEARCH §Summary, CONTEXT Claude's Discretion.)
</level_key_decision>

<tasks>

<task type="auto">
  <name>Task 1: Расширить Prisma-схему — 4 новые модели + поля User/Tournament (DATA-01..05, DATA-07)</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (ПОЛНОСТЬЮ — source of truth: конвенции @@map/@@unique/@@index, named relations Pair/Match, onDelete:Cascade, Pitfall-9 String-комментарии). НЕ трогать model Pair / model Match / model SetScore.
    - .planning/phases/07-multiformat-data-model/07-RESEARCH.md §"Code Examples" (точные имена моделей/полей/relations/индексов), §"Architecture Patterns" (Pattern 1/2/3), §"Anti-Patterns to Avoid".
    - .planning/phases/07-multiformat-data-model/07-CONTEXT.md §decisions (D4 архитектура).
  </read_first>
  <action>
    Аддитивно расширить schema.prisma. ЗАПРЕЩЕНО менять model Pair, model Match, model SetScore (locked D4 — playoff-регрессия). НЕ вводить Prisma `enum` (Pitfall 9 — String + inline-комментарий `// "a" | "b"`).

    (A) model User: изменить `skillLevel String?` → `skillLevel String` (убрать `?`; обновить inline-комментарий на 5 латинских значений `"beginner"|"progressing"|"intermediate"|"advanced"|"pro"`). Добавить `birthDate DateTime?`. Добавить 6 back-relations с именованными @relation: `tournamentPlayers TournamentPlayer[] @relation("TournamentPlayerUser")`, `roundMatchesAsA1 RoundMatch[] @relation("RMTeamA1")`, `roundMatchesAsA2 RoundMatch[] @relation("RMTeamA2")`, `roundMatchesAsB1 RoundMatch[] @relation("RMTeamB1")`, `roundMatchesAsB2 RoundMatch[] @relation("RMTeamB2")`, `playerMatchScores PlayerMatchScore[] @relation("PlayerMatchScoreUser")`. courtSide/nickname/phone/@@unique — НЕ трогать.

    (B) model Tournament: добавить после gamesPerSet — `format String @default("playoff")` (// "playoff"|"round_robin"|"americano"|"mexicano"), `participantMode String @default("pairs")` (// "pairs"|"singles"), `level String @default("intermediate")` (// один из 5 skillLevels — матчинг в Фазе 8), `price Int?` (// отображаемая цена, минорные единицы; null = бесплатно/не задано), `scoringMode String @default("sets")` (// "sets"|"points"), `targetPoints Int?` (// целевая сумма очков на матч в points-режиме; app-дефолт 24 — БЕЗ DB @default), `totalRounds Int?` (// число раундов американо/мексикано). setsPerMatch/gamesPerSet — оставить тип Int и @default(3)/(6) БЕЗ изменений, но обновить комментарий «настраиваемые, без верхнего лимита (cap снят на app-слое, Фаза 8)». Добавить back-relations `tournamentPlayers TournamentPlayer[]` и `rounds Round[]`.

    (C) Четыре новые модели (имена/поля/индексы — копировать из RESEARCH §Code Examples):
    - `TournamentPlayer`: id cuid; tournamentId + tournament @relation onDelete:Cascade; userId + user @relation("TournamentPlayerUser") (БЕЗ cascade на User); createdAt; `@@unique([tournamentId, userId])`; `@@index([tournamentId])`; `@@map("tournament_player")`.
    - `Round`: id cuid; tournamentId + tournament @relation onDelete:Cascade; roundNumber Int; `status String @default("pending")` (// "pending"|"in_progress"|"finished"); createdAt; matches RoundMatch[]; `@@unique([tournamentId, roundNumber])`; `@@map("round")`.
    - `RoundMatch`: id cuid; roundId + round @relation onDelete:Cascade; courtNumber Int; ЧЕТЫРЕ nullable User FK — teamA1Id String? + teamA1 User? @relation("RMTeamA1"); teamA2Id/teamA2 @relation("RMTeamA2"); teamB1Id/teamB1 @relation("RMTeamB1"); teamB2Id/teamB2 @relation("RMTeamB2") (все БЕЗ cascade на User); `pointsA Int?`; `pointsB Int?`; playerScores PlayerMatchScore[]; `@@index([roundId])`; `@@map("round_match")`. (RR-пары переиспользуют слоты: teamA1/A2 = пара A, teamB1/B2 = пара B — задокументировать комментарием; D4 + RESEARCH Open Q1.)
    - `PlayerMatchScore`: id cuid; roundMatchId + roundMatch @relation onDelete:Cascade; userId + user @relation("PlayerMatchScoreUser") (БЕЗ cascade на User); `teamSlot String` (// "A"|"B"); `pointsFor Int`; `pointsAgainst Int` (обязательный — тай-брейк + детерминизм мексикано); `@@unique([roundMatchId, userId])`; `@@index([userId])`; `@@map("player_match_score")`.

    cascade-дисциплина (Pattern 3): cascade ТОЛЬКО вниз по цепочке владения Tournament→Round→RoundMatch→PlayerMatchScore (и TournamentPlayer→Tournament). User-FK НЕ каскадировать (как existing Pair.player1).
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` завершается с кодом 0 (нет "missing an opposite relation field").
    - `grep -q "model TournamentPlayer" prisma/schema.prisma && grep -q "model Round" prisma/schema.prisma && grep -q "model RoundMatch" prisma/schema.prisma && grep -q "model PlayerMatchScore" prisma/schema.prisma` — все 4 модели присутствуют.
    - `grep -E "skillLevel\s+String\b" prisma/schema.prisma` находит non-nullable skillLevel (без `?`); `grep -q "birthDate\s\+DateTime?" prisma/schema.prisma`.
    - `grep -q "scoringMode" prisma/schema.prisma && grep -q "participantMode" prisma/schema.prisma && grep -q "targetPoints" prisma/schema.prisma` — поля Tournament на месте.
    - model Pair / model Match / model SetScore не изменены (git diff показывает изменения только в User/Tournament + 4 новых модели).
    - Prisma `enum` НЕ введён: `grep -c '^enum ' prisma/schema.prisma` == 0.
  </acceptance_criteria>
  <done>Схема валидна, содержит 4 новые модели и расширенные User/Tournament; playoff-модели нетронуты; нет Prisma enum.</done>
</task>

<task type="auto">
  <name>Task 2: Расширить skillLevels 4→5 + RU label-map; flip Better Auth required; добавить RU-лейбл progressing (DATA-01)</name>
  <files>src/lib/validation/auth.ts, src/lib/auth.ts, src/app/(public)/tournaments/[id]/page.tsx</files>
  <read_first>
    - src/lib/validation/auth.ts (skillLevels константа — единственная точка определения; потребляется register/profile-схемами и формами).
    - src/lib/auth.ts (additionalFields skillLevel required:false → требует flip).
    - src/app/(public)/tournaments/[id]/page.tsx (хардкоженный switch skillLevelLabel:28 — единственный label-switch).
    - .planning/phases/07-multiformat-data-model/07-RESEARCH.md §"Common Pitfalls" Pitfall 2 (required mismatch) + Pitfall 3 (label ripple), §"Code Examples" (точные значения константы + RU-map).
  </read_first>
  <action>
    (A) src/lib/validation/auth.ts: расширить `skillLevels` с 4 до 5 значений, вставив `"progressing"` МЕЖДУ `"beginner"` и `"intermediate"` → `["beginner", "progressing", "intermediate", "advanced", "pro"] as const`. Добавить co-located экспорт `skillLevelLabels: Record<(typeof skillLevels)[number], string>` со значениями: beginner→"новичок", progressing→"прогрессирующий", intermediate→"средний", advanced→"высокий", pro→"профессиональный" (RU-формулировки из CONTEXT — заменяют прежние начинающий/продвинутый/профессионал). registerSchema/loginSchema — НЕ трогать (skillLevel остаётся `.optional()` в register-схеме; форма register UX = Фаза 8/11, вне scope).

    (B) src/lib/auth.ts: в additionalFields изменить `skillLevel: { type: "string", required: false, input: true }` → `required: true`. (Pitfall 2 — иначе seed-signUp может не передать уровень и нарушить NOT NULL.) Остальные additionalFields (phone/nickname) — НЕ трогать.

    (C) src/app/(public)/tournaments/[id]/page.tsx: заменить хардкоженный `skillLevelLabel` switch на использование импортированного `skillLevelLabels` из `@/lib/validation/auth` (импорт добавить). Сохранить поведение `null/неизвестно → "—"` (например `skillLevelLabels[level as keyof typeof skillLevelLabels] ?? "—"`). Это добавляет лейбл для `progressing` и приводит RU-формулировки к CONTEXT-варианту. courtSideLabel — НЕ трогать.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "progressing" src/lib/validation/auth.ts` и константа содержит ровно 5 значений в порядке beginner, progressing, intermediate, advanced, pro.
    - `grep -q "skillLevelLabels" src/lib/validation/auth.ts` — RU-map экспортирована.
    - `grep -q "required: true" src/lib/auth.ts` в блоке skillLevel (additionalFields skillLevel теперь required).
    - `grep -q "прогрессирующий" src/app/(public)/tournaments/[id]/page.tsx` ЛИБО page импортирует skillLevelLabels и больше не содержит хардкод-switch с "начинающий".
    - `npx tsc --noEmit` проходит без ошибок (формы register-form/profile-form, читающие skillLevels, продолжают компилироваться — они рендерят значения из массива, новый тир добавляется автоматически).
    - registerSchema/loginSchema не изменены (git diff в auth.ts ограничен константой + новым label-map).
  </acceptance_criteria>
  <done>skillLevels = 5 латинских значений + RU label-map; Better Auth skillLevel required:true; страница турнира показывает RU-лейбл для progressing; typecheck зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signup/register → DB | Уровень/формат приходят как строки; валидируются app-слоем (zod-union) перед записью |
| Tournament → Round/RoundMatch/PlayerMatchScore | внутренняя цепочка владения; целостность через FK + cascade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | enum-подобные String-поля (format/scoringMode/participantMode/level/Round.status/teamSlot) | accept (schema) / mitigate (Phase 8) | Схема хранит String; валидация значения — zod `z.enum` на границе Server Action (Фаза 8). В этом плане поля документированы inline-комментарием допустимых значений; БД не enforce-ит — это сознательный Pitfall-9 паттерн. |
| T-07-02 | Information Disclosure | price-поле | accept | price — неденежное (отображение; оплата в клубе). Нет PII, нет платёжных данных. |
| T-07-03 | Tampering (integrity) | новые User-FK на RoundMatch/PlayerMatchScore/TournamentPlayer | mitigate | cascade ТОЛЬКО вниз по владению (Tournament→Round→RoundMatch→PlayerMatchScore, TournamentPlayer→Tournament); User-FK без cascade (как existing Pair.player1) — удаление юзера не стирает историю матчей молча. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a (mitigate by absence) | Фаза НЕ ставит пакетов (RESEARCH §Package Legitimacy Audit: zero installs). Нет surface для supply-chain. |
</threat_model>

<verification>
- `npx prisma validate` exits 0 (Task 1).
- `npx tsc --noEmit` exits 0 (Task 2).
- model Pair/Match/SetScore не изменены (git diff).
- Prisma enum не введён (`grep -c '^enum ' prisma/schema.prisma` == 0).
- Миграция/reset/сиды/тесты — в плане 07-02 (этот план только статические правки).
</verification>

<success_criteria>
- schema.prisma валидна, содержит TournamentPlayer/Round/RoundMatch/PlayerMatchScore + расширенные User/Tournament; playoff-стек нетронут.
- skillLevels = 5 латинских значений, RU label-map экспортирована, Better Auth skillLevel required:true, RU-лейбл progressing на странице турнира.
- Typecheck зелёный. Без новых зависимостей. Без Prisma enum.
</success_criteria>

<output>
Create `.planning/phases/07-multiformat-data-model/07-01-SUMMARY.md` when done
</output>

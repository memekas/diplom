---
phase: 13-auth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/tournament.ts
  - src/app/(public)/tournaments/page.tsx
  - src/app/(public)/tournaments/filter-bar.tsx
  - src/app/(public)/tournaments/tournaments.css
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "Desktop shows a dense aligned .trow grid (fixed status 118px + chevron 30px columns); phone reflows the same DOM to a card via @container, keeping all fields"
    - "Each row shows name, status badge, format tag + вид, level (lowercase), capacity X/size + progress, price (NNN ₽ / бесплатно), date (mono), location"
    - "A «Фильтры» button opens a 2×2 popover (Статус/Формат/Уровень/Вид) + search; the button shows a .fcount badge of active (non-all) facets; closes on outside-click"
    - "Filtering is SERVER-SIDE via searchParams → Prisma where; the existing ?status= param still works"
    - "Status seam colors come from tokens; readable at ~375px with no unintended horizontal scroll"
  artifacts:
    - path: "src/lib/services/tournament.ts"
      provides: "listTournaments extended with read-only facet where (status/format/level/mode/q) + registered-count for capacity"
      contains: "listTournaments"
    - path: "src/app/(public)/tournaments/filter-bar.tsx"
      provides: "Client leaf: popover open/close, outside-click, .fcount badge, facet submit to URL (no row hiding)"
      contains: "use client"
    - path: "src/app/(public)/tournaments/tournaments.css"
      provides: "Ported 004 classes (.trow/.list-head/.tr-*/.filter-*/.fcount/.sel-field/.search), token-only, @container reflow"
      contains: ".trow"
  key_links:
    - from: "src/app/(public)/tournaments/page.tsx"
      to: "listTournaments"
      via: "server-side facet query from searchParams"
      pattern: "listTournaments\\("
    - from: "src/app/(public)/tournaments/filter-bar.tsx"
      to: "searchParams"
      via: "facet submit (GET form or router.push) — NOT [hidden] row hiding"
      pattern: "use client"
    - from: "src/app/(public)/tournaments/page.tsx"
      to: "src/app/(public)/tournaments/tournaments.css"
      via: "import"
      pattern: "import.*tournaments\\.css"
---

<objective>
Restyle the public tournaments list onto Court (004 «Плотный список»): a dense aligned `.trow` grid on desktop that reflows to a card-per-row on phone via `@container`, with collapsed filters behind a «Фильтры» popover. Filtering is SERVER-SIDE (searchParams → Prisma `where`); the client leaf only manages the popover UI + facet submit. Add the minimal READ-ONLY data needed to render the design (facet `where` + a registered-count for capacity) — no schema/migrations/writes.

Purpose: Implements UI-03 — dense list + phone cards + collapsed-filter popover with active-count badge, server-side filtering (13-CONTEXT §Список; REQUIREMENTS UI-03).
Output: `listTournaments` extended (read-only) for facets + capacity count, the page restyled with server-side filtering, a new `filter-bar.tsx` client leaf, and a co-located `tournaments.css` (004 classes, token-only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-auth/13-CONTEXT.md
@.planning/phases/13-auth/13-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/lists-and-filters.md
@src/app/globals.css
</context>

<critical_constraints>
- Filtering is SERVER-SIDE. Do NOT port the sketch client `applyFilters`/`[hidden]` row-hide engine. Read facets from searchParams in the Server Component and build a Prisma `where`. The existing `?status=` param MUST keep working (validate against `tournamentStatuses` as today).
- Data-layer changes are READ-ONLY only: extend the `where` and add a registered-count. No schema/migration, no new/changed Server Actions, no writes. Keep `createTournament`/`getTournament` untouched.
- No hardcoded hex — status seams (`s-reg`→`--primary`, `s-prog`→`--badge-prog-fg`, `s-fin`→mix `--text-faint`), chevron masks, `.fmt-tag`, `.price-free` all via tokens + `color-mix`.
- Phone reflow keeps ALL fields (do NOT `display:none` формат/уровень) — reflow the same DOM via `grid-template-areas` under `@container (max-width: 780px)`. The grid is FIXED: `1.7fr 1fr .9fr 1.1fr .9fr 118px 30px` (status 118px + chevron 30px fixed). Wrapper gets `.cq`.
- Reuse the existing `<TournamentStatusBadge>` (already emits `.badge .badge-*`) for the status cell. Reuse label maps `formatLabels`/`tournamentKindLabels`/`skillLevelLabels` — do NOT re-derive RU labels.
- Screen-specific classes absent from `globals.css` (`.page-head/.ph-count/.filters/.filterbar/.search/.filter-btn/.fcount/.filter-panel/.sel-field/.sel/.list/.list-head/.list-rows/.trow/.tr-*/.fmt-tag/.price-free/.dot`) are ported 1:1 into `src/app/(public)/tournaments/tournaments.css`. Do NOT touch `globals.css`.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Extend listTournaments (READ-ONLY) for facet where + capacity count</name>
  <read_first>
    - src/lib/services/tournament.ts (current `tournamentSelect` + `listTournaments(opts?: { status })`; keep `createTournament`/`getTournament` untouched)
    - prisma/schema.prisma (Tournament columns: format/participantMode/level/price/date/location/size/status; relations `pairs` Pair[] + `tournamentPlayers` TournamentPlayer[] for the registered count)
    - src/lib/validation/tournament.ts (`tournamentStatuses`, `tournamentFormats`, `participantModes`), src/lib/validation/auth.ts (`skillLevels`)
  </read_first>
  <files>src/lib/services/tournament.ts</files>
  <action>
    Widen the `listTournaments` opts to an optional read-only filter shape: `{ status?, format?, level?, participantMode?, q? }`, each typed against the existing tuples (`TournamentStatus`, `tournamentFormats[]`, `skillLevels[]`, `participantModes[]`). Build a typed Prisma `where` that ANDs the present facets (omit any `all`/undefined facet so absence = no filter, preserving backward compat and the bare `?status=` path). For `q`, match name OR location with case-insensitive `contains`. Add a registered-count for capacity: include `_count: { select: { pairs: true, tournamentPlayers: true } }` in the select, so the page can derive registered = pairs + tournamentPlayers vs `size` (a pairs tournament counts Pair rows; a singles tournament counts TournamentPlayer rows — summing is safe since only one is non-zero per tournament). Keep `orderBy createdAt desc`. This is a read-only query extension — no writes, no schema change.
  </action>
  <verify>
    <automated>grep -q "_count" src/lib/services/tournament.ts && grep -q "tournamentPlayers" src/lib/services/tournament.ts && grep -q "listTournaments" src/lib/services/tournament.ts && npx tsc --noEmit</automated>
  </verify>
  <done>`listTournaments` accepts read-only facet opts (status/format/level/participantMode/q) building a typed `where`, returns a `_count` of pairs + tournamentPlayers for capacity, preserves the bare `?status=` path; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Port 004 list/filter CSS + build the server-filtering page (preserve ?status=)</name>
  <read_first>
    - src/app/(public)/tournaments/page.tsx (PRESERVE `?status=` validation against `tournamentStatuses`; Server Component reading `searchParams: Promise`)
    - .claude/skills/sketch-findings-diplom/sources/004-tournaments-list/index.html (CSS `.list-head/.trow` grid 238-270, `.tr-*` 272-295, phone reflow 300-327, `.filter-btn/.fcount/.filter-panel` 134-163; HTML rows 247-277, filter bar/panel 280-333)
    - .claude/skills/sketch-findings-diplom/references/lists-and-filters.md (§004 + collapsed filters 9-43, CSS 60-200, HTML 247-335; status option values are raw DB strings 334-336)
    - src/components/tournament-status-badge.tsx (already emits `.badge .badge-*` — reuse), src/lib/validation/auth.ts (`formatLabels`/`tournamentKindLabels`/`skillLevelLabels`)
  </read_first>
  <files>src/app/(public)/tournaments/tournaments.css, src/app/(public)/tournaments/page.tsx</files>
  <action>
    Create `tournaments.css` (plain global stylesheet) and port ONLY the 004 screen-specific classes absent from globals.css: `.page-head`/`.ph-count`, `.filters`/`.filterbar`/`.search`(+icon/clear)/`.filter-btn`(+`.on`)/`.fcount`/`.filter-panel`(+`.open`)/`.selrow`/`.sel-field`/`.sel`/`.filter-foot`, the `.list`/`.list-head`/`.list-rows`/`.trow`(+`.s-reg/.s-prog/.s-fin` seam via tokens + `color-mix`)/`.tr-main`/`.tr-sub`/`.dot`/`.tr-fmt`/`.fmt-tag`/`.tr-mode`/`.tr-lvl`/`.tr-cap`(+frac/progress)/`.tr-price`/`.price-free`/`.tr-status-cell`/`.tr-go`, with the FIXED grid `1.7fr 1fr .9fr 1.1fr .9fr 118px 30px` and the `@container (max-width: 780px)` `grid-template-areas` reflow (keep all fields) + `@container (max-width: 460px)` panel single-column. Token-mask chevrons; no hex.
    In `page.tsx`: import `./tournaments.css`; widen `searchParams` to read `q/status/format/level/mode`; keep the existing `status` validation against `tournamentStatuses`; validate `format`/`level`/`mode` against their tuples (drop unknown to undefined, treat `all`/absent as no filter); call `listTournaments(prisma, {…validated facets})`. Wrap in `.cq` → `.page-head` (eyebrow + `<h1>Турниры` + `.ph-count` showing filtered N of total M — fetch total via a second unfiltered `listTournaments` or a `tournament.count`) → render `<FilterBar/>` (the client leaf, Task 3, passed the current facet values) → `.list` with `.list-head` + `.list-rows` of `<Link class="trow s-…" href="/tournaments/{id}">` cells per the field list. Capacity cell: registered = `t._count.pairs + t._count.tournamentPlayers`, render `X/size` + `.progress` width `registered/size`. Price: `t.price ? "{price} ₽" : <span class="price-free">бесплатно`. Keep the empty state as `.empty`. Reuse `<TournamentStatusBadge>`.
  </action>
  <verify>
    <automated>grep -q '\.trow' 'src/app/(public)/tournaments/tournaments.css' && grep -q '1.7fr 1fr .9fr 1.1fr .9fr 118px 30px' 'src/app/(public)/tournaments/tournaments.css' && grep -q 'grid-template-areas' 'src/app/(public)/tournaments/tournaments.css' && grep -q 'tournamentStatuses' 'src/app/(public)/tournaments/page.tsx' && grep -q 'listTournaments' 'src/app/(public)/tournaments/page.tsx' && ! grep -nE '#[0-9a-fA-F]{3,8}\b' 'src/app/(public)/tournaments/tournaments.css'</automated>
  </verify>
  <done>tournaments.css has the fixed-grid `.trow` + container-query reflow (all fields kept); page reads validated facets from searchParams, server-filters via `listTournaments`, preserves `?status=`, renders the dense list with capacity X/size + progress + price; no hex.</done>
</task>

<task type="auto">
  <name>Task 3: Build the filter-bar client leaf (popover + outside-click + .fcount, facet submit to URL)</name>
  <read_first>
    - .claude/skills/sketch-findings-diplom/sources/004-tournaments-list/index.html (popover open/close + outside-click 898-907; `.fcount` non-all count; HTML filter bar/panel 280-333)
    - .claude/skills/sketch-findings-diplom/references/lists-and-filters.md (collapsed-filters 32-43; "real Next build is server-side; only badge + open/close + outside-click stay client; [hidden] trick is sketch-only" 241-243; status options = raw DB strings 334-336)
    - src/lib/validation/tournament.ts + src/lib/validation/auth.ts (facet option values + RU labels)
  </read_first>
  <files>src/app/(public)/tournaments/filter-bar.tsx</files>
  <action>
    Create `"use client"` `FilterBar` receiving the current facet values (status/format/level/mode/q) as props. Render the `.filters` → `.filterbar` (`.search` input + a `.filter-btn` «Фильтры» with the `.fcount` badge) and the `.filter-panel` 2×2 `.sel-field` selects (Статус/Формат/Уровень/Вид) + `.filter-foot`. Behavior (the ONLY client logic): toggle the panel open/close (`.open` on panel, `.on` on the button, `aria-expanded`/`aria-controls`), close on outside-click (document listener checking the panel ref does not contain the target), and compute `.fcount` = number of non-`all` facets (hidden at 0). Submit facets to the URL so the Server Component re-queries — use a GET `<form>` whose selects/search carry the param names (`status`/`format`/`level`/`mode`/`q`) OR `useRouter().push` with updated searchParams on change/apply; the `<Сбросить фильтры>` clears to the base URL. Status `<option value>` are raw DB strings (`registration`/`in_progress`/`finished`) with RU labels; format/level/mode values are the DB enum values mapped to RU labels via the label maps. Do NOT hide rows client-side; do NOT port `[hidden]`.
  </action>
  <verify>
    <automated>grep -q '"use client"' 'src/app/(public)/tournaments/filter-bar.tsx' && grep -q 'aria-expanded' 'src/app/(public)/tournaments/filter-bar.tsx' && grep -q 'fcount' 'src/app/(public)/tournaments/filter-bar.tsx' && ! grep -q 'hidden = ' 'src/app/(public)/tournaments/filter-bar.tsx' && npx tsc --noEmit</automated>
  </verify>
  <done>FilterBar is a client leaf rendering the search + «Фильтры» popover (2×2 facet selects), toggles open/close with outside-click, shows the `.fcount` active-facet badge, and submits facets to searchParams (no client row hiding); tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → Server Component query | untrusted facet/search input crosses here into the Prisma where |
| anonymous browser → public list | no auth; list is public-read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | searchParams facet input | mitigate | Validate every facet against its existing tuple (`tournamentStatuses`/`tournamentFormats`/`skillLevels`/`participantModes`) before building the Prisma `where`; unknown values drop to undefined (no filter). `q` flows only through Prisma's parameterized typed `contains` (no raw SQL). Mirrors the existing `?status=` validation. |
| T-13-07 | Information Disclosure | list read | accept | Tournament columns + registered counts are already public (no PII/credentials); `_count` exposes only aggregate registration totals. Public-read by design (CONTEXT). |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies installed (restyle uses existing libs). No package-install tasks. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx next build` compiles `/tournaments`.
- `grep -rE '#[0-9a-fA-F]{3,8}\b' 'src/app/(public)/tournaments/tournaments.css'` returns nothing outside comments.
- Page still validates `status` against `tournamentStatuses` and still works for `?status=finished`.
- `listTournaments` returns `_count` and accepts facet opts; `createTournament`/`getTournament` unchanged.
- filter-bar contains no `[hidden]` row-hiding; submits facets to the URL.
</verification>

<success_criteria>
UI-03 satisfied: dense aligned list on desktop, card-per-row on phone (all fields kept), a «Фильтры» popover (Статус/Формат/Уровень/Вид) + search with an active-filter count badge, server-side filtering via searchParams (existing `?status=` preserved), readable at ~375px with no unintended horizontal scroll.
</success_criteria>

<artifacts_produced>
- Extended (read-only) `listTournaments` opts: facet `where` (status/format/level/participantMode/q) + `_count` of pairs + tournamentPlayers for capacity.
- New file: `src/app/(public)/tournaments/filter-bar.tsx` (client leaf: popover, outside-click, `.fcount`, facet→URL submit).
- New file: `src/app/(public)/tournaments/tournaments.css` (004 screen-specific classes, token-only, `@container` reflow).
- No new Server Actions; no schema/migration; no additions to `globals.css`.
</artifacts_produced>

<output>
Create `.planning/phases/13-auth/13-03-SUMMARY.md` when done.
</output>

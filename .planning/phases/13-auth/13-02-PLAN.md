---
phase: 13-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/profile/page.tsx
  - src/app/(app)/profile/profile-form.tsx
  - src/app/(app)/profile/profile.css
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "Profile renders a Court player-pass idcard: initials avatar, name <h1>, @nickname mono, level + side chips"
    - "Email + phone appear read-only in the idcard contact strip; Email carries a «логин» ro-tag"
    - "Edit form edits real v2.0 fields (Имя, Никнейм, Сторона, Телефон, Уровень, Дата рождения); Email rendered read-only"
    - "Сторона корта is a 3-button .seg writing a hidden input name=courtSide; Save is diff-gated; form re-locks on success"
    - "updateProfileAction + parseProfileForm + field names + email-via-changeEmail flow unchanged; readable at ~375px"
  artifacts:
    - path: "src/app/(app)/profile/profile.css"
      provides: "Ported 006 classes (.idcard/.id-avatar/.id-chip/.seg/.select-wrap/.ro-tag/.changed-dot/.net-rule), token-only"
      contains: ".idcard"
    - path: "src/app/(app)/profile/profile-form.tsx"
      provides: "Court profile form: edit-toggle, diff-gated Save, courtSide .seg, preserved action"
      contains: "updateProfileAction"
    - path: "src/app/(app)/profile/page.tsx"
      provides: "Court idcard + read-only contact strip; preserved requireUser guard + getProfile read"
      contains: "getProfile"
  key_links:
    - from: "src/app/(app)/profile/page.tsx"
      to: "src/app/(app)/profile/profile.css"
      via: "import"
      pattern: "import.*profile\\.css"
    - from: "src/app/(app)/profile/profile-form.tsx"
      to: "courtSide"
      via: "hidden input mirrored by the seg"
      pattern: "name=\\\"courtSide\\\""
    - from: "src/app/(app)/profile/page.tsx"
      to: "requireUser"
      via: "auth guard"
      pattern: "requireUser"
---

<objective>
Restyle the profile screen onto Court (006 «Карточка + форма»): a "player-pass" `.idcard` on top, a dashed `.net-rule`, then the edit form. The form edits the real v2.0 fields; Email is read-only (login). Edit-toggle / diff-gated Save / per-field changed-dot / courtSide `.seg` are a client leaf over the EXISTING `updateProfileAction`.

Purpose: Implements UI-05 — profile in Court with player-pass + real-field edit form + read-only Email login (13-CONTEXT §Профиль; REQUIREMENTS UI-05).
Output: Profile page + form leaf restyled, co-located `profile.css` (006 screen-specific classes, token-only); auth guard, `getProfile`, `updateProfileAction`, `parseProfileForm`, field names, and the Better Auth changeEmail flow all unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-auth/13-CONTEXT.md
@.planning/phases/13-auth/13-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/forms-and-auth.md
@src/app/globals.css
</context>

<critical_constraints>
- Restyle-only. Do NOT change the `requireUser()` guard (redirect-only-on-Unauthorized/Forbidden), the `getProfile(prisma, user.id)` read, the `useActionState` + `parseProfileForm` + `updateProfileAction` flow, any field `name`, the RU error mapping, or the `pending` label swap. The action contract (incl. `email` routed via Better Auth `changeEmail`) is unchanged.
- Email is read-only (login). Render it in the idcard contact strip with `<span class="ro-tag">логин</span>`. Keep the existing `email` form `<input>` present but read-only/`disabled` so the action payload shape is unchanged — do NOT delete it, do NOT make it an enabled editable input.
- `courtSide` moves from `<select>` to the 006 `.seg` (3 `<button aria-pressed>` + hidden `<input name="courtSide">`). The hidden input KEEPS `name="courtSide"` so the action payload is identical. Do NOT drop `nickname`.
- No hardcoded hex — only Court tokens + `color-mix`. Select-chevron is the token CSS-mask (`var(--text-muted)`→`var(--ring)` on focus).
- Wrapper gets `.cq`; breakpoints use `@container`, not `@media`.
- Screen-specific classes absent from `globals.css` (`.idcard/.id-avatar/.id-handle/.id-chips/.id-chip/.id-contact/.seg/.select-wrap/.ro-tag/.changed-dot/.net-rule`) are ported 1:1 into `src/app/(app)/profile/profile.css`. Do NOT touch `globals.css`.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Port 006 profile screen CSS into a co-located profile.css (token-only)</name>
  <read_first>
    - .claude/skills/sketch-findings-diplom/sources/006-profile/index.html (CSS: `.net-rule` 16, `.idcard`/`::before`/`::after` 28-44, `.id-avatar` 45, `.id-handle` 58-64, `.id-chips/.id-chip` 65-78, `.id-contact` 81-84, `.ro-tag` 85, `.changed-dot`/`.is-changed` 99-103, `.select-wrap` 107-115, `.seg` 119-133, phone breakpoint 187-190)
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (aria-pressed seg 117-130, read-only field/`.ro-tag` 176-185, token chevron 81-93)
    - src/app/globals.css (do NOT re-port `.meta*`/`.field/.input/.btn*/.card/.eyebrow/.pill/.avatar`)
  </read_first>
  <files>src/app/(app)/profile/profile.css</files>
  <action>
    Create `src/app/(app)/profile/profile.css` as a plain global stylesheet (App Router permits global CSS imported into a page). Port ONLY the 006 screen-specific classes absent from globals.css: `.net-rule`, `.idcard` + `::before`/`::after` court motif, `.id-avatar`, `.id-name`/`.id-handle`, `.id-chips` + `.id-chip` (+`.id-chip.accent` + `.ck`), `.id-contact` + its `.meta-key`/`.meta-val`/`.mono` overrides, `.ro-tag`, `.changed-dot` + `.field.is-changed .changed-dot`, the `.select-wrap` token-mask chevron, and the three-column `.seg` button group (`button[aria-pressed="true"]` → `--primary-soft`/`--primary`; `.seg.locked`). Wrap phone tweaks (006 187-190) in `@container (max-width: …)`, NOT `@media`. All colors via Court tokens + `color-mix`; no hex literals.
  </action>
  <verify>
    <automated>grep -q '\.idcard' 'src/app/(app)/profile/profile.css' && grep -q '\.ro-tag' 'src/app/(app)/profile/profile.css' && grep -q 'aria-pressed' 'src/app/(app)/profile/profile.css' && grep -q '\.changed-dot' 'src/app/(app)/profile/profile.css' && ! grep -nE '#[0-9a-fA-F]{3,8}\b' 'src/app/(app)/profile/profile.css'</automated>
  </verify>
  <done>profile.css has `.idcard/.id-*/.seg/.select-wrap/.ro-tag/.changed-dot/.net-rule` ported; container-query breakpoints; no hex literals.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle profile page to the Court player-pass idcard (preserve guard + getProfile)</name>
  <read_first>
    - src/app/(app)/profile/page.tsx (PRESERVE requireUser try/catch 10-21 + `getProfile(prisma, user.id)`; `initial` prop shape 33-44)
    - .claude/skills/sketch-findings-diplom/sources/006-profile/index.html (idcard HTML 214-241; ro-tag rows 236-238)
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (player-pass + contact strip 306-327)
    - src/lib/validation/profile.ts (`courtSideLabels`), src/lib/validation/auth.ts (`skillLevelLabels`)
  </read_first>
  <files>src/app/(app)/profile/page.tsx</files>
  <action>
    Keep the Server Component, `requireUser()` guard, and `getProfile` read byte-for-byte. Import `./profile.css`. Replace the utility `<main>` with a `.cq` wrapper → `.card card-pad idcard` header: `.id-avatar` with initials from `profile.name`, `.eyebrow` "Личный кабинет", `<h1>{profile.name}`, `.id-handle mono` `@{profile.nickname}`, then `.id-chips` (`.id-chip accent` label "Уровень" value `skillLevelLabels[profile.skillLevel]` + `.id-chip` label "Сторона" value `courtSideLabels[profile.courtSide]`); then `.id-contact meta` with an Email `.meta-row` (`.meta-val mono` = email + `<span class="ro-tag">логин</span>`) and a Телефон `.meta-row` (value + `<span class="ro-tag">скрыт от соперников</span>`, em-dash when empty). Follow with `<hr class="net-rule">`, then `<ProfileForm initial={…}/>` with the SAME `initial` object (unchanged shape, birthDate still sliced to yyyy-MM-dd).
  </action>
  <verify>
    <automated>grep -q "requireUser" 'src/app/(app)/profile/page.tsx' && grep -q "getProfile" 'src/app/(app)/profile/page.tsx' && grep -q "idcard" 'src/app/(app)/profile/page.tsx' && grep -q "ro-tag" 'src/app/(app)/profile/page.tsx' && grep -q "profile.css" 'src/app/(app)/profile/page.tsx'</automated>
  </verify>
  <done>Profile page renders the Court idcard (avatar/name/@handle/level+side chips + read-only Email/Телефон strip with ro-tags) above the form; guard + getProfile + initial-prop shape unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restyle profile-form leaf with courtSide .seg + edit-toggle + diff-gated Save (preserve action)</name>
  <read_first>
    - src/app/(app)/profile/profile-form.tsx (PRESERVE useActionState, parseProfileForm, updateProfileAction, every `name` incl. `email`/`courtSide`, errors mapping, `pending` label swap, `state.ok` saved state)
    - .claude/skills/sketch-findings-diplom/sources/006-profile/index.html (edit form 255-320; seg 291-302; toggle/diff/changed-dot JS 491-610 — port BEHAVIOR, not vanilla DOM)
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (§006 editable fields 66-70; read-only email rule 364)
    - src/lib/validation/profile.ts (`courtSides`/`courtSideLabels`), src/lib/validation/auth.ts (`skillLevels`/`skillLevelLabels`)
  </read_first>
  <files>src/app/(app)/profile/profile-form.tsx</files>
  <action>
    Keep the `useActionState`/`parseProfileForm`/`updateProfileAction` flow intact. Restyle to Court `.field`/`.label`/`.input`. Replace the courtSide `<select>` with the 006 `.seg`: three `<button type="button" aria-pressed>` (Левая/Правая/Любая from `courtSides`/`courtSideLabels`) driven by local `useState` from `initial.courtSide`, plus a hidden `<input type="hidden" name="courtSide">` mirroring the selection (keeps `name="courtSide"`). Wrap the skillLevel `<select class="input">` in `.select-wrap`. Render `email` as read-only/`disabled` (keep `name="email"`, `defaultValue={initial.email}`). Add an edit-toggle: a `Редактировать` button enables inputs + seg (inputs start disabled in read mode); track per-field diffs vs `initial` via local state, set `.field.is-changed` on changed fields (drives `.changed-dot`); Save (`.btn .btn-primary`) is `disabled` until a field diffs OR while `pending`, label `pending ? "Сохраняем…" : "Сохранить"`. On `state.ok` re-lock to read mode + reset the diff baseline. Keep the `state.ok` success and `errors.form` rendered via `.error`. Do NOT change which fields submit.
  </action>
  <verify>
    <automated>grep -q "updateProfileAction" 'src/app/(app)/profile/profile-form.tsx' && grep -q "parseProfileForm" 'src/app/(app)/profile/profile-form.tsx' && grep -q 'name="courtSide"' 'src/app/(app)/profile/profile-form.tsx' && grep -q 'aria-pressed' 'src/app/(app)/profile/profile-form.tsx' && grep -q 'name="email"' 'src/app/(app)/profile/profile-form.tsx'</automated>
  </verify>
  <done>Profile form uses Court `.field/.input`, a courtSide `.seg` writing a hidden `name="courtSide"` input, a read-only `email` input, edit-toggle + diff-gated Save + changed-dots, and re-locks on `state.ok`; action/parse/field names unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → updateProfileAction | profile edits cross here; action + parseProfileForm re-validate server-side, unchanged by restyle |
| session cookie → requireUser | identity boundary; only requireUser() derives the user id, never client input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Elevation of Privilege | profile identity | mitigate | Identity stays from `requireUser()` (session cookie) → `getProfile(prisma, user.id)`; restyle introduces no client-supplied user id. Guard preserved verbatim. |
| T-13-04 | Tampering | courtSide seg → hidden input | accept | The seg only mirrors a value into the existing `name="courtSide"` field; `parseProfileForm`/`profileSchema` still validates it (`z.enum(courtSides)`). No new accepted field. |
| T-13-05 | Tampering | read-only email input | mitigate | Email read-only is client UX; the real boundary is the action routing email via Better Auth `changeEmail` (unchanged). |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies installed (restyle uses existing libs). No package-install tasks. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx next build` compiles `/profile`.
- `grep -rE '#[0-9a-fA-F]{3,8}\b' 'src/app/(app)/profile/'` returns nothing outside comments.
- profile-form contains `updateProfileAction`, `parseProfileForm`, `name="courtSide"`, `name="email"`, `name="nickname"`.
- profile page contains `requireUser` + `getProfile`.
</verification>

<success_criteria>
UI-05 satisfied: profile shows the Court player-pass idcard (name, @nickname, level + side chips) and an edit form over the real fields with Email as a read-only login; edit-toggle + diff-gated Save behave correctly; readable at ~375px; the v2.0 action contract is unchanged.
</success_criteria>

<artifacts_produced>
- New file: `src/app/(app)/profile/profile.css` (006 screen-specific classes, token-only).
- New client behavior in `profile-form.tsx`: edit-toggle, per-field diff/changed-dot, courtSide `.seg` → hidden input.
- No new query/service functions (reuses `getProfile`/`updateProfileAction`).
- No additions to `globals.css`.
</artifacts_produced>

<output>
Create `.planning/phases/13-auth/13-02-SUMMARY.md` when done.
</output>

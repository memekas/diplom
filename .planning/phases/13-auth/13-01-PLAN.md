---
phase: 13-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(auth)/register/register-form.tsx
  - src/app/(auth)/auth.css
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Login route renders a Court tab-card (≤440px) with Вход/Регистрация tabs; Вход is active"
    - "Register route renders the same Court tab-card with Регистрация active and all v2.0 register fields"
    - "Password fields have a working reveal-eye toggle; field errors render in the Court .error style"
    - "Login still authenticates via authClient.signIn.email and routes to /dashboard; register still signs up via authClient.signUp.email with the unchanged field set + RU error.code mapping"
    - "Both screens are readable at ~375px (card reflows, no unintended horizontal scroll)"
  artifacts:
    - path: "src/app/(auth)/auth.css"
      provides: "Ported 008 screen-specific classes (.modeseg/.cardA/.pw/.reveal/.sel-wrap) — token-only colors"
      contains: ".modeseg"
    - path: "src/app/(auth)/login/login-form.tsx"
      provides: "Court-restyled login form with reveal-eye, preserved signIn logic"
      contains: "authClient.signIn.email"
    - path: "src/app/(auth)/register/register-form.tsx"
      provides: "Court-restyled register form, preserved signUp + error.code mapping"
      contains: "authClient.signUp.email"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/app/(auth)/auth.css"
      via: "import"
      pattern: "import.*auth\\.css"
    - from: "src/app/(auth)/login/page.tsx"
      to: "/register"
      via: "modeseg tab Link"
      pattern: "href=\\\"/register\\\""
    - from: "src/app/(auth)/register/page.tsx"
      to: "/login"
      via: "modeseg tab Link"
      pattern: "href=\\\"/login\\\""
---

<objective>
Restyle the login and register screens onto the Court design language as a single tab-card (008 «Карточка с вкладками»), consuming the Phase 12 token + `_base` component layer. Keep TWO routes (`(auth)/login`, `(auth)/register`); the Вход/Регистрация tabs are `<Link>`s between routes, NOT the sketch's in-place `body.is-login` toggle.

Purpose: Implements UI-02 — auth screens in Court, readable on desktop and ~375px, with the locked "two routes preserved" decision (13-CONTEXT §Auth).
Output: Both auth pages + form leaves restyled, a co-located `auth.css` carrying the 008 screen-specific classes (token-only), with all v2.0 auth behavior (Server Actions, field names, Zod messages, error.code mapping) unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-auth/13-CONTEXT.md
@.planning/phases/13-auth/13-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/forms-and-auth.md
@src/app/globals.css
</context>

<critical_constraints>
- Restyle-only. Do NOT change Server Actions, field `name` attributes, Zod schemas (`loginSchema`/`registerSchema`), RU messages, the `authClient.signIn/signUp` calls, the `error.code` mapping, `noValidate`, or the `router.push("/dashboard")` + `router.refresh()` flow.
- Two routes preserved: tabs are `<Link href="/login">` / `<Link href="/register">`; active tab = current route (`.on`). Do NOT port the sketch `setMode()` / `body.is-login` JS, and do NOT merge the routes.
- `courtSide` is NOT collected at signup (forms-and-auth.md "What to Avoid"). Do not add it. Do not drop the `nickname` field. `skillLevel` stays a required select.
- No hardcoded hex anywhere — only Court tokens (`--primary/--accent/--danger/--text*/--ring/--surface*` + `color-mix`). The select-chevron is a CSS-mask filled by `var(--text-muted)` → `var(--ring)` on focus, never a baked SVG color.
- Each screen wrapper gets `.cq` (Phase 12 utility) and uses `@container (max-width: …)`, never `@media`.
- Screen-specific classes that are NOT in `globals.css` (`.modeseg/.cardA*/.pw/.reveal/.sel-wrap`) are ported 1:1 into the co-located `src/app/(auth)/auth.css`. Do NOT rename them. Do NOT touch `globals.css`.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Port 008 auth screen CSS into a co-located auth.css (token-only)</name>
  <read_first>
    - .claude/skills/sketch-findings-diplom/sources/008-auth/index.html (CSS `.modeseg` 71-92, `.pw/.reveal` 97-106, `.sel-wrap` 111-119, `.reg-only/.login-only` 122-124 — NOT ported, see action, `.cardA*` 135-147, phone breakpoint 231-232; HTML head 257-275)
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (CSS Patterns: password reveal 187-197, token select chevron 81-93, read-only field 176-185)
    - src/app/globals.css (confirm which classes already exist in @layer components — do NOT re-port `.field/.label/.input/.hint/.error/.btn*/.card/.eyebrow`)
  </read_first>
  <files>src/app/(auth)/auth.css</files>
  <action>
    Create `src/app/(auth)/auth.css` as a plain global stylesheet (App Router permits importing global CSS into a page). Port ONLY the 008 screen-specific classes that are absent from globals.css: `.cardA`, `.cardA-wrap`, `.cardA-head`, `.cardA-top`, `.cardA-body`, `.cardA-tabs`, `.cardA-seam`, `.cardA h1`, `.cardA .sub`, the `.modeseg` tablist + `.modeseg button` + `.modeseg button.on` (active tab), `.pw` + `.pw .input` + `.pw .reveal` (+ hover, + svg), and the `.sel-wrap` select-chevron (mask filled by `var(--text-muted)`, `:focus-within` → `var(--ring)`). Do NOT port `.reg-only`/`.login-only` or `body.is-login` rules — the two-routes model replaces that toggle. Wrap the screen-specific breakpoint tweaks (008 lines 231-232) in `@container (max-width: …)`, NOT `@media`. Every color resolves to a Court token (`--primary/--accent/--danger/--text-muted/--text-faint/--ring/--surface/--surface-2/--border*` + `color-mix`); no hex literals.
  </action>
  <verify>
    <automated>grep -q '\.modeseg' 'src/app/(auth)/auth.css' && grep -q '\.cardA' 'src/app/(auth)/auth.css' && grep -q '\.pw .reveal' 'src/app/(auth)/auth.css' && ! grep -nE '#[0-9a-fA-F]{3,8}\b' 'src/app/(auth)/auth.css'</automated>
  </verify>
  <done>auth.css exists with `.modeseg/.cardA*/.pw/.reveal/.sel-wrap` ported; no `body.is-login`/`.reg-only` toggle rules; no hex literals; container-query (not media) breakpoints.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle login page shell + login-form leaf to the Court tab-card (preserve signIn logic)</name>
  <read_first>
    - src/app/(auth)/login/page.tsx (current raw `<main>` + `<h1>Вход` + link)
    - src/app/(auth)/login/login-form.tsx (PRESERVE: loginSchema.safeParse, authClient.signIn.email, setErrors form mapping, router.push/refresh, `email`/`password` names, noValidate, submitting label swap)
    - .claude/skills/sketch-findings-diplom/sources/008-auth/index.html (HTML card head + tabs 257-275; login field block + `.pw` reveal markup ~291-310)
  </read_first>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/login/login-form.tsx</files>
  <action>
    In `page.tsx`: import `./auth.css`; replace the utility-class `<main>` with the `.cq` wrapper → `.cardA-wrap` → `.card cardA` court card containing `.cardA-head` (brand lockup + `.modeseg role="tablist"` whose two tabs are `<Link href="/login">Вход` with class `on` and `<Link href="/register">Регистрация`), a `.net-rule cardA-seam`, then `.cardA-body` rendering `<LoginForm/>`. Keep it a Server Component. In `login-form.tsx`: keep all state/handler logic byte-for-byte; only swap presentation — form gets `.authform` (or a `.field` stack), each input becomes `<div class="field"><label class="label">…<input class="input">` ; render the per-field error as `<span class="error">` and the form-level error as `<p class="error">`; wrap the password input in `.pw` with a `.reveal` button (type="button", aria-label "Показать пароль") that toggles the input `type` between `password`/`text` via local `useState` (this is the only new client behavior — a reveal toggle, no logic change); submit is `.btn .btn-primary .btn-block` with the existing `submitting ? "Вход…" : "Войти"` swap. `autocomplete=current-password` stays. Do NOT add fields.
  </action>
  <verify>
    <automated>grep -q "authClient.signIn.email" 'src/app/(auth)/login/login-form.tsx' && grep -q 'btn-primary' 'src/app/(auth)/login/login-form.tsx' && grep -q 'modeseg' 'src/app/(auth)/login/page.tsx' && grep -q 'href="/register"' 'src/app/(auth)/login/page.tsx' && grep -q "import \"./auth.css\"" 'src/app/(auth)/login/page.tsx'</automated>
  </verify>
  <done>Login page renders the `.cardA` tab-card with the active Вход tab + `<Link>` to /register; login-form uses `.field/.input/.btn-primary/.error` and a working `.pw .reveal` toggle; `signIn.email`, error mapping, and router flow unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restyle register page shell + register-form leaf to the Court tab-card (preserve signUp logic)</name>
  <read_first>
    - src/app/(auth)/register/page.tsx (current raw `<main>` + `<h1>Регистрация` + link)
    - src/app/(auth)/register/register-form.tsx (PRESERVE: registerSchema.safeParse with the full field set, authClient.signUp.email payload, the `error.code` → RU mapping (FAILED_TO_CREATE_USER / USER_ALREADY_EXISTS_USE_ANOTHER_EMAIL), `skillLevels`/`skillLevelLabels` options, all `name` attrs, noValidate, submitting label swap)
    - .claude/skills/sketch-findings-diplom/sources/008-auth/index.html (register field block 291-346; `.sel-wrap` skill select 334; optional markers `<span class="opt">` 323/327)
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (§008 register field set 75-77)
  </read_first>
  <files>src/app/(auth)/register/page.tsx, src/app/(auth)/register/register-form.tsx</files>
  <action>
    In `page.tsx`: import `./auth.css`; same `.cardA` shell as login but the active `.modeseg` tab is Регистрация (`.on`) and the other tab `<Link href="/login">Вход`; body renders `<RegisterForm/>`. In `register-form.tsx`: preserve all handler/state logic and the full field set in order (email, пароль, имя, никнейм, телефон, дата рождения, уровень); only restyle — wrap each in `.field`/`.label`/`.input`; password gets the `.pw` + `.reveal` toggle (local useState, same as login) and a `.hint` "Минимум 8 символов"; optional fields (Телефон, Дата рождения) mark optionality with `<span class="opt">(необязательно)</span>` inside the `.label`; nickname gets a `.hint` "Уникальный, 3–30 символов: буквы, цифры, _ и -"; the skill `<select class="input">` is wrapped in `.sel-wrap` (token-mask chevron) and keeps `defaultValue=""`, the disabled placeholder option, `required`, and the `skillLevels.map` options; per-field errors → `<span class="error">`, form error → `<p class="error">`; submit `.btn .btn-primary .btn-block` with the existing `submitting ? "Создаём аккаунт…" : "Зарегистрироваться"`. Do NOT add `courtSide`. Do NOT drop `nickname`.
  </action>
  <verify>
    <automated>grep -q "authClient.signUp.email" 'src/app/(auth)/register/register-form.tsx' && grep -q "FAILED_TO_CREATE_USER" 'src/app/(auth)/register/register-form.tsx' && grep -q 'name="nickname"' 'src/app/(auth)/register/register-form.tsx' && ! grep -q 'name="courtSide"' 'src/app/(auth)/register/register-form.tsx' && grep -q 'href="/login"' 'src/app/(auth)/register/page.tsx'</automated>
  </verify>
  <done>Register page renders the `.cardA` tab-card with the active Регистрация tab + `<Link>` to /login; register-form uses Court field classes + `.pw .reveal` + `.sel-wrap` skill select; `signUp.email`, error.code mapping, full field set, and absence of courtSide all preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → authClient | credentials cross here; handled by EXISTING Better Auth client + Zod, unchanged by this restyle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | login/register forms | accept | No new attack surface — Server Actions, field set, Zod schemas, and `authClient.signIn/signUp` are reused verbatim; this plan only swaps CSS classes and adds a client-only password-reveal toggle (no data path). |
| T-13-02 | Information Disclosure | password reveal toggle | accept | Reveal only flips the local input `type` attribute; value is never logged, transmitted differently, or persisted. Standard show-password UX. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies installed in this plan (restyle uses existing libs only). No package-install tasks. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx next build` compiles the `(auth)` routes.
- `grep -rE '#[0-9a-fA-F]{3,8}\b' src/app/(auth)/` returns nothing outside comments (no hardcoded hex in auth screens/CSS).
- Login form still contains `authClient.signIn.email`; register still contains `authClient.signUp.email` + `FAILED_TO_CREATE_USER`.
- `name="courtSide"` absent from register-form; `name="nickname"` present.
</verification>

<success_criteria>
UI-02 satisfied: login and register render the Court tab-card with Вход/Регистрация `<Link>` tabs (active = current route); fields, error states (`.error`), and disabled/submitting states are readable on desktop and ~375px; all v2.0 auth behavior is unchanged.
</success_criteria>

<artifacts_produced>
- New file: `src/app/(auth)/auth.css` (008 screen-specific classes: `.modeseg/.cardA*/.pw/.reveal/.sel-wrap`, token-only).
- New client behavior: password reveal-eye toggle (local `useState` inside `login-form.tsx` and `register-form.tsx`).
- No new query/service functions (auth reuses existing Better Auth client).
- No new CSS classes beyond the ported 008 names; no additions to `globals.css`.
</artifacts_produced>

<output>
Create `.planning/phases/13-auth/13-01-SUMMARY.md` when done.
</output>

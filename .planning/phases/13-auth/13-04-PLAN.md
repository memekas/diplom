---
phase: 13-auth
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/dashboard.ts
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/dashboard/dashboard.css
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "Dashboard shows an identity header (.who: avatar + name + @USER-NN/level/side pills) with Профиль + Найти турнир CTAs"
    - "«Мои турниры» is grouped into three sections Активные / Предстоящие / Завершённые with per-section eyebrow + count"
    - "Each tournament is a .tcard showing the player's role (пара с Имя + partner avatar, OR Одиночный) + status badge + format·вид·место"
    - "State-appropriate right side: active → round-progress + primary CTA; upcoming → ghost; finished → place + ghost; CTAs link to existing routes"
    - "Participation read is READ-ONLY over existing models, scoped to requireUser() id; readable at ~375px"
  artifacts:
    - path: "src/lib/services/dashboard.ts"
      provides: "READ-ONLY getMyTournaments(prisma, userId): pairs + tournamentPlayer participation, partner, status group, best-effort round progress"
      contains: "getMyTournaments"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Court sectioned dashboard; preserved requireUser guard"
      contains: "requireUser"
    - path: "src/app/(app)/dashboard/dashboard.css"
      provides: "Ported 005 classes (.lkA/.who/.head-cta/.sec-head/.tcard/.tc-*/.place/.net-rule), token-only"
      contains: ".tcard"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "getMyTournaments"
      via: "read-only participation query scoped to session user"
      pattern: "getMyTournaments"
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "requireUser"
      via: "auth guard (session-scoped)"
      pattern: "requireUser"
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "src/app/(app)/dashboard/dashboard.css"
      via: "import"
      pattern: "import.*dashboard\\.css"
---

<objective>
Restyle the ЛК-dashboard onto Court (005 «Мои турниры»): an identity header, then three status sections (Активные / Предстоящие / Завершённые) of `.tcard`s showing the player's role + a state-appropriate CTA. The v2.0 page only greeted the user, so add a READ-ONLY participation read over EXISTING models (scoped to the session user) — no schema/migrations/writes.

Purpose: Implements UI-04 — «Мои турниры» by section with player role + CTAs in Court (13-CONTEXT §ЛК-дашборд; REQUIREMENTS UI-04).
Output: A new read-only `dashboard.ts` service, the dashboard page restyled with sectioned cards, and a co-located `dashboard.css` (005 classes, token-only). The `requireUser()` guard is unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-auth/13-CONTEXT.md
@.planning/phases/13-auth/13-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/lists-and-filters.md
@src/app/globals.css
</context>

<critical_constraints>
- Data read is READ-ONLY over EXISTING models. No schema/migration, no new/changed Server Actions, no writes, no business-logic change. Scope EVERY read to the SESSION USER id from `requireUser()` — never client input (authz boundary).
- Preserve the `requireUser()` guard (redirect-only-on-Unauthorized/Forbidden) verbatim.
- Scope rule (CONTEXT §ЛК-дашборд + PATTERNS RISK): show role + status grouping for ALL of the player's tournaments cheaply. Round-progress «N/M» is best-effort for round-based formats (round_robin/americano/mexicano via `Round`); when not cheaply derivable (e.g. playoff has no `Round` rows), DEGRADE gracefully — render the card without the round block (or with a plain status), and note the omission in the SUMMARY as deferred. Do NOT fabricate write/business logic to synthesize progress.
- No hardcoded hex — accent bars (`is-active`→`--primary`, `is-upcoming`→`--accent`, `is-finished`→`--text-faint`), `.place` medal, all via tokens + `color-mix`. Reuse `<TournamentStatusBadge>` (`.badge .badge-*`) and label maps `formatLabels`/`tournamentKindLabels`/`skillLevelLabels`/`courtSideLabels`.
- Wrapper gets `.cq`; breakpoints use `@container`, not `@media`. CTAs link only to EXISTING routes (`/tournaments/{id}`, `/tournaments`, `/profile`).
- Screen-specific classes absent from `globals.css` (`.lkA/.who/.who-name/.who-sub/.head/.head-cta/.sec/.sec-head/.sec-count/.sec-line/.tlist/.tcard/.tc-main/.tc-top/.tc-name/.tc-meta/.tc-role/.solo/.rk/.tc-side/.tc-round/.tr-val/.tr-key/.place/.p-rank/.p-of/.net-rule`) are ported 1:1 into `src/app/(app)/dashboard/dashboard.css`. Do NOT touch `globals.css`.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Build the READ-ONLY participation read (getMyTournaments, session-scoped)</name>
  <read_first>
    - prisma/schema.prisma (Pair player1Id/player2Id + player1/player2 relations; TournamentPlayer userId; Tournament status/format/participantMode/level/date/location/size/totalRounds; Round roundNumber/status per tournament)
    - src/lib/services/profile.ts + src/lib/services/registration.ts (service convention: take `prisma` in, explicit safe `select`, never credential columns)
    - src/lib/validation/tournament.ts (`tournamentStatuses`) — status → group mapping (registration=upcoming, in_progress=active, finished=finished)
  </read_first>
  <files>src/lib/services/dashboard.ts</files>
  <action>
    Create `src/lib/services/dashboard.ts` exporting an async `getMyTournaments(prisma, userId)`. Compose two READ-ONLY queries over existing models, both filtered by `userId`: (1) `prisma.pair.findMany({ where: { OR: [{player1Id: userId},{player2Id: userId}] }, select: { id, player1:{select:{id,name}}, player2:{select:{id,name}}, tournament:{select: tournamentCardSelect} } })` — the partner is whichever player is NOT `userId` (role = pair, partnerName = partner.name); (2) `prisma.tournamentPlayer.findMany({ where: { userId }, select: { tournament:{select: tournamentCardSelect} } })` — role = solo. Define `tournamentCardSelect` = id/name/status/format/participantMode/level/date/location/size. Merge both into a single list of card view-models `{ id, name, status, format, participantMode, level, location, size, role: "pair"|"solo", partnerName?: string }`, de-duplicating by tournament id. Map status → group: `in_progress`→active, `registration`→upcoming, `finished`→finished. For round-based formats (round_robin/americano/mexicano) compute a best-effort round progress by reading the tournament's `Round` rows (e.g. `{ roundNumber, status }`): `done` = count of finished rounds, `total` = `tournament.totalRounds ?? roundCount`; attach `{ round: { done, total } }` only when `total > 0`. For playoff (no `Round` rows) OMIT the round block (degrade gracefully — do NOT fabricate). Return the grouped view-models (e.g. `{ active, upcoming, finished }`). All reads are scoped to `userId`; no writes, no schema change.
  </action>
  <verify>
    <automated>grep -q "getMyTournaments" src/lib/services/dashboard.ts && grep -q "tournamentPlayer.findMany" src/lib/services/dashboard.ts && grep -q "player1Id: userId" src/lib/services/dashboard.ts && ! grep -qE "\.(create|update|delete|upsert|createMany)\(" src/lib/services/dashboard.ts && npx tsc --noEmit</automated>
  </verify>
  <done>`getMyTournaments(prisma, userId)` returns the player's tournaments grouped active/upcoming/finished with role + partnerName + best-effort round progress (omitted for playoff); all reads scoped to userId; no write methods; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Port 005 dashboard CSS into a co-located dashboard.css (token-only)</name>
  <read_first>
    - .claude/skills/sketch-findings-diplom/sources/005-dashboard/index.html (CSS `.net-rule` 19, `.who/.who-name/.who-sub` 32-36, `.lkA/.head/.head-cta` 41-43, `.sec-head/.sec-count/.sec-line` 46-52, `.tcard`/`::before`/state 57-80, `.tc-main/.tc-role/.solo` 82-98, `.tc-side/.tc-round` 101-105, `.place` 106-112, `.tcard .btn` 120, phone reflow 122-127, 248-257; HTML sections 301-430)
    - .claude/skills/sketch-findings-diplom/references/lists-and-filters.md (§005 45-58, dashboard card CSS 202-217, HTML 338-364)
    - src/app/globals.css (do NOT re-port `.avatar/.pill/.badge*/.btn*/.eyebrow/.progress/.card`)
  </read_first>
  <files>src/app/(app)/dashboard/dashboard.css</files>
  <action>
    Create `src/app/(app)/dashboard/dashboard.css` (plain global stylesheet). Port ONLY the 005 screen-specific classes absent from globals.css: `.net-rule`, `.lkA` (≤760px), `.head`/`.head-cta`, `.who`/`.who-name`/`.who-sub` (+ `.who .avatar` size override), `.sec`/`.sec-head`/`.sec-count`/`.sec-line`/`.tlist`, `.tcard` + `::before` accent bar (`.is-active`→`--primary`, `.is-upcoming`→`--accent`, `.is-finished`→`--text-faint`), `.tc-main`/`.tc-top`/`.tc-name`/`.tc-meta`/`.tc-role`(+`.rk`/`.solo`)/`.tc-side`/`.tc-round`(+`.tr-val`/`.tr-key`)/`.place`(+`.p-rank`/`.p-of`), and `.tcard .btn` sizing. Port the phone reflow (`@container (max-width: 560px)` card single-column + full-width btn; `@container (max-width: 480px)` who tweaks) as `@container`, NOT `@media`. All colors via Court tokens + `color-mix`; no hex.
  </action>
  <verify>
    <automated>grep -q '\.tcard' 'src/app/(app)/dashboard/dashboard.css' && grep -q 'is-active' 'src/app/(app)/dashboard/dashboard.css' && grep -q '\.who' 'src/app/(app)/dashboard/dashboard.css' && grep -q '@container' 'src/app/(app)/dashboard/dashboard.css' && ! grep -nE '#[0-9a-fA-F]{3,8}\b' 'src/app/(app)/dashboard/dashboard.css'</automated>
  </verify>
  <done>dashboard.css has `.lkA/.who/.sec-head/.tcard`(+state bars)/`.tc-*/.place/.net-rule` ported; container-query reflow; no hex.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the dashboard page to the Court sectioned layout (preserve guard, consume getMyTournaments)</name>
  <read_first>
    - src/app/(app)/dashboard/page.tsx (PRESERVE requireUser try/catch 9-17; currently only greets the user)
    - src/lib/services/dashboard.ts (the new `getMyTournaments` — Task 1) + src/lib/db (`prisma`)
    - .claude/skills/sketch-findings-diplom/sources/005-dashboard/index.html (HTML header `.who` 282-299; sections + `.tcard` states 303-430)
    - src/lib/validation/auth.ts (`skillLevelLabels`/`formatLabels`/`tournamentKindLabels`), src/lib/validation/profile.ts (`courtSideLabels`), src/components/tournament-status-badge.tsx
  </read_first>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <action>
    Keep the Server Component + `requireUser()` guard verbatim. Import `./dashboard.css`. After the guard, call `getMyTournaments(prisma, user.id)` (session-scoped). Replace the greeting `<main>` with `.cq` → `.lkA`: a `.head` containing `.who` (`.avatar` initials from `user.name` + `.who-name` `{user.name}` + `.who-sub` `.pill` row: `@{user.nickname}` mono, level via `skillLevelLabels[user.skillLevel]`, side via `courtSideLabels[user.courtSide]` — read these from the session `user` fields) and `.head-cta` (`.btn .btn-ghost` Профиль `<Link href="/profile">` + `.btn .btn-primary` Найти турнир `<Link href="/tournaments">`); then `<hr class="net-rule">`; then three `<section class="sec">` for active/upcoming/finished, each with a `.sec-head` (eyebrow Активные/Предстоящие/Завершённые + `.sec-count` + `.sec-line`) and a `.tlist` of `<article class="tcard is-active|is-upcoming|is-finished">`. Each `.tcard`: `.tc-main` (`.tc-top` `.tc-name` + `<TournamentStatusBadge status>`, `.tc-meta` `formatLabels[format]`·`tournamentKindLabels[participantMode]`·location, `.tc-role` = pair → partner `.avatar` initials + `<span class="rk">пара с</span> <b>{partnerName}</b>`, solo → `.solo` glyph + «Одиночный зачёт»). `.tc-side` is state-appropriate: active → if a round block exists render `.tc-round` `{done} / {total}` + `.progress` + `.btn .btn-primary` «К текущему раунду» `<Link href="/tournaments/{id}">` (degrade to just the CTA when no round block); upcoming → `.btn .btn-ghost` «Открыть» `<Link href="/tournaments/{id}">`; finished → `.place` + `.btn .btn-ghost` «Результаты» `<Link href="/tournaments/{id}">`. Render `.empty` per empty section (or hide empty sections — discretion). All CTAs target existing routes only.
  </action>
  <verify>
    <automated>grep -q "requireUser" 'src/app/(app)/dashboard/page.tsx' && grep -q "getMyTournaments" 'src/app/(app)/dashboard/page.tsx' && grep -q "tcard" 'src/app/(app)/dashboard/page.tsx' && grep -q 'href="/tournaments"' 'src/app/(app)/dashboard/page.tsx' && grep -q "dashboard.css" 'src/app/(app)/dashboard/page.tsx' && npx tsc --noEmit</automated>
  </verify>
  <done>Dashboard renders the Court identity header + 3 sections of `.tcard`s with role + state CTAs to existing routes; consumes `getMyTournaments(prisma, user.id)`; guard preserved; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session cookie → requireUser → getMyTournaments | the user id MUST come only from the guard; the participation read scopes every query to it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Information Disclosure | getMyTournaments scoping | mitigate | Every read filters by `userId` derived solely from `requireUser()` (session cookie); no client-supplied id reaches the query. Pair/TournamentPlayer rows returned are the session user's own; partner name is the public display field already shown elsewhere (no email/credential columns selected). |
| T-13-09 | Elevation of Privilege | dashboard page | mitigate | `requireUser()` guard preserved verbatim; anonymous/banned bounce to /login. The read cannot be invoked for another user. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies installed (restyle uses existing libs). No package-install tasks. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx next build` compiles `/dashboard`.
- `grep -rE '#[0-9a-fA-F]{3,8}\b' 'src/app/(app)/dashboard/'` returns nothing outside comments.
- `dashboard.ts` contains no `.create/.update/.delete/.upsert/.createMany` calls (read-only) and scopes by `userId`.
- dashboard page contains `requireUser` + `getMyTournaments`; CTAs point only at `/tournaments`, `/tournaments/{id}`, `/profile`.
</verification>

<success_criteria>
UI-04 satisfied: dashboard shows «Мои турниры» by section (Активные / Предстоящие / Завершённые) with the player's role (пара с… / одиночный) and state-appropriate CTAs in Court; the participation read is read-only and session-scoped; readable at ~375px. Round-progress is best-effort (round-based formats); any omission for playoff is recorded as deferred in the SUMMARY.
</success_criteria>

<artifacts_produced>
- New file: `src/lib/services/dashboard.ts` — read-only `getMyTournaments(prisma, userId)` (pairs + tournamentPlayer participation, partner, status grouping, best-effort round progress).
- New file: `src/app/(app)/dashboard/dashboard.css` (005 screen-specific classes, token-only, `@container` reflow).
- No new Server Actions; no schema/migration; no additions to `globals.css`.
- Deferred (if applicable): playoff round-progress «N/M» (no `Round` rows) — recorded in SUMMARY, not fabricated.
</artifacts_produced>

<output>
Create `.planning/phases/13-auth/13-04-SUMMARY.md` when done.
</output>

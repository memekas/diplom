---
phase: 03-registration-pairs
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/validation/registration.ts
  - src/lib/validation/registration.test.ts
  - src/lib/services/registration.ts
  - src/lib/services/registration.test.ts
  - prisma/schema.prisma
autonomous: true
requirements: [REG-01, REG-02, REG-03]
must_haves:
  truths:
    - "registerPair rejects player1 == player2 (self-partner) without inserting"
    - "registerPair rejects a player already in a pair of this tournament (either slot) without inserting"
    - "registerPair rejects registration when pair count >= tournament size without inserting"
    - "registerPair rejects registration when tournament status != registration without inserting"
    - "registerPair inserts a Pair (player1Id, player2Id) on the happy path and returns it"
    - "Pair model exists in the DB with two @@unique slot constraints and cascade-on-tournament-delete"
  artifacts:
    - path: "src/lib/validation/registration.ts"
      provides: "registerPairSchema + parseRegisterPairForm (player2Id required, non-empty)"
      contains: "registerPairSchema"
    - path: "src/lib/services/registration.ts"
      provides: "registerPair transactional service + typed RegistrationError"
      contains: "export async function registerPair"
    - path: "src/lib/services/registration.test.ts"
      provides: "TDD assertion script: self/dup/capacity/status rejected, happy path inserts"
    - path: "prisma/schema.prisma"
      provides: "Pair model + Tournament.pairs + User.pairsAsP1/pairsAsP2 back-relations"
      contains: "model Pair"
  key_links:
    - from: "src/lib/services/registration.ts"
      to: "prisma.$transaction"
      via: "single transaction wrapping status re-read + count + duplicate check + insert"
      pattern: "\\$transaction"
    - from: "src/lib/services/registration.ts"
      to: "prisma.pair"
      via: "count + findFirst (either slot) + create inside the transaction"
      pattern: "pair\\.(count|findFirst|create)"
---

<objective>
Build the pair-registration data + integrity core: the `Pair` Prisma model (with applied migration) and the transactional `registerPair` service that atomically enforces every REG-02 integrity rule (no self-partner, no player in two pairs of one tournament, no over-capacity) plus the REG-03 capacity/status lock. This is the highest-risk piece of the phase (the pair-integrity race, PITFALLS Pitfall 6 + 7), so it is written test-first.

Purpose: A single atomic gate is the only correct way to satisfy REG-02/REG-03 — check-then-insert in separate steps races. Isolating it as a tested pure-logic service (fake-prisma harness, no live DB) lets Plan 02 wire a thin action over it with confidence.
Output: `Pair` model + migration; `registerPair(prisma, {tournamentId, player1Id, player2Id})` with typed errors; `registerPairSchema`/`parseRegisterPairForm`; dependency-free `tsx` test scripts (RED→GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-registration-pairs/03-CONTEXT.md

# Pattern sources to mirror (read these — do NOT re-derive conventions):
# - status-machine service + its fake-prisma test harness (the template for this plan's TDD)
@src/lib/services/tournament-status.ts
@src/lib/services/tournament-status.test.ts
# - validation schema + parse-form discriminated shape to mirror
@src/lib/validation/tournament.ts
# - service-takes-prisma-in + explicit safe select
@src/lib/services/profile.ts
# - current schema (Tournament is relation-less; add Pair + back-relations)
@prisma/schema.prisma
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: registerPair input validation schema (RED→GREEN)</name>
  <files>src/lib/validation/registration.ts, src/lib/validation/registration.test.ts</files>
  <read_first>
    - src/lib/validation/tournament.ts (mirror `parseTournamentForm` discriminated `{ok, data|errors}` shape + zod style)
    - src/lib/validation/tournament.test.ts (mirror the dependency-free assertion-script harness: `check()` counter, exit non-zero on failure)
  </read_first>
  <behavior>
    - registerPairSchema accepts `{ player2Id: "<non-empty string>" }` → ok, data.player2Id set
    - registerPairSchema rejects empty / missing player2Id (message in RU, e.g. "Выберите партнёра")
    - parseRegisterPairForm(formData) returns `{ ok: true, data }` for a valid `player2Id` field
    - parseRegisterPairForm(formData) returns `{ ok: false, errors: { player2Id } }` for empty/missing
    - NOTE: player1 identity is NOT in this schema — it comes from the session in the action (REG-01 / Pitfall 8), never the form. Only player2Id is client-supplied here.
  </behavior>
  <action>
    Write the RED test first in src/lib/validation/registration.test.ts using the same self-contained `tsx` harness as tournament.test.ts (import `assert from "node:assert/strict"`, a `check()` counter, `console.log` per assertion, exit non-zero on failure). Cover the four behaviors above. Run it and confirm it FAILS (module/symbols absent), then commit `test(03-01): add failing tests for registerPair validation`.
    Then create src/lib/validation/registration.ts: export `registerPairSchema = z.object({ player2Id: z.string().trim().min(1, "Выберите партнёра") })`, `RegisterPairInput = z.infer<...>`, and `parseRegisterPairForm(formData)` returning the discriminated `{ok,data}|{ok,errors}` shape exactly like `parseTournamentForm` (iterate `parsed.error.issues`, first message per path key). Do NOT add player1Id to the schema. Re-run to GREEN, commit `feat(03-01): registerPair validation schema`.
  </action>
  <verify>
    <automated>npx tsx src/lib/validation/registration.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>registration.test.ts passes (exit 0); schema accepts non-empty player2Id, rejects empty/missing with RU message; parseRegisterPairForm mirrors parseTournamentForm; player1Id absent from schema; tsc clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: registerPair transactional integrity service (RED→GREEN) — REG-02, REG-03</name>
  <files>src/lib/services/registration.ts, src/lib/services/registration.test.ts</files>
  <read_first>
    - src/lib/services/tournament-status.test.ts (COPY its `fakePrisma` + `async function main()` IIFE pattern — tsx emits CJS, so async assertions MUST be inside main(), invoked via `main()`; sync ones may stay top-level)
    - src/lib/services/tournament-status.ts (service-takes-prisma-in signature + DB-is-source-of-truth re-read style; mirror its typed-throw approach)
    - src/lib/services/profile.ts (explicit safe `select` convention)
  </read_first>
  <behavior>
    - self-partner: registerPair(prisma, {tournamentId, player1Id:"u1", player2Id:"u1"}) → rejects; no pair.create called
    - status guard (REG-03): tournament status != "registration" (e.g. "in_progress") → rejects; no create
    - capacity (REG-03): pair count already == tournament.size → rejects (over-capacity); no create
    - duplicate either-slot (REG-02): a player (player1 OR player2) already appears as player1Id OR player2Id in an existing pair of this tournament → rejects; no create
    - happy path (REG-01): status "registration", count < size, distinct new players → pair.create called once with {tournamentId, player1Id, player2Id}; returns the created pair
    - all checks AND the insert occur inside a single $transaction (Pitfall 7 — count+insert must not race)
  </behavior>
  <action>
    Write RED test src/lib/services/registration.test.ts first. Build a `fakePrisma` (copy the shape from tournament-status.test.ts) exposing: a `$transaction(fn)` that invokes `fn(tx)` with the same fake as `tx`; `tournament.findUniqueOrThrow` returning a configurable `{ id, status, size }`; `pair.count` returning a configurable count; `pair.findFirst` returning a configurable existing-pair-or-null; and `pair.create` that records the call and returns a synthetic pair. Each test configures these and asserts reject-without-create OR create-once. Wrap async cases in `async function main()` (tsx/CJS — no top-level await). Run → FAIL, commit `test(03-01): add failing tests for registerPair integrity`.
    Then create src/lib/services/registration.ts. Export a typed error: `class RegistrationError extends Error { constructor(public code: "self_partner"|"already_registered"|"tournament_full"|"not_open", message: string) }` (lets the action map to RU messages). Export `registerPair(prisma, { tournamentId, player1Id, player2Id })` that runs ENTIRELY inside `prisma.$transaction(async (tx) => { ... })` in this order (per CONTEXT pair-integrity steps): (1) `tx.tournament.findUniqueOrThrow({ where:{id:tournamentId}, select:{ id:true, status:true, size:true } })`; if status !== "registration" throw RegistrationError("not_open", "Регистрация на турнир закрыта"). (2) reject self: if player1Id === player2Id throw RegistrationError("self_partner", "Нельзя зарегистрироваться в паре с самим собой"). (3) `const count = await tx.pair.count({ where:{ tournamentId } })`; if count >= tournament.size throw RegistrationError("tournament_full", "Турнир заполнен"). (4) cross-slot duplicate: `tx.pair.findFirst({ where:{ tournamentId, OR:[ {player1Id:{in:[player1Id,player2Id]}}, {player2Id:{in:[player1Id,player2Id]}} ] } })`; if found throw RegistrationError("already_registered", "Один из игроков уже участвует в этом турнире"). (5) `return tx.pair.create({ data:{ tournamentId, player1Id, player2Id }, select: pairSelect })` where `pairSelect = { id:true, tournamentId:true, player1Id:true, player2Id:true, createdAt:true }`. The @@unique constraints (Task 3) are defense-in-depth; the cross-slot check is what catches a player as player1 in one pair and player2 in another. Re-run → GREEN, commit `feat(03-01): transactional registerPair integrity service (REG-02, REG-03)`.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/registration.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>registration.test.ts passes (exit 0): self-partner, not-open status, over-capacity, and either-slot duplicate each reject with NO pair.create; happy path calls pair.create exactly once and returns the pair; every branch runs inside $transaction. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Pair model + back-relations + migration</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (current: User has pairsAsP1/P2 absent; Tournament is relation-less)
    - .planning/research/ARCHITECTURE.md lines 143-165 (canonical Pair model shape)
  </read_first>
  <action>
    Edit prisma/schema.prisma. (a) Add `model Pair` per ARCHITECTURE: `id String @id @default(cuid())`; `tournamentId String` + `tournament Tournament @relation(fields:[tournamentId], references:[id], onDelete: Cascade)`; `player1Id String` + `player1 User @relation("PairPlayer1", fields:[player1Id], references:[id])`; `player2Id String` + `player2 User @relation("PairPlayer2", fields:[player2Id], references:[id])`; `seed Int?` (filled in Phase 4 — leave nullable, do NOT use it now); `createdAt DateTime @default(now())`; `@@unique([tournamentId, player1Id])`; `@@unique([tournamentId, player2Id])`; `@@map("pair")`. DO NOT add `matchesAsA/matchesAsB/matchesWon` Match relations — Phase 4 owns Match. (b) Add back-relation `pairs Pair[]` to `model Tournament`. (c) Add `pairsAsP1 Pair[] @relation("PairPlayer1")` and `pairsAsP2 Pair[] @relation("PairPlayer2")` to `model User`. Do NOT touch the kysely override, auth tables, or any other model. Then run the BLOCKING migration: `npx prisma migrate dev --name add_pair`. This must complete (a migration file under prisma/migrations and "up to date" status) before any Plan 02 work — Plan 02's queries and action import the generated Pair client.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma migrate status && npx tsc --noEmit && grep -q 'model Pair' prisma/schema.prisma && grep -q 'pairs Pair\[\]' prisma/schema.prisma</automated>
  </verify>
  <done>`npx prisma migrate dev --name add_pair` applied; `prisma validate` ok; `migrate status` reports up-to-date (no drift); a new migration dir exists under prisma/migrations; schema contains `model Pair` with both `@@unique` constraints + `onDelete: Cascade`, `Tournament.pairs`, and `User.pairsAsP1/pairsAsP2`; NO Match relations on Pair; kysely override + auth tables unchanged (git diff scoped to Pair/back-relations); tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → registerPair (via action, Plan 02) | player2Id is untrusted client input; player1 identity must come from session, never form |
| service → SQLite | concurrent count+insert could race over-capacity / double-register if not in one transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | registerPair player1 identity | mitigate | player1Id is the session user (set by the action in Plan 02), never read from the form; schema here does not accept player1Id |
| T-03-02 | Elevation/Integrity | over-capacity & double-register race (Pitfall 7) | mitigate | status re-read + count + duplicate check + insert all inside one `prisma.$transaction` (Task 2); `@@unique([tournamentId, player1Id])`/`([tournamentId, player2Id])` as DB-level defense-in-depth (Task 3) |
| T-03-03 | Tampering | self-partner / cross-slot duplicate (Pitfall 6) | mitigate | explicit `player1Id !== player2Id` guard + cross-slot `findFirst` (unique constraints alone cannot catch player-as-p1-here / p2-there) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this phase (no new deps — CONSTRAINTS); Pair uses existing prisma-client-js generator |
</threat_model>

<verification>
- `npx tsx src/lib/validation/registration.test.ts` → exit 0
- `npx tsx src/lib/services/registration.test.ts` → exit 0 (self/dup/capacity/status rejected with no create; happy path creates once)
- `npx prisma validate` → ok; `npx prisma migrate status` → up to date
- `npx tsc --noEmit` → clean
- `git diff prisma/schema.prisma` → only Pair model + 3 back-relations added; kysely/auth untouched
</verification>

<success_criteria>
- Pair model migrated with both `@@unique` slot constraints and `onDelete: Cascade`; back-relations on Tournament + User; no Match relations.
- `registerPair` enforces self-partner, cross-slot-duplicate, over-capacity, and not-open-status rejections AND the insert inside ONE `$transaction`; rejects without writing.
- Validation schema accepts only `player2Id` (player1 comes from session downstream).
- All TDD scripts green; tsc clean; no new dependencies.
</success_criteria>

<output>
Create `.planning/phases/03-registration-pairs/03-01-SUMMARY.md` when done.
</output>

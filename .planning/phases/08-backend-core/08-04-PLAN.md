---
phase: 08-backend-core
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth.ts
  - src/lib/validation/profile.ts
  - src/lib/validation/profile.test.ts
  - src/lib/services/profile.ts
  - src/app/(app)/profile/actions.ts
autonomous: true
requirements: [USR-03]
must_haves:
  truths:
    - "ЛК сохраняет правки name/skillLevel/phone/birthDate/courtSide/nickname"
    - "Конфликт ника даёт RU-ошибку 'Этот ник уже занят', а не падение (P2002 catch)"
    - "Смена email идёт через auth.api.changeEmail (НЕ prisma update), работает без SMTP при emailVerified=false"
    - "Смена на занятый email возвращает RU-ошибку (pre-check uniqueness до changeEmail)"
    - "Идентичность всегда из requireUser(), никогда из формы"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "user.changeEmail = {enabled:true, updateEmailWithoutVerification:true}"
      contains: "changeEmail"
    - path: "src/lib/validation/profile.ts"
      provides: "Расширенная profileSchema (+name/birthDate/nickname/email; skillLevel)"
      contains: "nickname"
    - path: "src/lib/services/profile.ts"
      provides: "updateProfile пишет name/skillLevel/phone/birthDate/courtSide/nickname (прямой prisma update)"
      contains: "nickname"
    - path: "src/app/(app)/profile/actions.ts"
      provides: "updateProfileAction: P2002→RU ник + pre-check email + auth.api.changeEmail"
      contains: "changeEmail"
    - path: "src/lib/validation/profile.test.ts"
      provides: "Тесты расширенной схемы (nickname format, birthDate union, email optional)"
  key_links:
    - from: "src/app/(app)/profile/actions.ts"
      to: "auth.api.changeEmail"
      via: "email change after pre-check uniqueness"
      pattern: "auth\\.api\\.changeEmail"
    - from: "src/app/(app)/profile/actions.ts"
      to: "updateProfile"
      via: "domain fields incl nickname, catch P2002"
      pattern: "P2002"
---

<objective>
Расширить личный кабинет (USR-03) до правки всех полей профиля, включая ник (с проверкой уникальности) и email (через флоу смены email Better Auth).

Purpose: Игрок должен править ФИО/уровень/телефон/дату рождения/сторону корта/ник/email. Ник и email уникальны: конфликт → понятная RU-ошибка, не падение. Email управляется Better Auth (session-cookie sync) — менять через `auth.api.changeEmail`, НЕ прямым prisma.update.

Output: `user.changeEmail` config в auth.ts, расширенная `profileSchema` + `parseProfileForm`, расширенный `updateProfile` (прямой prisma update новых полей), расширенный `updateProfileAction` (P2002→RU ник + pre-check email + changeEmail), расширенный test-скрипт. Никакого UI формы (Фаза 11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-core/08-CONTEXT.md
@.planning/phases/08-backend-core/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Включить changeEmail в auth.ts + расширить profileSchema/parseProfileForm</name>
  <files>src/lib/auth.ts, src/lib/validation/profile.ts</files>
  <read_first>
    - src/lib/auth.ts (текущая betterAuth-конфигурация: emailAndPassword, user.additionalFields {phone/skillLevel/nickname}, plugins [admin(), nextCookies()])
    - src/lib/validation/profile.ts (текущая `profileSchema` {courtSide/phone/skillLevel}, `parseProfileForm` discriminated-shape; courtSides)
    - src/lib/validation/auth.ts (skillLevels — переиспользовать; nickname-валидация в registerSchema: trim/min3/max30/regex `^[A-Za-z0-9_-]+$` — зеркалить для профиля)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Code Example «auth.ts — enable email change without verification» + Pitfall 2 «нужны ОБА флага + emailVerified=false»)
    - prisma/schema.prisma (User: name/email/courtSide/phone/skillLevel/nickname/birthDate — целевые поля; emailVerified @default(false))
  </read_first>
  <action>
    В auth.ts добавить в объект `user` (рядом с `additionalFields`) ключ `changeEmail: { enabled: true, updateEmailWithoutVerification: true }` per RESEARCH Code Example. Оба флага обязательны (Pitfall 2) — работает только потому что emailVerified дефолтит false в этом приложении. НЕ менять additionalFields/plugins/emailAndPassword.
    В validation/profile.ts расширить `profileSchema`: добавить `name` z.string().trim().min(1, "ФИО обязательно"); `nickname` зеркально registerSchema (trim, min3 «Минимум 3 символа», max30 «Максимум 30 символов», regex `^[A-Za-z0-9_-]+$` «Только буквы, цифры, _ и -»); `email` z.email("Введите корректный email").optional().or(z.literal("").transform(()=>undefined)); `birthDate` z.union([z.literal(""), z.coerce.date()]).optional().transform((v)=>(v===""||v===undefined?undefined:v)) (приём как у date в createTournamentSchema — RESEARCH Open Q1); сохранить courtSide/phone/skillLevel (skillLevel остаётся .optional() — пустой select = «не менять»).
    Расширить `parseProfileForm`: читать новые ключи из FormData (name, nickname, email, birthDate) — `formData.get(k) ?? undefined`, для email/nickname `formData.get(k) || undefined` где пустая строка → undefined. Расширить тип `ParseProfileFormResult` errors union: добавить "name"|"nickname"|"email"|"birthDate". Логику сбора issue→errors не менять.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -cE "auth\.ts|profile\.ts" | grep -qx 0 && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts содержит `changeEmail: { enabled: true, updateEmailWithoutVerification: true }` внутри user
    - profileSchema принимает name/nickname/email/birthDate (+ существующие courtSide/phone/skillLevel)
    - nickname "ab" (2 символа) → reject; "valid_nick" → ok; "плохой ник" (пробел/кириллица) → reject
    - birthDate "" → undefined (не reject); "1990-05-01" → Date
    - email "" → undefined; "bad" → reject; "a@b.com" → ok
    - тип errors включает name/nickname/email/birthDate
  </acceptance_criteria>
  <done>changeEmail включён в Better Auth; profileSchema/parseProfileForm покрывают все поля профиля USR-03.</done>
</task>

<task type="auto">
  <name>Task 2: Расширить updateProfile (прямой prisma update новых полей)</name>
  <files>src/lib/services/profile.ts</files>
  <read_first>
    - src/lib/services/profile.ts (текущий `updateProfile` пишет courtSide/phone/skillLevel; `safeProfileSelect` без credential-полей; `getProfile`)
    - src/lib/validation/profile.ts (после Task 1 — `ProfileInput` тип с новыми полями)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Pitfall 3/Pitfall 4 + Assumption A5 «прямой prisma.update для name/skillLevel/phone/birthDate/courtSide/nickname приемлем»; Don't Hand-Roll «email — единственное поле, которым владеет Better Auth»)
  </read_first>
  <action>
    Расширить `updateProfile(prisma, userId, data: ProfileInput)`: писать через прямой `prisma.user.update` (текущий паттерн, A5) поля: `name: data.name` (теперь в схеме), `courtSide`, `phone: data.phone ?? null`, `birthDate: data.birthDate ?? null`, `nickname: data.nickname`, и `skillLevel` только если определён (`...(data.skillLevel !== undefined ? { skillLevel: data.skillLevel } : {})`). EMAIL В updateProfile НЕ ПИСАТЬ (Pitfall 3 — email владеет Better Auth, прямой update рассинхронит session-cookie; email обрабатывается в action через changeEmail). userId из guard, не из формы. Расширить `safeProfileSelect` полями `nickname`/`birthDate` (никогда не credential-колонки). P2002 НЕ ловить здесь — пробрасывать наверх, action маппит (Pitfall 4 — полагаться на @@unique + catch в action, без TOCTOU pre-check).
    Сохранить `getProfile` (при необходимости расширить select новыми полями для чтения формой Фазы 11 — добавить name/nickname/birthDate в safeProfileSelect; name/email уже выбираются).
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "services/profile.ts" | grep -qx 0 && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - updateProfile пишет name/courtSide/phone/birthDate/nickname; skillLevel только если задан
    - updateProfile НЕ пишет email (Pitfall 3)
    - updateProfile не делает pre-check ника (P2002 пробрасывается наверх)
    - safeProfileSelect включает nickname/birthDate, не включает credential-колонки
    - userId берётся из аргумента (guard), не из data
  </acceptance_criteria>
  <done>updateProfile пишет все доменные поля профиля прямым prisma.update; email исключён (идёт через changeEmail в action).</done>
</task>

<task type="auto">
  <name>Task 3: Расширить updateProfileAction (P2002 ник + pre-check email + changeEmail) + тесты</name>
  <files>src/app/(app)/profile/actions.ts, src/lib/validation/profile.test.ts</files>
  <read_first>
    - src/app/(app)/profile/actions.ts (текущий `updateProfileAction`: requireUser→parseProfileForm→updateProfile→revalidatePath; ProfileActionState errors-тип)
    - src/lib/auth-guards.ts (`requireUser` возвращает session.user с id/email)
    - src/lib/validation/profile.test.ts (образец self-contained tsx-теста: `check`, `form(entries)`, 16 assertions, exits non-zero)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Code Example «updateProfileAction — nickname (P2002) + email (changeEmail) flow» — авторитетный образец; Pitfall 1 «changeEmail молча успешен при занятом email → pre-check»)
  </read_first>
  <action>
    Расширить `updateProfileAction` per RESEARCH Code Example: после `requireUser()` (identity NEVER from form) и `parseProfileForm`, разбить `const { email, ...profile } = parsed.data`. (1) Доменные поля + ник — прямой `try { await updateProfile(prisma, user.id, profile) } catch (e) { if (e?.code === "P2002") return { ok:false, errors:{ nickname:"Этот ник уже занят" } }; throw e }` (Pitfall 4). (2) Email только если изменился (`if (email && email !== user.email)`): pre-check уникальности (Pitfall 1) `prisma.user.findUnique({ where:{ email }, select:{ id:true } })`; если найден и `clash.id !== user.id` → `return { ok:false, errors:{ email:"Этот email уже используется" } }`; иначе `try { await auth.api.changeEmail({ body:{ newEmail: email }, headers: await headers() }) } catch { return { ok:false, errors:{ email:"Не удалось сменить email" } } }`. Затем `revalidatePath("/profile")` + `return { ok:true }`. Импорты: headers из next/headers, auth из @/lib/auth, prisma из @/lib/db. Расширить `ProfileActionState` errors union новыми ключами (name/nickname/email/birthDate). НЕ писать email через prisma (Pitfall 3).
    В profile.test.ts: расширить (это pure validation-тест, action не тестируем здесь — action использует БД/Better Auth) проверками новой схемы: nickname format (min3/max30/regex — accept "good_nick", reject "ab", reject с пробелом/кириллицей), birthDate union ("" → undefined, валидная дата → Date, мусор → reject), email optional ("" → undefined, валидный → ok, "bad" → reject), name required (пусто → reject). Сохранить существующие 16 assertion (courtSide/phone/skillLevel), обновив валидные FormData-кейсы под новые обязательные поля (name/nickname) чтобы они оставались valid. Обновить финальный счётчик.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsx src/lib/validation/profile.test.ts 2>&1 | tail -1 | grep -q "passed" && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -cE "profile/actions\.ts|profile\.test\.ts" | grep -qx 0 && echo "PLAN04_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx src/lib/validation/profile.test.ts` exits 0, печатает "passed" (≥ прежних 16 + новые)
    - updateProfileAction ловит P2002 → {ok:false, errors:{nickname:"Этот ник уже занят"}}
    - updateProfileAction pre-checks email uniqueness ДО changeEmail (path обновляет email только если свободен)
    - updateProfileAction вызывает auth.api.changeEmail (НЕ prisma update для email)
    - identity из requireUser(), email split out перед updateProfile
    - `npx tsc --noEmit` чисто по actions.ts/profile.test.ts
  </acceptance_criteria>
  <done>updateProfileAction правит все поля: ник через P2002-catch, email через changeEmail с pre-check; тесты схемы зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → updateProfileAction | Недоверенный FormData (ФИО/ник/email/дата); identity из session-guard, НЕ из формы; роль не редактируется |
| updateProfileAction → Better Auth (email) | email пересекает к auth.api.changeEmail (владеет session-cookie); прямой prisma update запрещён |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Spoofing | userId/identity из формы | mitigate | identity всегда из `requireUser()`, write по user.id; email split out, role не в схеме (Pitfall 8) |
| T-08-12 | Tampering | конфликт ника (TOCTOU/raw 500) | mitigate | прямой prisma.update + catch P2002 → RU «ник занят», без pre-check (Pitfall 4) |
| T-08-13 | Session desync | смена email мимо Better Auth | mitigate | auth.api.changeEmail (обновляет session-cookie); прямой prisma.update запрещён (Pitfall 3, V3) |
| T-08-14 | Information Disclosure | email enumeration через changeEmail | accept | BA маскирует (silent {status:true}); offline-demo — pre-check ради UX-корректности, не leak-защиты (Pitfall 1) |
| T-08-SC | Tampering | npm installs | accept | Фаза не ставит пакетов (RESEARCH audit — нет установок) |
</threat_model>

<verification>
- `npx tsc --noEmit` чисто по auth.ts/profile.ts/profile/actions.ts/profile.test.ts.
- `npx tsx src/lib/validation/profile.test.ts` зелёный (16 старых + новые поля).
- Регрессия: auth.ts изменение аддитивно (additionalFields/plugins не тронуты) — существующие auth-флоу (signup/login/nickname) не сломаны; полный фаза-инвариант в Plan 05.
</verification>

<success_criteria>
- changeEmail включён в Better Auth (оба флага), работает без SMTP при emailVerified=false.
- profileSchema/updateProfile правят name/skillLevel/phone/birthDate/courtSide/nickname.
- updateProfileAction: ник через P2002→RU, email через pre-check+changeEmail.
- profile validation тесты зелёные с новыми полями.
</success_criteria>

<output>
Create `.planning/phases/08-backend-core/08-04-SUMMARY.md` when done
</output>

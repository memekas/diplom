---
phase: 08-backend-core
plan: 05
type: execute
wave: 2
depends_on: ["08-02", "08-03"]
files_modified:
  - src/app/(public)/tournaments/[id]/actions.ts
autonomous: true
requirements: [REG-06, ADMN-01, ADMN-02]
must_haves:
  truths:
    - "participateSingleAction (requireUser) регистрирует игрока одиночно через registerSingle"
    - "removeRegistrationAction (requireAdmin) удаляет пару/участника по kind через removePair/removeParticipant"
    - "finishTournamentAction (requireAdmin) завершает турнир через finishTournament + revalidatePath"
    - "Все три action проходят server-side guard (requireUser/requireAdmin первой строкой) и возвращают типизированный RU-результат"
    - "Все существующие test-скрипты + новый admin остаются зелёными (playoff/validation инвариант не сломан)"
  artifacts:
    - path: "src/app/(public)/tournaments/[id]/actions.ts"
      provides: "participateSingleAction + removeRegistrationAction + finishTournamentAction"
      contains: "participateSingleAction"
  key_links:
    - from: "participateSingleAction"
      to: "registerSingle"
      via: "service call with session userId + bound tournamentId"
      pattern: "registerSingle\\(prisma"
    - from: "removeRegistrationAction"
      to: "removePair / removeParticipant"
      via: "kind-dispatched admin delete"
      pattern: "removePair|removeParticipant"
    - from: "finishTournamentAction"
      to: "finishTournament"
      via: "admin idempotent finish + revalidatePath"
      pattern: "finishTournament\\(prisma"
---

<objective>
Подключить три новых Server Action к сервисам Plan 02/03 и зафиксировать фаза-инвариант (все существующие тесты зелёные).

Purpose: Превратить серверную логику Фазы 8 в вызываемые endpoint'ы: одиночная регистрация (REG-06), админ-удаление регистрации (ADMN-01), ручной финиш (ADMN-02). Все три живут в общем `tournaments/[id]/actions.ts` (эксклюзивное владение файлом → отдельный план во второй волне). Финальный шаг — прогон всех существующих test-скриптов + нового admin как регрессия (playoff/validation не сломаны).

Output: `participateSingleAction`, `removeRegistrationAction`, `finishTournamentAction` в actions.ts; подтверждённая регрессия.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-core/08-CONTEXT.md
@.planning/phases/08-backend-core/08-RESEARCH.md
@.planning/phases/08-backend-core/08-02-SUMMARY.md
@.planning/phases/08-backend-core/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: participateSingleAction (одиночная регистрация, REG-06)</name>
  <files>src/app/(public)/tournaments/[id]/actions.ts</files>
  <read_first>
    - src/app/(public)/tournaments/[id]/actions.ts (файл, который меняем — `participateAction` образец: requireUser→guard→parse→registerPair в try/catch RegistrationError→generic fallback→revalidatePath; `.bind()` tournamentId; ParticipateActionState тип)
    - src/lib/services/registration.ts (Plan 02 — `registerSingle(prisma, {tournamentId, userId})`, RegistrationError коды включая wrong_mode/level_mismatch)
    - src/lib/validation/registration.ts (Plan 02 — `parseRegisterSingleForm`)
    - .planning/phases/08-backend-core/08-02-SUMMARY.md (фактические сигнатуры registerSingle + parseRegisterSingleForm)
  </read_first>
  <action>
    Добавить `participateSingleAction(tournamentId: string, _prev, formData)` зеркально `participateAction` (Pattern 1). FIRST line `const user = await requireUser()` (identity NEVER from form — T-08-11). Guard `if (!tournamentId) return { ok:false, error:"Турнир не найден" }`. При наличии полей — `parseRegisterSingleForm(formData)` и обработка !ok (singles не имеет партнёрских полей; если схема пустая — parse тривиально ok). В try: `await registerSingle(prisma, { tournamentId, userId: user.id })`. catch: `if (e instanceof RegistrationError) return { ok:false, error: e.message }` иначе generic RU fallback «Не удалось зарегистрироваться. Попробуйте ещё раз.» (T-08-07 — никакой raw Prisma наружу). `revalidatePath` на `/tournaments/${tournamentId}` + `return { ok:true }`. Тип состояния — переиспользовать `ParticipateActionState`. Импортировать `registerSingle` (+ parseRegisterSingleForm если используется) рядом с существующими импортами registration. tournamentId придёт через `.bind()` из листа (Фаза 11) — НЕ из формы (T-08-11).
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "tournaments/\[id\]/actions.ts" | grep -qx 0 && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - participateSingleAction экспортируется, сигнатура (tournamentId, _prev, formData)
    - requireUser() первой строкой; userId из guard, не из формы
    - вызывает registerSingle с user.id; RegistrationError → e.message, иначе generic RU
    - revalidatePath на `/tournaments/${tournamentId}` на успехе
    - `npx tsc --noEmit` чисто по actions.ts
  </acceptance_criteria>
  <done>Одиночная регистрация доступна как Server Action с правильным guard и маппингом ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: removeRegistrationAction + finishTournamentAction (ADMN-01, ADMN-02)</name>
  <files>src/app/(public)/tournaments/[id]/actions.ts</files>
  <read_first>
    - src/app/(public)/tournaments/[id]/actions.ts (после Task 1 — `startTournamentAction` образец admin-action: requireAdmin first line, .bind() tournamentId, типизированный error→RU, revalidatePath; State-типы)
    - src/lib/services/admin.ts (Plan 03 — `removePair({tournamentId, pairId})`, `removeParticipant({tournamentId, playerId})`, `finishTournament(prisma, tournamentId)`, `AdminError` с code not_open)
    - .planning/phases/08-backend-core/08-03-SUMMARY.md (фактические сигнатуры admin-сервисов)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Code Example «Admin remove + finish actions» — образец kind-dispatch + идемпотентный finish; Open Q2 — kind+id vs два action)
  </read_first>
  <action>
    Добавить два admin-action.
    `removeRegistrationAction(tournamentId: string, kind: "pair"|"player", id: string, _prev, _fd)` (per RESEARCH Open Q2 — единый action с kind-дискриминатором; kind+id+tournamentId через `.bind()` из листа, не из формы — T-08-08). FIRST line `await requireAdmin()` (throw "Forbidden" до работы — НЕ ловить). try: `if (kind === "pair") await removePair(prisma, { tournamentId, pairId: id }); else await removeParticipant(prisma, { tournamentId, playerId: id })`. catch: `if (e instanceof AdminError) return { ok:false, error: e.message }` иначе generic RU «Не удалось удалить регистрацию.» (T-08-07). `revalidatePath` на `/tournaments/${tournamentId}` + `{ ok:true }`. Новый State-тип `RemoveRegistrationActionState = { ok:true } | { ok:false; error:string } | null`.
    `finishTournamentAction(tournamentId: string, _prev, _fd)`. FIRST line `await requireAdmin()`. try: `await finishTournament(prisma, tournamentId)` (сервис идемпотентен — повторный финиш no-op, Pitfall 8/T-08-10). catch: `return { ok:false, error:"Не удалось завершить турнир. Попробуйте ещё раз." }` (transitionTournament бросает plain Error на mismatch — generic RU, не raw наружу). `revalidatePath` на `/tournaments/${tournamentId}` + `{ ok:true }`. State-тип `FinishTournamentActionState` аналогично.
    Импортировать `removePair, removeParticipant, finishTournament, AdminError` из `@/lib/services/admin`. requireAdmin уже импортирован.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "tournaments/\[id\]/actions.ts" | grep -qx 0 && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - removeRegistrationAction экспортируется; requireAdmin() первой строкой; kind-dispatch pair/player
    - removeRegistrationAction маппит AdminError → e.message, иначе generic RU
    - finishTournamentAction экспортируется; requireAdmin() первой строкой; вызывает finishTournament
    - оба action: tournamentId/id через bind, не из формы; revalidatePath на успехе
    - `npx tsc --noEmit` чисто по actions.ts
  </acceptance_criteria>
  <done>Админ-удаление (kind-dispatch) и ручной финиш доступны как guarded Server Action.</done>
</task>

<task type="auto">
  <name>Task 3: Регрессия — прогон всех test-скриптов + полный typecheck</name>
  <files>src/app/(public)/tournaments/[id]/actions.ts</files>
  <read_first>
    - .planning/phases/08-backend-core/08-RESEARCH.md (Validation Architecture — «Все существующие 163 assertions гонять как инвариант (playoff не сломан)»; 7 файлов *.test.ts через tsx)
    - src/lib/services/result.ts (playoff авто-финиш — НЕ должен быть затронут; косвенная проверка через result.test.ts)
  </read_first>
  <action>
    Верификационная задача — кода не добавляет (файл в списке для file-ownership; если по итогам Task 1/2 нужны мелкие правки импортов/типов — здесь). Прогнать ВСЕ test-скрипты как фаза-инвариант (RESEARCH: 163 baseline-assertions, playoff/registration/status/result/profile/validation не сломаны): bracket, registration, result, tournament-status, admin (services); profile, registration, tournament (validation). Любой красный — регрессия Фазы 8, чинить причину (не подгонять тест). Финально — полный `npx tsc --noEmit` без ошибок по всему проекту.
    Подтвердить, что playoff-стек не тронут: registerPair-pairs-путь (совпадающие уровни) проходит, авто-финиш в result.ts работает (result.test.ts зелёный), generateBracket не изменён (bracket.test.ts зелёный).
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && for f in src/lib/services/bracket.test.ts src/lib/services/registration.test.ts src/lib/services/result.test.ts src/lib/services/tournament-status.test.ts src/lib/services/admin.test.ts src/lib/validation/profile.test.ts src/lib/validation/registration.test.ts src/lib/validation/tournament.test.ts; do npx tsx "$f" 2>&1 | tail -1 | grep -q "passed" || { echo "FAIL: $f"; exit 1; }; done && npx tsc --noEmit && echo "ALL_GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - Все 8 test-скриптов (7 существующих + admin) печатают "passed", exit 0
    - `npx tsc --noEmit` exits 0 (весь проект чист)
    - bracket.test.ts/result.test.ts зелёные → playoff-стек + авто-финиш не сломаны
    - registration.test.ts зелёный → registerPair pairs-путь не сломан
  </acceptance_criteria>
  <done>Все три action подключены; полный набор тестов зелёный; playoff/validation регрессии нет.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → participateSingleAction | requireUser() first line; userId из session, tournamentId через .bind() |
| browser → removeRegistrationAction / finishTournamentAction | requireAdmin() first line; ids через .bind(), не из формы; роль из session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Elevation of Privilege | прямой POST в admin-action минуя UI | mitigate | `requireAdmin()` первой строкой в remove/finish — throw "Forbidden" до работы (НЕ ловится) |
| T-08-16 | Spoofing/Tampering | подмена userId/tournamentId/id через форму | mitigate | userId из requireUser(); tournamentId/kind/id через `.bind()` из листа, не из тела (T-08-08/11) |
| T-08-17 | Information Disclosure | raw Prisma/internal error → клиент | mitigate | только типизированные RegistrationError/AdminError RU; иначе generic RU fallback (T-08-07) |
| T-08-18 | Tampering | удаление/финиш в недопустимом статусе | mitigate | status-guard в сервисах (Plan 03) — действуют независимо от UI; идемпотентный финиш |
| T-08-SC | Tampering | npm installs | accept | Фаза не ставит пакетов (RESEARCH audit — нет установок) |
</threat_model>

<verification>
- `npx tsc --noEmit` чисто по всему проекту.
- Все 8 test-скриптов (bracket/registration/result/tournament-status/admin services + profile/registration/tournament validation) зелёные.
- Регрессия playoff/авто-финиш/validation: подтверждена прогоном bracket/result/registration тестов.
</verification>

<success_criteria>
- participateSingleAction / removeRegistrationAction / finishTournamentAction подключены к сервисам Plan 02/03.
- Все три проходят server-side guard и возвращают типизированный RU-результат.
- Полный набор тестов зелёный; playoff/validation инвариант не сломан.
</success_criteria>

<output>
Create `.planning/phases/08-backend-core/08-05-SUMMARY.md` when done
</output>

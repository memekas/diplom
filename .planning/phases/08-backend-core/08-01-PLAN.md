---
phase: 08-backend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validation/tournament.ts
  - src/lib/validation/tournament.test.ts
  - src/lib/services/tournament.ts
  - src/app/(app)/admin/tournaments/actions.ts
autonomous: true
requirements: [TOUR-05]
must_haves:
  truths:
    - "Создание playoff-турнира принимает только size ∈ {4,8,16}; size=6 отклоняется"
    - "Создание round_robin принимает свободный N≥3 (cap ≤24); size=2 отклоняется, size=25 отклоняется"
    - "americano принимает size≥4 и принуждает participantMode=singles; pairs отклоняется"
    - "mexicano принимает size≥8 и принуждает participantMode=singles; size=6 отклоняется"
    - "createTournament записывает format/participantMode/level/price/scoringMode/targetPoints/totalRounds/setsPerMatch/gamesPerSet; status сервер-set 'registration'"
  artifacts:
    - path: "src/lib/validation/tournament.ts"
      provides: "Расширенная createTournamentSchema (format/mode/level/price/scoring/rounds/sets) + superRefine + parseTournamentForm для новых полей"
      contains: "superRefine"
    - path: "src/lib/services/tournament.ts"
      provides: "createTournament пишет все новые поля турнира"
      contains: "scoringMode"
    - path: "src/app/(app)/admin/tournaments/actions.ts"
      provides: "createTournamentAction проводит новые поля через requireAdmin→parse→service"
    - path: "src/lib/validation/tournament.test.ts"
      provides: "Тесты format-зависимой валидации размера/режима"
  key_links:
    - from: "src/app/(app)/admin/tournaments/actions.ts"
      to: "createTournament"
      via: "service call after parseTournamentForm"
      pattern: "createTournament\\(prisma"
    - from: "src/lib/validation/tournament.ts"
      to: "skillLevels"
      via: "import for level enum"
      pattern: "from \"@/lib/validation/auth\""
---

<objective>
Расширить создание турнира (TOUR-05) до всех четырёх форматов с format-зависимой валидацией размера и режима.

Purpose: Админ должен создавать playoff / round_robin / americano / mexicano с корректными ограничениями размера и принуждением одиночного режима для американо/мексикано — фундамент, на котором Фаза 9 строит движки.

Output: Расширенная `createTournamentSchema` (+ `superRefine`), обновлённый `createTournament` (пишет все новые поля), обновлённый `createTournamentAction`, расширенный test-скрипт. Никакого UI (Фаза 11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-core/08-CONTEXT.md
@.planning/phases/08-backend-core/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить createTournamentSchema format-зависимой валидацией</name>
  <files>src/lib/validation/tournament.ts</files>
  <read_first>
    - src/lib/validation/tournament.ts (файл, который меняем — существующая `createTournamentSchema` с `.refine` на size ∈ {4,8,16}, `parseTournamentForm` discriminated shape)
    - src/lib/validation/auth.ts (источник `skillLevels` — 5 латинских уровней; переиспользовать, НЕ дублировать)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Code Example «validation/tournament.ts — format-dependent size/mode via superRefine» — авторитетный образец правил и RU-сообщений)
    - prisma/schema.prisma (Tournament-модель: поля format/participantMode/level/price/scoringMode/targetPoints/totalRounds/setsPerMatch/gamesPerSet с дефолтами — целевые колонки)
  </read_first>
  <action>
    Заменить старую `createTournamentSchema` (текущий `.refine` на size ∈ {4,8,16}) расширенной версией per D-TOUR-05. Экспортировать новые tuple-константы: `tournamentFormats = ["playoff","round_robin","americano","mexicano"]`, `participantModes = ["pairs","singles"]`, `scoringModes = ["sets","points"]`, `PLAYOFF_SIZES = [4,8,16]`, `SIZE_CAP = 24`. Сохранить экспорт существующего `tournamentSizes`, `tournamentStatuses`, `tournamentStatusSchema`, `TournamentStatus` (их импортирует tournament-status.ts — НЕ удалять).
    Поля схемы (на базе RESEARCH Code Example): `name` (как есть), `format` z.enum(tournamentFormats), `participantMode` z.enum(participantModes), `level` z.enum(skillLevels) (импорт из @/lib/validation/auth), `size` z.coerce.number().int().positive(), `price` z.coerce.number().int().min(0).optional(), `scoringMode` z.enum(scoringModes), `targetPoints` z.coerce.number().int().positive().optional(), `totalRounds` z.coerce.number().int().positive().optional(), `setsPerMatch` z.coerce.number().int().min(1).optional() (БЕЗ верхнего лимита — нижняя граница ≥1), `gamesPerSet` z.coerce.number().int().min(1).optional(), `date`/`location` (как есть).
    Добавить `.superRefine((d, ctx) => {...})` с правилами: playoff → size ∈ PLAYOFF_SIZES иначе issue path:["size"] «Размер должен быть 4, 8 или 16»; round_robin → size≥3 иначе «Минимум 3 участника», size>SIZE_CAP иначе `Максимум ${SIZE_CAP}`; americano → size≥4 иначе «Минимум 4 игрока», cap; mexicano → size≥8 иначе «Минимум 8 игроков», cap. participantMode-принуждение (D1): americano/mexicano с participantMode!=="singles" → issue path:["participantMode"] «Американо/Мексикано — только одиночная регистрация». scoringMode (D3): americano/mexicano со scoringMode==="sets" → issue path:["scoringMode"] «Для американо/мексикано используйте режим очков». points-режим: targetPoints определён и ≤0 → issue path:["targetPoints"] «Целевые очки > 0».
    НЕ добавлять Prisma-enum, НЕ добавлять верхний лимит на sets/games/points (D5 — без лимитов). Использовать существующий стиль RU-сообщений.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "src/lib/validation/tournament.ts" | grep -qx 0 && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `createTournamentSchema.safeParse({name:"T",format:"playoff",participantMode:"pairs",level:"intermediate",size:6,scoringMode:"sets"})` → success=false (path size)
    - `...size:8...` playoff → success=true
    - `...format:"round_robin",size:2...` → success=false; `...size:3...` → success=true; `...size:25...` → success=false
    - `...format:"americano",participantMode:"pairs",size:8,scoringMode:"points"...` → success=false (path participantMode)
    - `...format:"americano",participantMode:"singles",scoringMode:"sets",size:8...` → success=false (path scoringMode)
    - `...format:"mexicano",participantMode:"singles",scoringMode:"points",size:6...` → success=false (size<8)
    - Экспорты `tournamentFormats`, `participantModes`, `scoringModes`, `PLAYOFF_SIZES`, `SIZE_CAP`, `skillLevels`-import присутствуют; `tournamentStatuses`/`TournamentStatus` сохранены
  </acceptance_criteria>
  <done>createTournamentSchema валидирует размер/режим по формату согласно TOUR-05; старые status-экспорты не сломаны.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить parseTournamentForm под новые поля</name>
  <files>src/lib/validation/tournament.ts</files>
  <read_first>
    - src/lib/validation/tournament.ts (после Task 1 — расширенная схема + текущий `parseTournamentForm`, читающий только name/size/date/location)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Pattern 1 — parse helper = единый источник для client UX + server boundary, не должны разойтись)
  </read_first>
  <action>
    Расширить `parseTournamentForm(formData)`: читать из FormData все новые ключи — `format`, `participantMode`, `level`, `size`, `price`, `scoringMode`, `targetPoints`, `totalRounds`, `setsPerMatch`, `gamesPerSet` (опциональные числовые → `formData.get(k) ?? undefined`, пустая строка передаётся в z.coerce optional как есть — для optional чисел используем `formData.get(k) || undefined` чтобы "" → undefined). Сохранить чтение name/date/location. Обновить `CreateTournamentInput` (он `z.infer`, обновится автоматически) и тип `ParseTournamentFormResult` errors-ключи: расширить union до всех новых полей (`"name"|"size"|"format"|"participantMode"|"level"|"price"|"scoringMode"|"targetPoints"|"totalRounds"|"setsPerMatch"|"gamesPerSet"|"date"|"location"`). Логику сбора issue→errors (первый issue по path[0]) НЕ менять.
    Это серверная граница — клиентская форма Фазы 11 будет использовать тот же parse, поэтому ключи должны совпадать с именами полей формы.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsx src/lib/validation/tournament.test.ts 2>&1 | tail -1 | grep -q "passed" && echo "VALIDATION_TESTS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `parseTournamentForm(fd)` с валидным FormData (format=playoff,participantMode=pairs,level=intermediate,size=8,scoringMode=sets) → {ok:true, data:{...}}
    - `parseTournamentForm(fd)` с size=6,format=playoff → {ok:false, errors:{size: ...}}
    - Пустые опциональные поля (price="", targetPoints="") не вызывают ложного reject
    - Тип errors включает все новые поля
  </acceptance_criteria>
  <done>parseTournamentForm читает и валидирует все новые поля; старые тесты-кейсы parse сохраняются (после правки в Task 3 теста).</done>
</task>

<task type="auto">
  <name>Task 3: Записать новые поля в createTournament + расширить тесты + провести через action</name>
  <files>src/lib/services/tournament.ts, src/app/(app)/admin/tournaments/actions.ts, src/lib/validation/tournament.test.ts</files>
  <read_first>
    - src/lib/services/tournament.ts (текущий `createTournament` — пишет name/size/status/date/location, статус hard-set "registration")
    - src/app/(app)/admin/tournaments/actions.ts (текущий `createTournamentAction` — requireAdmin→parseTournamentForm→createTournament→redirect; errors-тип)
    - src/lib/validation/tournament.test.ts (образец self-contained tsx-теста: `check(name, fn)`, `form(entries)` helper, exits non-zero; стиль 32 assertions)
    - prisma/schema.prisma (Tournament — какие поля писать, дефолты setsPerMatch=3/gamesPerSet=6)
  </read_first>
  <action>
    В `createTournament` (services/tournament.ts): добавить в `data` запись новых полей из `data: CreateTournamentInput`: `format`, `participantMode`, `level`, `price: data.price ?? null`, `scoringMode`, `targetPoints: data.targetPoints ?? (data.scoringMode === "points" ? 24 : null)` (серверный дефолт 24 для points per D-TOUR-05), `totalRounds: data.totalRounds ?? null`, `setsPerMatch`/`gamesPerSet` писать только если переданы (иначе схема-дефолт): `...(data.setsPerMatch !== undefined ? { setsPerMatch: data.setsPerMatch } : {})` и аналогично gamesPerSet. `status: "registration"` остаётся hard-set сервером (НИКОГДА из input — T-02-02). Расширить `tournamentSelect` новыми полями (format/participantMode/level/price/scoringMode/targetPoints/totalRounds) чтобы возвращаемая строка их содержала.
    В `createTournamentAction` (actions.ts): расширить errors-тип union новыми ключами полей (синхронно с ParseTournamentFormResult). Логику requireAdmin→parse→createTournament→redirect НЕ менять (redirect вне try/catch — NEXT_REDIRECT).
    В tournament.test.ts: добавить блок проверок format-зависимой валидации (size×format матрица из acceptance Task 1), participantMode-принуждение americano/mexicano, scoringMode-правило. Сохранить существующие 32 assertion (playoff size {4,8,16} как подмножество). Обновить существующие parse-кейсы под новую обязательную форму (добавить format/participantMode/level/scoringMode в валидные FormData чтобы они оставались valid). Обновить счётчик в финальном `console.log`.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsx src/lib/validation/tournament.test.ts 2>&1 | tail -1 | grep -q "passed" && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -cE "tournament\.ts|actions\.ts" | grep -qx 0 && echo "PLAN01_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx src/lib/validation/tournament.test.ts` exits 0, печатает "passed" (≥ прежних 32 + новые)
    - Тест содержит кейс «createTournamentSchema rejects size=6 when format=playoff» и «americano forces singles»
    - `createTournament` пишет format/participantMode/level/scoringMode; points-режим без targetPoints → сервер ставит 24
    - `npx tsc --noEmit` чисто по tournament.ts/actions.ts
    - status в createTournament hard-set "registration" (не из input)
  </acceptance_criteria>
  <done>Новые поля турнира пишутся в БД, проходят format-зависимую валидацию и проводятся через admin-action; тесты зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → createTournamentAction | Недоверенный FormData (название/размер/формат/режим/уровень/цена) пересекает границу; роль и status НЕ из клиента |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | createTournamentAction | mitigate | `requireAdmin()` первой строкой — throw "Forbidden" до parse/DB (существующий паттерн, не трогается) |
| T-08-02 | Tampering | size/format/participantMode/scoringMode из формы | mitigate | server-side superRefine отвергает невалидные комбинации до записи; клиент cosmetic (V5) |
| T-08-03 | Tampering | status турнира из формы | mitigate | createTournament hard-set `status:"registration"`, НИКОГДА из input (T-02-02) |
| T-08-SC | Tampering | npm installs | accept | Фаза не ставит пакетов (RESEARCH Package Legitimacy Audit — нет установок); slopcheck не требуется |
</threat_model>

<verification>
- `npx tsc --noEmit` чисто (нет новых ошибок в изменённых файлах).
- `npx tsx src/lib/validation/tournament.test.ts` зелёный (расширенный набор).
- Регрессия: остальные 6 test-скриптов остаются зелёными (запускаются в Plan 05 как фаза-инвариант; здесь — не сломать validation/tournament.ts экспорты, которые импортирует tournament-status.ts).
</verification>

<success_criteria>
- createTournamentSchema валидирует size/режим по формату (playoff {4,8,16}; round_robin ≥3≤24; americano ≥4 singles-only; mexicano ≥8 singles-only).
- createTournament записывает все новые поля; status сервер-set; points-дефолт targetPoints=24.
- createTournamentAction проводит новые поля через requireAdmin+parse.
- tournament-validation тесты зелёные и покрывают новые правила.
</success_criteria>

<output>
Create `.planning/phases/08-backend-core/08-01-SUMMARY.md` when done
</output>

---
phase: 08-backend-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/admin.ts
  - src/lib/services/admin.test.ts
autonomous: true
requirements: [ADMN-01, ADMN-02]
must_haves:
  truths:
    - "removePair удаляет Pair по id ТОЛЬКО при status='registration'; иначе бросает not_open"
    - "removeParticipant удаляет TournamentPlayer по id ТОЛЬКО при status='registration'"
    - "finishTournament переводит in_progress→finished через transitionTournament"
    - "finishTournament идемпотентен: на уже-finished турнире — no-op (без ошибки)"
  artifacts:
    - path: "src/lib/services/admin.ts"
      provides: "removePair / removeParticipant (status-guarded delete) + finishTournament (идемпотентный финиш через transitionTournament)"
      contains: "finishTournament"
    - path: "src/lib/services/admin.test.ts"
      provides: "Тесты status-guard remove + идемпотентный finish"
  key_links:
    - from: "finishTournament"
      to: "transitionTournament"
      via: "reuse forward-only machine in_progress→finished"
      pattern: "transitionTournament\\("
    - from: "removePair/removeParticipant"
      to: "tx.tournament status guard"
      via: "delete only when registration"
      pattern: "registration"
---

<objective>
Реализовать админ-сервисы удаления регистрации (ADMN-01) и ручного завершения турнира (ADMN-02).

Purpose: Админ должен удалять зарегистрированную пару/участника на этапе открытой регистрации и вручную завершать турнир любого формата. Сервисный слой переиспользует существующую forward-only машину `transitionTournament` (не меняя её) и status-guarded delete. Никаких action/UI — actions проводит Plan 05.

Output: новый `src/lib/services/admin.ts` (`removePair`, `removeParticipant`, `finishTournament`) + self-contained test-скрипт. Playoff авто-финиш (result.ts) НЕ трогается — ручной финиш просто другой путь к тому же терминалу.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-core/08-CONTEXT.md
@.planning/phases/08-backend-core/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Реализовать removePair / removeParticipant / finishTournament</name>
  <files>src/lib/services/admin.ts</files>
  <read_first>
    - src/lib/services/tournament-status.ts (`transitionTournament(prisma, id, from, to)` — forward-only машина, re-читает DB status, бросает на mismatch; ПЕРЕИСПОЛЬЗОВАТЬ для finish, НЕ менять)
    - src/lib/services/registration.ts (стиль сервиса: `prisma` передаётся внутрь, `$transaction`, типизированный error-класс с code; образец для admin-сервисов)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Code Example «Admin remove + finish actions» + Pitfall 8 «ручной finish идемпотентен» + Don't Hand-Roll «Status-машина финиша»)
    - prisma/schema.prisma (Pair, TournamentPlayer — delete by id; Tournament.status; Pair onDelete:Cascade с tournament)
  </read_first>
  <action>
    Создать `src/lib/services/admin.ts`. Импорт: `PrismaClient` type, `transitionTournament` из `./tournament-status`. Определить типизированный error-класс `AdminError extends Error` с code union `"not_open"` (для удаления вне registration), name="AdminError" — зеркало RegistrationError, чтобы action (Plan 05) маппил code→RU без string-match.
    `removePair(prisma, { tournamentId, pairId })`: в `$transaction` re-read `tx.tournament.findUniqueOrThrow({ where:{id:tournamentId}, select:{status:true} })`; `if (status !== "registration") throw new AdminError("not_open", "Удаление возможно только до старта турнира")`; затем `tx.pair.delete({ where:{ id: pairId } })`. tournamentId передаётся для status-guard, pairId — что удаляем.
    `removeParticipant(prisma, { tournamentId, playerId })`: то же, но `tx.tournamentPlayer.delete({ where:{ id: playerId } })`.
    `finishTournament(prisma, tournamentId)`: per Pitfall 8 (идемпотентность) — сначала `prisma.tournament.findUniqueOrThrow({ where:{id:tournamentId}, select:{status:true} })`; `if (status === "finished") return` (no-op, без ошибки); иначе `await transitionTournament(prisma, tournamentId, "in_progress", "finished")`. НЕ дублировать status-машину (Don't Hand-Roll) — переиспользовать transitionTournament. Playoff авто-финиш в result.ts НЕ трогать.
    Удаление гейтить status'ом (ADMN-01 — только во время registration). finishTournament работает для всех форматов (для round-based — основной путь, для playoff — дополнение к авто-финишу).
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "admin.ts" | grep -qx 0 && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `removePair`, `removeParticipant`, `finishTournament`, `AdminError` экспортируются
    - removePair бросает AdminError("not_open") когда status!=="registration"; иначе вызывает pair.delete
    - removeParticipant бросает not_open вне registration; иначе tournamentPlayer.delete
    - finishTournament при status==="finished" → возврат без вызова transitionTournament (идемпотентность)
    - finishTournament при status==="in_progress" → вызывает transitionTournament(prisma,id,"in_progress","finished")
    - finishTournament НЕ переопределяет ALLOWED_TRANSITIONS (переиспользует существующую машину)
  </acceptance_criteria>
  <done>Админ-сервисы удаления (status-guarded) и идемпотентного финиша реализованы поверх существующей status-машины.</done>
</task>

<task type="auto">
  <name>Task 2: Тесты admin-сервисов (status-guard remove + идемпотентный finish)</name>
  <files>src/lib/services/admin.test.ts</files>
  <read_first>
    - src/lib/services/tournament-status.test.ts (образец self-contained tsx-теста с fakePrisma для async-кейсов transitionTournament: `check`/`checkAsync`, fake `tournament.findUniqueOrThrow`/`update`, exits non-zero)
    - src/lib/services/admin.ts (после Task 1 — сигнатуры removePair/removeParticipant/finishTournament, AdminError)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Validation Architecture — «remove + idempotent finish (новый admin/tournament-status тест)»)
  </read_first>
  <action>
    Создать `src/lib/services/admin.test.ts` в стиле существующих self-contained tsx-скриптов (import assert, `checkAsync`, счётчик passed, финальный console.log, exits non-zero на провале). Hand-written fakePrisma с конфигурируемым `status` и `$transaction(fn)=>fn(tx)`; tx содержит `tournament.findUniqueOrThrow` (возвращает {status}), `tournament.update` (для transitionTournament — возвращает {id,status}), `pair.delete` (записывает вызванный where.id в calls), `tournamentPlayer.delete` (то же). finishTournament вызывает `prisma.findUniqueOrThrow` вне tx и `transitionTournament` (который сам зовёт prisma.findUniqueOrThrow+update) — фейк должен покрывать оба уровня (тот же объект как prisma и как tx).
    Кейсы:
    - removePair: status="registration" → pair.delete вызван с переданным pairId; status="in_progress" → бросает AdminError code "not_open", delete НЕ вызван; status="finished" → not_open.
    - removeParticipant: status="registration" → tournamentPlayer.delete вызван; status≠registration → not_open.
    - finishTournament: status="finished" → no-op (update НЕ вызван, без throw); status="in_progress" → update вызван со status:"finished"; status="registration" → пробрасывает ошибку transitionTournament (expected in_progress but DB has registration) — проверить, что бросает.
    Финальный `console.log("admin: N assertions passed.")`.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsx src/lib/services/admin.test.ts 2>&1 | tail -1 | grep -q "passed" && echo "ADMIN_TESTS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx src/lib/services/admin.test.ts` exits 0, печатает "passed"
    - Кейс: removePair вне registration → AdminError not_open, delete не вызван
    - Кейс: finishTournament на уже-finished → no-op (idempotent), без throw
    - Кейс: finishTournament на in_progress → transitionTournament вызван, status→finished
    - Кейс: finishTournament на registration → throw (illegal/mismatch)
  </acceptance_criteria>
  <done>Тесты подтверждают status-guard удаления и идемпотентный финиш через transitionTournament.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action → removePair/removeParticipant/finishTournament (Plan 05) | requireAdmin в action; tournamentId/id через .bind(); сервис re-читает status из БД |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | удаление регистрации после старта турнира | mitigate | status-guard `=== "registration"` в транзакции remove; AdminError not_open (ADMN-01) |
| T-08-09 | Tampering | финиш из недопустимого статуса / минуя машину | mitigate | finishTournament делегирует transitionTournament — forward-only guard + DB source of truth (re-read) |
| T-08-10 | Denial of Service / повторный финиш | finishTournament на finished | mitigate | идемпотентный no-op (Pitfall 8) — повторный POST не бросает и не портит состояние |
| T-08-SC | Tampering | npm installs | accept | Фаза не ставит пакетов (RESEARCH audit — нет установок) |
</threat_model>

<verification>
- `npx tsc --noEmit` чисто по admin.ts/admin.test.ts.
- `npx tsx src/lib/services/admin.test.ts` зелёный.
- Регрессия: `transitionTournament` не модифицирован → tournament-status.test.ts остаётся зелёным (фаза-инвариант в Plan 05).
</verification>

<success_criteria>
- removePair/removeParticipant удаляют только при status="registration".
- finishTournament переводит in_progress→finished через существующую машину и идемпотентен на finished.
- admin тесты зелёные.
- Playoff авто-финиш (result.ts) не затронут.
</success_criteria>

<output>
Create `.planning/phases/08-backend-core/08-03-SUMMARY.md` when done
</output>

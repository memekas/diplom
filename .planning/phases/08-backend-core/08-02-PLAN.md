---
phase: 08-backend-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/registration.ts
  - src/lib/services/registration.test.ts
  - src/lib/validation/registration.ts
autonomous: true
requirements: [REG-05, REG-06]
must_haves:
  truths:
    - "registerPair отклоняет пару, если ЛЮБОЙ из двух игроков не равен tournament.level (строгое равенство)"
    - "registerSingle создаёт TournamentPlayer внутри транзакции с проверками статус/уровень/лимит/дубль/режим"
    - "registerSingle отклоняет регистрацию, если tournament.participantMode !== 'singles' (wrong_mode)"
    - "registerPair отклоняет регистрацию, если tournament.participantMode !== 'pairs' (wrong_mode)"
    - "Лимит singles считается по TournamentPlayer count vs tournament.size"
  artifacts:
    - path: "src/lib/services/registration.ts"
      provides: "registerSingle + level-check в registerPair + новые коды RegistrationError (level_mismatch, wrong_mode)"
      contains: "registerSingle"
    - path: "src/lib/validation/registration.ts"
      provides: "parseRegisterSingleForm (singles не требует партнёрских полей)"
      contains: "parseRegisterSingleForm"
    - path: "src/lib/services/registration.test.ts"
      provides: "Тесты level-mismatch (pair оба/один), registerSingle (full/dup/level/wrong_mode)"
  key_links:
    - from: "registerSingle"
      to: "tx.tournamentPlayer.count"
      via: "capacity gate vs size"
      pattern: "tournamentPlayer\\.count"
    - from: "registerPair"
      to: "tx.user.findMany skillLevel"
      via: "level equality check both players"
      pattern: "skillLevel"
---

<objective>
Реализовать матчинг уровня (REG-05) и одиночную регистрацию (REG-06) на сервисном слое.

Purpose: Игрок может записаться только на турнир своего уровня (строгое равенство; для пары — оба игрока), и может записаться одиночно на singles-турнир (americano/mexicano/round_robin/playoff-singles) с той же транзакционной целостностью, что и парная регистрация. Никаких action/UI — только сервисы + parse-хелпер (actions проводит Plan 05).

Output: `registerSingle` (новый сервис на TournamentPlayer), level-check внутри `registerPair`, новые коды `RegistrationError` (`level_mismatch`, `wrong_mode`), `parseRegisterSingleForm`, расширенный test-скрипт.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-core/08-CONTEXT.md
@.planning/phases/08-backend-core/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить коды ошибок + level-check в registerPair</name>
  <files>src/lib/services/registration.ts</files>
  <read_first>
    - src/lib/services/registration.ts (файл, который меняем — `RegistrationError` с union кодов, `registerPair` транзакция: status re-read → self-guard → capacity → cross-slot dup → insert; `pairSelect`; `findUserIdByNickname`)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Code Example «Level check injection into registerPair (REG-05)» + Pitfall 6 «для пары проверять ОБА игрока» + Pitfall 5 «participantMode mismatch»)
    - prisma/schema.prisma (Tournament.level + Tournament.participantMode; User.skillLevel)
  </read_first>
  <action>
    Расширить union кодов `RegistrationError` двумя новыми: `"level_mismatch"` и `"wrong_mode"` (добавить в literal union в конструкторе, сохранить существующие self_partner/already_registered/tournament_full/not_open/partner_not_found).
    В `registerPair` транзакции: расширить `select` в `findUniqueOrThrow` турнира полями `level: true, participantMode: true` (к существующим id/status/size). Сразу после проверки `status !== "registration"` добавить mode-guard per Pitfall 5: `if (t.participantMode !== "pairs") throw new RegistrationError("wrong_mode", "На этот турнир регистрация только одиночная")`. После self-guard/capacity/cross-slot-dup (перед insert, как в RESEARCH Code Example) добавить level-check: прочитать обоих игроков `tx.user.findMany({ where: { id: { in: [player1Id, player2Id] } }, select: { id: true, skillLevel: true } })` и `if (players.some((p) => p.skillLevel !== t.level)) throw new RegistrationError("level_mismatch", "Уровень игрока не совпадает с уровнем турнира")` — строгое равенство, оба игрока (D2/Pitfall 6).
    Порядок гейтов сохранить целостным: всё внутри одной существующей `$transaction` (Pitfall 7 — не выносить наружу). НЕ менять `pairSelect`, `findUserIdByNickname`, `listTournamentPairs`, cross-slot dup-логику.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "registration.ts" | grep -qx 0 && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `RegistrationError` принимает code "level_mismatch" и "wrong_mode" без TS-ошибки
    - registerPair бросает level_mismatch когда player1.skillLevel !== tournament.level
    - registerPair бросает level_mismatch когда только player2 не совпадает (оба проверяются)
    - registerPair бросает wrong_mode когда tournament.participantMode === "singles"
    - registerPair НЕ ломает существующий путь pairs/intermediate (все совпадают → проходит до insert)
    - level-check и mode-guard внутри той же $transaction
  </acceptance_criteria>
  <done>registerPair отвергает несовпадение уровня (оба игрока) и неверный режим; новые коды ошибок добавлены.</done>
</task>

<task type="auto">
  <name>Task 2: Реализовать registerSingle + parseRegisterSingleForm</name>
  <files>src/lib/services/registration.ts, src/lib/validation/registration.ts</files>
  <read_first>
    - src/lib/services/registration.ts (после Task 1 — образец транзакционного гейта registerPair; RegistrationError с новыми кодами)
    - src/lib/validation/registration.ts (текущий `registerPairSchema`/`parseRegisterPairForm` discriminated-shape — образец для singles parse)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Code Example «registerSingle — mirror registerPair on TournamentPlayer» + Pitfall 7 «capacity по TournamentPlayer, не Pair» + Pitfall 5 wrong_mode)
    - prisma/schema.prisma (TournamentPlayer-модель: `@@unique([tournamentId, userId])` → составной where `tournamentId_userId`; поля id/tournamentId/userId/createdAt)
  </read_first>
  <action>
    В registration.ts добавить экспортируемую `registerSingle(prisma, { tournamentId, userId })` (зеркало registerPair на TournamentPlayer, per RESEARCH Code Example). Внутри `prisma.$transaction`: (1) `tx.tournament.findUniqueOrThrow` select id/status/size/level/participantMode; (2) `status !== "registration"` → `RegistrationError("not_open", "Регистрация на турнир закрыта")`; (3) `participantMode !== "singles"` → `RegistrationError("wrong_mode", "На этот турнир регистрация только парой")`; (4) `tx.user.findUniqueOrThrow({ where:{id:userId}, select:{skillLevel:true} })`, `me.skillLevel !== t.level` → `RegistrationError("level_mismatch", "Уровень игрока не совпадает с уровнем турнира")`; (5) capacity per Pitfall 7: `tx.tournamentPlayer.count({ where:{tournamentId} }) >= t.size` → `RegistrationError("tournament_full", "Турнир заполнен")`; (6) dup: `tx.tournamentPlayer.findUnique({ where:{ tournamentId_userId:{ tournamentId, userId } }, select:{id:true} })` → `RegistrationError("already_registered", "Вы уже зарегистрированы в этом турнире")`; (7) `tx.tournamentPlayer.create({ data:{ tournamentId, userId }, select:{ id:true, tournamentId:true, userId:true, createdAt:true } })`. ВСЁ в одной $transaction (count+insert не гоняются).
    В validation/registration.ts добавить `parseRegisterSingleForm(formData)`: singles НЕ требует партнёрских полей — identity берётся из guard в action. Схема пустая/минимальная (`registerSingleSchema = z.object({})` или без полей) + discriminated-shape `{ok:true,data:{}}` / `{ok:false,errors:{}}` для единообразия с parseRegisterPairForm. Экспортировать тип `ParseRegisterSingleFormResult`.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -cE "registration\.ts" | grep -qx 0 && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `registerSingle` экспортируется; принимает {tournamentId, userId}
    - throws not_open когда status!=="registration"
    - throws wrong_mode когда participantMode!=="singles"
    - throws level_mismatch когда me.skillLevel!==tournament.level
    - throws tournament_full когда tournamentPlayer count >= size (НЕ pair count)
    - throws already_registered когда дубль по tournamentId_userId
    - всё внутри одной $transaction
    - parseRegisterSingleForm экспортируется и возвращает discriminated {ok}
  </acceptance_criteria>
  <done>registerSingle реализован зеркально registerPair на TournamentPlayer с лимитом по TournamentPlayer; singles parse-хелпер готов для action.</done>
</task>

<task type="auto">
  <name>Task 3: Расширить registration.test.ts (level + singles + wrong_mode)</name>
  <files>src/lib/services/registration.test.ts</files>
  <read_first>
    - src/lib/services/registration.test.ts (файл, который меняем — `fakePrisma(opts)` с `$transaction(fn)=>fn(tx)`, `tx.tournament.findUniqueOrThrow`, `tx.pair.*`, `checkAsync`, exits non-zero; стиль 8 assertions)
    - src/lib/services/registration.ts (после Task 1/2 — registerPair с level/mode, registerSingle)
    - .planning/phases/08-backend-core/08-RESEARCH.md (Validation Architecture — «level-mismatch + registerSingle + wrong_mode (registration.test.ts EXTEND)»)
  </read_first>
  <action>
    Расширить fakePrisma: добавить в `tournament.findUniqueOrThrow` возвращаемые поля `level` и `participantMode` (из opts с дефолтами level:"intermediate", participantMode:"pairs"). Добавить `tx.user.findMany` (для registerPair level-check — возвращает массив `{id,skillLevel}` из opts) и `tx.user.findUniqueOrThrow` (для registerSingle — `{skillLevel}` из opts). Добавить `tx.tournamentPlayer.{count,findUnique,create}` зеркально pair-фейку (count из opts, findUnique → existing dup или null, create → возвращает строку с id).
    Новые async-кейсы (checkAsync):
    - registerPair: оба игрока совпадают с level → проходит (insert вызван); player1 не совпадает → level_mismatch; player2 не совпадает → level_mismatch; participantMode="singles" → wrong_mode.
    - registerSingle: happy path → tournamentPlayer.create вызван; status!=="registration" → not_open; participantMode="pairs" → wrong_mode; skillLevel mismatch → level_mismatch; count>=size → tournament_full; dup найден → already_registered.
    Проверять `e instanceof RegistrationError && e.code === "..."`. Сохранить существующие 8 pair-assertions (обновить их fakePrisma-вызовы под новые поля level/participantMode так, чтобы они оставались зелёными — pairs/intermediate/совпадающие уровни). Обновить финальный счётчик в `console.log`.
  </action>
  <verify>
    <automated>cd /Users/memeka/diplom && npx tsx src/lib/services/registration.test.ts 2>&1 | tail -1 | grep -q "passed" && echo "REG_TESTS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx src/lib/services/registration.test.ts` exits 0, печатает "passed"
    - Есть кейс «registerSingle throws level_mismatch when player.skillLevel != tournament.level»
    - Есть кейс «registerPair throws level_mismatch when only player2 mismatches»
    - Есть кейс registerSingle tournament_full (по tournamentPlayer count)
    - Есть кейс wrong_mode для обоих путей
    - Существующие 8 pair-assertions сохранены и зелёные
  </acceptance_criteria>
  <done>Тесты покрывают level-matching (пара: оба игрока), singles-регистрацию и mode-guards; набор зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action → registerPair/registerSingle (Plan 05) | userId из session-guard, tournamentId через .bind(); сервис re-читает status/level/mode/capacity из БД |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Business-logic abuse | регистрация на чужой уровень | mitigate | строгое равенство `skillLevel === tournament.level` внутри транзакции (для пары — оба); level_mismatch (V5) |
| T-08-05 | Tampering | participantMode из формы vs DB | mitigate | re-read `tournament.participantMode` в транзакции; singles-путь требует "singles", pair-путь — "pairs"; wrong_mode (Pitfall 5) |
| T-08-06 | Tampering | переполнение singles-турнира гонкой | mitigate | count(TournamentPlayer)+insert в одной $transaction (Pitfall 7) |
| T-08-07 | Information Disclosure | raw Prisma error → клиент | mitigate | только типизированные RegistrationError RU-сообщения; action (Plan 05) даёт generic fallback |
| T-08-SC | Tampering | npm installs | accept | Фаза не ставит пакетов (RESEARCH audit — нет установок) |
</threat_model>

<verification>
- `npx tsc --noEmit` чисто по registration.ts/registration validation.
- `npx tsx src/lib/services/registration.test.ts` зелёный (8 старых + новые level/singles/mode).
- Регрессия: playoff registerPair-путь не сломан (совпадающие уровни/pairs проходят до insert) — покрыто в тестах; полный фаза-инвариант гоняется в Plan 05.
</verification>

<success_criteria>
- registerPair отвергает несовпадение уровня (оба игрока) строгим равенством и неверный режим.
- registerSingle создаёт TournamentPlayer с проверками status/level/capacity(по TournamentPlayer)/dup/mode в одной транзакции.
- parseRegisterSingleForm готов для action.
- registration тесты зелёные с новыми кейсами.
</success_criteria>

<output>
Create `.planning/phases/08-backend-core/08-02-SUMMARY.md` when done
</output>

---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/auth-guards.ts
  - prisma/seed.ts
  - .env.example
  - .env
  - src/app/(app)/admin/page.tsx
  - src/app/(app)/admin/actions.ts
autonomous: false
requirements: [AUTH-04, AUTH-05]
must_haves:
  truths:
    - "Running the seed creates exactly one admin account (role 'admin') from ADMIN_EMAIL/ADMIN_PASSWORD env vars"
    - "Re-running the seed does not create a duplicate admin"
    - "A Server Action guarded by requireAdmin rejects a non-admin (and anonymous) caller on direct invocation, not just by hiding UI"
    - "requireUser rejects an anonymous caller and returns the session user otherwise"
    - "The admin email/password are read from env and documented in .env.example (no committed credentials)"
  artifacts:
    - path: "src/lib/auth-guards.ts"
      provides: "Server-side auth guards requireUser() and requireAdmin() over auth.api.getSession({ headers })"
      exports: ["requireUser", "requireAdmin"]
    - path: "prisma/seed.ts"
      provides: "Idempotent admin seed from env: signUpEmail then set role 'admin'; existence-guarded"
    - path: ".env.example"
      provides: "Documented ADMIN_EMAIL / ADMIN_PASSWORD (+ DATABASE_URL, BETTER_AUTH_SECRET)"
    - path: "src/app/(app)/admin/actions.ts"
      provides: "An admin-guarded Server Action proving requireAdmin rejects non-admins"
  key_links:
    - from: "src/lib/auth-guards.ts"
      to: "src/lib/auth.ts"
      via: "auth.api.getSession({ headers: await headers() })"
      pattern: "getSession"
    - from: "prisma/seed.ts"
      to: "src/lib/auth.ts"
      via: "auth.api.signUpEmail"
      pattern: "signUpEmail"
    - from: "src/app/(app)/admin/actions.ts"
      to: "src/lib/auth-guards.ts"
      via: "requireAdmin()"
      pattern: "requireAdmin"
---

<objective>
Add the security spine for all future phases: server-side role guards (`requireUser`/`requireAdmin`) that derive identity from the Better Auth session (signed cookie) — the real authorization boundary, not UI hiding — and an idempotent admin seed that creates exactly one organization admin from env vars. Prove the guard with a real admin-only Server Action that rejects non-admins on direct invocation.

Implements AUTH-04 (idempotent env-based admin seed, role "admin") and AUTH-05 (server-side requireAdmin/requireUser guarding Server Actions).

Purpose: Every later mutation (create tournament, generate bracket, record result) calls these guards as its first line; without them, hidden-UI is the only "protection" — a known exploit (PITFALLS Pitfall 8). The single admin must exist deterministically from env so graders can recreate it.
Output: `lib/auth-guards.ts`, idempotent `prisma/seed.ts`, documented env, and a demonstrable guarded admin action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/AUTH.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Server-side guards requireUser() and requireAdmin()</name>
  <files>src/lib/auth-guards.ts</files>
  <read_first>
    - .planning/research/AUTH.md (Admin-only Server Action pattern: `auth.api.getSession({ headers: await headers() })`; admin check = `session.user.role === "admin"`; runs in Node runtime)
    - .planning/research/ARCHITECTURE.md (Auth boundary: requireUser/requireAdmin called inside every Server Action and protected Server Component; middleware is UX only, NOT security)
    - .planning/research/PITFALLS.md (Pitfall 8: every mutating action re-reads session server-side and asserts role === 'admin' as its FIRST line; never trust client-supplied role/id)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (guards in lib/auth-guards.ts via auth.api.getSession({ headers }))
    - src/lib/auth.ts (the `auth` instance from plan 01)
  </read_first>
  <action>
    Create `src/lib/auth-guards.ts` (server-only; import `auth` from `@/lib/auth` and `headers` from `next/headers`).
    Export `async function requireUser()`: call `auth.api.getSession({ headers: await headers() })`; if no session/user, throw a clear error (e.g. `throw new Error("Unauthorized")`) — callers in Server Components may convert to a `redirect("/login")`, but the guard's contract is to reject. Return the authenticated `session.user`.
    Export `async function requireAdmin()`: get the session the same way; if no session OR `session.user.role !== "admin"`, throw `new Error("Forbidden")`. Return the admin user. The admin check is `role === "admin"` per AUTH.md/CONTEXT.md.
    Both guards derive identity ONLY from the session cookie (never from arguments, props, or form data) — this is the defense-in-depth boundary (Pitfall 8). These run in the Node runtime; no Edge concerns.
    Add concise comments noting these guard Server Actions, not just UI.
  </action>
  <verify>
    <automated>grep -q "export async function requireUser" src/lib/auth-guards.ts && grep -q "export async function requireAdmin" src/lib/auth-guards.ts && grep -q "getSession" src/lib/auth-guards.ts && grep -q 'role !== "admin"\|role === "admin"' src/lib/auth-guards.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `requireUser` returns the session user or throws when anonymous.
    - `requireAdmin` returns the user only when `session.user.role === "admin"`, else throws.
    - Identity comes solely from `auth.api.getSession({ headers })`; no client-trusted role.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Reusable server-side guards exist; every future mutation can gate on them.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Idempotent admin seed from env + guarded admin Server Action</name>
  <files>prisma/seed.ts, .env.example, .env, src/app/(app)/admin/page.tsx, src/app/(app)/admin/actions.ts</files>
  <read_first>
    - .planning/research/AUTH.md (Seed pattern: existence check → `auth.api.signUpEmail({ body: { email, password, name } })` → `prisma.user.update({ role: "admin" })`; idempotent; run via `prisma db seed` / tsx)
    - .planning/research/PITFALLS.md (Pitfall 12: seed creds from env vars, never committed; idempotent so re-run doesn't duplicate)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md (Admin seed: env ADMIN_EMAIL/ADMIN_PASSWORD; create via signUpEmail then set role admin; re-run no duplicate)
    - src/lib/auth.ts, src/lib/db.ts (plan 01); src/lib/auth-guards.ts (Task 1)
    - package.json (the `prisma.seed` = `tsx prisma/seed.ts` hook from plan 01)
  </read_first>
  <action>
    Create `prisma/seed.ts`: read `ADMIN_EMAIL` and `ADMIN_PASSWORD` from `process.env` (throw a clear error if either is missing). Query `prisma.user.findUnique({ where: { email: ADMIN_EMAIL } })`. If it does NOT exist: `await auth.api.signUpEmail({ body: { email, password, name: "Org Admin" } })`, then `await prisma.user.update({ where: { email }, data: { role: "admin" } })`. If it already exists: do nothing (idempotent — re-run must not duplicate). Log a concise result line. Import `auth` from `@/lib/auth` and `prisma` from `@/lib/db` (or relative paths that resolve under tsx).
    Update `.env.example` to document `ADMIN_EMAIL` and `ADMIN_PASSWORD` (placeholders, no real secrets) alongside existing `DATABASE_URL`/`BETTER_AUTH_SECRET`. Add real values to `.env` (gitignored) for local seeding. Do NOT commit credentials (Pitfall 12).
    Create a minimal admin landing to host the guarded action: `src/app/(app)/admin/page.tsx` (Server Component) that calls `requireAdmin()` at the top (redirect/throw for non-admins) and renders a tiny placeholder; and `src/app/(app)/admin/actions.ts` (`"use server"`) exporting an action `adminPing()` whose FIRST line is `await requireAdmin()` then returns a success marker. This action exists to prove AUTH-05 — it must reject a non-admin/anonymous caller on direct invocation (not via hidden UI). Keep it minimal; later phases replace it with real admin mutations.
  </action>
  <verify>
    <automated>grep -q "signUpEmail" prisma/seed.ts && grep -q "findUnique" prisma/seed.ts && grep -q 'role: "admin"\|role:"admin"' prisma/seed.ts && grep -q "ADMIN_EMAIL" .env.example && grep -q "ADMIN_PASSWORD" .env.example && grep -q "requireAdmin" "src/app/(app)/admin/actions.ts" && grep -q "requireAdmin" "src/app/(app)/admin/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/seed.ts` reads ADMIN_EMAIL/ADMIN_PASSWORD from env, throws if missing, and is existence-guarded (idempotent).
    - Seed creates the admin via `auth.api.signUpEmail` then sets `role: "admin"`.
    - `.env.example` documents ADMIN_EMAIL and ADMIN_PASSWORD; no real credentials committed.
    - `src/app/(app)/admin/actions.ts` exports an action whose first line is `await requireAdmin()`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Idempotent env-based admin seed and a real guarded admin Server Action exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — admin seed idempotency + guard rejection</name>
  <files>(verification only — no files modified)</files>
  <action>
    Verification checkpoint — no code changes. Run the seed twice and exercise the guard from both a non-admin and the admin session, then pause for the reviewer's approval per the steps below.
  </action>
  <what-built>
    The idempotent admin seed and the server-side guards. This verifies one admin exists from env, re-seeding doesn't duplicate, and a guarded admin action actually rejects a non-admin (the AUTH-05 security proof).
  </what-built>
  <how-to-verify>
    1. Ensure `.env` has ADMIN_EMAIL and ADMIN_PASSWORD. Run `npx prisma db seed`. Expect a log line confirming the admin was created.
    2. Run `npx prisma db seed` AGAIN. Expect it to skip creation (idempotent — no error, no duplicate).
    3. In `npx prisma studio`, confirm exactly ONE user with that email and `role = "admin"`.
    4. Run `npm run dev`. Sign in as a NON-admin player (from plan 01) and visit /admin. Expect: rejected (redirect to /login or an error / not the admin page) — the page guard fires.
    5. Sign in as the admin (ADMIN_EMAIL/ADMIN_PASSWORD) and visit /admin. Expect: the admin placeholder renders.
    6. (Direct-call proof) Confirm `adminPing` in `src/app/(app)/admin/actions.ts` calls `await requireAdmin()` as its first line — so a non-admin POST to the action throws "Forbidden", not just a hidden button.
    Type "approved" if all pass, or describe the failure.
  </how-to-verify>
  <verify>
    <human-check>Seed creates one admin and is idempotent on re-run; non-admin is rejected from /admin and from the guarded action; admin can access /admin.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe the failure.</resume-signal>
  <acceptance_criteria>
    - Exactly one admin user (role "admin") after one or more seed runs.
    - Non-admin/anonymous rejected from the guarded action and admin page.
    - Admin can access /admin.
  </acceptance_criteria>
  <done>Admin seed idempotency and server-side guard rejection verified by a human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client ↔ Server Action | Server Actions are public HTTP endpoints; must re-check auth server-side regardless of UI |
| env ↔ seed script | Admin credentials enter via env, never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Elevation of Privilege | Admin-only Server Actions | mitigate | `requireAdmin()` as FIRST line of every admin action; role read from session cookie only (Pitfall 8); verified by direct-call rejection test |
| T-01-06 | Spoofing | requireUser/requireAdmin identity | mitigate | Identity solely from `auth.api.getSession({ headers })`; client-supplied role/id never trusted |
| T-01-07 | Information Disclosure | Admin credentials | mitigate | ADMIN_EMAIL/ADMIN_PASSWORD from env; `.env` gitignored; `.env.example` holds placeholders only (Pitfall 12) |
| T-01-08 | Tampering | Duplicate/weak admin via re-seed | mitigate | Existence-guarded idempotent seed; password owned/hashed by Better Auth (scrypt) |
</threat_model>

<verification>
- Seed creates exactly one admin from env; second run is a no-op (no duplicate).
- `requireUser`/`requireAdmin` reject anonymous; `requireAdmin` rejects non-admin role.
- Guarded admin action rejects non-admin on direct invocation.
- `npx tsc --noEmit` passes.
- No credentials committed (only `.env.example` placeholders).
</verification>

<success_criteria>
- AUTH-04: Idempotent admin seed from ADMIN_EMAIL/ADMIN_PASSWORD creates exactly one role "admin" account; re-run does not duplicate.
- AUTH-05: requireAdmin/requireUser guard Server Actions server-side; non-admin rejected on direct call, not just hidden UI.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md` when done.
</output>

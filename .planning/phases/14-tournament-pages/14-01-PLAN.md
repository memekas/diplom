---
phase: 14-tournament-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/(public)/tournaments/[id]/page.tsx"
  - "src/app/(public)/tournaments/[id]/tournament.css"
  - "src/app/(public)/tournaments/[id]/participate-form.tsx"
  - "src/app/(public)/tournaments/[id]/start-tournament-form.tsx"
  - "src/app/(public)/tournaments/[id]/score-form.tsx"
  - "src/app/(public)/tournaments/[id]/round-score-form.tsx"
  - "src/app/(public)/tournaments/[id]/remove-registration-form.tsx"
  - "src/app/(public)/tournaments/[id]/finish-tournament-form.tsx"
  - "src/components/tournament-status-badge.tsx"
autonomous: true
requirements: [UI-06]

must_haves:
  truths:
    - "Страница турнира открывается в Court programme-вёрстке: hero (eyebrow «Турнир · Формат» + status-badge + h1 + dot-separated lede) над регламент-метой над стартовым листом над CTA"
    - "Регламент — карточка с meta-строками Размер/Формат/Состав/Уровень/Подсчёт/Взнос[/Дата/Место]; значение Формат несёт hover/focus тултип"
    - "Стартовый лист рендерит нумерованные .pair с .avatar+именем+«сторона · уровень»; пара текущего пользователя подсвечена .is-you (через тот же userId-compare, что в v2.0)"
    - "Прогресс набора виден: .cap-count «<b>N</b> / size пар» + .progress бар + .empty «Осталось N мест»"
    - "Формы (participate/single/start/score/round-score/remove/finish) отрисованы на _base form-примитивах (.field/.input/.btn/.error), Server Actions и гейты неизменны"
    - "renderEntry показывается только admin+in_progress; readOnly = !isAdmin || finished — без изменений"
  artifacts:
    - path: "src/app/(public)/tournaments/[id]/tournament.css"
      provides: "002 screen-specific classes absent from globals (.net-rule/.tip/.plist/.pair/.pair-seed/.player/.player-sub/.vs/.you-tag/.cap-head/.cap-count/.cta-stack/.cta-row/.cta-price/.admin-box + .badge-prog::before live-dot)"
      contains: ".plist"
    - path: "src/app/(public)/tournaments/[id]/page.tsx"
      provides: "Court programme hub: .cq wrapper, hero, РЕГЛАМЕНТ .meta card, СТАРТОВЫЙ ЛИСТ .plist, CTA .cta-stack, .admin-box"
      contains: "tournament.css"
  key_links:
    - from: "src/app/(public)/tournaments/[id]/page.tsx"
      to: "tournament.css"
      via: 'import "./tournament.css"'
      pattern: 'import.*tournament\.css'
    - from: "src/app/(public)/tournaments/[id]/page.tsx"
      to: ".is-you userId compare"
      via: "userId === player.id highlight on .pair"
      pattern: "is-you"
---

<objective>
Restyle the tournament detail hub (`(public)/tournaments/[id]/page.tsx`) and its six sibling sub-forms onto the Court «Programme» language (sketch 002 + `_base` form primitives), satisfying UI-06. The hub is the shared shell for ALL formats — hero + РЕГЛАМЕНТ + СТАРТОВЫЙ ЛИСТ + CTA + admin-box stays; the bracket/standings body (rendered by BracketView/RoundRobinView/RotationView) is imported untouched.

Purpose: Page 002 is the entry point a player and the admin land on for every tournament; it must read as Court and preserve every v2.0 gate.
Output: A new co-located `tournament.css`, the restyled hub, six restyled form leaves, and the `TournamentStatusBadge` emitting a pulsing live-dot for the «Идёт» status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tournament-pages/14-CONTEXT.md
@.planning/phases/14-tournament-pages/14-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/tournament-pages.md
@.claude/skills/sketch-findings-diplom/references/forms-and-auth.md
@.planning/sketches/002-tournament-info/index.html

# Phase 13 structural precedent (co-located screen .css + client-leaf pattern)
@.planning/phases/13-auth/13-02-SUMMARY.md
@src/app/(app)/profile/profile.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tournament.css (002 screen-specific classes) + restyle TournamentStatusBadge live-dot</name>
  <read_first>
    - .claude/skills/sketch-findings-diplom/references/tournament-pages.md (CSS Patterns: «Status badge», «Court-net rule», «Dot-separated lede», «Format tooltip», «Start-list pair card + your-pair highlight»)
    - .planning/sketches/002-tournament-info/index.html (the .lede / .tip / .plist / .pair / .cap-head / .cta-stack / .admin-box rules — port verbatim, drop sketch-chrome)
    - src/app/(app)/profile/profile.css (the already-authored .net-rule rule, lines 10-18 — copy verbatim; header convention to mirror)
    - src/components/tournament-status-badge.tsx (already emits .badge .badge-reg/-prog/-fin)
    - src/app/globals.css (confirm .badge-reg::before liveDot already exists; .badge-prog::before may need the same pulse)
  </read_first>
  <action>
    Create `src/app/(public)/tournaments/[id]/tournament.css` carrying ONLY the 002 classes absent from globals.css (do NOT redefine .card/.meta/.badge/.progress/.empty/.field/.input/.btn — those are in globals). Port from sketch 002 + the reference CSS Patterns section, token-only (var(--…)/color-mix — NO hex): `.net-rule` (copy verbatim from profile.css), `.lede`+`.lede > span`+`.lede > span + span::before` dot-separator+`.lede .mono` brighten, `.tip`+`.tip::after`+`.tip:hover::after`+`.tip:focus-visible::after`, `.plist`, `.pair`+`.pair:hover`+`.pair.is-you`+`.pair.is-you .pair-seed`, `.pair-seed`, `.pair-players`, `.player`, `.player-sub`, `.vs`, `.you-tag`, `.cap-head`, `.cap-count`, `.cta-stack`/`.cta-row`/`.cta-price`, `.admin-box` (dashed accent border). Add the live-dot pulse for the prog status: `.badge-prog::before { animation: liveDot 1.8s ease-in-out infinite; }` (the `liveDot` keyframe already lives in globals.css; .badge-reg::before already pulses — only add .badge-prog::before if globals does not already cover it). Head the file with a comment matching profile.css's convention (screen-specific, Court tokens only, no hex). In `src/components/tournament-status-badge.tsx`: no class change needed (it already maps registration→.badge.badge-reg, in_progress→.badge.badge-prog, finished→.badge.badge-fin) — leave the component logic untouched; the live-dot is purely CSS added here.
  </action>
  <verify>
    <automated>test -f "src/app/(public)/tournaments/[id]/tournament.css" && grep -q "\.plist" "src/app/(public)/tournaments/[id]/tournament.css" && grep -q "\.pair\.is-you" "src/app/(public)/tournaments/[id]/tournament.css" && grep -q "\.tip" "src/app/(public)/tournaments/[id]/tournament.css" && grep -q "\.cta-stack\|\.cta-price" "src/app/(public)/tournaments/[id]/tournament.css" && ! grep -vE '^\s*(/\*|\*)' "src/app/(public)/tournaments/[id]/tournament.css" | grep -qE '#[0-9a-fA-F]{3,8}'</automated>
  </verify>
  <done>tournament.css exists with .net-rule/.lede/.tip/.plist/.pair/.is-you/.pair-seed/.player/.player-sub/.vs/.you-tag/.cap-head/.cap-count/.cta-stack/.cta-row/.cta-price/.admin-box and the .badge-prog::before live-dot; zero hex literals outside comments; TournamentStatusBadge logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the detail hub page.tsx onto Court programme (002, UI-06)</name>
  <read_first>
    - src/app/(public)/tournaments/[id]/page.tsx (the FULL file — preserve lines 46-128 data reads + branches + readOnly + renderEntry, and 291-349 visualization dispatch + playoff ScoreForm filter + finish block VERBATIM)
    - .claude/skills/sketch-findings-diplom/references/tournament-pages.md (Design Decisions «Tournament detail page — Programme» + HTML Structures hero/РЕГЛАМЕНТ/start-list)
    - .planning/sketches/002-tournament-info/index.html (hero / meta / plist / cta / admin-box markup)
  </read_first>
  <action>
    Add `import "./tournament.css";` at the top. Replace the `<main className="mx-auto … max-w-2xl …">` wrapper (line 131) with a `.cq` editorial column ≤720px (token-driven max-width; the `.cq` utility from Phase 12 supplies container-type). Restyle ONLY markup/classes — keep every existing data variable (`tournament`, `userId`, `isAdmin`, `isPairsMode`, `isPlayoff`, `pairs`, `players`, `registrantCount`, `isFull`, `alreadyRegistered`, `readOnly`, `renderEntry`, `nameById`) and the label maps (`formatLabels`, `tournamentKindLabels`, `skillLevelLabel`, `courtSideLabel`) untouched. (1) HERO: `eyebrow` «Турнир · {formatLabels[tournament.format]}» + `<TournamentStatusBadge status={tournament.status} />` + `<h1>{tournament.name}</h1>` + a `.lede` of dot-separated `<span>`s for date/location/price; wrap dates and price in `.mono`, and write the price as `Взнос&nbsp;<span className="mono">{tournament.price} ₽</span>&nbsp;с пары` using `&nbsp;` ({" "}) around the price (flex-trim gotcha). Show the date span only when `tournament.date`, location span only when `tournament.location`, price span only when `tournament.price != null`. (2) РЕГЛАМЕНТ: replace the `<dl>` with `.card.card-pad` > `.meta` of `.meta-row`(`.meta-key`/`.meta-val`): Размер сетки «{size} {isPairsMode?'пар':'участников'}», Формат (value wrapped in `<span className="tip" tabIndex={0} data-tip="…">{formatLabels[...]}</span>` with a per-format explanation string — Олимпийская «Игра на вылет: проигравшая пара выбывает…», Круговой «Каждый играет с каждым…», Американо/Мексикано «Одиночная ротация: партнёры меняются каждый раунд…»), Состав «{tournamentKindLabels[participantMode]}», Уровень (lowercase via skillLevelLabel), Подсчёт «{scoringMode==='sets'?'Сеты и геймы':'Очки'}», Взнос за пару (`.meta-val.mono`), then Дата/Место rows when present. (3) СТАРТОВЫЙ ЛИСТ: `.cap-head` (eyebrow «Стартовый лист» + `.cap-count` «<b>{registrantCount}</b> <span className="muted">/ {tournament.size} {isPairsMode?'пар':'участников'}</span>») + `.progress` with inner `<span style={{width: `${Math.min(100, registrantCount/tournament.size*100)}%`}} />` + `.plist`. Render each pair as a `.pair` with `.pair-seed` (1-based index) and `.pair-players` of two `.player` (`.avatar` initials of player.name + `.player-text` > `.player-name` + `.player-sub` «{courtSideLabel(side)} сторона · {skillLevelLabel(level)}») split by a `.vs` divider; add `.is-you` to the pair when the existing `mine` condition (`userId !== null && (pair.player1.id===userId || pair.player2.id===userId)`) is true, with a `.you-tag` «Ваша пара». Mirror the singles branch (one `.player` per `.pair`, `mine` = `p.user.id===userId`). Keep the `RemoveRegistrationForm` injection inside each pair/player when `isAdmin && status==='registration'`. Below the list show `.empty` «Осталось {size - registrantCount} свободных мест» when not full (and the existing empty-state when registrantCount===0). (4) CTA: wrap the registration ladder in `.card.card-pad.cta-stack` (eyebrow «Участие» + `.cta-row` with `.cta-price` «{price} ₽» when price set + the existing `<ParticipateForm>`/`<SingleParticipateForm>`); keep the `userId===null` / `alreadyRegistered` / `isFull` / pairs-vs-singles ladder EXACTLY. (5) ADMIN-BLOCK: wrap `<StartTournamentForm>` in `.admin-box` (dashed) with its existing props. (6) Leave the visualization dispatch (BracketView/RoundRobinView/RotationView import + props), the playoff ScoreForm filter block, and the FinishTournamentForm block structurally unchanged (only the surrounding section heading/spacing may use `.net-rule`/`h2` Court classes). Do NOT change BracketView/RoundRobinView/RotationView props or imports.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'import "./tournament.css"' "src/app/(public)/tournaments/[id]/page.tsx" && grep -q "is-you" "src/app/(public)/tournaments/[id]/page.tsx" && grep -q "data-tip\|tip" "src/app/(public)/tournaments/[id]/page.tsx" && grep -q "cap-count\|plist" "src/app/(public)/tournaments/[id]/page.tsx" && grep -q "renderEntry" "src/app/(public)/tournaments/[id]/page.tsx" && grep -q 'status === "in_progress"' "src/app/(public)/tournaments/[id]/page.tsx" && grep -q "!isAdmin || tournament.status === \"finished\"" "src/app/(public)/tournaments/[id]/page.tsx"</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; hub imports tournament.css, renders .cq + hero(eyebrow+badge+h1+.lede with &nbsp; price) + РЕГЛАМЕНТ .meta with format .tip + .plist with .is-you highlight + .cap-count/.progress + .cta-stack + .admin-box; readOnly (`!isAdmin || finished`) and renderEntry (`isAdmin && in_progress`) gates byte-identical to v2.0; BracketView/RoundRobinView/RotationView props unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the six sub-forms onto _base form primitives</name>
  <read_first>
    - src/app/(public)/tournaments/[id]/participate-form.tsx, start-tournament-form.tsx, score-form.tsx, round-score-form.tsx, remove-registration-form.tsx, finish-tournament-form.tsx (FULL — preserve every useActionState hook, .bind(null,…) action binding, name= attribute, the {ok:false} error branch, pending-label swap, and score-form/round-score-form add/remove-row useState machinery + dynamic name={`set${n}_a`}/points_a/points_b + scoringMode branch — engine-coupled, DO NOT TOUCH the logic)
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (Design Decisions «Form primitives» — .field/.label/.input/.hint/.error/.btn.btn-primary.btn-block)
  </read_first>
  <action>
    Swap ONLY Tailwind utility classes → Court `_base` classes; touch no logic, no hook, no action binding, no name= attribute. In all six leaves: replace the `<label className="flex flex-col gap-1 text-sm">` wrappers with `<div className="field"><label className="label">…</label><input className="input" …/></div>` structure; replace the error `<p className="…bg-red-900/40… / …bg-red-100…">` with `<p className="error">` (the `.error` _base class); replace the submit `<button className="…bg-foreground…">` with `<button className="btn btn-primary btn-block">` (keep `disabled={pending}` and the pending-label swap). participate-form: both `ParticipateForm` + `SingleParticipateForm`, keep `name="player2Nickname"`. start-tournament-form: the `!canStart` early-return note → `.empty` muted note; submit → `.btn.btn-primary`; keep `canStart`/`pairCount`/`size` props and the disabled-when-not-full UX. score-form + round-score-form: each set-row number input → `.input` (narrow — keep a width utility or add a class), the `Сет {n}` label → `.label`, the «+ Добавить сет» and «✕» buttons → `.btn-ghost` (small), submit → `.btn.btn-primary`; KEEP all `name={`set${n}_a`}`/`name={`set${n}_b`}`/`name="points_a"`/`name="points_b"` and the `scoringMode==="points"` branch verbatim. remove-registration-form: button → `.btn-ghost` (small), error → `.error`, keep the `kind`/`id` binding. finish-tournament-form: submit → `.btn.btn-primary` (danger-tinted ghost acceptable via token, no hex), error → `.error`. NO hardcoded hex anywhere — error/buttons use the token-driven `_base` classes.
  </action>
  <verify>
    <automated>npx tsc --noEmit && cd "src/app/(public)/tournaments/[id]" && grep -q 'name="player2Nickname"' participate-form.tsx && grep -q 'participateAction.bind' participate-form.tsx && grep -q 'set${n}_a' score-form.tsx && grep -q 'recordResultAction.bind' score-form.tsx && grep -q 'points_a' round-score-form.tsx && grep -q 'scoringMode === "points"' round-score-form.tsx && grep -q 'removeRegistrationAction.bind' remove-registration-form.tsx && grep -q 'finishTournamentAction.bind' finish-tournament-form.tsx && grep -rl 'className="btn btn-primary' . && ! grep -rIE 'bg-red-900|bg-red-100|bg-foreground' .</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; all six leaves use .field/.label/.input/.error/.btn(.btn-primary|.btn-ghost); every useActionState/.bind/name=/scoringMode branch/set-row machinery preserved verbatim; no bg-red-*/bg-foreground utilities and no hex remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Server Action | participate/start/score/round-score/remove/finish FormData crosses to existing actions (unchanged) |
| anon/player vs admin | renderEntry (score entry) and admin-box (start) must remain admin-gated; finished must remain read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | renderEntry / admin-box gating in page.tsx | mitigate | Preserve `renderEntry = isAdmin && in_progress` and the `isAdmin && status==='registration'` admin-box condition byte-identical; restyle must not render score-entry or start forms to non-admins (grep-verified in Task 2 acceptance) |
| T-14-02 | Tampering | sub-form Server Action bindings | accept | Restyle touches only presentation; `.bind(null,…)` argument binding and `name=` attributes preserved verbatim — server actions + requireAdmin/requireUser guards remain the authoritative boundary (no new attack surface) |
| T-14-03 | Information Disclosure | finished tournaments | mitigate | `readOnly = !isAdmin || status==='finished'` kept verbatim — a finished tournament never re-exposes entry forms after restyle |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (pure restyle, no new deps) — N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `grep -rnE '#[0-9a-fA-F]{3,8}' "src/app/(public)/tournaments/[id]/"` returns no hex outside comments.
- The hub preserves `renderEntry`, the `in_progress`/`registration`/`finished` gates, and `!isAdmin || finished` readOnly verbatim.
- Sub-forms preserve every `useActionState`/`.bind`/`name=` and the score-row machinery.
- `.cq` wrapper present; no `@media` introduced (only `@container` if any breakpoint is added).
</verification>

<success_criteria>
UI-06 met: страница турнира — Court programme (hero name+status, регламент-мета с тултипом формата, стартовый лист с посевом + «ваша пара», прогресс набора, CTA участия); все формы на _base-примитивах; Server Actions, format/mode branching, renderEntry и readOnly гейты сохранены; нулевой hex; tsc зелёный.
</success_criteria>

<output>
Create `.planning/phases/14-tournament-pages/14-01-SUMMARY.md` when done. Record whether the .badge-prog live-dot needed adding (vs already in globals) and any deviation in the РЕГЛАМЕНТ tooltip copy.
</output>

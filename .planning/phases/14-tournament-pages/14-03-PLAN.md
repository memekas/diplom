---
phase: 14-tournament-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/components/round-robin-view.tsx"
  - "src/components/rotation-view.tsx"
  - "src/components/formats.css"
autonomous: true
requirements: [UI-09]

must_haves:
  truths:
    - "Круговой (round-robin-view): «Матчи» сгруппированы по раундам (.round-block/.round-label), каждый .mrow = .court «Корт N» · .matchup (.side.win/.lose + .mdash) · .score (проигравшее число .dim) либо .await «Ожидает счёта»"
    - "Круговой: «Турнирная таблица» в .standings-wrap > .standings-scroll > table.standings, колонки Место/Участник/Игр/Победы/Поражения/Очки/Разница; лидер .leader, призёры .podium; Разница .diff.pos/.neg/.zero"
    - "Американо/Мексикано (rotation-view): «Текущие игры» .matches.live (target/await пилюли) + «Прошедшие игры» (реальные .score) + «Рейтинг игроков»"
    - "Рейтинг игроков: table.standings колонки Место/Игрок/Сыграно/Победы/Очки/Разница, Игрок в .unit-cell (avatar+имя); НЕТ cut-line (чистый рейтинг)"
    - "Обе таблицы скроллятся горизонтально ВНУТРИ .standings-scroll, не страница; computeStandings никогда не пересчитывается"
  artifacts:
    - path: "src/components/formats.css"
      provides: "009 screen-specific classes (.t-head/.meta-strip/.round-prog/.round-block/.round-label/.rl-tag/.matches/.matches.live/.mrow/.court/.matchup/.side/.mdash/.score/.await/.standings-wrap/.standings-scroll/table.standings/.rank/.leader/.podium/.diff/.unit-cell)"
      contains: ".standings-scroll"
    - path: "src/components/round-robin-view.tsx"
      provides: "Court round-robin: .round-block matches + .standings table"
      contains: "round-block"
    - path: "src/components/rotation-view.tsx"
      provides: "Court rotation: .matches.live + past + player rating .standings (no cut-line)"
      contains: "matches live"
  key_links:
    - from: "src/components/round-robin-view.tsx"
      to: "formats.css"
      via: 'import "./formats.css"'
      pattern: 'import.*formats\.css'
    - from: "src/components/rotation-view.tsx"
      to: ".standings-scroll"
      via: "wraps the player rating table for in-box horizontal scroll"
      pattern: "standings-scroll"
---

<objective>
Restyle the two non-playoff format views (`components/round-robin-view.tsx` sketch 009A «Круговой», `components/rotation-view.tsx` sketch 009B «Американо/Мексикано») onto Court, satisfying UI-09: per-round match rows with a court chip, winner/loser sides, dimmed losing score or an «Ожидает счёта» pill, and a wide standings/rating table that scrolls inside its own box. Both share the `.matches`/`.mrow`/`.standings` shell from a single co-located `formats.css`.

Purpose: These are the result surfaces for three of the four formats; they must read as Court and keep `computeStandings` authoritative (never recomputed) and the admin `renderEntry` injection intact.
Output: Restyled `round-robin-view.tsx` + `rotation-view.tsx` and a new shared `formats.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tournament-pages/14-CONTEXT.md
@.planning/phases/14-tournament-pages/14-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/tournament-pages.md
@.planning/sketches/009-tournament-formats/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create formats.css (009 A+B shared match-rows + standings classes)</name>
  <read_first>
    - .claude/skills/sketch-findings-diplom/references/tournament-pages.md (CSS Patterns «Match rows + standings table», «Status badge», «Court-net rule», Responsive container-query collapses for .mrow)
    - .planning/sketches/009-tournament-formats/index.html (.t-head/.matches/.mrow/.court/.matchup/.side/.score/.await/.standings/.rank/.leader/.podium/.diff/.unit-cell CSS — port verbatim, drop sketch-chrome)
    - src/app/(app)/profile/profile.css (the .net-rule rule to copy verbatim if a divider is used)
  </read_first>
  <action>
    Create `src/components/formats.css` carrying ONLY the 009 classes absent from globals (token-only, NO hex): `.t-head`/`.meta-strip`/`.round-prog`, `.net-rule` (copy verbatim), `.round-block`/`.round-label`/`.rl-tag`, `.matches`/`.matches.live`+`.matches.live .mrow` tint, `.mrow`+`.mrow:first-child`, `.court`+`.court i` (net glyph), `.matchup`, `.side`+`.side.win::after` (primary dot)+`.side.lose` (muted)+`.mdash`, `.score`+`.score .a`/`.score .b`+`.score .a.dim`/`.score .b.dim` (losing number muted)+`.score .sep`, `.await` (accent pill, NOT the prog-badge token), `.standings-wrap`/`.standings-scroll` (the ONLY horizontal-scroll container for the tables), `table.standings`+`th`/`td` alignment (Место+Участник/Игрок left, rest right, tabular-nums), `tr.leader`+`tr.leader .rank`, `tr.podium .rank`, `.rank` chip, `.diff.pos`/`.diff.neg`/`.diff.zero`, and `.unit-cell` (avatar + name). Add the `@container (max-width: 480px)` `.mrow` reflow (court spans full width) — `@container`, NOT `@media`. Head the file with the screen-specific/no-hex comment convention.
  </action>
  <verify>
    <automated>test -f src/components/formats.css && grep -q "\.standings-scroll" src/components/formats.css && grep -q "\.mrow" src/components/formats.css && grep -q "\.await" src/components/formats.css && grep -q "\.diff\.pos\|\.diff\.neg\|\.diff\.zero" src/components/formats.css && grep -q "\.unit-cell" src/components/formats.css && grep -q "@container" src/components/formats.css && ! grep -q "@media" src/components/formats.css && ! grep -vE '^\s*(/\*|\*)' src/components/formats.css | grep -qE '#[0-9a-fA-F]{3,8}'</automated>
  </verify>
  <done>formats.css has .round-block/.matches(.live)/.mrow/.court/.matchup/.side(.win/.lose)/.score(.dim)/.await/.standings-wrap/.standings-scroll/table.standings/.rank/.leader/.podium/.diff(.pos/.neg/.zero)/.unit-cell with @container (not @media) and zero hex.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle round-robin-view.tsx (009 A) onto Court match-rows + standings</name>
  <read_first>
    - src/components/round-robin-view.tsx (FULL — preserve teamLabel, isRecorded, showEntry = !readOnly && renderEntry, the per-round map, renderEntry!(m) call for unrecorded matches, and the standings map over computeStandings units — NEVER recompute winners/standings)
    - .claude/skills/sketch-findings-diplom/references/tournament-pages.md (Design Decisions «A — Круговой» + HTML Structures «Round-robin match row + standings»)
    - .planning/sketches/009-tournament-formats/index.html (A variant markup)
  </read_first>
  <action>
    Add `import "./formats.css";`. Restyle markup/classes only — keep `teamLabel`, `isRecorded`, `showEntry`, the `RoundRead`/`UnitStanding` prop types, the per-round map, and `nameById[u.unitId]` + every `u.*` field. «Матчи»: render each round as `.round-block` (`.round-label` «Раунд {round.roundNumber}» + optional `.rl-tag`) wrapping `.matches > .mrow`. Each `.mrow` = `.court` («<i></i>Корт {m.courtNumber}» — net glyph `<i>`) · `.matchup` (`.side` for teamA / `.mdash` «—» / `.side` for teamB) · then the score cell: when `isRecorded(m)` render `.score` with `<span className="a">{m.pointsA}</span><span className="sep">:</span><span className="b">{m.pointsB}</span>` and add `.dim` to the LOSING number (display-only: compare m.pointsA vs m.pointsB — do NOT compute new winners, this is pure presentation of the already-stored points), and add `.win`/`.lose` to the two `.side` spans by the same display comparison; when NOT recorded, render `showEntry ? renderEntry!(m) : <span className="await">Ожидает счёта</span>` (keep the exact renderEntry injection path for the admin). «Турнирная таблица»: wrap in `.standings-wrap > .standings-scroll > table.standings` with the existing columns Место/Участник/Игр/Победы/Поражения/Очки/Разница; row index 0 → `tr.leader`, index 1-2 → `tr.podium`; Место cell is a `.rank` chip; Разница cell gets `.diff` + `.pos`(>0)/`.neg`(<0)/`.zero`(==0) by sign of `u.pointDiff`. Keep `nameById[u.unitId] ?? "—"` for Участник. Use the shared `.t-head`/`.net-rule` only if the existing section headings warrant it (the hub already provides the page hero — do not duplicate the tournament name here).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'import "./formats.css"' src/components/round-robin-view.tsx && grep -q "round-block" src/components/round-robin-view.tsx && grep -q "Корт" src/components/round-robin-view.tsx && grep -q "await" src/components/round-robin-view.tsx && grep -q "standings-scroll" src/components/round-robin-view.tsx && grep -q "diff" src/components/round-robin-view.tsx && grep -q "renderEntry" src/components/round-robin-view.tsx</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; round-robin-view imports formats.css, renders .round-block matches with .court/.matchup/.side(.win/.lose)/.score(.dim) or .await, and the .standings table with .rank/.leader/.podium/.diff; renderEntry injection + standings-over-computeStandings preserved (no recompute).</done>
</task>

<task type="auto">
  <name>Task 3: Restyle rotation-view.tsx (009 B) onto Court live/past + player rating (no cut-line)</name>
  <read_first>
    - src/components/rotation-view.tsx (FULL — preserve roundsWith, the current(unrecorded)/past(recorded) split, showEntry, the renderEntry injection, and the player standings map over PlayerStanding — NEVER recompute)
    - .claude/skills/sketch-findings-diplom/references/tournament-pages.md (Design Decisions «B — Американо/Мексикано» + HTML Structures «Rotation page sections + player rating» + What to Avoid «no qualification cut-line»)
    - .planning/sketches/009-tournament-formats/index.html (B variant markup)
  </read_first>
  <action>
    Add `import "./formats.css";`. Restyle markup/classes only — keep `roundsWith`, `current`/`past`, `showEntry`, the `RoundRead`/`PlayerStanding` types, `teamLabel`, and `nameById[p.userId]` + every `p.*` field. «Текущие игры»: render the current rounds inside `.matches.live` boxes; each `.mrow` = `.court` «<i></i>Корт {m.courtNumber}» · `.matchup` (transient pairings via teamLabel — `.side` / `.mdash` / `.side`) · then `showEntry ? renderEntry!(m) : <span className="await">Ожидает счёта</span>` (use a target pill «До N» ONLY if a target value is already available in the data; otherwise «Ожидает счёта» — do not invent a target). Keep the renderEntry injection (the admin entry path) verbatim. «Прошедшие игры»: `.matches > .mrow` with a real `.score` (`<span className="a">{m.pointsA}</span><span className="sep">:</span><span className="b">{m.pointsB}</span>`, `.dim` on the losing number by display comparison). «Рейтинг игроков»: `.standings-wrap > .standings-scroll > table.standings` with the existing columns Место/Игрок/Сыграно/Победы/Очки/Разница; Игрок cell → `.unit-cell` (`.avatar` initials of `nameById[p.userId]` + the name); row 0 `tr.leader`, rows 1-2 `tr.podium`; Место `.rank` chip; Разница `.diff.pos/.neg/.zero` by sign of `p.pointDiff`. ABSOLUTELY NO knockout/qualification cut-line or «qualified» styling — it is a pure rating ladder (What to Avoid).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'import "./formats.css"' src/components/rotation-view.tsx && grep -q "matches live" src/components/rotation-view.tsx && grep -q "unit-cell" src/components/rotation-view.tsx && grep -q "standings-scroll" src/components/rotation-view.tsx && grep -q "renderEntry" src/components/rotation-view.tsx && ! grep -qiE 'cut-line|qualif|вылет|cutline' src/components/rotation-view.tsx</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; rotation-view imports formats.css, renders .matches.live current games + past games with real .score + the player rating .standings with .unit-cell/.rank/.leader/.podium/.diff and NO cut-line; roundsWith/current/past split + renderEntry + standings-over-computeStandings preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin renderEntry vs read-only | round/rotation views inject the admin score form only via the hub-supplied renderEntry (admin+in_progress); read-only otherwise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Elevation of Privilege | renderEntry injection in both views | mitigate | Preserve `showEntry = !readOnly && Boolean(renderEntry)` and the `renderEntry!(m)` call path verbatim; restyle must not render the entry form when readOnly (grep-verified in Tasks 2/3) — the hub's `isAdmin && in_progress` gate stays authoritative |
| T-14-07 | Tampering | standings integrity | accept | Views are pure display Server Components reading `computeStandings` output; restyle adds no recompute and no write — the win/lose `.dim`/`.win` styling is presentation of already-stored points, not a new computation |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (pure restyle, no new deps) — N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `grep -rnE '#[0-9a-fA-F]{3,8}' src/components/formats.css` returns no hex outside comments.
- `.standings-scroll` is the only intentional horizontal-scroll container for the tables; the page body never scrolls for them.
- No `@media` (only `@container`).
- rotation-view has NO cut-line / qualification styling.
- Both views keep `computeStandings`-derived standings unrecomputed and the `renderEntry` admin path intact.
</verification>

<success_criteria>
UI-09 met: Круговой показывает «Матчи» по раундам (court/matchup/score/await) + «Турнирную таблицу» (rank/leader/podium/diff); Американо/Мексикано — «Текущие/Прошедшие игры» + «Рейтинг игроков» в .unit-cell без cut-line; обе таблицы скроллятся внутри .standings-scroll; computeStandings и renderEntry сохранены; нулевой hex; tsc зелёный.
</success_criteria>

<output>
Create `.planning/phases/14-tournament-pages/14-03-SUMMARY.md` when done. Note whether a «До N» target pill was used (data available) or only «Ожидает счёта», and how win/lose styling derives from the stored points (display-only).
</output>

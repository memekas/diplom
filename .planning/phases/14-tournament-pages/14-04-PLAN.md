---
phase: 14-tournament-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/(app)/admin/tournaments/new/create-tournament-form.tsx"
  - "src/app/(app)/admin/tournaments/new/page.tsx"
  - "src/app/(app)/admin/tournaments/new/create-tournament.css"
autonomous: true
requirements: [UI-07]

must_haves:
  truths:
    - "Форма создания — секционная Court-форма (≤640px, .cq, три fieldset-секции, .net-rule разделители) с token-chevron на select"
    - "Формат-select драйвит условные поля: playoff→select 4/8/16 + Тип selectable + Подсчёт selectable; round_robin→number min 3 + selectable; americano→number min 4 + Тип=Одиночный/Подсчёт=Очки forced+locked + Число раундов optional; mexicano→number min 8 + forced+locked + Число раундов REQUIRED"
    - "Размер-контрол свапает select↔number — только видимый несёт name=\"size\" (не дабл-сабмит)"
    - "Server Action createTournamentAction + parseTournamentForm + Zod + free-form подсчёт (БЕЗ setsPerMatch/gamesPerSet/targetPoints) сохранены без изменений"
    - "playoff/round_robin Тип НЕ авто-локается"
  artifacts:
    - path: "src/app/(app)/admin/tournaments/new/create-tournament.css"
      provides: "007 screen-specific classes (.fset/.sec/.net-rule/.sel-wrap chevron/.seg/.seg-lock/.cond/.opt-tag size-swap helpers)"
      contains: ".sel-wrap"
    - path: "src/app/(app)/admin/tournaments/new/create-tournament-form.tsx"
      provides: "Court sectioned create form: format-driven conditional fields, size select↔number swap, forced+locked americano/mexicano, totalRounds"
      contains: "create-tournament.css"
  key_links:
    - from: "src/app/(app)/admin/tournaments/new/create-tournament-form.tsx"
      to: "createTournamentAction"
      via: "useActionState wrapping parseTournamentForm pre-check → createTournamentAction"
      pattern: "createTournamentAction"
    - from: "src/app/(app)/admin/tournaments/new/create-tournament-form.tsx"
      to: 'name="size" swap'
      via: "only the visible size control carries name='size'"
      pattern: 'name="size"'
---

<objective>
Restyle the admin create-tournament form (`(app)/admin/tournaments/new/create-tournament-form.tsx`, sketch 007 «Секционная форма») onto Court, satisfying UI-07: a ≤640px sectioned form whose Формат select drives the conditional fields (playoff→4/8/16 select; round_robin→number; americano/mexicano→forced+locked Одиночный+Очки + Число раундов), with a token-chevron select and the size select↔number swap. The existing Server Action, Zod validation, and free-form scoring (no setsPerMatch/gamesPerSet/targetPoints) are preserved exactly.

Purpose: This is the admin's only path to create a tournament; the format-conditional rules mirror `createTournamentSchema` and MUST keep submitting the correct (and, for americano/mexicano, forced) values.
Output: A new co-located `create-tournament.css`, the restyled form leaf, and the restyled page shell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tournament-pages/14-CONTEXT.md
@.planning/phases/14-tournament-pages/14-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/forms-and-auth.md
@.planning/sketches/007-create-tournament/index.html

# Phase 13 co-located CSS + select-wrap chevron precedent
@.planning/phases/13-auth/13-02-SUMMARY.md
@src/app/(app)/profile/profile.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create create-tournament.css (007 sectioned-form + chevron + seg + cond classes)</name>
  <read_first>
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (CSS Patterns «Token-driven select chevron», «Segmented radio pills», «Conditional-field fade-in», «Size control name-swap»; Design Decisions «007 — Conditional create form»)
    - .planning/sketches/007-create-tournament/index.html (.fset/.sec/.sel-wrap/.seg/.seg-lock/.cond/.opt-tag CSS — port verbatim, drop sketch-chrome)
    - src/app/(app)/profile/profile.css (the .net-rule rule + the .select-wrap CSS-mask chevron already authored — reuse the mask flavour)
  </read_first>
  <action>
    Create `src/app/(app)/admin/tournaments/new/create-tournament.css` carrying ONLY the 007 classes absent from globals (token-only, NO hex — never bake a color into the chevron SVG; use the CSS-mask + `background: var(--text-muted)` / focus `var(--ring)` flavour): `.fset`/`.sec` (numbered fieldset section), `.net-rule` (copy verbatim from profile.css), the `.sel-wrap` select chevron (`select.input { appearance:none; padding-right:34px }` + `.sel-wrap::after` CSS-mask chevron + `:focus-within` recolor), `.opt-tag` (faint «необязательно» marker), and the `.cond` fade-in (`@keyframes condIn` + `[hidden]{display:none!important}`). Also port the 007 `.seg` radio-pill styles (`.seg` container, `.seg label`, `.seg input` visually-hidden, `.seg label:has(input:checked)` primary fill, `.seg.is-locked`, `.seg-lock` caption) IN CASE the executor migrates the freely-selectable Тип/Подсчёт to seg pills — but see Task 2's risk note: the locked forced values keep the controlled-select mechanism. Head the file with the screen-specific/no-hex comment convention.
  </action>
  <verify>
    <automated>test -f "src/app/(app)/admin/tournaments/new/create-tournament.css" && grep -q "\.sel-wrap" "src/app/(app)/admin/tournaments/new/create-tournament.css" && grep -q "\.cond\|condIn" "src/app/(app)/admin/tournaments/new/create-tournament.css" && grep -q "\.fset\|\.sec" "src/app/(app)/admin/tournaments/new/create-tournament.css" && grep -q "mask:\|-webkit-mask:" "src/app/(app)/admin/tournaments/new/create-tournament.css" && ! grep -vE '^\s*(/\*|\*)' "src/app/(app)/admin/tournaments/new/create-tournament.css" | grep -qE '#[0-9a-fA-F]{3,8}'</automated>
  </verify>
  <done>create-tournament.css has .fset/.sec/.net-rule/.sel-wrap(token-mask chevron)/.opt-tag/.cond(+condIn)/[hidden] and the .seg/.seg-lock pills; the chevron color is a token (no hex), zero hex outside comments.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle create-tournament-form.tsx onto the 007 sectioned Court form (preserve action + size-swap + forced locks)</name>
  <read_first>
    - src/app/(app)/admin/tournaments/new/create-tournament-form.tsx (FULL — preserve the useActionState wrapping parseTournamentForm → createTournamentAction, EVERY field name= attribute, the format-driven useState (`format`/`scoringMode`, `isRoundFormat`, `effectiveMode`/`effectiveScoring`), the size element-swap (`select` playoff ↔ `number` with min={sizeMinByFormat[format]}), the forced singles+points hidden-input + disabled-select mechanism for americano/mexicano (lines 99-113, 160-174), and totalRounds shown only isRoundFormat with required={format==="mexicano"}; NO setsPerMatch/gamesPerSet/targetPoints — keep absent)
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (Design Decisions «007» + Hard rules + HTML Structures «007 conditional block» + What to Avoid «Double-submitting size», «Auto-locking Тип for playoff/round_robin», «no setsPerMatch…»)
    - .planning/sketches/007-create-tournament/index.html (section layout + conditional markup)
  </read_first>
  <action>
    Add `import "./create-tournament.css";`. Restyle markup/classes only — DO NOT touch the action wiring, the useState branching, or any `name=` attribute. Wrap the form in a `.cq` ≤640px column. Section the fields into three `<fieldset className="fset sec">` (1 «Основное»: Название, Формат, Тип, Уровень · 2 «Формат и подсчёт»: Размер/Количество, Подсчёт, Число раундов · 3 «Время и место»: Цена, Дата, Место) separated by `.net-rule`. Convert each `<label className="flex flex-col gap-1 text-sm">` to `.field` + `.label` + control; wrap every `<select>` in a `.sel-wrap` for the token chevron and give controls the `.input` class; the field error `<span className={fieldErrorClass}>` → `.error` styling; `(необязательно)` markers → `<span className="opt-tag">необязательно</span>`. SIZE control: keep rendering EITHER the `<select name="size">` (playoff, options 4/8/16) OR the `<input name="size" type="number" min={sizeMinByFormat[format]}>` (others) — render exactly ONE so only the visible control carries `name="size"` (never both); wrap the select in `.sel-wrap`. Тип + Подсчёт: KEEP the existing controlled-select + hidden-input forced-value mechanism (proven to submit the forced singles/points for americano/mexicano) — the SAFE path per pattern-mapper risk #2. You MAY restyle them as `.seg` radio pills ONLY for the freely-selectable cases (playoff/round_robin) and ONLY if the native value still submits identically; for the forced/locked americano/mexicano cases retain the controlled-select+hidden-input (add `.is-locked` + a `.seg-lock` caption «Формат играется только одиночно»/«Подсчёт — только очки» for the visual lock). DO NOT auto-lock Тип for playoff/round_robin (they stay user-selectable, may be Одиночный). Число раундов: keep the `isRoundFormat &&` gate, the `required={format==="mexicano"}` attr, and label it with `.opt-tag` «необязательно» for americano / a `--danger`-toned «обязательно» for mexicano (token, no hex); wrap in `.cond` for the fade-in. Submit button → `.btn.btn-primary.btn-block` keeping `disabled={pending}` and the «Создание…»/«Создать турнир» label swap. NO setsPerMatch/gamesPerSet/targetPoints inputs (keep the comment). NO hardcoded hex.
  </action>
  <verify>
    <automated>npx tsc --noEmit && cd "src/app/(app)/admin/tournaments/new" && grep -q 'import "./create-tournament.css"' create-tournament-form.tsx && grep -q "createTournamentAction" create-tournament-form.tsx && grep -q "parseTournamentForm" create-tournament-form.tsx && grep -q 'name="size"' create-tournament-form.tsx && grep -q 'effectiveMode\|effectiveScoring' create-tournament-form.tsx && grep -q 'name="totalRounds"' create-tournament-form.tsx && grep -q 'required={format === "mexicano"}\|format === "mexicano"' create-tournament-form.tsx && grep -q 'sel-wrap\|fset\|opt-tag' create-tournament-form.tsx && grep -q 'btn btn-primary' create-tournament-form.tsx && ! grep -qE 'setsPerMatch|gamesPerSet|targetPoints' create-tournament-form.tsx && ! grep -qIE 'bg-red-900|bg-foreground|border-current/30' create-tournament-form.tsx</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; form imports create-tournament.css, is sectioned (.fset.sec + .net-rule, .cq ≤640px), every select uses .sel-wrap chevron + .input, size renders exactly one control carrying name="size", Тип/Подсчёт forced+locked for americano/mexicano via the preserved controlled-select+hidden-input (playoff/round_robin NOT auto-locked), totalRounds gated isRoundFormat with required for mexicano, submit is .btn.btn-primary.btn-block; createTournamentAction/parseTournamentForm/useState branching and all name= preserved; no setsPerMatch/gamesPerSet/targetPoints and no hex.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the create page shell (page.tsx)</name>
  <read_first>
    - src/app/(app)/admin/tournaments/new/page.tsx (FULL — preserve the requireAdmin() guard + the Unauthorized/Forbidden → redirect("/login") branch verbatim)
    - .claude/skills/sketch-findings-diplom/references/forms-and-auth.md (Design Decisions «007» — page is a centered single column; status implicit «Регистрация» as a submit-note, not a field)
  </read_first>
  <action>
    Restyle only the `<main>` shell and heading — keep the `requireAdmin()` try/catch and the `redirect("/login")` on Unauthorized/Forbidden (rethrow otherwise) byte-identical. Replace the `<main className="flex flex-1 flex-col items-center gap-6 p-8">` + `<h1 className="text-2xl font-bold">` with a Court editorial shell: an `.eyebrow` «Новый турнир» + `<h1>Создать турнир</h1>` above `<CreateTournamentForm />`, sized to the form's ≤640px column. Optionally add a faint `submit-note`/`.hint` «Создаётся со статусом «Регистрация»» (status is implicit — NOT a field). Do not move the form's `.cq` wrapper (it lives in the form leaf from Task 2) — the page just provides the heading + container alignment. No hardcoded hex.
  </action>
  <verify>
    <automated>npx tsc --noEmit && cd "src/app/(app)/admin/tournaments/new" && grep -q "requireAdmin" page.tsx && grep -q 'redirect("/login")' page.tsx && grep -q 'Unauthorized\|Forbidden' page.tsx && grep -q "CreateTournamentForm" page.tsx && grep -q "eyebrow\|Создать турнир" page.tsx && ! grep -qIE 'text-2xl font-bold' page.tsx</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; page.tsx keeps requireAdmin + redirect("/login") on Unauthorized/Forbidden verbatim and renders a Court heading (eyebrow + h1) above CreateTournamentForm; old Tailwind text-2xl heading gone; no hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→createTournamentAction | form FormData (incl. forced singles/points for americano/mexicano) crosses to the existing Server Action + Zod superRefine (unchanged) |
| anon/non-admin vs admin | page.tsx requireAdmin guard is the authoritative gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | size double-submit / forced-value bypass | mitigate | Only the visible size control carries `name="size"` (no double submit); americano/mexicano forced Одиночный+Очки keep the controlled-select+hidden-input mechanism so the locked values still post — restyle must not break native submission (pattern-mapper risk #2); `createTournamentSchema` superRefine remains the authoritative server check (grep-verified absence of setsPerMatch/gamesPerSet/targetPoints) |
| T-14-09 | Elevation of Privilege | create page access | mitigate | `requireAdmin()` try/catch + `redirect("/login")` on Unauthorized/Forbidden preserved verbatim in page.tsx (grep-verified) — restyle does not weaken the admin gate |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (pure restyle, no new deps) — N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `grep -rnE '#[0-9a-fA-F]{3,8}' "src/app/(app)/admin/tournaments/new/"` returns no hex outside comments (and no color baked into the chevron SVG).
- Exactly one size control carries `name="size"` per format; americano/mexicano still submit forced singles + points (controlled-select+hidden-input preserved).
- No setsPerMatch/gamesPerSet/targetPoints inputs.
- requireAdmin + redirect("/login") on Unauthorized/Forbidden preserved.
- No `@media` (only `@container` via .cq).
</verification>

<success_criteria>
UI-07 met: форма создания в Court — секционная (≤640px, .cq, .fset.sec + .net-rule), token-chevron select, формат-драйвен условные поля (playoff→4/8/16 select; round_robin→number; americano/mexicano→Одиночный+Очки forced+locked + Число раундов), size select↔number swap без дабл-сабмита; createTournamentAction/Zod/free-form подсчёт сохранены; playoff/round_robin не авто-локаются; нулевой hex; tsc зелёный.
</success_criteria>

<output>
Create `.planning/phases/14-tournament-pages/14-04-SUMMARY.md` when done. Record whether Тип/Подсчёт were migrated to .seg pills for the free formats or kept as controlled-selects throughout, and confirm the forced-value submission path for americano/mexicano was preserved.
</output>

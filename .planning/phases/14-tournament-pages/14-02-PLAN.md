---
phase: 14-tournament-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/components/bracket-view.tsx"
  - "src/components/bracket-scroll-client.tsx"
  - "src/components/bracket.css"
autonomous: true
requirements: [UI-08]

must_haves:
  truths:
    - "Сетка плей-офф рендерит колонки раундов слева-направо в .bracket-scroll, лейблы по depth-from-final (Финал/Полуфинал/Четвертьфинал/«1/8 финала»…)"
    - "Каждая карточка матча — два .slot с ПОЛНЫМИ именами (white-space:normal, не обрезаются); winner-слот .win"
    - "Set-tally (число выигранных сетов на пару) выведен из m.sets (count where games > opponent), выводится одной .sl-tally на слот; winner-tally — primary"
    - "Счёт по геймам показывается в position:fixed popover .sd-pop по наведению(desktop)/тапу(mobile), одна .sd-col на сет; нигде нет inline-счёта по геймам"
    - "НЕТ счёта финала нигде; champion-banner показывает только имя чемпиона (без счёта)"
    - "Геометрия колонок/линий корректна для фактического размера турнира (4/8/16); НЕТ size-toggle"
  artifacts:
    - path: "src/components/bracket-scroll-client.tsx"
      provides: "client leaf owning the games popover (fixed-position place/dismiss) + optional connector geometry; receives serializable bracket data"
      exports: ["BracketScrollClient"]
    - path: "src/components/bracket.css"
      provides: "003 screen-specific classes (.bracket-scroll/.bracket/.round/.is-final/.round-label/.match/.slot/.win/.sl-name/.sl-tally/.has-detail/.sd-pop/.sd-grid/.sd-col/.champ-banner/.ch-name + optional .connectors/.elbow/.spine/.out)"
      contains: ".sd-pop"
    - path: "src/components/bracket-view.tsx"
      provides: "Server Component computing depth-from-final labels + derived set-tally + champion name, passing serializable data to the client leaf"
      contains: "champ-banner"
  key_links:
    - from: "src/components/bracket-view.tsx"
      to: "src/components/bracket-scroll-client.tsx"
      via: "renders <BracketScrollClient> with rounds + per-match tally + sets"
      pattern: "BracketScrollClient"
    - from: "src/components/bracket-scroll-client.tsx"
      to: ".sd-pop popover"
      via: "fixed-position popover toggled on hover/tap, dismissed on outside click/scroll"
      pattern: "sd-pop"
---

<objective>
Restyle the playoff bracket (`components/bracket-view.tsx`, sketch 003 «Classic columns») onto Court, satisfying UI-08: round columns, full-name match cards with a derived per-pair set-tally, per-set games revealed in a fixed-position popover on hover/tap, NO final score anywhere, and a champion banner showing the name only. The bracket-view stays a Server Component computing labels + tally + champion; a new `"use client"` leaf owns the popover (and optionally the connector geometry).

Purpose: This is the most-watched visualization of a playoff; the validated score design (tally + games-popover, never inline, never a final score) and full untruncated names are explicit design contracts.
Output: Restyled `bracket-view.tsx`, a new `bracket-scroll-client.tsx` interactive leaf, and a new co-located `bracket.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tournament-pages/14-CONTEXT.md
@.planning/phases/14-tournament-pages/14-PATTERNS.md
@.claude/skills/sketch-findings-diplom/references/tournament-pages.md
@.planning/sketches/003-playoff-bracket/index.html

# Phase 13 client-leaf precedent
@.planning/phases/13-auth/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: bracket.css (003 columns/match/slot/tally/popover/champion) + depth-from-final labels + derived tally in bracket-view.tsx</name>
  <read_first>
    - src/components/bracket-view.tsx (FULL — preserve the BracketMatch prop shape, the champion-from-final derivation at 60-69, and the byRound grouping + position sort at 72-92; REPLACE roundLabel 23-28, Slot 30-49, setsLabel 51-54/106-108)
    - .claude/skills/sketch-findings-diplom/references/tournament-pages.md (Design Decisions «Playoff bracket» + CSS Patterns «Bracket scroller + round columns + match slots» + «Per-set games popover» + JS notes «Round label by depth-from-final», «Set tally derived from per-set games»)
    - .planning/sketches/003-playoff-bracket/index.html (roundLabel/buildRounds/tallyOf JS + .bracket/.round/.match/.slot/.sl-tally/.sd-pop/.champ-banner CSS — port classes verbatim, drop the size-toggle/sketch-chrome)
  </read_first>
  <action>
    Create `src/components/bracket.css` with the 003 classes absent from globals (token-only, NO hex): `.bracket-scroll` (intentional overflow-x:auto), `.bracket`, `.round`, `.round.is-final`, `.round-label`, `.match`+`.match.dim`+`.match.has-detail`+`.match.has-detail .sl-tally` (dotted hint), `.slot`+`.slot + .slot`+`.slot.win`+`.slot.win::before` (3px primary bar)+`.slot.win .sl-name`+`.slot .sl-name` (`white-space:normal; line-height:1.18` — NEVER truncate)+`.slot.tbd .sl-name` (italic faint)+`.sl-tally`+`.slot.win .sl-tally` (primary), `.winmark` (checkmark), the `.sd-pop`/`.sd-pop.show`/`.sd-grid`/`.sd-col`/`.sd-col:nth-child(odd)` tint/`.sd-col b`/`.sd-col b.w`, and `.champ-banner`/`.ch-label`/`.ch-name`. Head the file with the screen-specific/no-hex comment convention. In `bracket-view.tsx`: keep `import` none-of-prisma constraint but add `import "./bracket.css";`. Replace `roundLabel(round, totalRounds)` with a depth-from-final mapping: `depth = totalRounds - round` → 0:«Финал», 1:«Полуфинал», 2:«Четвертьфинал», 3:«1/8 финала», else `«1/${2**depth} финала»`; append a muted ` ×N` when a round has >1 match. Add a `tallyOf` helper deriving set-tally per pair from `m.sets`: pair-A tally = count of sets where `s.gamesPair1 > s.gamesPair2`, pair-B tally = count where `s.gamesPair2 > s.gamesPair1` (mirror the 003 `tallyOf`). REMOVE `setsLabel` and its inline render (no inline games anywhere). Keep the champion-from-final block (name only, NO score) and the byRound/position grouping. The actual JSX (rounds → .round → .match → two .slot with .sl-name + .sl-tally + .winmark) and the popover wiring move into the client leaf in Task 2 — in this task, refactor `bracket-view.tsx` to compute `championName`, the per-round label strings, and a serializable per-match payload (`{id, slotA:{name, tally, isWinner}, slotB:{name, tally, isWinner}, sets, isFinal}`) and render `<BracketScrollClient rounds={…} championName={championName} />` (the client component is created in Task 2; this task may stub the import and the file compiles once Task 2 lands — run tsc after Task 2).
  </action>
  <verify>
    <automated>test -f src/components/bracket.css && grep -q "\.sd-pop" src/components/bracket.css && grep -q "white-space: *normal" src/components/bracket.css && grep -q "\.sl-tally" src/components/bracket.css && grep -q "\.champ-banner" src/components/bracket.css && ! grep -vE '^\s*(/\*|\*)' src/components/bracket.css | grep -qE '#[0-9a-fA-F]{3,8}' && grep -q "Полуфинал\|Четвертьфинал\|финала" src/components/bracket-view.tsx && grep -q "tally" src/components/bracket-view.tsx && ! grep -q "setsLabel" src/components/bracket-view.tsx</automated>
  </verify>
  <done>bracket.css has the 003 columns/match/slot/tally/popover/champion classes with .sl-name white-space:normal and zero hex; bracket-view.tsx computes depth-from-final labels + derived per-pair tally (no setsLabel, no inline games) + champion name only, and delegates rendering to BracketScrollClient.</done>
</task>

<task type="auto">
  <name>Task 2: bracket-scroll-client.tsx — render columns/cards + fixed-position games popover (hover/tap) + degradable connectors</name>
  <read_first>
    - .claude/skills/sketch-findings-diplom/references/tournament-pages.md (CSS Patterns «Per-set games popover», «Connector elbows», JS notes — popover is position:fixed placed against the trigger's getBoundingClientRect, flipped below if no room above, dismissed on outside click/scroll; connectors degradable)
    - .planning/sketches/003-playoff-bracket/index.html (the popover place/dismiss JS lines ~430-470 and positionConnectors ~781-828 — port the popover; connectors optional)
    - src/components/bracket-view.tsx (the serializable payload shape produced in Task 1)
  </read_first>
  <action>
    Create `src/components/bracket-scroll-client.tsx` as a `"use client"` component `BracketScrollClient` receiving the serializable rounds payload + championName from Task 1. Render: the `.champ-banner` (when championName) showing «Чемпион турнира» + `.ch-name` {championName} — NAME ONLY, no score; then `.bracket-scroll > .bracket` of `.round` columns (final column `.is-final`) each with its label and `.match` cards. Each `.match` = two `.slot` (`.win` when isWinner → primary bar + `.winmark` checkmark; TBD/null name → `.tbd` italic faint «Победитель пары»), each slot has a `.sl-name` (full name, never truncated) + `.sl-tally` (the derived tally; winner's in primary). A match that has recorded sets gets `.has-detail` (cursor pointer + dotted tally underline). POPOVER: implement a single `.sd-pop` (`position:fixed`) rendered once; on pointer-enter (desktop hover) or tap/click of a `.has-detail` match, populate it with one `.sd-col` per set (header «С1/С2/…», then the two games stacked, the set-winning game gets `.w`) and place it against the trigger's `getBoundingClientRect()` — flip below the trigger if there is no room above; add `.show`; dismiss on outside click, on scroll, and on pointer-leave (desktop). Use `useState` for the active match + `useRef`/`useLayoutEffect` for placement; the popover is `pointer-events:none`. CONNECTORS ARE DEGRADABLE (CONTEXT 14): if implementing elbow connectors, port the 003 geometry faithfully — `useLayoutEffect`; pass-1 translateY-center each next card on its two feeders' midpoint cascading L→R; pass-2 size each `.elbow` from feeder getBoundingClientRect; re-measure on `resize` AND `document.fonts.ready`; NEVER measure a card mid-`transform`-transition (do not add a transition on the measured transform). If a stable React impl proves fragile or costly, SHIP static columns WITHOUT measured connectors and record the degradation in the SUMMARY — connectors must not block the core tally/popover/no-final-score/champion/alignment deliverable. Then update `bracket-view.tsx` to import and render `<BracketScrollClient>`; run `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f src/components/bracket-scroll-client.tsx && head -1 src/components/bracket-scroll-client.tsx | grep -q '"use client"' && grep -q "BracketScrollClient" src/components/bracket-scroll-client.tsx && grep -q "sd-pop" src/components/bracket-scroll-client.tsx && grep -q "position:.*fixed\|getBoundingClientRect" src/components/bracket-scroll-client.tsx && grep -q "ch-name" src/components/bracket-scroll-client.tsx && grep -q "BracketScrollClient" src/components/bracket-view.tsx && ! grep -qE 'gamesPair1.*:.*gamesPair2|setsLabel' src/components/bracket-scroll-client.tsx</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; BracketScrollClient is "use client", renders .champ-banner (name only) + .bracket-scroll columns + full-name .slot cards with derived .sl-tally + a fixed-position .sd-pop games popover (hover/tap, placed via getBoundingClientRect, dismissed on outside click/scroll); no inline games score and no final score; connectors either ported faithfully or degraded-and-noted; bracket-view.tsx renders the leaf.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client serialization | bracket-view (RSC) passes serializable match data to the client leaf; no prisma in the leaf |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | client leaf payload | accept | Only display fields (names, tally, per-set games) cross to the client — same data the v2.0 Server Component already rendered publicly; no new data exposure |
| T-14-05 | Tampering | bracket-view stays read-only | accept | No Server Action, no write, no prisma in the leaf — the bracket is display-only; restyle adds no mutation surface |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (popover/connectors are hand-rolled React/DOM, no new deps) — N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `grep -rnE '#[0-9a-fA-F]{3,8}' src/components/bracket.css` returns no hex outside comments.
- Set-tally is DERIVED from `m.sets` (not a new prop); no inline per-set games and no final score render anywhere (grep for absence of setsLabel / inline `gamesPair1:gamesPair2`).
- Champion banner shows the name only.
- `.bracket-scroll` is the only intentional horizontal-scroll container; no `@media` (only `@container` if a breakpoint is added).
- Names use `white-space: normal` (no ellipsis/truncate).
</verification>

<success_criteria>
UI-08 met: сетка плей-офф в Court — колонки раундов (depth-from-final лейблы), карточки с полными именами + set-tally (выведен из m.sets), счёт по геймам в fixed-popover по наведению/тапу, БЕЗ счёта финала, баннер чемпиона (только имя); геометрия корректна для фактического размера 4/8/16; size-toggle отсутствует; нулевой hex; tsc зелёный. Connector-elbows — реализованы или деградированы с записью в SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/14-tournament-pages/14-02-SUMMARY.md` when done. MUST record the connector decision: faithful geometry implemented, OR degraded to static columns (with the reason). Note the popover dismiss triggers wired.
</output>

---
phase: 11-tournament-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/rounds.ts
  - src/components/round-robin-view.tsx
  - src/components/rotation-view.tsx
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "A thin read helper returns every Round with its RoundMatch rows and the 4 team-member display names + points, ordered by round then court"
    - "A thin read helper returns the singles participants (TournamentPlayer) with safe display fields"
    - "RoundRobinView renders a matches table (rounds × courts) plus a standings table built from computeStandings unit rows"
    - "RotationView renders americano/mexicano rounds split into current vs past games plus a player rating table built from computeStandings player rows"
    - "Both views accept a readOnly prop and render no score-entry controls themselves (entry is wired by Plan 04)"
  artifacts:
    - path: "src/lib/services/rounds.ts"
      provides: "listRounds + listTournamentPlayers read-only helpers with explicit safe selects"
      exports: ["listRounds", "listTournamentPlayers"]
    - path: "src/components/round-robin-view.tsx"
      provides: "Round-robin matches table + unit standings table (readOnly-aware presentational component)"
      contains: "RoundRobinView"
    - path: "src/components/rotation-view.tsx"
      provides: "Americano/mexicano current/past games panels + player rating table (readOnly-aware)"
      contains: "RotationView"
  key_links:
    - from: "src/lib/services/rounds.ts listRounds"
      to: "prisma.round + RoundMatch + team user names"
      via: "explicit safe select (no email/birthDate), ordered roundNumber asc, courtNumber asc"
      pattern: "prisma\\.round\\.findMany"
    - from: "src/components/round-robin-view.tsx + rotation-view.tsx"
      to: "computeStandings output shapes (units / players)"
      via: "typed props consuming UnitStanding[] / PlayerStanding[] + a userId→name map"
      pattern: "standings"
---

<objective>
Add the only two missing read-only helpers (`listRounds`, `listTournamentPlayers`) and build the two new per-format presentational view components (`RoundRobinView`, `RotationView`) that VIS-01 needs. These are pure reads + presentation: the round-based generators, `recordRoundResult`, and `computeStandings` already exist and are tested — this plan adds NO engine logic. The components are the reusable artifacts Plan 04 wires into the detail page (active VIS-01 + finished VIS-02 via a `readOnly` prop). They render the data and a placeholder slot for per-match entry; Plan 04 supplies the actual score form into that slot.

Purpose: VIS-01 building blocks. `BracketView` (playoff) is untouched and reused as-is by Plan 04. RoundRobinView/RotationView mirror its plain-Tailwind, prop-fed, Server-Component style.
Output: `src/lib/services/rounds.ts` (2 helpers), `src/components/round-robin-view.tsx`, `src/components/rotation-view.tsx`; tsc + build green; a focused read-helper test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-ux/11-CONTEXT.md
@.planning/phases/11-tournament-ux/11-RESEARCH.md
@.planning/research/FORMATS.md

# Shapes the helpers/views must match + the safe-select discipline to mirror
@prisma/schema.prisma
@src/lib/services/standings.ts
@src/lib/services/registration.ts
@src/components/bracket-view.tsx
@src/lib/services/tournament.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add listRounds + listTournamentPlayers read-only helpers</name>
  <files>src/lib/services/rounds.ts, src/lib/services/rounds.test.ts</files>
  <read_first>
    - prisma/schema.prisma (Round, RoundMatch with teamA1/A2/B1/B2 User FKs + pointsA/pointsB, TournamentPlayer, User; lines 243-316)
    - src/lib/services/standings.ts (roundsSelect lines 79-96 — mirror its select shape; computeStandings returns ids only so the view needs names from here)
    - src/lib/services/registration.ts (playerSelect lines 204-209 + listTournamentPairs lines 213-224 — mirror the safe-select + read-helper convention; NEVER select email/credential columns, Pitfall 12 / T-03-06)
    - 11-RESEARCH.md Pitfall 6 (no listRounds/listTournamentPlayers exists — recommended selects) + Security Domain "new read helpers MUST use explicit safe select; do NOT select birthDate into public lists"
    - src/lib/services/round-result.test.ts (fake-prisma test style to mirror for the helper test)
  </read_first>
  <behavior>
    - listRounds(prisma, tournamentId): resolves to an array of rounds, each `{ roundNumber, matches: RoundMatchRead[] }`, ordered roundNumber asc; matches ordered courtNumber asc.
    - Each RoundMatchRead: `{ id, courtNumber, pointsA, pointsB, teamA1, teamA2, teamB1, teamB2 }` where each team slot is `{ id, name } | null` (null for unused slots in singles 1v1) and NEVER carries email/birthDate.
    - listTournamentPlayers(prisma, tournamentId): resolves to TournamentPlayer rows `{ id, user: { id, name, skillLevel, courtSide } }`, oldest first; NEVER email/birthDate.
    - With a fake prisma returning two rounds (round 2 listed before round 1, court 2 before court 1), listRounds returns them sorted round 1 then round 2, court 1 then court 2.
  </behavior>
  <action>
    Create `src/lib/services/rounds.ts` exporting two pure read functions that take the PrismaClient in (mirroring tournament.ts / registration.ts):

    Define a module-level `teamMemberSelect = { id: true, name: true } as const` (display-only; explicitly NO email/birthDate/credential columns — Security Domain constraint).

    `export async function listRounds(prisma, tournamentId)`:
    - `prisma.round.findMany({ where: { tournamentId }, orderBy: { roundNumber: "asc" }, select: { roundNumber: true, matches: { orderBy: { courtNumber: "asc" }, select: { id: true, courtNumber: true, pointsA: true, pointsB: true, teamA1: { select: teamMemberSelect }, teamA2: {...}, teamB1: {...}, teamB2: {...} } } } })`.
    - Use the relation fields (teamA1, not teamA1Id) so each slot is the joined `{ id, name } | null`.
    - Export the inferred row type (e.g. `export type RoundRead = ...` via Prisma's payload type or a hand-written interface matching the select) so the view component and Plan 04 share one shape.

    `export async function listTournamentPlayers(prisma, tournamentId)`:
    - Define `const tpPlayerSelect = { id: true, name: true, skillLevel: true, courtSide: true } as const` (mirror registration.ts playerSelect; NO email/birthDate).
    - `prisma.tournamentPlayer.findMany({ where: { tournamentId }, orderBy: { createdAt: "asc" }, select: { id: true, user: { select: tpPlayerSelect } } })`.
    - Export its row type too.

    Both functions are READ-ONLY (no writes, no transactions, no engine calls) — CONTEXT marks them as thin reads, not engine logic.

    Create `src/lib/services/rounds.test.ts` mirroring the fake-prisma style of round-result.test.ts: stub a prisma whose `round.findMany` ignores orderBy and returns out-of-order data, assert listRounds still returns rounds sorted by roundNumber and matches sorted by courtNumber (so the ordering contract is enforced by the query, not the test data) — OR, if simpler, assert that the passed query object contains `orderBy: { roundNumber: "asc" }` and the nested `orderBy: { courtNumber: "asc" }` and the select omits email/birthDate. Use `npx tsx` runnable assertions (process.exit nonzero on failure) consistent with the existing service tests. Keep it focused (the heavy logic is already covered by standings/round-result tests).
  </action>
  <acceptance_criteria>
    - rounds.ts exports listRounds and listTournamentPlayers, both pure reads taking prisma in.
    - listRounds select uses relation fields teamA1/A2/B1/B2 with `{ id, name }` only (no email/birthDate); orders roundNumber asc, courtNumber asc.
    - listTournamentPlayers selects user `{ id, name, skillLevel, courtSide }` only (no email/birthDate); oldest first.
    - rounds.test.ts asserts the ordering + safe-select contract and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsx src/lib/services/rounds.test.ts</automated>
  </verify>
  <done>Two safe read-only helpers exist with exported row types; the focused test passes; no engine logic added.</done>
</task>

<task type="auto">
  <name>Task 2: Build RoundRobinView (matches table + unit standings)</name>
  <files>src/components/round-robin-view.tsx</files>
  <read_first>
    - src/components/bracket-view.tsx (presentational Server Component style: prop-fed, no "use client", no prisma, plain Tailwind, RU labels — mirror this exactly)
    - src/lib/services/standings.ts (UnitStanding shape lines 23-33; computeStandings returns { kind:"units"; units: UnitStanding[] } for round_robin)
    - src/lib/services/rounds.ts (RoundRead shape from Task 1 — the rounds prop)
    - .planning/research/FORMATS.md §1 (round-robin table: played/wins/losses/points + tiebreak order)
    - 11-RESEARCH.md Pattern 3 (readOnly prop) + Open Q2 (build a unitId→name map for display) + Pitfall 7 (dark classes)
  </read_first>
  <action>
    Create `src/components/round-robin-view.tsx` — a pure presentational Server Component (no "use client", no prisma) named `RoundRobinView`.

    Props: `{ rounds: RoundRead[]; standings: UnitStanding[]; nameById: Record<string, string>; readOnly: boolean; renderEntry?: (match: RoundReadMatch) => React.ReactNode }`. (`nameById` maps a unit/player id to a display label; Plan 04 supplies it. `renderEntry` is an optional slot — when provided and `!readOnly`, the view calls it per unrecorded match to inject the score form; the view itself renders NO form.)

    Render two sections, both RU, plain Tailwind, dark-theme-safe (use border-current/15, no light bg-* error colors):
    1. "Матчи" — for each round (roundNumber asc), a labeled block "Раунд {n}" listing its matches as rows. Each match row shows the two teams (resolve names: for pairs both teamA1+teamA2 / teamB1+teamB2; for singles just teamA1 vs teamB1 — show non-null members joined by " / "), the court number ("Корт {courtNumber}"), and the recorded score when `pointsA != null && pointsB != null` as "{pointsA}:{pointsB}". When a match is unrecorded and `!readOnly` and `renderEntry` is provided, render `renderEntry(match)` in the row (the slot); otherwise render nothing extra.
    2. "Турнирная таблица" — a table over `standings` (already rank-sorted by computeStandings) with columns: место (rank), участник (nameById[unitId] ?? "—"), игр (played), победы (wins), поражения (losses), очки (pointsFor), разница (pointDiff). Highlight nothing special; keep it simple.

    Handle empty `rounds` (return a "Матчи ещё не сгенерированы" note) and empty `standings` (skip the table) gracefully. Do NOT compute standings or winners in the component (display-only; computeStandings is authoritative — Anti-Pattern "Re-implementing winner in the UI"). Do NOT import prisma or engine functions.
  </action>
  <acceptance_criteria>
    - RoundRobinView is a presentational component (no "use client", no prisma import) accepting rounds/standings/nameById/readOnly/renderEntry.
    - Renders a per-round matches list (RU "Раунд {n}", "Корт {n}", "{pointsA}:{pointsB}") and a unit standings table (rank/participant/played/wins/losses/points/diff).
    - Calls renderEntry(match) only for unrecorded matches when !readOnly and renderEntry is provided; renders no form itself.
    - Uses dark-safe Tailwind classes; no winner/standings recomputation in the view.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>RoundRobinView renders RR matches + standings from props with a readOnly-gated entry slot; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Build RotationView (americano/mexicano current/past + player rating)</name>
  <files>src/components/rotation-view.tsx</files>
  <read_first>
    - src/components/bracket-view.tsx (presentational style to mirror)
    - src/components/round-robin-view.tsx (the sibling view from Task 2 — same prop conventions)
    - src/lib/services/standings.ts (PlayerStanding shape lines 13-21; computeStandings returns { kind:"players"; players: PlayerStanding[] } for americano/mexicano)
    - .planning/research/FORMATS.md §2/§3 (americano/mexicano: individual points, player rating result, current round = highest materialized round for mexicano)
    - 11-RESEARCH.md Pitfall 5 (current vs past = pointsA/pointsB null vs non-null; mexicano active round = max roundNumber) + Pattern 3 (readOnly) + Open Q2 (userId→name map)
  </read_first>
  <action>
    Create `src/components/rotation-view.tsx` — a pure presentational Server Component named `RotationView` for americano + mexicano.

    Props: `{ rounds: RoundRead[]; standings: PlayerStanding[]; nameById: Record<string, string>; readOnly: boolean; renderEntry?: (match: RoundReadMatch) => React.ReactNode }` (same conventions as RoundRobinView).

    Compute, for display only, which matches are "current" vs "past" (Pitfall 5): a match is PAST when `pointsA != null && pointsB != null`, else CURRENT (unrecorded). Render two panels:
    1. "Текущие игры" — unrecorded matches. For mexicano the active round is the highest roundNumber present (only one is materialized at a time); for americano all rounds are pre-generated. Practically: list unrecorded matches grouped by round ("Раунд {n}", "Корт {n}"), showing team A (teamA1 / teamA2 names) vs team B (teamB1 / teamB2 names). When `!readOnly` and `renderEntry` is provided, render `renderEntry(match)` in the row.
    2. "Прошедшие игры" — recorded matches, grouped by round, showing the same team labels plus the score "{pointsA}:{pointsB}".
    3. "Рейтинг игроков" — a table over `standings` (rank-sorted by computeStandings): место (rank), игрок (nameById[userId] ?? "—"), сыграно (played), победы (wins), очки (pointsFor), разница (pointDiff).

    Handle empty rounds ("Раунды ещё не сгенерированы") and empty standings (skip the table). Display-only: never recompute the rating or materialize a next round (Anti-Pattern "Calling engine functions from UI"; the next mexicano round is materialized inside recordRoundResult). No prisma import, no "use client".
  </action>
  <acceptance_criteria>
    - RotationView is presentational (no "use client", no prisma) accepting rounds/standings/nameById/readOnly/renderEntry.
    - Splits matches into "Текущие игры" (unrecorded) and "Прошедшие игры" (recorded) panels grouped by round, with RU labels and team names.
    - Renders a "Рейтинг игроков" table (rank/player/played/wins/points/diff) from PlayerStanding[].
    - Calls renderEntry only for unrecorded matches when !readOnly and provided; never materializes rounds or recomputes rating.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>RotationView renders current/past games + player rating from props with a readOnly-gated entry slot; tsc + build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services (rounds.ts reads) → RSC → rendered HTML | Reads cross into the public detail page; projection must not leak PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information Disclosure | listRounds / listTournamentPlayers projections | mitigate | Both helpers use explicit safe `select` returning only `{ id, name }` (round members) and `{ id, name, skillLevel, courtSide }` (players) — NEVER email/birthDate/credential columns (mirrors playerSelect/safeProfileSelect; Security Domain constraint). Task 1 test asserts the safe-select contract |
| T-11-09 | Tampering | view recomputing winner/standings | mitigate | Views are display-only; standings come from the tested `computeStandings`, winners are read from stored pointsA/pointsB — the view never re-derives them (Anti-Pattern guard) |
| T-11-10 | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (no new deps); slopcheck N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors.
- `npx tsx src/lib/services/rounds.test.ts` → exit 0.
- `npx next build` → exit 0.
- Read-helper projections contain no email/birthDate (asserted by the test + grep on the select).
</verification>

<success_criteria>
- listRounds + listTournamentPlayers exist as thin safe-select reads with exported row types.
- RoundRobinView + RotationView are presentational, readOnly-aware, with an injectable per-match entry slot and no engine/winner recomputation.
- Components reuse the BracketView plain-Tailwind dark-theme style; no new deps.
- tsc + build + read-helper test green.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-ux/11-03-SUMMARY.md` when done.
</output>

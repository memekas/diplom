---
phase: 11-tournament-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/admin/tournaments/new/create-tournament-form.tsx
autonomous: true
requirements: [FORM-01, SCORE-02]
must_haves:
  truths:
    - "Admin sees format/participantMode/level/size/price/scoringMode controls on the create form"
    - "Selecting americano or mexicano forces participantMode=singles and scoringMode=points (locked)"
    - "scoringMode=sets reveals setsPerMatch + gamesPerSet inputs (no upper cap); scoringMode=points reveals targetPoints"
    - "americano/mexicano reveal a totalRounds input (required for mexicano)"
    - "playoff renders size as a 4/8/16 select; other formats render size as a number input"
    - "Submitting a valid configuration creates the tournament via the existing createTournamentAction (redirects to detail)"
  artifacts:
    - path: "src/app/(app)/admin/tournaments/new/create-tournament-form.tsx"
      provides: "Extended create-tournament form with conditional fields by format/scoringMode"
      contains: "scoringMode"
  key_links:
    - from: "src/app/(app)/admin/tournaments/new/create-tournament-form.tsx"
      to: "createTournamentAction"
      via: "useActionState + parseTournamentForm pre-check (unchanged wiring)"
      pattern: "createTournamentAction"
    - from: "create form FormData field names"
      to: "parseTournamentForm"
      via: "exact name= attributes"
      pattern: "name=\"(format|participantMode|level|size|price|scoringMode|targetPoints|totalRounds|setsPerMatch|gamesPerSet)\""
---

<objective>
Extend the admin create-tournament form to expose every field the already-shipped `createTournamentSchema`/`parseTournamentForm`/`createTournamentAction` accept (FORM-01) plus the scoring-mode selector and conditional sets/points/rounds fields (SCORE-02, create side). The form currently renders only name/size/date/location; add format, participantMode, level, price, scoringMode, and the conditional fields, driving show/hide entirely on the client by the selected `format` + `scoringMode`.

Purpose: FORM-01 + SCORE-02 (create side) — make the create UI cover the multiformat backend that Phases 8–9 already built. Backend untouched; this is pure presentation + client field-visibility.
Output: Extended `create-tournament-form.tsx` with conditional fields; build + tsc green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-ux/11-CONTEXT.md
@.planning/phases/11-tournament-ux/11-RESEARCH.md

# Schema + action this form wires to (read for exact FormData contract + field rules)
@src/lib/validation/tournament.ts
@src/app/(app)/admin/tournaments/actions.ts
@src/app/(app)/admin/tournaments/new/create-tournament-form.tsx
@src/lib/validation/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add format/mode/level/price/scoringMode fields + client conditional visibility</name>
  <files>src/app/(app)/admin/tournaments/new/create-tournament-form.tsx</files>
  <read_first>
    - src/app/(app)/admin/tournaments/new/create-tournament-form.tsx (current form: useActionState + parseTournamentForm pre-check, renders name/size/date/location only)
    - src/lib/validation/tournament.ts (createTournamentSchema superRefine field rules + parseTournamentForm exact FormData field names lines 117-133)
    - src/lib/validation/auth.ts (formatLabels, tournamentKindLabels, skillLevelLabels; skillLevels tuple; tournamentFormats/participantModes/scoringModes are in validation/tournament.ts)
    - 11-RESEARCH.md "Pattern 2: Conditional create-form fields" field-visibility table + "FormData field names parseTournamentForm reads" + Pitfall 7 (dark-theme error classes)
  </read_first>
  <action>
    Convert the form into a client-stateful leaf (keep "use client" + the existing useActionState wrapper that runs parseTournamentForm before createTournamentAction — DO NOT change the action wiring or add a success state; createTournamentAction redirects on success per admin/tournaments/actions.ts).

    Add two pieces of local React state: `format` (init "playoff") and `scoringMode` (init "sets"), each driven by the corresponding `<select>`'s onChange. Render these controls with exact `name=` attributes matching parseTournamentForm (validation/tournament.ts:117-133):
    - `name="format"` select over tournamentFormats using formatLabels (RU) — controlled by `format` state.
    - `name="participantMode"` select over participantModes using tournamentKindLabels (RU). When `format` is "americano" or "mexicano", force value to "singles" and render the select disabled (per D — americano/mexicano singles only); otherwise selectable.
    - `name="level"` select over skillLevels using skillLevelLabels (RU). Required.
    - `name="size"`: when `format==="playoff"` render a select over tournamentSizes (4/8/16, label "{n} пар"); otherwise render `<input type="number" name="size" min={...}>` (min 3 round_robin, 4 americano, 8 mexicano — advisory; server superRefine is authoritative). Label changes to "Количество участников" for non-playoff, "Размер сетки" for playoff.
    - `name="price"` number input, optional, RU label "Цена, ₽ (необязательно)", min 0.
    - `name="scoringMode"` select over scoringModes (RU labels: sets="Сеты/геймы", points="Очки") — controlled by `scoringMode` state. When `format` is americano/mexicano, force value "points" and disable the select (per D — those formats use points).

    Conditional fields (rendered only when their condition holds; when hidden the input is NOT in the DOM so its FormData entry is absent → parser coerces "" to undefined):
    - When `scoringMode==="sets"` (only reachable for playoff/round_robin): show `name="setsPerMatch"` and `name="gamesPerSet"` number inputs, min 1, NO max attribute (no upper cap, per SCORE-02/D5). RU labels "Сетов в матче" / "Геймов в сете".
    - When `scoringMode==="points"`: show `name="targetPoints"` number input, optional, min 1, RU label "Целевые очки (необязательно — по умолчанию 24)".
    - When `format` is "americano" or "mexicano": show `name="totalRounds"` number input, min 1, RU label "Число раундов". Mark required when `format==="mexicano"` (server superRefine rejects null totalRounds for mexicano).

    Keep the existing date (datetime-local) + location optional inputs.

    Error display: keep the per-field `errors.{key}` spans and the top-level `errors.form`. Use the dark-theme error class `bg-red-900/40 text-red-300` for the form-level error box (Pitfall 7 — the existing `bg-red-100 text-red-800` is a light-theme carry-forward; fixing it here is in-scope cleanup). Per-field error spans use `text-red-400`.

    Do NOT import prisma/db. Do NOT change createTournamentAction or parseTournamentForm. Do NOT add upper-bound max attributes on sets/games/points/rounds.
  </action>
  <acceptance_criteria>
    - Form renders selects for format, participantMode, level, scoringMode + inputs for size, price, date, location with the exact name= attributes parseTournamentForm reads.
    - Selecting format=americano or format=mexicano forces participantMode select value to "singles" (disabled) and scoringMode select value to "points" (disabled).
    - scoringMode=sets shows setsPerMatch + gamesPerSet number inputs (no max); scoringMode=points shows targetPoints; hidden fields are absent from the DOM.
    - format=americano|mexicano shows totalRounds input; for mexicano it is required.
    - format=playoff renders size as a 4/8/16 select; other formats render size as a number input.
    - Form-level error box uses bg-red-900/40 text-red-300 (dark theme).
    - Submit path unchanged: parseTournamentForm pre-check then createTournamentAction (which redirects).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>create-tournament-form.tsx renders all FORM-01 + SCORE-02 create fields with client-side conditional visibility; tsc passes; action wiring unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify production build compiles the extended create form</name>
  <files>src/app/(app)/admin/tournaments/new/create-tournament-form.tsx</files>
  <read_first>
    - src/app/(app)/admin/tournaments/new/create-tournament-form.tsx (the edited file)
    - 11-RESEARCH.md Environment Availability (next build is the verification gate)
  </read_first>
  <action>
    Run the Next.js production build to confirm the edited client component compiles with no type or lint-blocking errors. Fix any compile error introduced by Task 1 (e.g. controlled-select typing, missing key props). Do not introduce new dependencies or change backend files.
  </action>
  <acceptance_criteria>
    - `npx next build` exits 0 with no type errors referencing create-tournament-form.tsx.
  </acceptance_criteria>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>Production build succeeds with the extended create form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → createTournamentAction (Server Action) | Untrusted FormData crosses here; the action is the real authz + validation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | create-tournament-form FormData (format/size/mode forced client-side) | mitigate | Client-side forcing of singles/points + size widget is UX only; `createTournamentSchema.superRefine` (validation/tournament.ts:53-91) re-validates every field server-side via `parseTournamentForm` inside `createTournamentAction` — a tampered POST that bypasses the disabled selects is rejected with a typed RU field error |
| T-11-02 | Elevation | Non-admin POST to createTournamentAction | accept (already mitigated upstream) | `requireAdmin()` is the FIRST line of createTournamentAction (admin/tournaments/actions.ts:47); hidden/disabled UI is cosmetic. No new auth code in this plan |
| T-11-03 | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (no new deps); slopcheck N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors.
- `npx next build` → exit 0.
- Manual (deferred to phase UAT): create each of the 4 formats and confirm conditional fields show/hide and a tournament is created.
</verification>

<success_criteria>
- Create form exposes format, participantMode, level, size, price, scoringMode + conditional setsPerMatch/gamesPerSet (sets), targetPoints (points), totalRounds (americano/mexicano).
- americano/mexicano force singles + points in the UI; mexicano requires totalRounds.
- No upper cap on sets/games/points inputs.
- tsc + next build green; createTournamentAction/parseTournamentForm/schema untouched.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-ux/11-01-SUMMARY.md` when done.
</output>

---
phase: 11-tournament-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth.ts
  - src/lib/validation/auth.ts
  - src/app/(auth)/register/register-form.tsx
  - src/app/(app)/profile/profile-form.tsx
  - src/app/(app)/profile/page.tsx
autonomous: true
requirements: [FORM-02, FORM-03]
must_haves:
  truths:
    - "Register form requires an explicit skill-level selection (no silent beginner default)"
    - "Register form collects an optional date of birth that persists to User.birthDate after signup"
    - "Profile form edits all profile fields: name, level, phone, birthDate, court side, nickname, email"
    - "A nickname-taken or email-taken conflict on profile save shows a RU error, not a crash"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "birthDate declared as a Better Auth additionalField (input:true) so signup persists it"
      contains: "birthDate"
    - path: "src/lib/validation/auth.ts"
      provides: "registerSchema with required skillLevel + optional birthDate"
      contains: "birthDate"
    - path: "src/app/(auth)/register/register-form.tsx"
      provides: "Required level select + birthDate date input wired to authClient.signUp.email"
      contains: "birthDate"
    - path: "src/app/(app)/profile/profile-form.tsx"
      provides: "Full profile edit form (name/level/phone/birthDate/courtSide/nickname/email)"
      contains: "nickname"
  key_links:
    - from: "src/app/(auth)/register/register-form.tsx"
      to: "authClient.signUp.email"
      via: "spread birthDate (ISO string) into the signup call when present"
      pattern: "signUp\\.email"
    - from: "src/lib/auth.ts additionalFields.birthDate"
      to: "prisma User.birthDate"
      via: "Better Auth createUser spread (input:true)"
      pattern: "birthDate"
    - from: "src/app/(app)/profile/profile-form.tsx"
      to: "updateProfileAction"
      via: "useActionState + parseProfileForm pre-check (unchanged wiring)"
      pattern: "updateProfileAction"
---

<objective>
Close the two register-form gaps and expand the profile form (FORM-02 register side + FORM-03 profile side). Register: make the skill-level select REQUIRED with an explicit placeholder (drop the `?? "beginner"` default-slip that fails level-equality at registration — Phase 7 WR-01/IN-01), and add an optional date-of-birth input wired through Better Auth signup so it persists to `User.birthDate`. Profile: expand `profile-form.tsx` from courtSide/phone/skillLevel to all `updateProfileAction` fields (name, level, phone, birthDate, courtSide, nickname, email) with RU labels and taken-error display for nickname/email.

Purpose: FORM-02 (account registration) + FORM-03 (profile edits). The `updateProfileAction`/`profileSchema` already accept every profile field; this exposes them. `birthDate` needs real wiring through `auth.ts` + `registerSchema` (the only code-level gap besides the default-slip).
Output: edited auth.ts, validation/auth.ts, register-form.tsx, profile-form.tsx, profile/page.tsx; build + tsc green; register/profile no longer carry the default-slip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-ux/11-CONTEXT.md
@.planning/phases/11-tournament-ux/11-RESEARCH.md

# Files this plan edits + the action/service/schema they wire to
@src/lib/auth.ts
@src/lib/validation/auth.ts
@src/lib/validation/profile.ts
@src/app/(auth)/register/register-form.tsx
@src/app/(app)/profile/profile-form.tsx
@src/app/(app)/profile/page.tsx
@src/app/(app)/profile/actions.ts
@src/lib/services/profile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire birthDate through Better Auth + make register skillLevel required</name>
  <files>src/lib/auth.ts, src/lib/validation/auth.ts, src/app/(auth)/register/register-form.tsx</files>
  <read_first>
    - src/lib/auth.ts (additionalFields declares phone/skillLevel/nickname only — birthDate absent; lines 17-25)
    - src/lib/validation/auth.ts (registerSchema lacks birthDate; skillLevel is .optional() line 47)
    - src/lib/validation/profile.ts (proven birthDate date-union pattern lines 45-48 — reuse the same trick)
    - src/app/(auth)/register/register-form.tsx (skillLevel ?? "beginner" default-slip line 52; optional select lines 141-156; signUp.email call lines 47-54; dark error classes line 79)
    - 11-RESEARCH.md Pitfall 1 (birthDate not wired) + Pitfall 2 (skillLevel default-slip) + Assumption A1 (use type:"string" fallback for the additionalField)
  </read_first>
  <action>
    In `src/lib/auth.ts`: add `birthDate` to `user.additionalFields` next to phone/skillLevel/nickname as `{ type: "string", required: false, input: true }`. Use `type: "string"` (NOT "date") per RESEARCH Assumption A1 — the SQLite-via-Prisma adapter round-trips an ISO date string reliably (the `birthDate` column is `DateTime?`; Better Auth/Prisma coerces the ISO string). Add a one-line comment that this is the A1-safe fallback so signup persists birthDate without the "date"-type risk. Do NOT touch the existing changeEmail block or plugins order (nextCookies stays last).

    In `src/lib/validation/auth.ts`: (a) change `skillLevel: z.enum(skillLevels).optional()` to REQUIRED `skillLevel: z.enum(skillLevels, { message: "Выберите уровень" })` (no `.optional()`) so a missing level is a validation error, not a silent default. (b) Add `birthDate` to `registerSchema` using the exact same union trick as profileSchema: `z.union([z.literal(""), z.coerce.date()]).optional().transform((v) => (v === "" || v === undefined ? undefined : v))`.

    In `src/app/(auth)/register/register-form.tsx`:
    - Add `birthDate` to the registerSchema.safeParse input object: `birthDate: fd.get("birthDate") ?? undefined`.
    - Remove the `skillLevel ?? "beginner"` fallback (line 52): pass `skillLevel` directly (it is now required + validated). Update the comment to state level is explicitly required, no fallback.
    - Convert the level `<select>` to required: drop the `<option value="">—</option>` placeholder-with-empty-value in favor of a DISABLED placeholder `<option value="" disabled>Выберите уровень</option>`, set the select `required`, set `defaultValue=""`, change the label from "Уровень (необязательно)" to "Уровень" (no "необязательно" span).
    - Add an optional date-of-birth field: `<label>Дата рождения <span ...>(необязательно)</span><input name="birthDate" type="date" .../></label>` after the phone field, mirroring the existing input styling.
    - In the `authClient.signUp.email({...})` call, conditionally spread birthDate ONLY when present, sending it as an ISO date string: `...(birthDate ? { birthDate: birthDate.toISOString() } : {})`. (`parsed.data.birthDate` is a Date after the zod transform; send `.toISOString()` so the string-typed additionalField receives a string.) Add `birthDate` to the destructure of `parsed.data`.
    - Add "birthDate" to the FieldErrors type union so a validation issue on birthDate can surface.
    - Keep the existing error.code branches (FAILED_TO_CREATE_USER → nickname taken, USER_ALREADY_EXISTS → email taken) unchanged. Keep dark error classes.

    Do NOT import prisma. Do NOT change the changeEmail flow. Do NOT add new dependencies.
  </action>
  <acceptance_criteria>
    - auth.ts additionalFields includes `birthDate: { type: "string", required: false, input: true }`.
    - registerSchema.skillLevel is required (no .optional()); registerSchema includes birthDate (date-union, optional).
    - register-form.tsx: level select is required with a disabled "Выберите уровень" placeholder and no empty-value selectable option; the `?? "beginner"` fallback is gone.
    - register-form.tsx renders an optional `name="birthDate" type="date"` input and spreads `birthDate.toISOString()` into signUp.email only when present.
    - A signup with a chosen level + a birthDate persists `User.birthDate` (verified at phase UAT; type-level wiring verified here).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>birthDate flows register-form → registerSchema → authClient.signUp.email → auth.ts additionalField → User.birthDate; skillLevel is explicitly required with no default-slip; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Expand profile form to all editable fields + render initial values from the profile read</name>
  <files>src/app/(app)/profile/profile-form.tsx, src/app/(app)/profile/page.tsx</files>
  <read_first>
    - src/app/(app)/profile/profile-form.tsx (current form renders courtSide/phone/skillLevel only; Initial type lines 11-15; dark error/success classes lines 41-45)
    - src/app/(app)/profile/page.tsx (passes initial = {courtSide, phone, skillLevel}; getProfile returns name/email/courtSide/phone/skillLevel/nickname/birthDate)
    - src/lib/validation/profile.ts (profileSchema fields: name, courtSide, phone, skillLevel, nickname, email, birthDate; parseProfileForm exact FormData names lines 69-78)
    - src/app/(app)/profile/actions.ts (updateProfileAction maps nickname P2002 → {nickname:"Этот ник уже занят"} line 49; email clash → {email:"Этот email уже используется"} line 64)
    - src/lib/services/profile.ts (safeProfileSelect: id/name/email/courtSide/phone/skillLevel/nickname/birthDate)
    - 11-RESEARCH.md Pitfall 7 (dark error classes already correct in this file — keep them)
  </read_first>
  <action>
    In `src/app/(app)/profile/page.tsx`: expand the `initial` prop passed to `<ProfileForm>` to include all editable fields from the `profile` read: `name`, `email`, `nickname`, `courtSide`, `phone` (`?? ""`), `skillLevel` (`?? ""`), and `birthDate` formatted as a `yyyy-MM-dd` string for the date input (`profile.birthDate ? profile.birthDate.toISOString().slice(0,10) : ""`). Keep the requireUser guard + redirect logic unchanged.

    In `src/app/(app)/profile/profile-form.tsx`:
    - Widen the `Initial` type to `{ name: string; email: string; nickname: string; courtSide: string; phone: string; skillLevel: string; birthDate: string }`.
    - Keep the existing useActionState wrapper that runs parseProfileForm before updateProfileAction (UX pre-check; action is the real boundary) and the success banner.
    - Add inputs with exact `name=` attributes matching parseProfileForm (validation/profile.ts:69-78), each pre-filled from `initial`:
      - `name="name"` text input, required, label "ФИО", defaultValue={initial.name}, error span `errors.name`.
      - keep the `name="courtSide"` select (courtSideLabels).
      - keep the `name="phone"` input.
      - keep the `name="skillLevel"` select (skillLevelLabels) — profile level stays optional (empty = no change), unlike register.
      - `name="birthDate"` date input, optional, label "Дата рождения (необязательно)", defaultValue={initial.birthDate}, error span `errors.birthDate`.
      - `name="nickname"` text input, required, label "Никнейм", defaultValue={initial.nickname}, error span `errors.nickname` (this surfaces the "Этот ник уже занят" RU error the action returns on P2002).
      - `name="email"` email input, label "Email", defaultValue={initial.email}, error span `errors.email` (surfaces "Этот email уже используется").
    - Use the existing dark-theme classes already in this file (bg-red-900/40 text-red-300, bg-green-900/40 text-green-300, text-red-400 for field spans) — do NOT regress to light classes.

    Do NOT import prisma. Do NOT change updateProfileAction, profileSchema, or updateProfile. Do NOT add new dependencies.
  </action>
  <acceptance_criteria>
    - profile/page.tsx passes name/email/nickname/courtSide/phone/skillLevel/birthDate (birthDate as yyyy-MM-dd) into ProfileForm.
    - profile-form.tsx renders inputs for name, courtSide, phone, skillLevel, birthDate, nickname, email with the exact parseProfileForm name= attributes, each pre-filled from initial.
    - nickname-taken and email-taken errors render via errors.nickname / errors.email spans (RU).
    - All error/success boxes use dark-theme classes.
    - updateProfileAction / profileSchema / updateProfile untouched.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Profile form edits all FORM-03 fields with pre-filled values and RU taken-error display; tsc passes; backend untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build + register/login service tests still green</name>
  <files>src/lib/auth.ts, src/lib/validation/auth.ts</files>
  <read_first>
    - src/lib/validation/auth.ts (the edited registerSchema)
    - 11-RESEARCH.md Environment Availability (next build + per-file npx tsx tests are the gates)
  </read_first>
  <action>
    Run `npx next build` to confirm the edited auth.ts + both forms compile. Then run the registration service test (the only test touching registerSchema-adjacent logic) to confirm the required-skillLevel change did not break existing assertions. If the build or test fails because a fixture relied on the old optional skillLevel, fix the FIXTURE/form (not the schema requirement) so the explicit-required decision stands. Do not change backend logic.
  </action>
  <acceptance_criteria>
    - `npx next build` exits 0.
    - `npx tsx src/lib/services/registration.test.ts` exits 0 (all assertions pass).
  </acceptance_criteria>
  <verify>
    <automated>npx next build && npx tsx src/lib/services/registration.test.ts</automated>
  </verify>
  <done>Build succeeds; registration service tests pass with the required-skillLevel + birthDate changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| register form → authClient.signUp.email | Untrusted signup fields cross here; Better Auth + zod validate |
| profile form → updateProfileAction | Untrusted profile edits cross here; requireUser() + parseProfileForm are the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information Disclosure | birthDate (PII) at signup/profile | mitigate | birthDate stays on the owning User row; `safeProfileSelect` already excludes credential columns; this plan never adds birthDate to any PUBLIC participant projection (that constraint is enforced in Plan 03's read helpers). The additionalField is `input:true` scoped to signup; it is not returned to other users |
| T-11-05 | Elevation | role tampering via profile form | accept (already mitigated) | `profileSchema` intentionally omits `role`; identity comes from `requireUser()` (actions.ts:33), never the form — no new code touches authz |
| T-11-06 | Spoofing | nickname/email uniqueness bypass | accept (already mitigated) | Nickname @@unique (P2002→RU) and email pre-check + changeEmail are enforced in updateProfileAction (unchanged); the form only surfaces the typed RU errors |
| T-11-07 | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (no new deps); slopcheck N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors.
- `npx next build` → exit 0.
- `npx tsx src/lib/services/registration.test.ts` → exit 0.
- Manual (deferred to phase UAT): register a new user with a chosen level + birthDate → confirm dashboard + that `User.birthDate` is set (Prisma Studio); edit every profile field incl. nickname/email and confirm taken-errors show RU text.
</verification>

<success_criteria>
- Register form requires explicit level (no beginner default-slip) and collects an optional birthDate that persists.
- Profile form edits name/level/phone/birthDate/courtSide/nickname/email with pre-filled values and RU taken-errors.
- birthDate wired end-to-end (form → schema → signUp → additionalField → DB).
- tsc + next build + registration test green; no schema/action/service logic changed beyond the registerSchema required-skillLevel + birthDate additions and the auth.ts additionalField.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-ux/11-02-SUMMARY.md` when done.
</output>

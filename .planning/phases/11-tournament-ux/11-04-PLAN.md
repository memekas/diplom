---
phase: 11-tournament-ux
plan: 04
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - src/app/(public)/tournaments/[id]/round-score-form.tsx
  - src/app/(public)/tournaments/[id]/remove-registration-form.tsx
  - src/app/(public)/tournaments/[id]/finish-tournament-form.tsx
  - src/app/(public)/tournaments/[id]/participate-form.tsx
  - src/app/(public)/tournaments/[id]/page.tsx
autonomous: true
requirements: [FORM-02, FORM-03, SCORE-02, VIS-01, VIS-02]
must_haves:
  truths:
    - "On a singles tournament the detail page shows an «Участвовать» button calling participateSingleAction; on a pairs tournament it shows the partner-nickname form calling participateAction"
    - "Singles participants (TournamentPlayer) are listed alongside pairs, by tournament mode"
    - "playoff detail still renders BracketView; round_robin renders RoundRobinView; americano/mexicano render RotationView"
    - "Admin sees a remove button per registration during registration and a «Завершить турнир» button during in_progress"
    - "Round-based result entry renders points fields (points_a/points_b) in points mode and set rows (set{n}_a/set{n}_b) in sets mode, submitting to recordResultAction bound to the RoundMatch id"
    - "A finished tournament renders the per-format view read-only with no entry controls (VIS-02)"
  artifacts:
    - path: "src/app/(public)/tournaments/[id]/round-score-form.tsx"
      provides: "Mode-branched round-match score form (points vs sets) — separate from playoff score-form"
      contains: "recordResultAction"
    - path: "src/app/(public)/tournaments/[id]/remove-registration-form.tsx"
      provides: "Admin remove-pair/remove-player leaf"
      contains: "removeRegistrationAction"
    - path: "src/app/(public)/tournaments/[id]/finish-tournament-form.tsx"
      provides: "Admin finish-tournament leaf"
      contains: "finishTournamentAction"
    - path: "src/app/(public)/tournaments/[id]/page.tsx"
      provides: "Format-dispatched + mode-branched detail page wiring all controls and views"
      contains: "participantMode"
  key_links:
    - from: "src/app/(public)/tournaments/[id]/page.tsx"
      to: "listRounds + listTournamentPlayers (Plan 03) + computeStandings"
      via: "RSC awaits the reads for non-playoff formats and feeds the views"
      pattern: "listRounds"
    - from: "src/app/(public)/tournaments/[id]/round-score-form.tsx"
      to: "recordResultAction"
      via: "recordResultAction.bind(null, tournamentId, roundMatch.id, setsPerMatch)"
      pattern: "recordResultAction\\.bind"
    - from: "src/app/(public)/tournaments/[id]/page.tsx"
      to: "participateSingleAction / participateAction"
      via: "participantMode branch selects the form"
      pattern: "participantMode"
---

<objective>
Wire the tournament detail page (the shared hub for FORM-02 participate, FORM-03 admin controls, SCORE-02 entry, VIS-01 active visualization, VIS-02 finished history). Branch the participation section by `tournament.participantMode` (pairs nickname form vs singles button), render the participant LIST in both shapes, dispatch the visualization by `tournament.format` (BracketView playoff — UNCHANGED; RoundRobinView; RotationView — both from Plan 03), add admin remove/finish controls, add the new mode-branched round-match score form, and render finished tournaments read-only. The playoff path (BracketView, tennis ScoreForm, listBracket) stays byte-for-byte; everything new branches beside it.

Purpose: the final user path through tournaments. All backend actions/services (participate/single, remove/finish, recordResult dispatch, computeStandings) and the views/read-helpers (Plan 03) already exist — this is presentation + bound-action wiring.
Output: 3 new form leaves, an updated participate-form (or branch), an updated detail page; tsc + build green; playoff path unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-ux/11-CONTEXT.md
@.planning/phases/11-tournament-ux/11-RESEARCH.md
@.planning/research/FORMATS.md

# Detail page + leaves + the actions/services/views being wired
@src/app/(public)/tournaments/[id]/page.tsx
@src/app/(public)/tournaments/[id]/actions.ts
@src/app/(public)/tournaments/[id]/participate-form.tsx
@src/app/(public)/tournaments/[id]/score-form.tsx
@src/app/(public)/tournaments/[id]/start-tournament-form.tsx
@src/lib/services/tournament.ts
@src/lib/services/registration.ts
@src/lib/services/standings.ts
@src/lib/services/rounds.ts
@src/components/bracket-view.tsx
@src/components/round-robin-view.tsx
@src/components/rotation-view.tsx
@src/lib/validation/auth.ts
@src/lib/validation/round-result.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the round-score, remove-registration, and finish-tournament form leaves</name>
  <files>src/app/(public)/tournaments/[id]/round-score-form.tsx, src/app/(public)/tournaments/[id]/remove-registration-form.tsx, src/app/(public)/tournaments/[id]/finish-tournament-form.tsx</files>
  <read_first>
    - src/app/(public)/tournaments/[id]/score-form.tsx (existing playoff leaf: useActionState + recordResultAction.bind(null, tournamentId, matchId, setsPerMatch); set{n}_a/set{n}_b row naming lines 52-65 — mirror for sets mode; do NOT edit this file)
    - src/app/(public)/tournaments/[id]/start-tournament-form.tsx (single-button leaf pattern: useActionState + bound action)
    - src/app/(public)/tournaments/[id]/actions.ts (recordResultAction signature (tournamentId, matchId, _setsPerMatch, prev, formData) lines 249-258; removeRegistrationAction (tournamentId, kind:"pair"|"player", id, prev, formData) lines 169-175; finishTournamentAction (tournamentId, prev, formData) line 210; all return {ok}/{error} RU)
    - src/lib/validation/round-result.ts (points mode reads points_a/points_b; sets mode reads set{n}_a/set{n}_b for n=1..setsPerMatch — exact field names; partial rows error)
    - 11-RESEARCH.md Pitfall 4 (FormData contract by mode) + Pitfall 5 (mexicano stale_pairings RU error on edit-after-successor) + Pitfall 7 (dark error classes) + "Admin remove control bind" note (kind="player" uses TournamentPlayer.id)
  </read_first>
  <action>
    Create three "use client" leaves (each NEVER imports prisma; each binds ids into the action so they are not client-tamperable; each shows {ok:false} errors with dark classes bg-red-900/40 text-red-300):

    1. `round-score-form.tsx` — `RoundScoreForm` for round-based matches. Props: `{ tournamentId: string; roundMatchId: string; scoringMode: "sets" | "points"; setsPerMatch: number; teamAName: string; teamBName: string; pointsA: number | null; pointsB: number | null; existingSets?: { gamesPair1: number; gamesPair2: number }[] }`. Bind `recordResultAction.bind(null, tournamentId, roundMatchId, setsPerMatch)` (matchId = the RoundMatch id, NOT a playoff Match id — the dispatcher routes by DB format). Render the team-vs-team header (RU "против"). Branch the inputs by `scoringMode`:
       - `"points"`: exactly two number inputs `name="points_a"` and `name="points_b"`, min 0, defaultValue = `pointsA ?? ""` / `pointsB ?? ""`, RU aria-labels.
       - `"sets"`: up to `setsPerMatch` rows named `set${n}_a` / `set${n}_b` (n=1..setsPerMatch), min 0, mirroring score-form.tsx exactly (pre-fill from existingSets if provided). NOTE: round-based sets store sets-won in pointsA/pointsB; existingSets is optional — if not available, render blank rows.
       Submit button "Сохранить счёт" (pending "Сохранение…"). The action's typed RU errors (incl. mexicano stale_pairings "следующий раунд уже сформирован") surface verbatim via the {ok:false} branch — no special handling needed.

    2. `remove-registration-form.tsx` — `RemoveRegistrationForm`. Props: `{ tournamentId: string; kind: "pair" | "player"; id: string }`. Bind `removeRegistrationAction.bind(null, tournamentId, kind, id)`. Render a small "Удалить" submit button (pending "Удаление…") plus the {ok:false} error. (Confirmation optional — Claude's discretion; a native confirm is acceptable but not required.) For singles the `id` is the `TournamentPlayer.id` (the row id), for pairs it is the `Pair.id`.

    3. `finish-tournament-form.tsx` — `FinishTournamentForm`. Props: `{ tournamentId: string }`. Bind `finishTournamentAction.bind(null, tournamentId)`. Render a "Завершить турнир" submit button (pending "Завершение…") plus the {ok:false} error.

    Do NOT edit score-form.tsx (playoff). Do NOT change any action. Do NOT add dependencies.
  </action>
  <acceptance_criteria>
    - round-score-form.tsx: points mode renders points_a/points_b; sets mode renders set{n}_a/set{n}_b rows (1..setsPerMatch); binds recordResultAction.bind(null, tournamentId, roundMatchId, setsPerMatch).
    - remove-registration-form.tsx binds removeRegistrationAction.bind(null, tournamentId, kind, id); finish-tournament-form.tsx binds finishTournamentAction.bind(null, tournamentId).
    - All three are "use client", never import prisma, show {ok:false} RU errors with dark classes.
    - score-form.tsx (playoff) is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Three new bound-action client leaves exist (round-score by mode, remove, finish); playoff score-form untouched; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Branch participation UI + participant list by participantMode</name>
  <files>src/app/(public)/tournaments/[id]/participate-form.tsx</files>
  <read_first>
    - src/app/(public)/tournaments/[id]/participate-form.tsx (current pairs-only nickname form → participateAction; dark error class needs fixing per Pitfall 7 — it uses bg-red-100)
    - src/app/(public)/tournaments/[id]/actions.ts (participateAction (pairs, reads player2Nickname) vs participateSingleAction (singles, empty form) — both bound by tournamentId)
    - 11-RESEARCH.md Pitfall 3 (participantMode branch; round_robin is either mode, americano/mexicano always singles) + Pitfall 7
  </read_first>
  <action>
    Extend `participate-form.tsx` to support both modes. Keep the existing `ParticipateForm` (pairs nickname → participateAction) but fix its error box to the dark class `bg-red-900/40 text-red-300` (Pitfall 7). Add a sibling `SingleParticipateForm` in the same file: props `{ tournamentId: string }`, binds `participateSingleAction.bind(null, tournamentId)`, renders a single "Участвовать" button (pending "Регистрация…") with the {ok:false} RU error shown in a dark box. (No client-supplied fields — registerSingleSchema is empty; identity comes from the session server-side.)

    Both forms surface the action's typed RU errors verbatim (level_mismatch → "Уровень игрока не совпадает с уровнем турнира", wrong_mode, tournament_full, already_registered, partner_not_found). Neither imports prisma.

    (The detail page Task 3 chooses which form to render by `tournament.participantMode`.)
  </action>
  <acceptance_criteria>
    - participate-form.tsx exports ParticipateForm (pairs nickname → participateAction) AND SingleParticipateForm (singles button → participateSingleAction).
    - SingleParticipateForm renders a single "Участвовать" button, no client fields, binds tournamentId.
    - Both use dark error classes; neither imports prisma.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Both participation leaves exist for the page to pick by mode; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the detail page — mode-branched participation/list, format-dispatched views, admin controls, finished read-only</name>
  <files>src/app/(public)/tournaments/[id]/page.tsx</files>
  <read_first>
    - src/app/(public)/tournaments/[id]/page.tsx (current playoff-only page: getTournament, listTournamentPairs, listBracket, BracketView, ScoreForm loop, hardcoded "Single-elimination (пары)" label line 75)
    - src/lib/services/tournament.ts (getTournament returns format/participantMode/level/price/scoringMode/targetPoints/totalRounds/setsPerMatch/gamesPerSet)
    - src/lib/services/registration.ts (listTournamentPairs shape)
    - src/lib/services/rounds.ts (listRounds + listTournamentPlayers from Plan 03 — RoundRead shape)
    - src/lib/services/standings.ts (computeStandings → {kind:"units"|"players"})
    - src/components/round-robin-view.tsx, src/components/rotation-view.tsx (Plan 03 view props: rounds/standings/nameById/readOnly/renderEntry)
    - src/lib/validation/auth.ts (formatLabels, tournamentKindLabels, skillLevelLabels for metadata)
    - 11-RESEARCH.md "Format dispatch in the RSC page" + "Admin remove control bind" + Pattern 3 (readOnly = !isAdmin || finished) + Pitfall 5 (current vs past) + Security Domain (do NOT select birthDate into public lists)
  </read_first>
  <action>
    Update the detail page (stays an async public Server Component, no auth guard, getOptionalSession only). Keep all playoff behavior intact and branch beside it.

    METADATA: replace the hardcoded "Single-elimination (пары)" label with `formatLabels[tournament.format]`; add rows for вид (`tournamentKindLabels[participantMode]`), уровень (`skillLevelLabels[level]`), цена (price != null ? "{price} ₽" : "—"), режим подсчёта (scoringMode === "sets" ? "Сеты/геймы" : "Очки"). Keep size/date/location. For non-pairs, the size label should read "{size} участников" not "{size} пар".

    READS: always `getTournament`, `getOptionalSession`. Then branch:
    - pairs mode → `listTournamentPairs` (existing) for the participant list + capacity.
    - singles mode → `listTournamentPlayers` (Plan 03) for the participant list + capacity (count vs size).
    - playoff format → `listBracket` (existing, UNCHANGED).
    - non-playoff format → `listRounds` (Plan 03) + `computeStandings`. Build a `nameById` map: from listRounds team members `{id,name}` and (for singles) listTournamentPlayers, and (for pairs RR) a pairId→"name1 / name2" label from listTournamentPairs — supply unitId/userId → label for the standings table.

    PARTICIPANT LIST: render pairs as today for pairs mode; for singles mode render the TournamentPlayer list (name + RU level + courtSide via the existing label helpers). When `isAdmin && status==="registration"`, render `<RemoveRegistrationForm tournamentId kind id />` next to each entry (kind="pair" + Pair.id for pairs; kind="player" + TournamentPlayer.id for singles).

    PARTICIPATION (status==="registration", logged-in, not full, not already registered): branch by participantMode → `<ParticipateForm>` (pairs) or `<SingleParticipateForm>` (singles). Keep the existing not-logged-in / already-registered / full notices (compute alreadyRegistered for singles from listTournamentPlayers userId match too). Keep the admin `<StartTournamentForm>` block for registration.

    VISUALIZATION (when matches/rounds exist): dispatch by format.
    - playoff → existing `<BracketView matches={...} />` (UNCHANGED).
    - round_robin → `<RoundRobinView rounds standings nameById readOnly={...} renderEntry={...} />`.
    - americano|mexicano → `<RotationView rounds standings nameById readOnly={...} renderEntry={...} />`.
    Compute `readOnly = !isAdmin || tournament.status === "finished"`. When `isAdmin && status==="in_progress"`, pass a `renderEntry={(m) => <RoundScoreForm tournamentId={id} roundMatchId={m.id} scoringMode={tournament.scoringMode} setsPerMatch={tournament.setsPerMatch} teamAName={...} teamBName={...} pointsA={m.pointsA} pointsB={m.pointsB} />}` so unrecorded round matches get the mode-branched form; otherwise omit renderEntry (read-only). Keep the existing playoff `ScoreForm` loop for `format==="playoff" && isAdmin && in_progress` UNCHANGED.

    ADMIN FINISH: when `isAdmin && status==="in_progress"`, render `<FinishTournamentForm tournamentId={id} />` (for ALL formats — ADMN-02). For playoff this complements the existing auto-finish.

    FINISHED (VIS-02): when `status==="finished"`, the same format-dispatched views render with `readOnly` true (no entry controls, no participation, no admin remove) — the match history per format is the read-only view. No separate page needed (the ?status=finished filter on /tournaments already lists finished tournaments).

    Do NOT change listBracket/BracketView/score-form/result.ts/bracket.ts. Do NOT select birthDate anywhere. Do NOT call any engine/generate/materialize function from the page (reads only).
  </action>
  <acceptance_criteria>
    - Metadata shows formatLabels[format], вид, уровень, цена, режим подсчёта; size label is "пар" for pairs, "участников" otherwise.
    - Singles tournament: participant list from listTournamentPlayers; «Участвовать» button → participateSingleAction. Pairs: nickname form → participateAction, pairs list as before.
    - Format dispatch: playoff → BracketView (unchanged); round_robin → RoundRobinView; americano/mexicano → RotationView; all non-playoff fed by listRounds + computeStandings + a nameById map.
    - Admin sees a remove button per registration during registration and «Завершить турнир» during in_progress (all formats).
    - In_progress + admin + round-based: unrecorded matches render RoundScoreForm by scoringMode via the view's renderEntry slot; playoff keeps its existing ScoreForm loop.
    - finished tournaments render the per-format view read-only with no entry/participation/remove controls.
    - playoff path (BracketView, ScoreForm, listBracket) byte-for-byte unchanged; no birthDate selected.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>Detail page branches participation/list by mode, dispatches views by format, wires admin remove/finish + round-based entry, and renders finished read-only; playoff untouched; tsc + build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser leaf forms → Server Actions (participate/single, remove, finish, recordResult) | Untrusted POSTs cross here; the guarded actions are the real boundary |
| RSC reads (rounds/players/pairs) → public detail page | Reads cross to anonymous viewers; projection must not leak PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Elevation | Non-admin POST to remove/finish/recordResult | accept (already mitigated) | Each action's FIRST line is `requireAdmin()` (actions.ts:176/216/259); hiding the buttons for non-admins is cosmetic UX — the server guard is authoritative. No new auth code |
| T-11-12 | Tampering | Tampered roundMatchId/pairId/playerId or format/scoringMode in a POST | mitigate | All ids are bound from the RSC via `.bind()` (never read from the form body); `recordFormatResult` re-reads format/scoringMode/setsPerMatch from the DB (format-engine.ts:69-72) so the client value cannot misroute the write; services re-read status guards in-transaction |
| T-11-13 | Information Disclosure | participant lists / round views exposing PII | mitigate | The page consumes only the safe-select reads (listTournamentPairs/listTournamentPlayers/listRounds — `{id,name,skillLevel,courtSide}` only); the page selects NO birthDate; raw action errors are typed RU only |
| T-11-14 | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (no new deps); slopcheck N/A |
| T-11-15 | Business-logic | mexicano score edit after next round materialized | accept | `recordRoundResult` rejects with `stale_pairings` RU error (round-result.ts:198-208); the RoundScoreForm surfaces it verbatim — surfaced, not silent |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors.
- `npx next build` → exit 0.
- Playoff regression: BracketView/score-form/listBracket/result.ts/bracket.ts unmodified (git diff shows no changes to those files).
- Manual (deferred to phase UAT): for each format, register participants, start, enter results by mode, finish, and view the finished read-only history; confirm admin-only controls are hidden for anonymous/player.
</verification>

<success_criteria>
- Detail page is format-dispatched (playoff BracketView unchanged; RR/americano/mexicano new views) and mode-branched (singles button vs pairs nickname + matching participant lists).
- Admin remove (registration) + finish (in_progress, all formats) controls wired to the guarded actions.
- Round-based result entry renders points vs sets inputs by scoringMode, bound to the RoundMatch id, dispatched server-side.
- Finished tournaments render per-format read-only history (VIS-02).
- Playoff path untouched; no new deps; no PII in reads; tsc + build green.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-ux/11-04-SUMMARY.md` when done.
</output>

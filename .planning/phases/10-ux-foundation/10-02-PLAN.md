---
phase: 10-ux-foundation
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/lib/services/tournament.ts
  - src/lib/services/tournament.test.ts
  - src/app/page.tsx
autonomous: true
requirements: [HOME-01]
must_haves:
  truths:
    - "Главная показывает список открытых для регистрации турниров (status=registration)"
    - "С карточки главной можно перейти на /tournaments/[id]"
    - "При отсутствии открытых турниров главная показывает понятный пустой стейт"
    - "Карточка показывает RU-подписи формата/вида/уровня и размер/дату"
  artifacts:
    - path: "src/lib/services/tournament.ts"
      provides: "listTournaments с фильтром по статусу (открытые турниры)"
      contains: "status"
    - path: "src/lib/services/tournament.test.ts"
      provides: "Юнит-тест фильтра по статусу (fake prisma, node:assert)"
      contains: "listTournaments"
    - path: "src/app/page.tsx"
      provides: "Главная — Server Component со списком открытых турниров"
      contains: "listTournaments"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/services/tournament.ts"
      via: "listTournaments(prisma, фильтр статуса)"
      pattern: "listTournaments"
    - from: "src/app/page.tsx"
      to: "/tournaments/[id]"
      via: "Link href на карточке"
      pattern: "tournaments/\\$\\{"
---

<objective>
Заменить статичный английский splash главной (`src/app/page.tsx`) на Server Component, который читает открытые для регистрации турниры и рендерит их карточками со ссылкой на детальную страницу (HOME-01, success criterion 4). Расширить `listTournaments` фильтром по статусу и покрыть его юнит-тестом.

Purpose: главная — точка входа в Core Value: пользователь видит, на какие турниры можно записаться, и переходит к ним.
Output: фильтр статуса в сервисе + тест, локализованная тёмная адаптивная главная.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ux-foundation/10-CONTEXT.md

@CLAUDE.md
@src/lib/services/tournament.ts
@src/app/(public)/tournaments/page.tsx
@src/lib/services/standings.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Фильтр по статусу в listTournaments + юнит-тест</name>
  <files>src/lib/services/tournament.ts, src/lib/services/tournament.test.ts</files>
  <read_first>src/lib/services/tournament.ts, src/lib/services/standings.test.ts, src/lib/services/admin.test.ts</read_first>
  <behavior>
    - listTournaments без аргумента статуса возвращает все турниры (orderBy createdAt desc) — обратная совместимость с `(public)/tournaments/page.tsx`.
    - listTournaments с `{ status: "registration" }` строит `where: { status: "registration" }` в запросе.
    - listTournaments с `{ status: "finished" }` строит `where: { status: "finished" }` (используется шапкой плана 03 «Прошедшие турниры» — фильтр уже доступен).
    - select и orderBy не меняются.
  </behavior>
  <action>
    Расширить сигнатуру `listTournaments(prisma, opts?: { status?: TournamentStatus })`. Если `opts.status`
    задан — добавить `where: { status: opts.status }` в `findMany`; иначе `where` отсутствует (как сейчас).
    Импортировать тип `TournamentStatus` из `@/lib/validation/tournament`. НЕ ломать существующий вызов
    `listTournaments(prisma)` в `src/app/(public)/tournaments/page.tsx` (без аргумента). select/orderBy
    оставить. Затем создать `src/lib/services/tournament.test.ts` по образцу `standings.test.ts`/`admin.test.ts`
    (self-contained, `node:assert/strict`, hand-written fake prisma, exit non-zero on failure, шапка-комментарий
    с командой запуска `npx tsx src/lib/services/tournament.test.ts`). Тесты: (a) без аргумента — fake prisma
    фиксирует `findMany`-аргумент, проверить что `where` отсутствует; (b) `{status:"registration"}` -> `where.status === "registration"`;
    (c) `{status:"finished"}` -> `where.status === "finished"`. Fake возвращает массив-заглушку.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/tournament.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `listTournaments(prisma)` без аргумента работает как раньше (тест a зелёный).
    - `listTournaments(prisma, { status })` подставляет `where.status` (тесты b, c зелёные).
    - `npx tsx src/lib/services/tournament.test.ts` завершается успешно (exit 0).
    - `npx tsc --noEmit` -> 0; вызов в `(public)/tournaments/page.tsx` не сломан.
  </acceptance_criteria>
  <done>Сервис умеет отдавать только открытые (или только завершённые) турниры; фильтр покрыт юнит-тестом.</done>
</task>

<task type="auto">
  <name>Task 2: Главная — Server Component со списком открытых турниров</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx, src/app/(public)/tournaments/page.tsx, src/lib/services/tournament.ts, src/lib/validation/auth.ts</read_first>
  <action>
    Переписать `src/app/page.tsx` как `async` Server Component (как `(public)/tournaments/page.tsx`):
    импортировать `prisma` из `@/lib/db`, `listTournaments` из `@/lib/services/tournament`,
    `TournamentStatusBadge` из `@/components/tournament-status-badge`, и
    `formatLabels`/`tournamentKindLabels`/`skillLevelLabels` из `@/lib/validation/auth` (созданы в плане 10-01).
    Вызвать `listTournaments(prisma, { status: "registration" })`. Рендерить заголовок RU (напр.
    «Открытые турниры»), затем список карточек-ссылок на `/tournaments/${t.id}`. Каждая карточка показывает:
    `t.name`, RU-формат `formatLabels[t.format]`, RU-вид `tournamentKindLabels[t.participantMode]`,
    RU-уровень `skillLevelLabels[t.level]`, размер (`t.size`) и дату (`t.date`, если есть — форматировать
    как локальную дату RU; если null — не показывать). Пустой стейт: понятный RU-текст
    (напр. «Сейчас нет открытых турниров») в стиле пустого стейта `(public)/tournaments/page.tsx`.
    Адаптив (SITE-03): mobile-first; список — `flex flex-col gap-3`, карточки переносят контент
    (`flex-wrap` / стек на узком экране), контейнер `mx-auto w-full max-w-2xl p-8` с `sm:`/`md:` где нужно,
    без горизонтального скролла. Тёмная тема наследуется от globals (план 10-01) — использовать семантические
    `border-current/15`, `opacity-70` как в образце, не светлые палитры. Никакой английской строки не оставлять.
  </action>
  <verify>
    <automated>grep -q "listTournaments(prisma, { status: \"registration\" })" src/app/page.tsx &amp;&amp; grep -q "tournaments/\${" src/app/page.tsx &amp;&amp; ! grep -qiE "Register as a pair|Log in|Padel Tournaments" src/app/page.tsx &amp;&amp; echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` — `async` Server Component, вызывает `listTournaments(prisma, { status: "registration" })`.
    - Карточки ссылаются на `/tournaments/${t.id}` и показывают RU-формат/вид/уровень (через *Labels-карты) + размер/дату.
    - Пустой стейт на русском при отсутствии открытых турниров.
    - В файле нет английских строк (`Register as a pair` / `Log in` / `Padel Tournaments`); grep печатает OK.
    - `npx next build` проходит; `npx tsc --noEmit` -> 0.
  </acceptance_criteria>
  <done>Главная показывает открытые турниры карточками со ссылками; пустой стейт локализован; адаптив и тёмная тема.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| анонимный посетитель -> главная | Публичное чтение списка турниров (без auth) |

Главная читает ТОЛЬКО открытые для регистрации турниры (публичные данные, как существующий `(public)/tournaments`). Нет мутаций, нет нового input, нет auth-обхода. select сервиса не содержит непубличных полей.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | src/app/page.tsx (главная) | mitigate | Рендерить только `status="registration"` через серверный фильтр `where`; не выводить непубличные/чужие данные. Все поля select публичны (как `(public)/tournaments`). |
| T-10-03 | Information Disclosure | listTournaments(opts) | accept | Фильтр статуса серверный (Prisma where), не из клиентского ввода; значения статуса типизированы `TournamentStatus`. |
| T-10-SC | Tampering | npm/pnpm installs | mitigate | Зависимости не добавляются; install-задач нет. |
</threat_model>

<verification>
- `npx tsx src/lib/services/tournament.test.ts` -> exit 0.
- `npx tsc --noEmit` -> 0.
- `npx next build` проходит без ошибок.
- Главная не содержит английских строк; ссылки на `/tournaments/[id]` присутствуют.
- Регрессия: `npx tsx src/lib/services/tournament-status.test.ts` (если затронут статус-тип) — зелёный.
- Визуальная проверка (карточки/адаптив/тёмная) — human-verify в конце фазы.
</verification>

<success_criteria>
- Главная (HOME-01) показывает список открытых для регистрации турниров с переходом на детальную страницу.
- Понятный пустой стейт на русском.
- Фильтр статуса в `listTournaments` покрыт юнит-тестом.
- Карточки используют RU-подписи формата/вида/уровня и тёмную палитру.
- build + tsc зелёные.
</success_criteria>

<output>
Create `.planning/phases/10-ux-foundation/10-02-SUMMARY.md` when done.
</output>

---
phase: 10-ux-foundation
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/nav.tsx
  - src/components/logout-button.tsx
  - src/lib/validation/profile.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(auth)/register/register-form.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/profile/page.tsx
  - src/app/(app)/profile/profile-form.tsx
autonomous: true
requirements: [SITE-01, SITE-02, SITE-03, HDR-01]
must_haves:
  truths:
    - "Шапка содержит название клуба + логотип на всех страницах"
    - "Шапка содержит кнопку «Прошедшие турниры» (ссылка на завершённые)"
    - "Залогиненный пользователь видит вход в личный кабинет (ссылка на /profile)"
    - "Все экраны входа/регистрации/dashboard/профиля/навигации на русском"
    - "Уровни и сторона корта выводятся RU-подписями (skillLevelLabels / courtSideLabels)"
    - "Светлые алерт-пилюли в формах заменены тёмными"
    - "Шапка и формы адаптивны на мобильном (без горизонтального скролла)"
  artifacts:
    - path: "src/components/nav.tsx"
      provides: "Шапка: CLUB_NAME + логотип + Турниры/Прошедшие турниры + ЛК + имя/Выйти; гости Войти/Регистрация"
      contains: "CLUB_NAME"
    - path: "src/lib/validation/profile.ts"
      provides: "courtSideLabels RU-карта"
      contains: "courtSideLabels"
    - path: "src/app/(app)/profile/profile-form.tsx"
      provides: "RU-форма профиля с RU-подписями уровня/стороны и тёмными алертами"
      contains: "courtSideLabels"
  key_links:
    - from: "src/components/nav.tsx"
      to: "/profile"
      via: "Link href=/profile для залогиненного (вход в ЛК)"
      pattern: "/profile"
    - from: "src/components/nav.tsx"
      to: "/tournaments (завершённые)"
      via: "Link Прошедшие турниры (фильтр finished)"
      pattern: "Прошедшие"
---

<objective>
Перестроить шапку (HDR-01) и локализовать оставшийся английский UI (SITE-01) на русский, заменить светлые алерт-пилюли в формах на тёмные (SITE-02) и пройтись по адаптиву ключевых экранов (SITE-03): шапка, вход, регистрация, dashboard, профиль.

Purpose: шапка — навигационный каркас всего сайта (название клуба, логотип, «Прошедшие турниры», вход в ЛК); оставшиеся английские строки и светлые пилюли — последние нарушения SITE-01/02. Опирается на RU-карты и тёмную тему из плана 10-01.
Output: новая RU-шапка, локализованные формы/страницы, `courtSideLabels`, тёмные алерты, адаптив.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ux-foundation/10-CONTEXT.md

@CLAUDE.md
@src/components/nav.tsx
@src/lib/validation/profile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Перестроить шапку nav.tsx (HDR-01) — клуб, логотип, Прошедшие турниры, вход в ЛК</name>
  <files>src/components/nav.tsx</files>
  <read_first>src/components/nav.tsx, src/components/logout-button.tsx</read_first>
  <action>
    Перестроить `Nav` (остаётся async Server Component, читает `getOptionalSession()` — гварды/контракт
    сессии не менять). Объявить вверху файла константу `const CLUB_NAME = "Падел Клуб";` с комментарием
    «placeholder, переименуемо» (CONTEXT: имя клуба placeholder). Слева — ссылка на `/`: инлайновый
    SVG-логотип-placeholder (простой: круг/ракетка/инициалы, без внешнего ассета в `public/`) + `CLUB_NAME`.
    Справа: «Турниры» (`/tournaments`), «Прошедшие турниры» — ссылка на список завершённых
    (`/tournaments?status=finished` ИЛИ существующий маршрут списка; фильтр сервиса доступен из плана 10-02 —
    полноценная per-format история это Phase 11 VIS-02, здесь достаточно ссылки), для admin — «Создать турнир»
    (`/admin/tournaments/new`, оставить), для залогиненного — ссылка «Личный кабинет» на `/profile` (СЕЙЧАС
    ОТСУТСТВУЕТ — добавить), имя пользователя, `<LogoutButton/>` с RU-текстом «Выйти» (см. Task 2 по кнопке);
    для гостей — «Войти» (`/login`) и «Регистрация» (`/register`). ВСЕ строки RU (заменить `Log in`/`Register`).
    Адаптив (SITE-03): на мобильном шапка не должна вызывать горизонтальный скролл — использовать
    `flex-wrap` с `gap`, скрывать второстепенное или переносить на новую строку через `flex-wrap`
    (burger vs wrap — на усмотрение исполнителя, без тяжёлых компонентов/библиотек); на `sm:`/`md:` —
    горизонтальная раскладка как сейчас. Семантические цвета (`bg-foreground`/`text-background`,
    `border-current/15`) — тёмная тема из плана 10-01.
  </action>
  <verify>
    <automated>grep -q "CLUB_NAME" src/components/nav.tsx &amp;&amp; grep -q "Прошедшие" src/components/nav.tsx &amp;&amp; grep -q "/profile" src/components/nav.tsx &amp;&amp; grep -q "Войти" src/components/nav.tsx &amp;&amp; ! grep -qE ">[[:space:]]*Log in|>[[:space:]]*Register<" src/components/nav.tsx &amp;&amp; echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `nav.tsx` объявляет `CLUB_NAME` и рендерит инлайн-SVG-логотип + название слева.
    - Есть ссылки «Турниры», «Прошедшие турниры» (на завершённые), «Личный кабинет» -> `/profile` для залогиненного.
    - Гости видят «Войти»/«Регистрация»; нет английских `Log in`/`Register`.
    - Шапка использует `flex-wrap`/`sm:`/`md:` — без горизонтального скролла на мобильном.
    - `getOptionalSession`/admin-гвард не изменены логически.
  </acceptance_criteria>
  <done>Шапка содержит клуб+логотип, «Прошедшие турниры» и вход в ЛК на всех страницах, на русском, адаптивна.</done>
</task>

<task type="auto">
  <name>Task 2: RU-локализация login/register/dashboard + тёмные алерты + кнопка выхода</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/login/login-form.tsx, src/app/(auth)/register/page.tsx, src/app/(auth)/register/register-form.tsx, src/app/(app)/dashboard/page.tsx, src/components/logout-button.tsx</files>
  <read_first>src/app/(auth)/login/page.tsx, src/app/(auth)/login/login-form.tsx, src/app/(auth)/register/page.tsx, src/app/(auth)/register/register-form.tsx, src/app/(app)/dashboard/page.tsx, src/components/logout-button.tsx, src/lib/validation/auth.ts</read_first>
  <action>
    Перевести все английские строки UI на русский (SITE-01), логику/валидацию/`error.code`-ветвление не трогать:
    - `login/page.tsx`: «Log in» -> «Вход», «No account yet? Register» -> «Нет аккаунта? Регистрация».
    - `login-form.tsx`: лейблы «Email»/«Password» -> «Почта»/«Пароль», кнопка «Log in»/«Signing in…» ->
      «Войти»/«Вход…», fallback `error.message ?? "Invalid email or password"` -> RU «Неверная почта или пароль».
    - `register/page.tsx`: «Create your account» -> «Регистрация», «Already have an account? Log in» ->
      «Уже есть аккаунт? Войти».
    - `register-form.tsx`: лейблы «Email»/«Password»/«Name»/«Phone (optional)»/«Skill level (optional)» ->
      «Почта»/«Пароль»/«Имя (ФИО)»/«Телефон (необязательно)»/«Уровень (необязательно)»; кнопка
      «Register»/«Creating account…» -> «Зарегистрироваться»/«Создаём аккаунт…»; в `<select>` уровня
      выводить `skillLevelLabels[lvl]` вместо сырого `{lvl}` (импорт из `@/lib/validation/auth`); fallback
      `error.message ?? "Registration failed"` -> RU «Не удалось зарегистрироваться».
    - `dashboard/page.tsx`: «Welcome, {name}» -> «Добро пожаловать, {name}», «You are signed in.» ->
      «Вы вошли в систему.».
    - `logout-button.tsx`: текст кнопки -> «Выйти» (если сейчас «Log out»).
    - Тёмные алерты (SITE-02): в `login-form.tsx` и `register-form.tsx` заменить `bg-red-100 text-red-800`
      на тёмный эквивалент (напр. `bg-red-900/40 text-red-300`); inline-ошибки `text-red-600` -> читаемый
      на тёмном (`text-red-400`). Адаптив форм (SITE-03): формы уже `max-w-sm` — убедиться, что на мобильном
      нет горизонтального скролла (контейнер `w-full`, padding); добавить `sm:` только если ломается.
  </action>
  <verify>
    <automated>! grep -rqiE "Welcome,|You are signed in|Log in|Log out|Sign in|Signing in|Create your account|Already have|No account|Skill level|Registration failed|Invalid email" src/app/\(auth\) src/app/\(app\)/dashboard src/components/logout-button.tsx &amp;&amp; ! grep -rqE "bg-red-100" src/app/\(auth\) &amp;&amp; grep -q "skillLevelLabels" src/app/\(auth\)/register/register-form.tsx &amp;&amp; echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - В `(auth)/*`, `dashboard/page.tsx`, `logout-button.tsx` нет английских user-facing строк (grep печатает OK).
    - Уровень в register-select выводится через `skillLevelLabels`.
    - Светлых алертов `bg-red-100` в `(auth)` нет; ошибки читаемы на тёмном.
    - Логика авторизации, zod-валидация и `error.code`-ветвление не изменены.
    - `npx tsc --noEmit` -> 0.
  </acceptance_criteria>
  <done>Вход/регистрация/dashboard на русском с тёмными алертами и RU-подписями уровня; кнопка выхода RU.</done>
</task>

<task type="auto">
  <name>Task 3: RU-локализация профиля + courtSideLabels + тёмные алерты</name>
  <files>src/lib/validation/profile.ts, src/app/(app)/profile/page.tsx, src/app/(app)/profile/profile-form.tsx</files>
  <read_first>src/lib/validation/profile.ts, src/app/(app)/profile/page.tsx, src/app/(app)/profile/profile-form.tsx, src/lib/validation/auth.ts</read_first>
  <action>
    1) В `src/lib/validation/profile.ts` добавить display-only RU-карту рядом с `courtSides`:
    `courtSideLabels: Record<(typeof courtSides)[number], string>` =
    { left: «Левая», right: «Правая», either: «Любая» }. Схему/парсинг не трогать.
    2) `profile/page.tsx`: «Profile» -> «Личный кабинет» (заголовок); подзаголовки (name/email) оставить
    как данные. Без английских лейблов.
    3) `profile-form.tsx`: лейблы «Court side» -> «Сторона корта», «Phone (optional)» -> «Телефон (необязательно)»,
    «Skill level (optional)» -> «Уровень (необязательно)»; кнопка «Save»/«Saving…» -> «Сохранить»/«Сохраняем…»;
    success «Saved.» -> «Сохранено.». В `<select>` стороны корта выводить `courtSideLabels[side]` вместо сырого
    `{side}`; в `<select>` уровня — `skillLevelLabels[lvl]` вместо `{lvl}` (импорт из `@/lib/validation/auth`).
    Тёмные алерты (SITE-02): `bg-green-100 text-green-800` -> `bg-green-900/40 text-green-300`,
    `bg-red-100 text-red-800` -> `bg-red-900/40 text-red-300`, inline `text-red-600` -> `text-red-400`.
    Адаптив: контейнер уже `max-w-sm w-full` — без горизонтального скролла на мобильном.
    Client-leaf/`useActionState`-логику и `parseProfileForm` не менять.
  </action>
  <verify>
    <automated>grep -q "courtSideLabels" src/lib/validation/profile.ts &amp;&amp; grep -q "courtSideLabels" src/app/\(app\)/profile/profile-form.tsx &amp;&amp; grep -q "skillLevelLabels" src/app/\(app\)/profile/profile-form.tsx &amp;&amp; ! grep -rqiE "Court side|Skill level|>[[:space:]]*Save|Saving|Saved\.|>[[:space:]]*Profile<" src/app/\(app\)/profile &amp;&amp; ! grep -rqE "bg-(green|red)-100" src/app/\(app\)/profile &amp;&amp; echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `profile.ts` экспортирует `courtSideLabels` (left/right/either), типизированную по `courtSides`.
    - В профиле сторона корта и уровень выводятся RU-подписями (courtSideLabels / skillLevelLabels).
    - Нет английских строк («Court side»/«Skill level»/«Save»/«Profile») и нет светлых алертов `*-100` (grep печатает OK).
    - `useActionState`/`parseProfileForm`/server-action не изменены логически.
    - `npx tsc --noEmit` -> 0.
  </acceptance_criteria>
  <done>Профиль на русском с RU-подписями уровня/стороны и тёмными алертами.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| гость/пользователь -> шапка | Nav читает сессию (getOptionalSession) для показа имени/ссылок |
| пользователь -> /profile | Защищённый маршрут (requireUser в page.tsx) — гвард НЕ трогаем |

Презентационная фаза: меняем подписи, классы и навигационные ссылки. Ссылка «Личный кабинет» ведёт на `/profile`, но сам guard `requireUser` в `profile/page.tsx` остаётся — анонимный клик редиректит на `/login`. Логика auth/валидации/server-actions не изменяется.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Elevation of Privilege | nav.tsx ссылка на ЛК / admin-ссылка | mitigate | Ссылка не = доступ: `requireUser` в `profile/page.tsx` и admin-гварды не меняются; nav показывает admin-пункт по `session.user.role === "admin"` как раньше. |
| T-10-05 | Information Disclosure | nav.tsx (имя пользователя) | accept | Имя берётся из существующего `getOptionalSession()`; публичных утечек нет, контракт сессии не изменён. |
| T-10-06 | Spoofing | login/register формы | accept | Тексты/классы локализованы; authClient-флоу, zod и `error.code`-ветвление нетронуты. |
| T-10-SC | Tampering | npm/pnpm installs | mitigate | Зависимости не добавляются; install-задач нет. |
</threat_model>

<verification>
- `npx tsc --noEmit` -> 0.
- `npx next build` проходит.
- Grep по английским user-facing строкам в `src/app/(auth)`, `src/app/(app)`, `src/components` (исключая комментарии/атрибуты `name=`/`autoComplete=`/`type=`) -> 0 совпадений в видимом тексте.
- Светлые алерты `bg-(green|red)-100` в `(auth)`/`profile` отсутствуют.
- Шапка: `CLUB_NAME`, «Прошедшие», `/profile`, «Войти» присутствуют.
- Регрессия профиля/валидации: `npx tsx src/lib/validation/profile.test.ts` -> зелёный.
- Визуальная проверка (шапка/адаптив/тёмные алерты) — human-verify в конце фазы.
</verification>

<success_criteria>
- Шапка (HDR-01): клуб+логотип, «Прошедшие турниры», вход в ЛК на всех страницах.
- Полная RU-локализация (SITE-01) входа/регистрации/dashboard/профиля/навигации + RU-подписи уровня и стороны корта.
- Тёмные алерты во всех формах (SITE-02).
- Адаптив шапки и форм (SITE-03) — без горизонтального скролла на мобильном.
- tsc + build зелёные; гварды/валидация нетронуты.
</success_criteria>

<output>
Create `.planning/phases/10-ux-foundation/10-03-SUMMARY.md` when done.
</output>

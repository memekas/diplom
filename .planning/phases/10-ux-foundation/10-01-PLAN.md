---
phase: 10-ux-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/validation/auth.ts
  - src/components/tournament-status-badge.tsx
autonomous: true
requirements: [SITE-01, SITE-02]
must_haves:
  truths:
    - "Сайт принудительно тёмный независимо от темы ОС"
    - "Корневой документ объявлен русским (html lang=ru)"
    - "Статус-бейджи турниров читаемы на тёмном фоне (нет светлых пилюль)"
    - "Метаданные вкладки/описание на русском"
    - "Доступны RU-карты формата (formatLabels) и вида (tournamentKindLabels) для переиспользования"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Принудительная тёмная тема (root тёмный, нет prefers-color-scheme переключения на светлое)"
      contains: "--background"
    - path: "src/app/layout.tsx"
      provides: "lang=ru + RU metadata"
      contains: "lang=\"ru\""
    - path: "src/lib/validation/auth.ts"
      provides: "formatLabels + tournamentKindLabels RU-карты (+ существующая skillLevelLabels)"
      contains: "formatLabels"
    - path: "src/components/tournament-status-badge.tsx"
      provides: "Тёмные классы пилюль статуса"
      contains: "STATUS_CLASSES"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import globals.css"
      pattern: "globals\\.css"
---

<objective>
Заложить визуальный и локализационный фундамент Phase 10: принудительная тёмная тема (SITE-02), `lang="ru"` + русские метаданные (SITE-01), и общие RU-карты подписей формата/вида турнира (`formatLabels`, `tournamentKindLabels`), которые переиспользуют главная (план 02) и Phase 11.

Purpose: остальные планы фазы (главная, шапка, формы) строятся поверх единой тёмной палитры и единых RU-подписей. Это shared-foundation плана wave 1, от которого зависят планы 02 и 03.
Output: тёмный globals.css, локализованный layout, расширенный auth.ts с двумя новыми картами, исправленный status-badge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ux-foundation/10-CONTEXT.md

@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Принудительная тёмная тема в globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css</read_first>
  <action>
    Перевести `:root` сразу на тёмные значения: `--background: #0a0a0a`, `--foreground: #ededed`
    (значения из CONTEXT, точный контраст — на усмотрение исполнителя). УБРАТЬ блок
    `@media (prefers-color-scheme: dark)` целиком — чтобы светлая ОС не переключала сайт на светлый
    (SITE-02: одна тема, не OS-driven). Оставить `@theme inline` маппинг
    `--color-background`/`--color-foreground` и правило `body { background/color }` как есть —
    они уже читают переменные. Не добавлять библиотеку тем / toggle (CLAUDE.md: без новых зависимостей).
  </action>
  <verify>
    <automated>grep -q "prefers-color-scheme" src/app/globals.css &amp;&amp; echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `:root` задаёт тёмный `--background` (тёмное значение, не `#ffffff`) и светлый `--foreground`.
    - В файле нет `@media (prefers-color-scheme: dark)` (grep печатает OK).
    - `@theme inline` и `body`-правило сохранены.
  </acceptance_criteria>
  <done>Сайт рендерится тёмным независимо от системной темы; светлый дефолт удалён.</done>
</task>

<task type="auto">
  <name>Task 2: lang=ru + RU-метаданные в layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>src/app/layout.tsx</read_first>
  <action>
    Заменить `<html lang="en" ...>` на `<html lang="ru" ...>` (SITE-01, success criterion 1).
    Перевести `metadata`: `title` -> «Падел турниры» (или близко), `description` -> русское описание
    (напр. «Турниры по паделу: регистрируйтесь, играйте, следите за сеткой.»). Структуру компонента и
    классы не трогать.
  </action>
  <verify>
    <automated>grep -q 'lang="ru"' src/app/layout.tsx &amp;&amp; ! grep -qE 'Padel Tournaments|Organize and follow' src/app/layout.tsx &amp;&amp; echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` содержит `<html lang="ru"`.
    - `metadata.title` и `metadata.description` на русском (нет «Padel Tournaments» / «Organize and follow»).
  </acceptance_criteria>
  <done>Корневой документ объявлен русским; вкладка и описание на русском.</done>
</task>

<task type="auto">
  <name>Task 3: formatLabels + tournamentKindLabels RU-карты и тёмные пилюли статуса</name>
  <files>src/lib/validation/auth.ts, src/components/tournament-status-badge.tsx</files>
  <read_first>src/lib/validation/auth.ts, src/components/tournament-status-badge.tsx, src/lib/validation/tournament.ts</read_first>
  <action>
    1) В `src/lib/validation/auth.ts` (рядом со `skillLevelLabels`) добавить две display-only RU-карты,
    типизированные по кортежам из `@/lib/validation/tournament`:
    `formatLabels: Record<(typeof tournamentFormats)[number], string>` =
    { playoff: «Олимпийская», round_robin: «Круговой», americano: «Американо», mexicano: «Мексикано» };
    `tournamentKindLabels: Record<(typeof participantModes)[number], string>` =
    { pairs: «Парный», singles: «Одиночный» }. Импортировать `tournamentFormats`, `participantModes`
    из `@/lib/validation/tournament` для типобезопасности (ключи синхронны с zod-кортежами). Эти карты
    переиспользуют план 02 (карточки главной) и Phase 11. Существующую `skillLevelLabels` не менять.
    2) В `src/components/tournament-status-badge.tsx` заменить светлые палитры в `STATUS_CLASSES` и в
    fallback (`bg-gray-200 text-gray-700`) на тёмные эквиваленты с достаточным контрастом, напр.
    `registration: bg-green-900/40 text-green-300`, `in_progress: bg-amber-900/40 text-amber-200`,
    `finished: bg-white/10 text-foreground/70`; fallback -> `bg-white/10 text-foreground/70`.
    Конкретные оттенки — на усмотрение исполнителя (CONTEXT discretion), но без `*-100`/`*-200`
    светлых фонов. Логику `isKnownStatus`/структуру компонента не менять.
  </action>
  <verify>
    <automated>grep -q "formatLabels" src/lib/validation/auth.ts &amp;&amp; grep -q "tournamentKindLabels" src/lib/validation/auth.ts &amp;&amp; ! grep -qE "bg-(green|amber|red|gray)-(100|200)" src/components/tournament-status-badge.tsx &amp;&amp; echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `auth.ts` экспортирует `formatLabels` (4 формата) и `tournamentKindLabels` (singles/pairs), типизированные по кортежам tournament.ts.
    - `tournament-status-badge.tsx` не содержит светлых фонов `*-100`/`*-200`; все 3 статуса + fallback читаемы на тёмном.
    - `npx tsc --noEmit` -> 0.
  </acceptance_criteria>
  <done>RU-карты формата/вида доступны для переиспользования; статус-бейджи тёмные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ОС-тема -> сайт | Системная тема не должна влиять на отрисовку (форсим тёмную) |

Презентационный план: нет новых input-границ, нет мутаций, нет чтения непубличных данных. Изменения — CSS, метаданные, статические RU-карты, классы бейджа.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | globals.css / layout.tsx | accept | Статические презентационные правки; нет пользовательского ввода, нет данных. |
| T-10-SC | Tampering | npm/pnpm installs | mitigate | План не добавляет зависимостей (CLAUDE.md инвариант); install-задач нет. |
</threat_model>

<verification>
- `npx tsc --noEmit` -> exit 0.
- `grep -q "prefers-color-scheme" src/app/globals.css` -> отсутствует (нет OS-переключения).
- `grep 'lang="ru"' src/app/layout.tsx` -> найдено.
- `grep "formatLabels\|tournamentKindLabels" src/lib/validation/auth.ts` -> обе найдены.
- Светлые пилюли (`*-100`/`*-200`) в status-badge отсутствуют.
- Визуальная проверка тёмной темы — human-verify в конце фазы.
</verification>

<success_criteria>
- Принудительная тёмная тема активна (SITE-02): нет prefers-color-scheme, `:root` тёмный.
- `<html lang="ru">` + русские метаданные (SITE-01).
- Статус-бейджи читаемы на тёмном фоне.
- `formatLabels` и `tournamentKindLabels` доступны для планов 02 и Phase 11.
- tsc зелёный.
</success_criteria>

<output>
Create `.planning/phases/10-ux-foundation/10-01-SUMMARY.md` when done.
</output>

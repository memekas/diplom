---
phase: 09-format-engines
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/americano.ts
  - src/lib/services/americano.test.ts
autonomous: true
requirements: [FMT-02]
must_haves:
  truths:
    - "Старт американо создаёт все Round + RoundMatch одной транзакцией"
    - "Каждый игрок партнёрит с каждым другим ровно один раз (0 дублирующих партнёрств)"
    - "Партнёрство = teamA1+teamA2 против teamB1+teamB2; корт k = партнёрство(2k) vs партнёрство(2k+1)"
    - "Sit-outs распределяются по чётности (N≡0 mod4 → 0; нечётное → 1; N≡2 mod4 → 2)"
  artifacts:
    - path: "src/lib/services/americano.ts"
      provides: "generateAmericano + americanoSchedule pure fn (partner-once circle)"
      contains: "export function americanoSchedule"
    - path: "src/lib/services/americano.test.ts"
      provides: "FMT-02 partner-once invariant (N=4/8/12/16, 0 dup partners)"
      contains: "americanoSchedule"
  key_links:
    - from: "src/lib/services/americano.ts"
      to: "src/lib/services/tournament-status.ts"
      via: "transitionTournament registration→in_progress"
      pattern: "transitionTournament"
    - from: "generateAmericano"
      to: "tx.roundMatch.create (teamA1/A2 vs teamB1/B2)"
      via: "prisma.$transaction"
      pattern: "roundMatch\\.create"
---

<objective>
Реализовать движок генерации американо (FMT-02): pure-функция `americanoSchedule` (circle-method на ИГРОКАХ, гарантия «партнёр-once», FORMATS.md §2) + транзакционный `generateAmericano`. Регистрация одиночная (TournamentPlayer); всё расписание создаётся сразу (от результатов не зависит). totalRounds для американо = N−1 (полная ротация, derive из circle — RESEARCH OQ1/A4: totalRounds трактуется как информационный, расписание берётся из circle).

Purpose: американо — второй из трёх форматов; partner-once circle — отдельный алгоритм от RR (партнёрства, не противостояния). FMT-02 (часть «американо»).
Output: `src/lib/services/americano.ts` (новый), `src/lib/services/americano.test.ts` (новый).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-format-engines/09-RESEARCH.md
@.planning/phases/09-format-engines/09-CONTEXT.md
@.planning/research/FORMATS.md
@prisma/schema.prisma
@src/lib/services/bracket.ts
@src/lib/services/tournament-status.ts
@src/lib/services/registration.ts
@src/lib/services/result.test.ts
@src/lib/services/admin.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure americanoSchedule + americano.test.ts (partner-once)</name>
  <files>src/lib/services/americano.ts, src/lib/services/americano.test.ts</files>
  <read_first>
    .planning/research/FORMATS.md §2 («Алгоритм circle method на ИГРОКАХ» — точный псевдокод, ⚠️ партнёр-once гарантирован, уникальность соперников НЕ гарантируется; пример N=4: R1 0&3vs1&2, R2 0&2vs3&1, R3 0&1vs2&3) и «Verified americano algorithm» в 09-RESEARCH.md; src/lib/services/result.test.ts (харнесс check/assert/tsx).
  </read_first>
  <behavior>
    - americanoSchedule(players: T[]) на N (чётное) → N-1 раундов; в каждом раунде N/2 партнёрств и N/4 кортов (матчей).
    - Инвариант партнёр-once: за все раунды каждый игрок партнёрит каждого другого ровно один раз; 0 дублирующих партнёрств (тест N=4/8/12/16: собрать множество неупорядоченных пар-партнёров, размер == C(N,2), без повторов).
    - В раунде r: arr=[fixed]+ring; партнёрства из позиций (i, N-1-i); корт k = партнёрство(2k) vs партнёрство(2k+1); ротация кольца на 1 (НЕ трогая arr[0]).
    - Нечётное N → BYE-сентинел (1 sit-out); N≡2 mod4 → одно лишнее партнёрство (2 игрока) сидит этот раунд — матч для непарного партнёрства не создаётся.
    - Детерминизм при фиксированном входе (shuffle снаружи).
  </behavior>
  <action>
    Создать `src/lib/services/americano.ts`. Экспортировать `class FormatError` (тот же union, что в round-robin.ts — но это отдельный файл; объявить локально с кодами `not_open`|`wrong_format`|`already_generated`|`no_units`, чтобы не вводить кросс-импорт между сервисами; Plan 06 dispatch обработает оба типа). Экспортировать generic pure-функцию `americanoSchedule<T>(players: T[]): { roundNumber: number; courts: { courtNumber: number; teamA: [T, T]; teamB: [T, T] }[] }[]` строго по FORMATS.md §2: если нечётное — добавить BYE (null); N=длина; fixed=players[0]; ring=players.slice(1); for r=0..N-2: arr=[fixed,...ring]; построить N/2 партнёрств из позиций (i, N-1-i) для i=0..N/2-1 как пары [arr[i], arr[N-1-i]]; отфильтровать партнёрства, содержащие BYE (null) — они сидят; корты: для k=0.. формировать партнёрство(2k) vs партнёрство(2k+1); если нечётное число валидных партнёрств — последнее непарное сидит (N≡2 mod4); courtNumber 0-based по реально созданным кортам; teamA/teamB — двухэлементные кортежи userId-ов; ротация: ring=[ring[ring.length-1], ...ring.slice(0, ring.length-1)]; ⚠️ arr[0] фиксирован (FORMATS.md §2). Создать `src/lib/services/americano.test.ts` (харнесс result.test.ts): partner-once для N=4 (3 раунда, точный пример R1 0&3vs1&2/R2 0&2vs3&1/R3 0&1vs2&3 при входе [0,1,2,3]), N=8 (7 раундов, 0 дублей партнёров), N=12, N=16 (инвариант множества партнёрств == C(N,2)); число кортов в раунде == N/4 для кратных 4; N=6 → 1 лишнее партнёрство сидит (sit-out=2); N=5 → 1 BYE (sit-out=1).
  </action>
  <verify>
    <automated>npx tsx src/lib/services/americano.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>americano.test.ts зелёный; americanoSchedule даёт N-1 раундов, инвариант партнёр-once (0 дублей) для N=4/8/12/16, корректные sit-outs; tsc чист.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Транзакционный generateAmericano (mirror generateBracket)</name>
  <files>src/lib/services/americano.ts, src/lib/services/americano.test.ts</files>
  <read_first>
    src/lib/services/bracket.ts (generate-once транзакционный паттерн + shuffle); prisma/schema.prisma (Round, RoundMatch teamA1/A2/B1/B2 + courtNumber, @@unique([tournamentId, roundNumber])); src/lib/services/registration.ts (источник singles = TournamentPlayer.userId; американо всегда singles по валидации Phase 8); src/lib/services/admin.test.ts (fake-prisma $transaction:(fn)=>fn(prisma)).
  </read_first>
  <action>
    Добавить `export async function generateAmericano(prisma: PrismaClient, tournamentId: string)` в americano.ts, зеркало generateRoundRobin/generateBracket. Внутри $transaction: (1) findUniqueOrThrow select { status, format, participantMode }; status!=="registration" → FormatError("not_open"); format!=="americano" → FormatError("wrong_format"). (2) generate-once: round.count>0 → FormatError("already_generated"). (3) загрузить игроков: `tx.tournamentPlayer.findMany` select { userId } (американо = singles, Pitfall 3 — НЕ читать Pair). Если < 4 → FormatError("no_units"). (4) `americanoSchedule(shuffle(userIds))` — shuffle один раз; скопировать shuffle из bracket.ts локально. (5) для каждого round: `tx.round.create({ data: { tournamentId, roundNumber }, select:{id:true} })`; для каждого корта — `tx.roundMatch.create` с teamA1Id=teamA[0], teamA2Id=teamA[1], teamB1Id=teamB[0], teamB2Id=teamB[1], courtNumber (Pattern 2 — partnership-вариант). (6) `transitionTournament(tx as unknown as PrismaClient, tournamentId, "registration", "in_progress")`. (7) return { tournamentId, roundsCreated, matchesCreated }. Дописать в americano.test.ts fake-prisma async-тесты (харнесс admin.test.ts): 8 игроков → 7 Round + по 2 RoundMatch (16 кортов всего) с заполненными 4 FK партнёрств; status!=="registration" → FormatError("not_open"); существующий Round → FormatError("already_generated"); <4 игроков → FormatError("no_units"); transitionTournament вызван registration→in_progress.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/americano.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>generateAmericano создаёт все Round+RoundMatch в одной транзакции из TournamentPlayer, заполняет 4 FK как партнёрства, блокирует повторную генерацию, флипает статус; новые fake-prisma тесты зелёные; tsc чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → generateAmericano (Plan 06) | tournamentId из leaf; status/format перечитываются из БД внутри tx |
| generateAmericano → DB | Round/RoundMatch строго внутри $transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | tournamentId/format в форме старта | mitigate | format перечитывается из БД внутри tx; клиент не доверяется (mirror bracket.ts) |
| T-09-06 | Tampering/race | конкурентный двойной старт | mitigate | generate-once `round.count>0`; @@unique([tournamentId, roundNumber]) backstop |
| T-09-07 | Information Disclosure | сырой Prisma-текст клиенту | mitigate | типизированный FormatError; Plan 06 action маппит только его |
| T-09-08 | Elevation of Privilege | не-админ POST старта | accept | requireAdmin() первая строка action (Plan 06); сервис auth-free by design |
</threat_model>

<verification>
- `npx tsx src/lib/services/americano.test.ts` — все assertions зелёные.
- `npx tsc --noEmit` exit 0.
- Регрессия (на merge волны, Plan 06): 8 существующих `*.test.ts` (224 assertions) зелёные; playoff не тронут.
</verification>

<success_criteria>
- americanoSchedule — Prisma-free, гарантия партнёр-once для N=4/8/12/16.
- generateAmericano зеркалит паттерн генератора: одна транзакция, generate-once, источник = TournamentPlayer, 4 FK = партнёрства, transitionTournament.
- FMT-02 (американо) удовлетворено: старт формирует и фиксирует составы по раундам.
- Ни одного нового пакета; миграций нет; playoff не тронут.
</success_criteria>

<output>
Create `.planning/phases/09-format-engines/09-02-SUMMARY.md` when done
</output>

---
phase: 09-format-engines
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/standings.ts
  - src/lib/services/standings.test.ts
autonomous: true
requirements: [FMT-02, FMT-03]
must_haves:
  truths:
    - "computeStandings(americano/mexicano) возвращает рейтинг игроков по сумме личных очков desc"
    - "Тай-брейк детерминирован и завершается стабильным userId-фолбэком (критично для нарезки мексикано)"
    - "computeStandings(round_robin) возвращает таблицу единиц (пара/игрок) по победам в матчах + тай-брейкам формата"
    - "Standings вычисляется из RoundMatch/PlayerMatchScore, нигде не материализуется"
  artifacts:
    - path: "src/lib/services/standings.ts"
      provides: "computeStandings (player-rating + unit-table) + чистая sort-функция"
      contains: "export async function computeStandings"
    - path: "src/lib/services/standings.test.ts"
      provides: "FMT-03 порядок standings + детерминизм тай-брейков (player + unit)"
      contains: "computeStandings"
  key_links:
    - from: "src/lib/services/standings.ts"
      to: "PlayerMatchScore / RoundMatch"
      via: "prisma findMany aggregate"
      pattern: "playerMatchScore\\.findMany|roundMatch\\.findMany"
---

<objective>
Реализовать `computeStandings(prisma, tournamentId)` (FMT-02/FMT-03): для americano/mexicano — рейтинг ИГРОКОВ (сумма личных очков desc + детерминированный тай-брейк), для round_robin — таблица ЕДИНИЦ (пара/игрок). Standings ВЫЧИСЛЯЕТСЯ (не хранится). Детерминированный тай-брейк со стабильным userId-фолбэком — обязателен, т.к. его использует нарезка мексикано на четвёрки (Plan 04).

Purpose: standings — общий потребитель для финиш-условий и материализации мексикано; должен существовать до mexicano (Plan 04) и round-result (Plan 05). FMT-03 (standings) + вход для FMT-02 (рейтинг-ротация мексикано).
Output: `src/lib/services/standings.ts` (новый), `src/lib/services/standings.test.ts` (новый).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-format-engines/09-RESEARCH.md
@.planning/phases/09-format-engines/09-CONTEXT.md
@.planning/research/FORMATS.md
@prisma/schema.prisma
@src/lib/services/registration.ts
@src/lib/services/result.test.ts
@src/lib/services/admin.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чистая sort-логика рейтинга игроков + standings.test.ts</name>
  <files>src/lib/services/standings.ts, src/lib/services/standings.test.ts</files>
  <read_first>
    .planning/research/FORMATS.md §2 «Итог/тай-брейкеры» (американо) и §3 «Итог/тай-брейкеры» (мексикано — детерминизм обязателен); 09-RESEARCH.md «Standings tiebreaker chains» (americano/mexicano: сумма pointsFor desc → point diff (sum pointsFor − sum pointsAgainst) → число побед → стабильный userId-фолбэк); src/lib/services/result.test.ts (харнесс).
  </read_first>
  <behavior>
    - Чистая функция rankPlayers принимает агрегаты [{ userId, sumFor, sumAgainst, wins }] → возвращает отсортированный массив с полем rank (1-based).
    - Порядок: sumFor desc → (sumFor−sumAgainst) desc → wins desc → userId asc (стабильный детерминированный фолбэк).
    - Равные по всем метрикам, кроме userId, игроки сортируются по userId asc одинаково при повторных вызовах (детерминизм — тест: перемешать вход, отсортировать дважды → идентичный порядок userId).
    - Сидевшие раунды не штрафуются (просто меньше вкладов — нулевые слагаемые отсутствуют, не отрицательные).
  </behavior>
  <action>
    Создать `src/lib/services/standings.ts`. Экспортировать тип `PlayerStanding = { userId: string; rank: number; played: number; wins: number; pointsFor: number; pointsAgainst: number; pointDiff: number }` и `UnitStanding = { unitId: string; kind: "pair"|"user"; rank: number; played: number; wins: number; losses: number; pointsFor: number; pointsAgainst: number; pointDiff: number }`. Экспортировать ЧИСТУЮ функцию `rankPlayers(rows: { userId: string; sumFor: number; sumAgainst: number; wins: number; played: number }[]): PlayerStanding[]` — отсортировать копию по цепочке sumFor desc → pointDiff desc → wins desc → userId asc (localeCompare/строковое сравнение), присвоить rank 1..n. ⚠️ Не мутировать вход. Создать `src/lib/services/standings.test.ts` (харнесс result.test.ts): порядок по сумме очков; тай-брейк по point diff при равной сумме; по wins при равных сумме+diff; стабильный userId при полной равности; ДЕТЕРМИНИЗМ — два вызова на по-разному перемешанных копиях одного набора дают идентичную последовательность userId (это инвариант, на который опирается мексикано).
  </action>
  <verify>
    <automated>npx tsx src/lib/services/standings.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>rankPlayers детерминированно сортирует по полной цепочке тай-брейков с userId-фолбэком; тесты зелёные; tsc чист.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeStandings (read+aggregate) для player-rating и unit-table</name>
  <files>src/lib/services/standings.ts, src/lib/services/standings.test.ts</files>
  <read_first>
    prisma/schema.prisma (PlayerMatchScore: userId/teamSlot/pointsFor/pointsAgainst, @@unique([roundMatchId,userId]); RoundMatch: teamA1/A2/B1/B2 + pointsA/pointsB; Round: tournamentId/roundNumber; Pair: tournamentId/player1Id/player2Id — для recovery pair-identity по (tournamentId, player1Id), RESEARCH OQ2/A2); 09-RESEARCH.md «Standings tiebreaker chains» (round_robin sets: match wins → set diff → h2h → id; points: match wins → point diff → total → h2h → id; «sets-mode contribution = sets-won»); src/lib/services/admin.test.ts (fake-prisma harness).
  </read_first>
  <action>
    Добавить `export async function computeStandings(prisma: PrismaClient, tournamentId: string): Promise<{ kind: "players"; format: string; players: PlayerStanding[] } | { kind: "units"; format: string; units: UnitStanding[] }>`. Прочитать tournament select { format, participantMode, scoringMode }. Загрузить раунды с матчами и playerScores: `prisma.round.findMany({ where:{tournamentId}, select:{ roundNumber, matches:{ select:{ id, courtNumber, teamA1Id, teamA2Id, teamB1Id, teamB2Id, pointsA, pointsB, playerScores:{ select:{ userId, teamSlot, pointsFor, pointsAgainst } } } } } } })`. Ветвление по format:
    — americano/mexicano (kind:"players"): агрегировать PlayerMatchScore по userId: sumFor += pointsFor, sumAgainst += pointsAgainst, played += 1, wins += (pointsFor > pointsAgainst ? 1 : 0) — считать только записанные матчи (где есть playerScores); вызвать rankPlayers → вернуть { kind:"players", format, players }.
    — round_robin (kind:"units"): единица = пара (participantMode=pairs) или игрок (singles). Для pairs восстановить идентичность пары: загрузить `prisma.pair.findMany({ where:{tournamentId}, select:{ id, player1Id, player2Id } })`, построить map player1Id→pairId (recovery по (tournamentId, player1Id), A2). Для каждого записанного RoundMatch (pointsA!=null && pointsB!=null) определить unitA/unitB (pairs: pairId по teamA1Id/teamB1Id; singles: userId=teamA1Id/teamB1Id), начислить: победа единице с большим pointsA/pointsB (в sets-режиме pointsA/B = sets-won — победитель = больше сетов; ничьи в round_robin запрещены валидацией Plan 05); wins/losses, pointsFor/pointsAgainst (для sets — sets-won как метрика вклада, RESEARCH discretion DECIDED), played. Отсортировать единицы: wins desc → pointDiff desc → pointsFor desc → unitId asc (h2h — опционально; задокументировать что per-game тай-брейк недоступен в no-migration дизайне, A1). Вернуть { kind:"units", format, units }. Дописать в standings.test.ts fake-prisma тесты: americano — рейтинг по сумме очков из PlayerMatchScore (оба партнёра одной команды получили одно командное pointsFor); round_robin pairs — таблица пар, восстановление pairId по player1Id, победы по pointsA/pointsB; round_robin singles — таблица игроков; матчи без результата (pointsA/B null) не учитываются.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/standings.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>computeStandings возвращает player-rating для americano/mexicano и unit-table для round_robin, восстанавливает идентичность пары по player1Id, игнорирует незаписанные матчи, использует sets-won как метрику вклада в sets-режиме; fake-prisma тесты зелёные; tsc чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| computeStandings → DB (read-only) | агрегация по уже валидированным RoundMatch/PlayerMatchScore; ничего не пишет |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | подмена очков для манипуляции рейтингом/нарезкой мексикано | mitigate | standings читает только server-derived pointsFor/pointsAgainst, записанные recordRoundResult (Plan 05) из server-side вычисления; клиент не пишет PlayerMatchScore напрямую |
| T-09-10 | Information Disclosure | сырой Prisma-текст | accept | computeStandings — derived read, вызывается из Server Component (Phase 11); ошибки чтения не несут PII; типизация не требуется (нет reject-путей) |
| T-09-11 | (determinism, не STRIDE) | недетерминированная сортировка → невоспроизводимая нарезка мексикано | mitigate | обязательный стабильный userId-фолбэк в rankPlayers (Pitfall 2); покрыто тестом детерминизма |
</threat_model>

<verification>
- `npx tsx src/lib/services/standings.test.ts` — все assertions зелёные.
- `npx tsc --noEmit` exit 0.
- Регрессия (на merge волны, Plan 06): 8 существующих `*.test.ts` (224 assertions) зелёные; playoff не тронут.
</verification>

<success_criteria>
- rankPlayers — детерминированная чистая сортировка со стабильным userId-фолбэком (вход для мексикано-нарезки).
- computeStandings: player-rating (americano/mexicano) + unit-table (round_robin) из RoundMatch/PlayerMatchScore, без материализации.
- Идентичность пары для pairs-RR восстанавливается по (tournamentId, player1Id) (A2).
- FMT-03 (standings) удовлетворено для всех round-based форматов.
- Ни одного нового пакета; миграций нет; playoff не тронут.
</success_criteria>

<output>
Create `.planning/phases/09-format-engines/09-03-SUMMARY.md` when done
</output>

---
phase: 09-format-engines
plan: 06
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-04", "09-05"]
files_modified:
  - src/lib/services/format-engine.ts
  - src/lib/services/format-engine.test.ts
  - src/app/(public)/tournaments/[id]/actions.ts
autonomous: true
requirements: [FMT-01, FMT-02, FMT-03, SCORE-01]
must_haves:
  truths:
    - "startTournamentAction ветвится по tournament.format: playoff → generateBracket; round_robin/americano/mexicano → соответствующий генератор"
    - "recordResultAction ветвится по tournament.format: playoff → recordResult; round-based → recordRoundResult"
    - "playoff-путь (generateBracket/recordResult) по поведению не изменён"
    - "Все 224 существующих assertions остаются зелёными; playoff bracket/result не тронуты"
  artifacts:
    - path: "src/lib/services/format-engine.ts"
      provides: "startFormat() + recordFormatResult() — dispatch по format"
      contains: "export async function startFormat"
    - path: "src/app/(public)/tournaments/[id]/actions.ts"
      provides: "startTournamentAction/recordResultAction с format-dispatch"
      contains: "startFormat"
  key_links:
    - from: "src/lib/services/format-engine.ts"
      to: "round-robin.ts/americano.ts/mexicano.ts/round-result.ts/bracket.ts/result.ts"
      via: "import generators + recorders"
      pattern: "generateRoundRobin|generateAmericano|generateMexicanoRound1|recordRoundResult"
    - from: "src/app/(public)/tournaments/[id]/actions.ts"
      to: "src/lib/services/format-engine.ts"
      via: "startFormat/recordFormatResult в actions"
      pattern: "startFormat|recordFormatResult"
---

<objective>
Связать движки (FMT-01/02/03, SCORE-01): сервис-диспетчер `format-engine.ts` (`startFormat` ветвится по format на generateBracket/generateRoundRobin/generateAmericano/generateMexicanoRound1; `recordFormatResult` ветвится на recordResult/recordRoundResult) + минимальные ветки в существующих `startTournamentAction`/`recordResultAction`. Playoff-путь остаётся неизменным по поведению. Завершить полной регрессией: новые per-format тесты + неизменные 224 assertions.

Purpose: финальная волна — соединяет всё в точках входа, не ломая playoff. Покрывает все 4 требования фазы на уровне dispatch. Зависит от всех генераторов (01,02,04) и recordRoundResult (05).
Output: `src/lib/services/format-engine.ts` (новый), `src/lib/services/format-engine.test.ts` (новый), правки `src/app/(public)/tournaments/[id]/actions.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-format-engines/09-RESEARCH.md
@.planning/phases/09-format-engines/09-CONTEXT.md
@.planning/research/FORMATS.md
@prisma/schema.prisma
@src/app/(public)/tournaments/[id]/actions.ts
@src/lib/services/bracket.ts
@src/lib/services/result.ts
@src/lib/services/round-robin.ts
@src/lib/services/americano.ts
@src/lib/services/mexicano.ts
@src/lib/services/round-result.ts
@src/lib/validation/round-result.ts
@src/lib/validation/result.ts
@src/lib/validation/tournament.ts
@src/lib/services/admin.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: format-engine.ts dispatch (startFormat + recordFormatResult) + test</name>
  <files>src/lib/services/format-engine.ts, src/lib/services/format-engine.test.ts</files>
  <read_first>
    src/lib/services/bracket.ts (generateBracket(prisma, tournamentId) сигнатура); src/lib/services/round-robin.ts/americano.ts/mexicano.ts (generateRoundRobin/generateAmericano/generateMexicanoRound1 сигнатуры + их FormatError); src/lib/services/result.ts (recordResult(prisma, matchId, sets)); src/lib/validation/result.ts (parseRecordResultForm(formData, setsPerMatch)); src/lib/services/round-result.ts (recordRoundResult(prisma, roundMatchId, input) + RoundResultError); src/lib/validation/round-result.ts (parseRoundResultForm(formData, {scoringMode, setsPerMatch})); 09-RESEARCH.md System Architecture Diagram (dispatch keyed on tournament.format); src/lib/services/admin.test.ts (fake-prisma harness).
  </read_first>
  <action>
    Создать `src/lib/services/format-engine.ts`. Экспортировать `export async function startFormat(prisma: PrismaClient, tournamentId: string)`: прочитать tournament select { format } (DB-authoritative, не из клиента); switch(format): "playoff" → return generateBracket(prisma, tournamentId); "round_robin" → generateRoundRobin; "americano" → generateAmericano; "mexicano" → generateMexicanoRound1; default → бросить Error (неизвестный формат). Экспортировать `export async function recordFormatResult(prisma: PrismaClient, tournamentId: string, matchId: string, formData: FormData)`: прочитать tournament select { format, scoringMode, setsPerMatch }; если format=="playoff" → parseRecordResultForm(formData, setsPerMatch); при parse ok:false бросить ResultError-совместимую ошибку с message (или вернуть дискриминированный {ok:false,error}); иначе recordResult(prisma, matchId, parsed.sets) (СУЩЕСТВУЮЩИЙ путь — поведение неизменно); если format round-based → parseRoundResultForm(formData, {scoringMode, setsPerMatch}); при ok:false — вернуть {ok:false,error}; иначе recordRoundResult(prisma, matchId /*=roundMatchId*/, input). Выбрать ЕДИНЫЙ контракт возврата: `{ ok: true; result } | { ok: false; error: string }` — чтобы action (Task 2) единообразно маппил и форму-ошибку, и сервис-reject. ⚠️ Не дублировать логику генераторов/рекордеров — только маршрутизация и выбор парсера. Создать `src/lib/services/format-engine.test.ts` (fake-prisma, harness admin.test.ts): startFormat читает format и вызывает корректный генератор — по одному фейку на формат: round_robin → создаётся Round (а не Match); playoff → создаётся Match (а не Round); americano/mexicano → Round. recordFormatResult для playoff идёт через recordResult (SetScore-путь, set{n}_a/set{n}_b форма), для round_robin points — через recordRoundResult (RoundMatch.pointsA/pointsB, points_a/points_b форма). Проверять флагами в фейке, какой путь сработал.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/format-engine.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>startFormat/recordFormatResult маршрутизируют по tournament.format (из БД); playoff → generateBracket/recordResult, round-based → новые генераторы/recordRoundResult; единый контракт возврата ok/error; тесты подтверждают маршрутизацию; tsc чист.</done>
</task>

<task type="auto">
  <name>Task 2: Ветвление actions.ts (start + record) с сохранением playoff-поведения</name>
  <files>src/app/(public)/tournaments/[id]/actions.ts</files>
  <read_first>
    src/app/(public)/tournaments/[id]/actions.ts (startTournamentAction ~118-142: requireAdmin → generateBracket → catch BracketError; recordResultAction ~236-265: requireAdmin → parseRecordResultForm → recordResult → catch ResultError); src/lib/services/format-engine.ts (startFormat/recordFormatResult + контракт возврата); src/lib/services/round-robin.ts/americano.ts/mexicano.ts/round-result.ts (FormatError/RoundResultError для catch-маппинга).
  </read_first>
  <action>
    Изменить `startTournamentAction`: requireAdmin() первой строкой и tournamentId-guard без изменений; заменить `generateBracket(prisma, tournamentId)` на `startFormat(prisma, tournamentId)`; в catch расширить — ловить BracketError И FormatError (оба несут RU-message → вернуть {ok:false,error:e.message}), иначе generic RU fallback; revalidatePath без изменений; импорты startFormat + FormatError-типы добавить. Изменить `recordResultAction`: requireAdmin() + matchId-guard без изменений; заменить блок parseRecordResultForm+recordResult на `const r = await recordFormatResult(prisma, tournamentId, matchId, formData)`; если r.ok===false → return {ok:false, error:r.error}; обернуть в try/catch, ловить ResultError И RoundResultError (e.message), иначе generic fallback; revalidatePath без изменений. ⚠️ НЕ менять сигнатуры action (Plan 11 UI на них завязан): startTournamentAction(tournamentId, _prev, _formData); recordResultAction(tournamentId, matchId, setsPerMatch, _prev, formData) — параметр setsPerMatch сохранить в сигнатуре (recordFormatResult читает реальный setsPerMatch из БД, но удалять параметр нельзя — UI его передаёт). Playoff-вызов идёт через format-engine тем же парсером (parseRecordResultForm) и рекордером (recordResult) → поведение для playoff неизменно.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "src/app/(public)/tournaments/[id]/actions.ts" src/lib/services/format-engine.ts</automated>
  </verify>
  <done>startTournamentAction/recordResultAction ветвятся через format-engine; requireAdmin первой строкой сохранён; сигнатуры actions неизменны; catch маппит BracketError/FormatError и ResultError/RoundResultError; tsc + eslint чисты.</done>
</task>

<task type="auto">
  <name>Task 3: Полная регрессия — 224 baseline + все новые per-format тесты зелёные</name>
  <files>src/lib/services/format-engine.test.ts</files>
  <read_first>
    src/lib/services/result.test.ts/bracket.test.ts (playoff baseline — остаются зелёными неизменными); 09-RESEARCH.md «Validation Architecture» (на merge волны прогнать ВСЕ *.test.ts; 224-assertion baseline — инвариант); CONTEXT «Specifics» (инвариант: все существующие тесты зелёные, playoff не тронут).
  </read_first>
  <action>
    Прогнать полный набор тестов и подтвердить инвариант. Выполнить каждый `src/lib/**/*.test.ts` через npx tsx, посчитать суммарное число строк `ok -`. Подтвердить: (1) 8 ИСХОДНЫХ тестов (admin/bracket/registration/result/tournament-status + validation/profile,registration,tournament) дают ровно 224 assertions, все зелёные (playoff-инвариант — bracket.ts/result.ts не изменялись по поведению). (2) Новые тесты (round-robin/americano/standings/mexicano/round-result/validation round-result/format-engine) все зелёные. Если число исходных assertions != 224 ИЛИ любой исходный тест упал — это регрессия playoff/Phase-8: найти и устранить ПРИЧИНУ (не подгонять числа). Никаких правок поведения в bracket.ts/result.ts. При необходимости дописать в format-engine.test.ts интеграционные проверки полноты dispatch.
  </action>
  <verify>
    <automated>total=0; for f in $(find src -name "*.test.ts" | sort); do n=$(npx tsx "$f" 2>/dev/null | grep -c "ok -"); total=$((total+n)); done; echo "TOTAL=$total"; test "$total" -ge 224</automated>
  </verify>
  <done>Все *.test.ts зелёные; суммарно ≥224 assertions с неизменными 224 исходными; playoff bracket/result не изменены по поведению; новые per-format тесты проходят.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → startTournamentAction/recordResultAction | публичные HTTP-эндпоинты; requireAdmin() первая строка; tournamentId/matchId связаны из leaf |
| Server Action → format-engine | format перечитывается из БД внутри сервиса, не доверяется клиенту |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-22 | Elevation of Privilege | не-админ direct POST старта/записи | mitigate | requireAdmin() — ПЕРВАЯ строка обоих actions, throw не ловится (сохранено без изменений, VERIFIED actions.ts:123,243) |
| T-09-23 | Tampering | подмена format в форме для misroute генератора/рекордера | mitigate | format перечитывается из БД в startFormat/recordFormatResult; клиентский format игнорируется |
| T-09-24 | Tampering | tampered matchId — записать round-based в playoff-матч или наоборот | mitigate | recordFormatResult выбирает рекордер по DB-format; recordRoundResult бросает not_round_based для playoff; recordResult требует pairAId/pairBId — round-based RoundMatch ему недоступен по id-пространству |
| T-09-25 | Information Disclosure | сырой Prisma/internal-текст клиенту | mitigate | catch маппит только типизированные BracketError/FormatError/ResultError/RoundResultError; всё прочее → generic RU fallback (WR-02) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | пакеты НЕ устанавливаются в этой фазе (09-RESEARCH «Package Legitimacy Audit: not applicable»); зависимости только существующие (@prisma/client, zod, tsx) |
</threat_model>

<verification>
- `npx tsx src/lib/services/format-engine.test.ts` зелёный; `npx tsc --noEmit` exit 0; eslint чист на изменённых файлах.
- Полный прогон: суммарно ≥224 `ok -`, из них ровно 224 от 8 исходных тестов без падений (playoff-инвариант).
- Manual smoke (опционально, не блокирует): playoff-турнир по-прежнему стартует/принимает счёт как раньше (поведение через тот же generateBracket/recordResult).
</verification>

<success_criteria>
- startFormat/recordFormatResult маршрутизируют по DB-format; все 4 формата стартуют и принимают результат через единые точки входа (FMT-01/02/03, SCORE-01).
- Playoff-путь (generateBracket/recordResult) неизменён по поведению; 224 исходных assertions зелёные.
- Сигнатуры actions сохранены (UI Phase 11 совместим).
- Ни одного нового пакета; миграций нет; bracket.ts/result.ts не изменены по поведению.
</success_criteria>

<output>
Create `.planning/phases/09-format-engines/09-06-SUMMARY.md` when done
</output>

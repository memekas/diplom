---
phase: 09-format-engines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/round-robin.ts
  - src/lib/services/round-robin.test.ts
autonomous: true
requirements: [FMT-01]
must_haves:
  truths:
    - "Старт round-robin создаёт все Round + RoundMatch одной транзакцией"
    - "Каждая единица (пара или игрок) встречает каждую другую ровно один раз"
    - "Нечётное N → одна единица сидит ровно один раунд (BYE-матч не создаётся)"
    - "Повторный старт при существующих Round запрещён (generate-once)"
  artifacts:
    - path: "src/lib/services/round-robin.ts"
      provides: "generateRoundRobin + circleMethodSchedule pure fn + FormatError"
      contains: "export function circleMethodSchedule"
    - path: "src/lib/services/round-robin.test.ts"
      provides: "FMT-01 circle-method completeness + odd-N BYE + no-arr0-rotation tests"
      contains: "circleMethodSchedule"
  key_links:
    - from: "src/lib/services/round-robin.ts"
      to: "src/lib/services/tournament-status.ts"
      via: "transitionTournament registration→in_progress"
      pattern: "transitionTournament"
    - from: "generateRoundRobin"
      to: "tx.round.create / tx.roundMatch.create"
      via: "prisma.$transaction"
      pattern: "roundMatch\\.create"
---

<objective>
Реализовать движок генерации round-robin (FMT-01): pure-функция `circleMethodSchedule` (Prisma-free, тестируется без БД) + транзакционный `generateRoundRobin`, зеркало `generateBracket`. Единица = `Pair` (participantMode=pairs) или `TournamentPlayer` (singles). Все Round + RoundMatch создаются сразу (stateless single RR, D5), нечётное N → BYE sit-out без матча.

Purpose: round-robin — первый из трёх форматов слоя 2; его pure-circle-fn также является эталоном для americano. FMT-01 — обязательное требование фазы.
Output: `src/lib/services/round-robin.ts` (новый), `src/lib/services/round-robin.test.ts` (новый).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-format-engines/09-RESEARCH.md
@.planning/phases/09-format-engines/09-CONTEXT.md
@.planning/research/FORMATS.md
@prisma/schema.prisma
@src/lib/services/bracket.ts
@src/lib/services/tournament-status.ts
@src/lib/services/registration.ts
@src/lib/services/result.test.ts
@src/lib/services/admin.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure circleMethodSchedule + round-robin.test.ts (RED→GREEN)</name>
  <files>src/lib/services/round-robin.ts, src/lib/services/round-robin.test.ts</files>
  <read_first>
    .planning/research/FORMATS.md §1 (round-robin circle method — точный пошаговый псевдокод, ⚠️ ошибки) и «Verified circle-method algorithm» в 09-RESEARCH.md; src/lib/services/bracket.ts (shuffle Fisher–Yates на копии, строки ~60-67 — скопировать ту же семантику); src/lib/services/result.test.ts (паттерн self-contained `check`/`assert` через npx tsx).
  </read_first>
  <behavior>
    - circleMethodSchedule(units: T[]) на n0 чётных единицах → n0-1 раундов, каждая пара units встречается ровно раз; всего матчей n0*(n0-1)/2.
    - n0 нечётное → внутри добавляется BYE (null-сентинел), раундов n0, каждая единица сидит ровно один раунд; BYE-стороны НЕ дают матч.
    - arr[0] фиксирован навсегда; ротирует только хвост [1..n-1] (тест: arr[0] участвует в каждом раунде, дублей/пропусков нет).
    - Каждый RoundMatch несёт courtNumber = индекс i внутри раунда; unitA=arr[i], unitB=arr[n-1-i].
    - Детерминизм при фиксированном входе (shuffle вынесен наружу — circleMethodSchedule НЕ перемешивает).
  </behavior>
  <action>
    Создать `src/lib/services/round-robin.ts`. Экспортировать `class FormatError extends Error` с `code` union (минимум `not_open` | `wrong_format` | `already_generated` | `no_units`) — зеркало `BracketError`, чтобы action ловил типизированно и не светил сырой Prisma-текст. Экспортировать generic pure-функцию `circleMethodSchedule<T>(units: T[]): { roundNumber: number; matches: { courtNumber: number; unitA: T; unitB: T }[] }[]` строго по FORMATS.md §1: если units.length нечётное — добавить BYE-сентинел (использовать `null` как BYE и тип `(T|null)[]` внутри), n=длина, rounds=n-1, half=n/2; arr=units.slice(); цикл r=0..rounds-1, i=0..half-1: home=arr[i], away=arr[n-1-i]; если home===null||away===null — пропустить (sit-out, матч не создаётся), иначе добавить { courtNumber: i, unitA: home, unitB: away }; ротация: fixed=arr[0]; tail=arr.slice(1); tail.unshift(tail.pop()); arr=[fixed,...tail]. ⚠️ НЕ ротировать arr[0] (FORMATS.md §1 ошибка 1). roundNumber = r+1. courtNumber переиндексировать на 0-based по реально созданным матчам раунда (после пропуска BYE), чтобы не было дыр в нумерации кортов. Скопировать `shuffle<T>` из bracket.ts как НЕэкспортируемую функцию (понадобится в Task 2). Создать `src/lib/services/round-robin.test.ts` по харнессу result.test.ts (`import assert from "node:assert/strict"`, локальный `check`, `let passed`, лог `ok -`, exit non-zero на падении; запуск `npx tsx`). Покрыть: N=4 (3 раунда, 6 матчей, каждый-с-каждым), N=6 (5 раундов, 15 матчей), N=3 (3 раунда, по 1 матчу, каждая единица сидит 1 раз — 3 матча всего = 3*2/2), N=5 (5 раундов, 10 матчей, каждый сидит 1 раз), инвариант «каждая пара ровно раз» (собрать множество встреч и сверить с C(n,2)), arr[0]-фиксирован (units[0] встречается в каждом раунде). Использовать строковые id-сентинелы как T.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/round-robin.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>round-robin.test.ts проходит (все `ok -`, exit 0); circleMethodSchedule даёт корректное число раундов/матчей и инвариант каждый-с-каждым для чётных и нечётных N; tsc чист.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Транзакционный generateRoundRobin (mirror generateBracket)</name>
  <files>src/lib/services/round-robin.ts, src/lib/services/round-robin.test.ts</files>
  <read_first>
    src/lib/services/bracket.ts (generateBracket: $transaction, findUniqueOrThrow status/format guard, existing-row generate-once guard, transitionTournament в конце); prisma/schema.prisma (Round, RoundMatch — поля teamA1Id/teamA2Id/teamB1Id/teamB2Id/courtNumber; Pair — player1Id/player2Id; TournamentPlayer — userId; @@unique([tournamentId, roundNumber]) на Round); src/lib/services/registration.ts (источник участников по participantMode: Pair vs TournamentPlayer — генератор обязан читать тот же источник, Pitfall 3); src/lib/services/admin.test.ts (фейк-prisma с $transaction:(fn)=>fn(prisma) для async-сервис-тестов).
  </read_first>
  <action>
    Добавить `export async function generateRoundRobin(prisma: PrismaClient, tournamentId: string)` в round-robin.ts, зеркало generateBracket. Внутри `prisma.$transaction(async (tx) => {...})`: (1) findUniqueOrThrow tournament select { status, format, participantMode }; если status!=="registration" → FormatError("not_open"); если format!=="round_robin" → FormatError("wrong_format"). (2) generate-once: `tx.round.count({ where: { tournamentId } })` > 0 → FormatError("already_generated"). (3) загрузить units по participantMode (Pitfall 3): pairs → `tx.pair.findMany` select { id, player1Id, player2Id } → массив { kind:"pair", pairId, player1Id, player2Id }; singles → `tx.tournamentPlayer.findMany` select { userId } → { kind:"user", userId }. Если units.length < 2 → FormatError("no_units"). (4) `circleMethodSchedule(shuffle(units))` (shuffle ОДИН раз). (5) для каждого round: `tx.round.create({ data: { tournamentId, roundNumber }, select: { id: true } })`; для каждого match — заполнить 4 FK по виду: pairs → teamA1Id=unitA.player1Id, teamA2Id=unitA.player2Id, teamB1Id=unitB.player1Id, teamB2Id=unitB.player2Id (schema.prisma:273-277 подтверждает reuse); singles 1v1 → teamA1Id=unitA.userId, teamB1Id=unitB.userId, teamA2Id/teamB2Id=null; всегда courtNumber. (6) `transitionTournament(tx as unknown as PrismaClient, tournamentId, "registration", "in_progress")`. (7) return { tournamentId, roundsCreated, matchesCreated }. Дописать в round-robin.test.ts фейк-prisma async-тесты (харнесс admin.test.ts): pairs-режим 4 пары → 3 Round + 6 RoundMatch с корректными 4 FK; singles 4 игрока → teamA2/teamB2 null; status!=="registration" → бросает FormatError("not_open"); существующий Round (count>0) → FormatError("already_generated"); transitionTournament вызван с registration→in_progress.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/round-robin.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>generateRoundRobin создаёт Round+RoundMatch в одной транзакции для pairs и singles, заполняет 4 FK по правилам Pattern 2, блокирует повторную генерацию, флипает статус через transitionTournament; новые fake-prisma тесты зелёные; tsc чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → generateRoundRobin (Plan 06) | tournamentId связывается из leaf; статус/format/participantMode перечитываются из БД внутри tx, не из клиента |
| generateRoundRobin → DB | запись Round/RoundMatch строго внутри $transaction (rollback на любом throw) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | tournamentId/format в форме старта | mitigate | format/participantMode перечитываются из БД findUniqueOrThrow внутри tx; клиентский ввод не используется (mirror bracket.ts) |
| T-09-02 | Tampering/race | конкурентный двойной старт | mitigate | generate-once через `round.count > 0` guard; @@unique([tournamentId, roundNumber]) на Round — backstop при гонке (бросок откатывает tx) |
| T-09-03 | Information Disclosure | сырой Prisma-текст ошибки клиенту | mitigate | типизированный FormatError с RU-кодами; action (Plan 06) маппит только его, иначе generic RU fallback |
| T-09-04 | Elevation of Privilege | не-админ POST старта | accept | requireAdmin() — первая строка startTournamentAction (Plan 06); этот плана-сервис auth-free by design как bracket.ts |
</threat_model>

<verification>
- `npx tsx src/lib/services/round-robin.test.ts` — все assertions зелёные.
- `npx tsc --noEmit` exit 0.
- Регрессия (проверяется на merge волны в Plan 06): существующие 8 `*.test.ts` (224 assertions) остаются зелёными; playoff bracket.ts/result.ts НЕ изменялись этим планом.
</verification>

<success_criteria>
- circleMethodSchedule — Prisma-free, экспортируемая, покрывает чётные/нечётные N с инвариантом каждый-с-каждым.
- generateRoundRobin зеркалит generateBracket: одна транзакция, generate-once, ветвление units по participantMode, корректное заполнение 4 FK, transitionTournament.
- FMT-01 удовлетворено: старт round-robin генерирует полное расписание «каждый-с-каждым».
- Ни одного нового пакета; миграций нет; playoff не тронут.
</success_criteria>

<output>
Create `.planning/phases/09-format-engines/09-01-SUMMARY.md` when done
</output>

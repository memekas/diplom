---
phase: 09-format-engines
plan: 04
type: execute
wave: 2
depends_on: ["09-03"]
files_modified:
  - src/lib/services/mexicano.ts
  - src/lib/services/mexicano.test.ts
autonomous: true
requirements: [FMT-02]
must_haves:
  truths:
    - "Старт мексикано создаёт ТОЛЬКО Round 1 (случайные четвёрки), последующие раунды не на старте"
    - "Round 1: четвёрка [p0,p1,p2,p3] → команда A=(p0,p1), B=(p2,p3); корт = индекс четвёрки"
    - "materializeNextMexicanoRound пересортировывает по кумулятивным очкам, режет на последовательные четвёрки по рейтингу"
    - "Внутри четвёрки кросс-разведение «1+4 vs 2+3» (A=(s0,s3), B=(s1,s2)) — LOCKED D1"
    - "Материализация раунда r+1 идемпотентна (materialize-once)"
  artifacts:
    - path: "src/lib/services/mexicano.ts"
      provides: "generateMexicanoRound1 + materializeNextMexicanoRound + чистые cut/cross-pair fn"
      contains: "export function crossPairQuad"
    - path: "src/lib/services/mexicano.test.ts"
      provides: "FMT-02 R1 cut + 1+4vs2+3 + детерминизм нарезки через стабильный тай-брейк"
      contains: "crossPairQuad"
  key_links:
    - from: "src/lib/services/mexicano.ts"
      to: "src/lib/services/standings.ts"
      via: "computeStandings/rankPlayers для пересортировки рейтинга"
      pattern: "computeStandings|rankPlayers"
    - from: "materializeNextMexicanoRound"
      to: "tx.round.create / tx.roundMatch.create"
      via: "prisma.$transaction"
      pattern: "roundMatch\\.create"
---

<objective>
Реализовать движок мексикано (FMT-02): `generateMexicanoRound1` (старт — только Round 1, случайные четвёрки) + `materializeNextMexicanoRound` (рейтинг-зависимая ротация: пересортировать по кумулятивным очкам → последовательные четвёрки → кросс-разведение «1+4 vs 2+3» LOCKED D1). Чистые функции нарезки/кросс-пар — Prisma-free, тестируются на детерминизм. Материализация следующего раунда вызывается из recordRoundResult (Plan 05) по gate; здесь — сама механика + materialize-once.

Purpose: мексикано — единственный формат с пораундовой материализацией (зависит от standings). FMT-02 (мексикано). Зависит от Plan 03 (computeStandings/rankPlayers — детерминированный рейтинг как вход нарезки).
Output: `src/lib/services/mexicano.ts` (новый), `src/lib/services/mexicano.test.ts` (новый).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-format-engines/09-RESEARCH.md
@.planning/phases/09-format-engines/09-CONTEXT.md
@.planning/research/FORMATS.md
@prisma/schema.prisma
@src/lib/services/bracket.ts
@src/lib/services/tournament-status.ts
@src/lib/services/standings.ts
@src/lib/services/registration.ts
@src/lib/services/result.test.ts
@src/lib/services/admin.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чистые quadCut + crossPairQuad («1+4 vs 2+3») + generateMexicanoRound1</name>
  <files>src/lib/services/mexicano.ts, src/lib/services/mexicano.test.ts</files>
  <read_first>
    .planning/research/FORMATS.md §3 (мексикано пошагово; ⚠️ КЛЮЧЕВАЯ ПОПРАВКА — оба паттерна задокументированы, проект LOCKED «1+4 vs 2+3»; §0 — старт только Round 1) и «Verified mexicano algorithm» в 09-RESEARCH.md (LOCKED D1: A=(s0,s3), B=(s1,s2)); src/lib/services/bracket.ts (shuffle + транзакционный паттерн); src/lib/services/result.test.ts (харнесс).
  </read_first>
  <behavior>
    - crossPairQuad([s0,s1,s2,s3]) (s0<s1<s2<s3 по рейтингу) → { teamA:[s0,s3], teamB:[s1,s2] } (LOCKED «1+4 vs 2+3», суммы рангов 5=5).
    - quadCut(rankedIds: string[]) → массив четвёрок ranks[4g..4g+3]; группа g = места 4g+1..4g+4 (g+1 = корт). Остаток <4 (N≢0 mod4) → 1-3 сидят, по правилу равномерного распределения byes (счётчик пропусков — или для простоты: остаток сидит этот раунд).
    - Round 1 (generateMexicanoRound1): shuffle игроков → нарезать по порядку на четвёрки [p0,p1,p2,p3] → команда A=(p0,p1), B=(p2,p3) (НЕ cross-pair — Round 1 baseline случайный); корт = индекс четвёрки.
    - Детерминизм: при одинаковом ranked-входе quadCut+crossPairQuad дают идентичный результат.
  </behavior>
  <action>
    Создать `src/lib/services/mexicano.ts`. Экспортировать `class FormatError` (локально, коды `not_open`|`wrong_format`|`already_generated`|`no_units`|`round_incomplete`). Экспортировать ЧИСТЫЕ функции: `quadCut(ids: string[]): { courtNumber: number; quad: string[] }[]` — нарезать на последовательные четвёрки (ranks[4g..4g+3]), courtNumber=g (0-based); остаток <4 НЕ образует корт (сидят). `crossPairQuad(quad: [string,string,string,string]): { teamA: [string,string]; teamB: [string,string] }` — строго LOCKED D1: teamA=[quad[0], quad[3]], teamB=[quad[1], quad[2]]. `round1Cut(ids: string[])` — нарезать на четвёрки, teamA=[q[0],q[1]], teamB=[q[2],q[3]] (baseline, без cross). Экспортировать `export async function generateMexicanoRound1(prisma, tournamentId)` (mirror generateBracket, но создаёт ТОЛЬКО Round 1): $transaction → findUniqueOrThrow { status, format }; status!=="registration"→FormatError("not_open"); format!=="mexicano"→FormatError("wrong_format"); generate-once round.count>0→FormatError("already_generated"); загрузить TournamentPlayer.userId (singles); <8→FormatError("no_units") (мексикано мин 8, FORMATS.md §3 размер); `round1Cut(shuffle(userIds))`; создать ОДИН Round (roundNumber=1) + его RoundMatch (teamA1=teamA[0], teamA2=teamA[1], teamB1=teamB[0], teamB2=teamB[1], courtNumber); transitionTournament registration→in_progress; вернуть { tournamentId, roundsCreated:1, matchesCreated }. Создать `src/lib/services/mexicano.test.ts`: crossPairQuad даёт ровно A=(s0,s3)/B=(s1,s2); quadCut на 8 ids → 2 четвёрки (корты 0,1), на 12 → 3, на 10 → 2 четвёрки + 2 сидят; round1Cut baseline A=(q0,q1)/B=(q2,q3); детерминизм quadCut+crossPairQuad на фиксированном ranked-входе; generateMexicanoRound1 fake-prisma: 8 игроков → 1 Round + 2 RoundMatch, повторный старт→already_generated, <8→no_units.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/mexicano.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>crossPairQuad реализует LOCKED «1+4 vs 2+3»; quadCut/round1Cut корректны; generateMexicanoRound1 создаёт только Round 1; тесты зелёные; tsc чист.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: materializeNextMexicanoRound (gate + re-sort + cross-pair, materialize-once)</name>
  <files>src/lib/services/mexicano.ts, src/lib/services/mexicano.test.ts</files>
  <read_first>
    .planning/research/FORMATS.md §3 (раунды 2..R: пересчёт таблицы → сортировка с детерминированным тай-брейком → последовательные четвёрки → cross-pair); 09-RESEARCH.md Pattern 5 (mexicano gate: материализовать r+1 только когда все RoundMatch текущего записаны; materialize-once через @@unique([tournamentId, roundNumber]) на Round); src/lib/services/standings.ts (computeStandings/rankPlayers — детерминированный рейтинг); prisma/schema.prisma (Round @@unique([tournamentId, roundNumber])).
  </read_first>
  <action>
    Добавить `export async function materializeNextMexicanoRound(tx: <prisma-or-tx type>, tournamentId: string, completedRoundNumber: number): Promise<{ createdRoundNumber: number } | null>` в mexicano.ts. Принимать tx (вызывается ВНУТРИ транзакции recordRoundResult, Plan 05 — НЕ открывает свою $transaction; принять PrismaClient-совместимый аргумент так же, как transitionTournament). Логика: (1) gate — посчитать RoundMatch в раунде completedRoundNumber, у которых pointsA==null||pointsB==null; если есть незаписанные → вернуть null (раунд ещё не полон, ничего не материализовать). (2) materialize-once — если Round с roundNumber=completedRoundNumber+1 уже существует → вернуть null. (3) пересчитать рейтинг: вызвать computeStandings(tx, tournamentId) (kind:"players") ИЛИ напрямую rankPlayers по агрегату — получить детерминированный ranked userId[]. (4) `quadCut(rankedIds)` → для каждой четвёрки `crossPairQuad` → создать Round (roundNumber=completedRoundNumber+1) + RoundMatch (teamA1/A2/B1/B2 + courtNumber). (5) обернуть create в try/catch P2002 (isUniqueViolation как в bracket.ts) — гонка двойной материализации ловится @@unique([tournamentId,roundNumber]) → вернуть null. Вернуть { createdRoundNumber }. ⚠️ Эта функция НЕ решает «финиш на последнем раунде» — это делает recordRoundResult (Plan 05), сравнивая completedRoundNumber+1 с totalRounds. Дописать в mexicano.test.ts fake-prisma: незаписанный матч текущего раунда → materializeNextMexicanoRound вернул null (gate); все записаны → создан Round r+1 с четвёрками по рейтингу и cross-pair «1+4vs2+3»; повторный вызов при уже существующем Round r+1 → null (materialize-once); детерминизм — при равных очках нарезка стабильна (опирается на userId-фолбэк rankPlayers из Plan 03).
  </action>
  <verify>
    <automated>npx tsx src/lib/services/mexicano.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>materializeNextMexicanoRound gate'ит по незаписанным матчам, materialize-once'ит по существующему Round r+1, пересортировывает детерминированно и создаёт следующий раунд с cross-pair LOCKED D1; принимает tx (не открывает свою транзакцию); тесты зелёные; tsc чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → generateMexicanoRound1 (Plan 06) | старт админ-only; status/format из БД |
| recordRoundResult (Plan 05, server) → materializeNextMexicanoRound | материализация r+1 server-gated, никогда не из клиента |
| mexicano → DB | Round/RoundMatch строго в транзакции (generateRound1 — своя; materialize — внешняя tx Plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Tampering | клиент пытается форсировать материализацию след. раунда раньше времени | mitigate | материализация только server-side из recordRoundResult по gate (все RoundMatch текущего записаны); нет клиентского входа |
| T-09-13 | Tampering/race | конкурентная двойная материализация r+1 | mitigate | materialize-once: проверка существующего Round r+1 + @@unique([tournamentId,roundNumber]) backstop (P2002 → null) |
| T-09-14 | (determinism) | равные очки → невоспроизводимая нарезка четвёрок | mitigate | rankPlayers (Plan 03) со стабильным userId-фолбэком; покрыто тестом детерминизма |
| T-09-15 | Elevation of Privilege | не-админ POST старта мексикано | accept | requireAdmin() первая строка action (Plan 06); сервис auth-free by design |
</threat_model>

<verification>
- `npx tsx src/lib/services/mexicano.test.ts` — все assertions зелёные.
- `npx tsc --noEmit` exit 0.
- Регрессия (на merge волны, Plan 06): 8 существующих `*.test.ts` (224 assertions) зелёные; playoff не тронут.
</verification>

<success_criteria>
- crossPairQuad реализует LOCKED D1 «1+4 vs 2+3» (A=(s0,s3), B=(s1,s2)).
- generateMexicanoRound1 создаёт только Round 1 (случайные четвёрки baseline).
- materializeNextMexicanoRound: gate по полноте раунда, materialize-once, детерминированная рейтинг-нарезка, принимает внешнюю tx.
- FMT-02 (мексикано) удовлетворено: ротация составов по рейтингу пораундово.
- Ни одного нового пакета; миграций нет; playoff не тронут.
</success_criteria>

<output>
Create `.planning/phases/09-format-engines/09-04-SUMMARY.md` when done
</output>

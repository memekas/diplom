---
phase: 09-format-engines
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified:
  - src/lib/services/round-result.ts
  - src/lib/services/round-result.test.ts
  - src/lib/validation/round-result.ts
  - src/lib/validation/round-result.test.ts
autonomous: true
requirements: [FMT-03, SCORE-01]
must_haves:
  truths:
    - "recordRoundResult ветвится по scoringMode: points = два целых; sets = sets-won через обобщённый setWinner/matchWinnerFromSets"
    - "points-режим: round_robin запрещает равные очки; americano/mexicano допускают ничью"
    - "После записи пишется PlayerMatchScore для всех участников (оба партнёра — одно командное pointsFor)"
    - "round_robin/americano авто-finished когда ВСЕ RoundMatch всех раундов записаны"
    - "mexicano: следующий раунд материализуется по gate; на totalRounds → finished"
    - "recordRoundResult ОТДЕЛЁН от playoff recordResult (нет ложного авто-финиша по nextMatchId)"
  artifacts:
    - path: "src/lib/services/round-result.ts"
      provides: "recordRoundResult + scorePointsMode/scoreSetsMode + RoundResultError"
      contains: "export async function recordRoundResult"
    - path: "src/lib/validation/round-result.ts"
      provides: "parseRoundResultForm (points: два int; sets: N сет-строк)"
      contains: "parseRoundResultForm"
  key_links:
    - from: "src/lib/services/round-result.ts"
      to: "src/lib/services/result.ts"
      via: "reuse setWinner/matchWinnerFromSets (sets-режим)"
      pattern: "setWinner|matchWinnerFromSets"
    - from: "src/lib/services/round-result.ts"
      to: "src/lib/services/mexicano.ts"
      via: "materializeNextMexicanoRound по gate"
      pattern: "materializeNextMexicanoRound"
    - from: "recordRoundResult"
      to: "PlayerMatchScore (deleteMany + create fan-out)"
      via: "prisma.$transaction"
      pattern: "playerMatchScore\\.(deleteMany|create)"
---

<objective>
Реализовать `recordRoundResult` (FMT-03/SCORE-01): транзакционная запись результата RoundMatch, ОТДЕЛЁННАЯ от playoff `recordResult` (его шаг авто-финиша по nextMatchId==null сломал бы round-based — FORMATS.md §5). Ветвление по scoringMode (points = два целых; sets = sets-won через обобщённый setWinner/matchWinnerFromSets из result.ts), fan-out PlayerMatchScore для всех участников, финиш-условие round-based + gate материализации мексикано. Плюс парсер формы `parseRoundResultForm`.

Purpose: запись результатов — ядро FMT-03 и SCORE-01; связывает standings (Plan 03) и материализацию мексикано (Plan 04). Зависит от Plan 03 (метрика вклада) и Plan 04 (materializeNextMexicanoRound).
Output: `src/lib/services/round-result.ts` (новый), `src/lib/services/round-result.test.ts` (новый), `src/lib/validation/round-result.ts` (новый), `src/lib/validation/round-result.test.ts` (новый).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-format-engines/09-RESEARCH.md
@.planning/phases/09-format-engines/09-CONTEXT.md
@.planning/research/FORMATS.md
@prisma/schema.prisma
@src/lib/services/result.ts
@src/lib/services/tournament-status.ts
@src/lib/services/standings.ts
@src/lib/services/mexicano.ts
@src/lib/validation/result.ts
@src/lib/services/result.test.ts
@src/lib/services/admin.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseRoundResultForm + scorePointsMode/scoreSetsMode (чистая валидация режимов)</name>
  <files>src/lib/validation/round-result.ts, src/lib/validation/round-result.test.ts, src/lib/services/round-result.ts</files>
  <read_first>
    src/lib/validation/result.ts (parseRecordResultForm: сканирование set{n}_a/set{n}_b, integer-coerce, пропуск пустых строк — зеркалить для sets-ветки); .planning/research/FORMATS.md §1 «⚠️ В points-режиме возможна НИЧЬЯ» (D2: запрет равных для round_robin) и §2 (americano 12:12 допустимо); 09-RESEARCH.md «Code Examples» (scoreSetsMode/scorePointsMode сигнатуры); src/lib/services/result.ts (setWinner(gamesA,gamesB,gamesPerSet)/matchWinnerFromSets(setWins,setsPerMatch) — переиспользовать, обобщены на gamesPerSet/setsPerMatch).
  </read_first>
  <behavior>
    - scorePointsMode(pointsA, pointsB, format, targetPoints?): отрицательные → reject "invalid_points"; round_robin && pointsA===pointsB → reject "draw_not_allowed" (D2); americano/mexicano равные → допустимо (winner=null = ничья); targetPoints задан && сумма!=target → reject "bad_sum" (опционально-advisory, см. discretion); возвращает { pointsA, pointsB, winner: "A"|"B"|null }.
    - scoreSetsMode(sets[], gamesPerSet, setsPerMatch): каждый сет через setWinner (бросает invalid_set), tally setsWonA/B, matchWinnerFromSets → null reject "no_winner"; возвращает { pointsA: setsWonA, pointsB: setsWonB, winner } (pointsA/B = sets-won).
    - parseRoundResultForm(formData, { scoringMode, setsPerMatch }): points → читать поля points_a/points_b как неотриц. целые; sets → сканировать set{n}_a/set{n}_b как parseRecordResultForm; вернуть размеченный объект для recordRoundResult.
  </behavior>
  <action>
    Создать `src/lib/services/round-result.ts` с `export class RoundResultError extends Error` (code union: `invalid_points`|`draw_not_allowed`|`bad_sum`|`invalid_set`|`no_winner`|`not_round_based`|`already_finished`|`match_not_found`). Экспортировать ЧИСТЫЕ `scorePointsMode(pointsA, pointsB, format, targetPoints?)` и `scoreSetsMode(sets: { gamesPair1: number; gamesPair2: number }[], gamesPerSet: number, setsPerMatch: number)` строго по 09-RESEARCH.md Code Examples — scoreSetsMode переиспользует `setWinner`/`matchWinnerFromSets` из `./result` (импорт), коллапсируя к двум целым sets-won; пробросить ResultError("invalid_set") как RoundResultError("invalid_set"). Создать `src/lib/validation/round-result.ts` с `parseRoundResultForm(formData, { scoringMode, setsPerMatch })`: points-ветка — `points_a`/`points_b` через z.coerce.number().int().min(0); sets-ветка — зеркало parseRecordResultForm (set{n}_a/set{n}_b, пропуск пустых). Создать `src/lib/validation/round-result.test.ts` (харнесс): points валидный 15:9→winner A; round_robin 12:12→ошибка draw_not_allowed; americano 12:12→winner null (допустимо); отрицательные→invalid_points; sets 6:4,6:3→pointsA=2 winner A; невалидный сет 6:5→invalid_set; недостаточно сетов→no_winner. (Тесты на scorePointsMode/scoreSetsMode держать в round-result.test.ts Task 2 либо здесь — допустимо здесь, т.к. чистые функции.)
  </action>
  <verify>
    <automated>npx tsx src/lib/validation/round-result.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>scorePointsMode/scoreSetsMode реализуют SCORE-01 ветвление (points/sets) с D2-запретом ничьи в round_robin и допуском ничьи в americano/mexicano, переиспользуя setWinner/matchWinnerFromSets; parseRoundResultForm разбирает обе формы; валидационные тесты зелёные; tsc чист.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recordRoundResult (transaction: запись + PlayerMatchScore fan-out + финиш/материализация gate)</name>
  <files>src/lib/services/round-result.ts, src/lib/services/round-result.test.ts</files>
  <read_first>
    src/lib/services/result.ts (recordResult — образец транзакции: load match+config, delete+recreate дочерних строк (SetScore → здесь PlayerMatchScore), update, финиш-условие; ⚠️ шаг 9 nextMatchId==null НЕ переносить); .planning/research/FORMATS.md §5 (почему recordResult нельзя переиспользовать; мексикано gating) + 09-RESEARCH.md Pattern 3/4/5; prisma/schema.prisma (RoundMatch teamA1/A2/B1/B2 + pointsA/pointsB; PlayerMatchScore @@unique([roundMatchId,userId]) → delete+recreate; Round @@unique([tournamentId,roundNumber])); src/lib/services/mexicano.ts (materializeNextMexicanoRound — принимает tx); src/lib/services/tournament-status.ts (transitionTournament in_progress→finished); src/lib/services/admin.test.ts (fake-prisma harness).
  </read_first>
  <action>
    Добавить `export async function recordRoundResult(prisma: PrismaClient, roundMatchId: string, input: { pointsA: number; pointsB: number } | { sets: { gamesPair1: number; gamesPair2: number }[] }): Promise<{ roundMatchId: string; pointsA: number; pointsB: number; winner: "A"|"B"|null; finished: boolean; nextRoundCreated: boolean }>`. В `prisma.$transaction(async (tx) => {...})`: (1) загрузить RoundMatch с round+tournament: select { id, courtNumber, teamA1Id, teamA2Id, teamB1Id, teamB2Id, round: { select: { id, roundNumber, tournamentId } }, ...} и отдельно tournament select { format, scoringMode, gamesPerSet, setsPerMatch, targetPoints, totalRounds, status }. Если format=="playoff" → RoundResultError("not_round_based") (защита — playoff идёт через recordResult). (2) вычислить { pointsA, pointsB, winner } по scoringMode: points → scorePointsMode(input.pointsA, input.pointsB, format, targetPoints); sets → scoreSetsMode(input.sets, gamesPerSet, setsPerMatch). (3) `tx.roundMatch.update({ where:{id}, data:{ pointsA, pointsB } })`. (4) PlayerMatchScore fan-out (Pattern 4 + Pitfall 6): `tx.playerMatchScore.deleteMany({ where:{ roundMatchId } })` затем create по одной строке на каждый НЕ-null FK команды: A-сторона (teamA1Id, teamA2Id) → pointsFor=pointsA, pointsAgainst=pointsB, teamSlot="A"; B-сторона → зеркало. (5) ФИНИШ/МАТЕРИАЛИЗАЦИЯ gate по format (Pattern 5): — round_robin/americano: посчитать все RoundMatch всех раундов турнира с pointsA==null||pointsB==null; если 0 незаписанных → transitionTournament(tx, tournamentId, "in_progress", "finished") (с guard на уже-"finished" как в result.ts шаг 9, без nextMatchId-логики), finished=true. — mexicano: вызвать `materializeNextMexicanoRound(tx, tournamentId, round.roundNumber)` → если вернул объект, nextRoundCreated=true; если round.roundNumber>=totalRounds И все RoundMatch текущего раунда записаны → transitionTournament→finished, finished=true. (6) вернуть сводку. ⚠️ НЕ трогать nextMatchId/advancement (round-based их не имеет). Дописать `src/lib/services/round-result.test.ts` (harness admin.test.ts) с fake-prisma, реализующим roundMatch/playerMatchScore/round/tournament методы и $transaction:(fn)=>fn(prisma): points round_robin запись 15:9 → pointsA/B на матче + 0-4 PlayerMatchScore (оба партнёра одной команды одно pointsFor); points round_robin 12:12 → бросает draw_not_allowed; americano 12:12 → winner null, записано; sets-режим 6:4,6:3 → pointsA=2 (sets-won); re-record → deleteMany вызван перед create (нет дублей по @@unique); round_robin все матчи записаны → transitionTournament in_progress→finished; americano частично записан → НЕ finished; mexicano раунд полон && roundNumber<totalRounds → materializeNextMexicanoRound вызван (nextRoundCreated); mexicano roundNumber==totalRounds && полон → finished; format=="playoff" → not_round_based.
  </action>
  <verify>
    <automated>npx tsx src/lib/services/round-result.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>recordRoundResult записывает RoundMatch + PlayerMatchScore fan-out, ветвится по scoringMode, авто-финиширует round_robin/americano по полноте всех матчей, gate'ит/материализует мексикано и финиширует на totalRounds, отделён от playoff (not_round_based для playoff, без nextMatchId-авто-финиша); fake-prisma тесты зелёные; tsc чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action recordResultAction (Plan 06) → recordRoundResult | админ-only; roundMatchId связан из leaf; format/scoringMode/config перечитываются из БД внутри tx |
| recordRoundResult → DB | RoundMatch/PlayerMatchScore/материализация — строго в одной $transaction (rollback на любом throw) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Tampering | подмена pointsA/pointsB/winner с клиента | mitigate | winner/sets-won вычисляются server-side (scorePointsMode/scoreSetsMode) из валидированного ввода; winner НЕ принимается от клиента (mirror recordResult T-05-01) |
| T-09-17 | Tampering | tampered roundMatchId/format чтобы записать в чужой матч или сломать round-based | mitigate | roundMatchId связан из leaf (Plan 06); format/scoringMode перечитываются из БД внутри tx; playoff → not_round_based (нельзя записать round-based в playoff-матч) |
| T-09-18 | Input Validation | невалидный счёт (отрицательный, нелегальный сет, ничья в RR) | mitigate | zod в parseRoundResultForm + server-re-validate в scorePointsMode/scoreSetsMode (setWinner) — DB-authoritative, mirror T-05-06 |
| T-09-19 | Information Disclosure | сырой Prisma-текст клиенту | mitigate | типизированный RoundResultError с RU-кодами; Plan 06 action маппит только его, иначе generic RU fallback |
| T-09-20 | Tampering/race | двойная запись → дубли PlayerMatchScore / двойная материализация | mitigate | deleteMany перед create (Pitfall 6); materialize-once в materializeNextMexicanoRound (Plan 04) |
| T-09-21 | Elevation of Privilege | не-админ POST записи | accept | requireAdmin() первая строка recordResultAction (Plan 06); сервис auth-free by design как result.ts |
</threat_model>

<verification>
- `npx tsx src/lib/services/round-result.test.ts` и `npx tsx src/lib/validation/round-result.test.ts` — все assertions зелёные.
- `npx tsc --noEmit` exit 0.
- Регрессия (на merge волны, Plan 06): 8 существующих `*.test.ts` (224 assertions) зелёные; playoff result.ts НЕ изменён (setWinner/matchWinnerFromSets только импортируются).
</verification>

<success_criteria>
- recordRoundResult ветвится по scoringMode (SCORE-01): points (два целых, D2-запрет ничьи RR) + sets (sets-won через обобщённый setWinner/matchWinnerFromSets).
- PlayerMatchScore fan-out: оба партнёра команды получают одно командное pointsFor.
- Финиш-условие: round_robin/americano авто-finished по полноте; mexicano по gate+totalRounds.
- Отделён от playoff recordResult — без ложного авто-финиша (FMT-03 «playoff без регрессии»).
- Ни одного нового пакета; миграций нет; playoff result.ts/bracket.ts не изменены.
</success_criteria>

<output>
Create `.planning/phases/09-format-engines/09-05-SUMMARY.md` when done
</output>

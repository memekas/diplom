---
phase: 02-tournaments-status-machine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(app)/admin/tournaments/new/page.tsx
  - src/app/(app)/admin/tournaments/new/create-tournament-form.tsx
  - src/app/(app)/admin/tournaments/actions.ts
  - src/components/nav.tsx
autonomous: true
requirements: [TOUR-01]
must_haves:
  truths:
    - "Admin opens /admin/tournaments/new and creates a tournament (name, size 4/8/16, optional date/location)"
    - "The created tournament persists with status 'registration'"
    - "A non-admin or anonymous direct call to the create action is rejected server-side (Forbidden)"
    - "The 'Create tournament' link is shown to admins only and the create page redirects non-admins"
  artifacts:
    - path: "src/app/(app)/admin/tournaments/actions.ts"
      provides: "createTournamentAction — requireAdmin first, zod parse, createTournament, revalidate, redirect"
      exports: ["createTournamentAction"]
    - path: "src/app/(app)/admin/tournaments/new/page.tsx"
      provides: "Admin-guarded create page (requireAdmin → redirect non-admin)"
    - path: "src/app/(app)/admin/tournaments/new/create-tournament-form.tsx"
      provides: "Client form leaf (name, size select 4/8/16, optional date/location) wired to the action"
    - path: "src/components/nav.tsx"
      provides: "Admin-only 'Create tournament' nav link"
      contains: "tournaments/new"
  key_links:
    - from: "src/app/(app)/admin/tournaments/actions.ts"
      to: "requireAdmin"
      via: "first line of the action (AUTH-05/Pitfall 8 boundary)"
      pattern: "requireAdmin\\(\\)"
    - from: "src/app/(app)/admin/tournaments/actions.ts"
      to: "createTournament service"
      via: "parsed form → service call → revalidatePath"
      pattern: "createTournament\\("
---

<objective>
Admin create vertical slice (TOUR-01): an admin opens `/admin/tournaments/new`, fills name + size (4/8/16) + optional date/location, submits, and the tournament is persisted at status `registration`. The Server Action's first line is `requireAdmin()` (the real boundary — a non-admin direct POST is rejected even though the nav link is hidden), then it validates with the Plan-01 zod schema and calls the Plan-01 `createTournament` service, revalidates, and redirects to the new tournament's public page.

Purpose: This is the producer side of the phase — the only write path. It proves AUTH-05's pattern carries into the tournament domain and that status is initialized server-side, not from the client.

Output: guarded create action, guarded create page + client form, admin-only nav link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-tournaments-status-machine/02-CONTEXT.md
@.planning/phases/02-tournaments-status-machine/02-01-SUMMARY.md

# Patterns to follow EXACTLY (Phase 1 templates)
@src/app/(app)/profile/actions.ts
@src/app/(app)/profile/profile-form.tsx
@src/app/(app)/admin/page.tsx
@src/app/(app)/admin/actions.ts
@src/components/nav.tsx
@src/lib/auth-guards.ts
@src/lib/db.ts

# Interfaces this plan consumes (from Plan 01)
@src/lib/validation/tournament.ts
@src/lib/services/tournament.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guarded createTournamentAction</name>
  <files>src/app/(app)/admin/tournaments/actions.ts</files>
  <read_first>
    - src/app/(app)/profile/actions.ts ("use server"; first-line guard; parseXForm; service call; revalidatePath; discriminated return state)
    - src/app/(app)/admin/actions.ts (requireAdmin as the first line — the AUTH-05 proof pattern)
    - src/lib/validation/tournament.ts (parseTournamentForm, createTournamentSchema from Plan 01)
    - src/lib/services/tournament.ts (createTournament from Plan 01)
    - .planning/research/PITFALLS.md Pitfall 8 (admin authz server-side, first line) + Pitfall 10 (revalidatePath after write)
  </read_first>
  <action>
    Create `src/app/(app)/admin/tournaments/actions.ts` with `"use server"`. Define `createTournamentAction(prev, formData)` mirroring `updateProfileAction`'s shape: FIRST line `await requireAdmin()` (per CONTEXT D — create + all transitions are admin-only; this is the server boundary, not UI hiding). Then `parseTournamentForm(formData)`; on `!ok` return `{ ok: false, errors }`. On ok, call `createTournament(prisma, parsed.data)`, then `revalidatePath("/tournaments")` (and `/admin/tournaments/new` if needed) so the public list reflects the new row (Pitfall 10), then `redirect(\`/tournaments/${created.id}\`)`. Export a `CreateTournamentActionState` type matching the discriminated `{ ok: true } | { ok: false; errors } | null` shape used by profile. Status is never read from the form — `createTournament` hard-sets "registration" (Plan 01). Do NOT catch the requireAdmin throw here — the action may throw Forbidden to the caller, and that IS the rejection (the page guard handles the UX redirect).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - First executable line is `await requireAdmin()` before any parse/DB work.
    - On valid admin submit: createTournament called, revalidatePath("/tournaments") fired, redirect to the new tournament page.
    - On invalid input: returns `{ ok: false, errors }` without a DB write.
    - tsc clean.
  </acceptance_criteria>
  <done>createTournamentAction exists, guarded first, validates, creates, revalidates, redirects.</done>
</task>

<task type="auto">
  <name>Task 2: Guarded create page + client form + admin nav link</name>
  <files>src/app/(app)/admin/tournaments/new/page.tsx, src/app/(app)/admin/tournaments/new/create-tournament-form.tsx, src/components/nav.tsx</files>
  <read_first>
    - src/app/(app)/admin/page.tsx (requireAdmin in an RSC → try/catch → redirect("/login") on Unauthorized/Forbidden, rethrow else)
    - src/app/(app)/profile/profile-form.tsx ("use client"; useActionState; select with mapped options; pending state; error rendering)
    - src/components/nav.tsx (Server Component reading getOptionalSession; conditional links by auth state — add an admin-only link)
    - src/lib/validation/tournament.ts (tournamentSizes for the size select)
  </read_first>
  <action>
    Create `src/app/(app)/admin/tournaments/new/page.tsx` as a Server Component guarded exactly like `admin/page.tsx`: try `await requireAdmin()`, catch → on "Unauthorized"/"Forbidden" `redirect("/login")`, else rethrow. Render a heading + the create form. Create `src/app/(app)/admin/tournaments/new/create-tournament-form.tsx` as a `"use client"` leaf following `profile-form.tsx`: `useActionState` over `createTournamentAction`; fields — `name` (text, required), `size` (select mapping `tournamentSizes` → options like "4 пары"/"8 пар"/"16 пар"), `date` (input type="datetime-local", optional), `location` (text, optional); a submit button with pending state; render per-field errors from the action state. Minimal functional Tailwind matching profile-form's classes (backend-focused — no heavy design). Form field labels in Russian are fine but not required (CONTEXT mandates RU only for STATUS labels, which live in Plan 03). Then edit `src/components/nav.tsx`: when `session?.user?.role === "admin"`, render an extra `<Link href="/admin/tournaments/new">` ("Создать турнир"). The link is purely cosmetic — the page + action guards are the real boundary (Pitfall 8). Also add a public `<Link href="/tournaments">` ("Турниры") visible to everyone so the public list (Plan 03) is reachable.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/tournaments/new` renders for an admin and redirects a non-admin/anon to /login (guard is server-side).
    - Form posts name + size (4/8/16) + optional date/location to createTournamentAction.
    - Nav shows "Создать турнир" only when role === "admin"; shows "Турниры" link for everyone.
    - tsc + next build succeed (if the known 01-01 kysely build issue resurfaces, dev-run is the documented target — note it, do not block).
  </acceptance_criteria>
  <done>Guarded create page + client form render; admin-only create link + public tournaments link in nav.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → createTournamentAction | untrusted form input + a potentially non-admin/anon caller crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Elevation of Privilege | createTournamentAction | mitigate | `requireAdmin()` is the FIRST line; non-admin/anon direct invocation throws Forbidden before any DB work (AUTH-05, Pitfall 8) |
| T-02-05 | Tampering | size / status form fields | mitigate | size validated to {4,8,16} via Plan-01 zod; status never read from form — service hard-sets "registration" |
| T-02-06 | Spoofing | role from client | mitigate | role read from the signed session via requireAdmin, never from form/props (Pitfall 8) |
| T-02-SC | Tampering | npm installs | accept | no new packages; reuses Phase-1 next/react/zod; no install task |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx next build` succeeds (or, if the documented 01-01 kysely build blocker recurs, confirm the dev-run path works — that is the thesis run target).
- Manual/automatable: a non-admin session calling `createTournamentAction` directly is rejected (Forbidden) — same proof shape as the Phase-1 `adminPing` guard test.
- After an admin create, the tournament exists at status "registration" (verifiable via sqlite3 or the Plan-03 list).
</verification>

<success_criteria>
- Admin creates a tournament via the guarded page/form; it persists at status "registration" (TOUR-01).
- Non-admin/anon is rejected server-side at the action and redirected at the page.
- Nav exposes the create link to admins only and a public tournaments link to all.
</success_criteria>

<output>
Create `.planning/phases/02-tournaments-status-machine/02-02-SUMMARY.md` when done.
</output>

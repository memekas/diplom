---
phase: 05-results-advancement
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/lib/validation/result.ts
  - src/app/(public)/tournaments/[id]/actions.ts
  - src/app/(public)/tournaments/[id]/score-form.tsx
  - src/components/bracket-view.tsx
  - src/app/(public)/tournaments/[id]/page.tsx
  - src/lib/services/bracket.ts
autonomous: false
requirements: [MATCH-01, MATCH-02, MATCH-03, MATCH-04, MATCH-05]
must_haves:
  truths:
    - "An admin viewing an in-progress tournament sees a per-set score-entry form on matches that have both pairs, can save a score, and sees the winner advance + the score appear in the bracket"
    - "An admin re-opens a recorded match, edits the score, saves, and the bracket reflects the new winner immediately"
    - "Any visitor sees each completed match's per-set score and a highlighted winner; when the tournament is finished the champion is shown"
    - "A non-admin or anonymous direct POST to the result action is rejected server-side before any DB work"
  artifacts:
    - path: "src/lib/validation/result.ts"
      provides: "parseRecordResultForm — parses N per-set games-pair inputs from FormData into sets[]"
      exports: ["parseRecordResultForm"]
    - path: "src/app/(public)/tournaments/[id]/actions.ts"
      provides: "recordResultAction (requireAdmin → recordResult → revalidatePath)"
      contains: "requireAdmin"
    - path: "src/app/(public)/tournaments/[id]/score-form.tsx"
      provides: "Admin-only \"use client\" per-set score-entry leaf"
      min_lines: 30
    - path: "src/components/bracket-view.tsx"
      provides: "Per-set scores + winner highlight + champion display in the bracket"
  key_links:
    - from: "src/app/(public)/tournaments/[id]/actions.ts (recordResultAction)"
      to: "src/lib/services/result.ts (recordResult)"
      via: "guarded server action calls the transactional service then revalidatePath"
      pattern: "recordResult\\("
    - from: "src/app/(public)/tournaments/[id]/score-form.tsx"
      to: "recordResultAction"
      via: "useActionState(recordResultAction.bind(null, ...))"
      pattern: "recordResultAction"
    - from: "src/app/(public)/tournaments/[id]/page.tsx"
      to: "src/components/bracket-view.tsx"
      via: "passes set scores + admin score-form into BracketView"
      pattern: "BracketView"
---

<objective>
Expose the scoring engine to users — the Phase 5 Core Value slice. Add a guarded `recordResultAction` (first line `requireAdmin`) that calls `recordResult` then `revalidatePath`; an admin-only `"use client"` per-set score-entry leaf rendered on matches with both pairs filled (existing score pre-filled + editable); and extend `BracketView` to show each match's per-set score, highlight the winner (lighting up the inert hook from Phase 4), and display the champion when the tournament is finished. Verified on a production build (the bracket must update immediately after a result — Pitfall 10).

Purpose: Completes MATCH-01 (admin enters per-set games, validated), MATCH-02 (winner advances), MATCH-03 (final → finished + champion shown), MATCH-04 (free edit re-renders immediately), MATCH-05 (per-set scores visible). Mirrors the Phase 4 action + client-leaf + public-RSC split (participateAction / startTournamentAction). This is the final slice of milestone v1.0.
Output: `parseRecordResultForm` validation; `recordResultAction`; `score-form.tsx`; extended `bracket-view.tsx` + `listBracket` + `page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-results-advancement/05-CONTEXT.md

# The service this action wraps + ResultError codes to map.
@src/lib/services/result.ts
# Mirror these EXACTLY: action boundary (requireAdmin first line, typed-error mapping, revalidatePath, bound tournamentId), client-leaf (useActionState + bound id), public RSC (isAdmin from optional session), bracket read path + winner-highlight hook.
@src/app/(public)/tournaments/[id]/actions.ts
@src/app/(public)/tournaments/[id]/participate-form.tsx
@src/app/(public)/tournaments/[id]/start-tournament-form.tsx
@src/app/(public)/tournaments/[id]/page.tsx
@src/components/bracket-view.tsx
@src/lib/services/bracket.ts
# FormData parsing convention (discriminated {ok,data|errors}).
@src/lib/validation/registration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: parseRecordResultForm + guarded recordResultAction</name>
  <files>src/lib/validation/result.ts, src/app/(public)/tournaments/[id]/actions.ts</files>
  <read_first>src/lib/validation/registration.ts (parse → discriminated {ok,data|errors}); src/app/(public)/tournaments/[id]/actions.ts (startTournamentAction: requireAdmin first line + typed-error mapping + bound id + revalidatePath, NO redirect); src/lib/services/result.ts (recordResult signature + ResultError codes)</read_first>
  <action>
    Per D (Access & UI: ввод/правка результата — admin-only, Server Action первой строкой requireAdmin; revalidatePath после записи).
    Create `src/lib/validation/result.ts` exporting `parseRecordResultForm(formData)` → discriminated `{ ok: true, data: { sets: { gamesPair1: number; gamesPair2: number }[] } } | { ok: false, errors: ... }` (mirror parseRegisterPairForm). The form posts up to `setsPerMatch` rows named like `set{n}_a` / `set{n}_b` (or an array convention you pick — document it); coerce each present, non-empty pair of inputs to integers via zod, skip fully-empty rows (a match may end in 2 sets — empty/extra rows ignored, per Claude's Discretion in CONTEXT), reject negatives/non-integers at the zod layer. Do NOT re-implement set/match validity here — that is recordResult/setWinner's job; this parser only shapes + integer-coerces input.
    Add `recordResultAction(tournamentId, matchId, _prev, formData)` to the existing `actions.ts` (append; do not rewrite participate/start actions). FIRST line `await requireAdmin();` — the security boundary (T-05-04 / Pitfall 8 / AUTH-05): a non-admin/anon direct POST throws "Forbidden" before any parse or DB work; that throw is intentionally NOT caught. Bind both tournamentId AND matchId from the leaf (never trust them from the form body). Parse via parseRecordResultForm; on parse failure return `{ ok:false, error }`. Call `recordResult(prisma, matchId, parsed.data.sets)` in try/catch; map `ResultError` codes (invalid_set / slots_unfilled / no_winner / empty) to `e.message` (RU), and any other throw to a generic RU fallback (never forward raw Prisma text — WR-02). On success `revalidatePath(`/tournaments/${tournamentId}`)` (Pitfall 10) and return `{ ok:true }`. No redirect — stay on the page. Export a `RecordResultActionState` type matching the participate/start pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -nE "await requireAdmin\\(\\)" src/app/(public)/tournaments/[id]/actions.ts && grep -nE "recordResult\\(|revalidatePath\\(" src/app/(public)/tournaments/[id]/actions.ts</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; recordResultAction exists with `requireAdmin()` as its literal first statement, binds tournamentId+matchId (not from form), maps ResultError to a friendly RU message, calls recordResult then revalidatePath, returns the discriminated state; parseRecordResultForm coerces sets to integers and skips empty rows; participate/start actions unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Admin score-entry leaf + BracketView scores/winner/champion + page wiring</name>
  <files>src/app/(public)/tournaments/[id]/score-form.tsx, src/components/bracket-view.tsx, src/app/(public)/tournaments/[id]/page.tsx, src/lib/services/bracket.ts</files>
  <read_first>src/app/(public)/tournaments/[id]/participate-form.tsx + start-tournament-form.tsx (the "use client" + useActionState + bound-id leaf shape, error banner, disabled-while-pending); src/components/bracket-view.tsx (BracketMatch shape + the existing winner-highlight Slot hook, currently inert); src/lib/services/bracket.ts (listBracket select shape to extend); src/app/(public)/tournaments/[id]/page.tsx (isAdmin from getOptionalSession; the Сетка section gating)</read_first>
  <action>
    Per D (UI: для матча с обоими заполненными слотами админ видит форму ввода счёта по сетам, setsPerMatch строк, gamesPair1/gamesPair2; существующий счёт показывается и доступен к правке; все видят итоговый счёт + победителя; чемпион при finished; revalidatePath проверить на prod-сборке). Keep it minimal functional Tailwind, RU, no new deps.

    1. Extend `listBracket` (bracket.ts) to also select each match's `setScores` (orderBy setNumber: gamesPair1, gamesPair2, setNumber) and `setsWonA`/`setsWonB`; map into the returned shape as e.g. `sets: { gamesPair1, gamesPair2 }[]`. Keep the existing safe name-only select for pairs (no PII added). Also surface the tournament `setsPerMatch` to the page (read it where the page already loads the tournament) so the form knows the row count — do not over-fetch.
    2. Extend `BracketMatch` type + `BracketView` (bracket-view.tsx) to render, under each match, the per-set score (e.g. `6:4 6:3` or set rows) when sets exist, and light up the winner Slot via the EXISTING `pairId === winnerId` hook (no markup restructure — winnerId now arrives non-null). Add a champion display: when the final match has a winnerId, show the champion pair name prominently (BracketView can derive the final as the match with the highest round / no parent in the passed data, or the page can pass a `champion` prop — pick the simpler; the page already knows tournament.status === "finished").
    3. Create `score-form.tsx` — a `"use client"` leaf (NEVER imports prisma): `useActionState(recordResultAction.bind(null, tournamentId, matchId), null)`. Renders up to `setsPerMatch` rows, each with two number inputs (gamesPair1 / gamesPair2) named per the parser convention from Task 1, pre-filled from the match's existing `sets` (editable — MATCH-04), a «Сохранить счёт» submit (disabled while pending), and the red error banner (reuse participate-form's class) showing `state.error`. Label the two sides with the pair names.
    4. Wire into `page.tsx`: the page already computes `isAdmin`. For an `in_progress` tournament, render the admin score-form for each match where both pair slots are filled (pass tournamentId, matchId, the two pair names, setsPerMatch, and existing sets). Simplest placement is fine (a per-match admin section beside/under the bracket, or inline) — Claude's Discretion per CONTEXT. The public BracketView (scores + winner highlight + champion) renders for everyone whenever matches exist, as today. Do NOT guard the page (anon still views the bracket).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; `npm run build` succeeds; BracketView renders per-set scores + a highlighted winner when winnerId is set and the champion when the final is decided; score-form is a "use client" leaf binding tournamentId+matchId into recordResultAction, pre-fills existing sets, shows ≤ setsPerMatch rows, and surfaces RU errors; the admin score-form appears only for matches with both pairs filled on an in_progress tournament; page still renders for anonymous visitors; no prisma import in score-form.tsx; participate/start forms unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full result-entry slice on a production build: admin enters/edits per-set scores, the winner advances and the bracket updates immediately (revalidatePath — Pitfall 10), the final decides the champion + finishes the tournament, and anonymous visitors see scores + winner + champion.</what-built>
  <how-to-verify>
    1. `npm run build && npm start` (production build — Pitfall 10 says caching bugs only show on `next start`, not `next dev`).
    2. As the seeded admin, open a tournament that is `in_progress` with at least one round-1 match having both pairs. Enter a valid set score (e.g. 6:4, 6:3) on a round-1 match and save. EXPECT: the page updates without a manual hard-refresh — that match shows 6:4 6:3 with the winner highlighted, and the winner now appears in the next-round match's correct slot (A/B).
    3. Try an invalid score (e.g. 6:5 with no tiebreak, or a single 6:4 set for a best-of-3) → EXPECT a RU validation error, no advancement.
    4. Edit the saved match: change the score so the OTHER pair wins, save → EXPECT the bracket immediately reflects the new winner and the next-round slot now shows the new winner (MATCH-04 free edit, immediate via revalidatePath).
    5. Record results down to the final; record the final → EXPECT the tournament badge flips to «завершён»/finished and the champion is displayed.
    6. In a logged-out browser (or incognito), open the same tournament → EXPECT to see the per-set scores, highlighted winners, and the champion (read-only, no score form).
    7. (Authz spot-check, optional) Confirm the action rejects a non-admin — `requireAdmin()` is the action's first line; a non-admin POST throws "Forbidden" before any DB work.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered/advanced incorrectly (wrong slot, stale bracket, missing champion, validation gap).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → recordResultAction | Untrusted FormData (per-set games) + a client-invoked Server Action crosses here; matchId/tournamentId are bound server-side, role is derived from the session cookie only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Elevation of Privilege | recordResultAction | mitigate | `await requireAdmin()` is the action's literal first line — a non-admin/anon direct POST throws "Forbidden" before any parse/DB work (AUTH-05, Pitfall 8); the throw is NOT caught |
| T-05-05 | Tampering | matchId / tournamentId | mitigate | both bound via `.bind(null, tournamentId, matchId)` from the server-rendered leaf, never read from the form body — a tampered form cannot redirect the write to another match/tournament |
| T-05-06 | Tampering | per-set games input | mitigate | parseRecordResultForm zod-coerces to non-negative integers; recordResult/setWinner reject any invalid set server-side regardless of client UI (T-05-02) |
| T-05-07 | Information Disclosure | reject error messages | mitigate | only typed ResultError RU messages are surfaced; any other throw maps to a generic RU fallback — no raw Prisma/internal text reaches the client (WR-02) |
| T-05-08 | Information Disclosure | bracket read path | accept | listBracket selects player display names only (no PII), unchanged from Phase 4's safe select; scores are non-sensitive |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dependencies (constraint: no new deps); nothing to install, no package-legitimacy gate required |
</threat_model>

<verification>
- `npx tsc --noEmit` → clean.
- `npm run build` → success.
- `grep -nE "await requireAdmin\\(\\)" src/app/(public)/tournaments/[id]/actions.ts` → recordResultAction's first line.
- `grep -nE "\"use client\"" src/app/(public)/tournaments/[id]/score-form.tsx` and `grep -L "@/lib/db\|@prisma/client" src/app/(public)/tournaments/[id]/score-form.tsx` → leaf has no prisma import.
- Production-build behavior (the human-verify checkpoint): bracket updates immediately after a result + after an edit on `next start`; champion shows on finish; anon sees scores+winner+champion.
</verification>

<success_criteria>
- Admin enters per-set games on a both-pairs match; each set validated (server-side); winner derived (MATCH-01).
- Winner advances into the next match's correct slot in one transaction; visible immediately (MATCH-02).
- Final result finishes the tournament and shows the champion (MATCH-03).
- Admin edits a recorded result; bracket reflects the new winner immediately via revalidatePath, verified on a prod build (MATCH-04).
- Everyone sees structured per-set scores + highlighted winner; counts driven by Tournament.setsPerMatch (MATCH-05).
- Non-admin/anon result POST rejected server-side (AUTH-05).
</success_criteria>

<output>
Create `.planning/phases/05-results-advancement/05-03-SUMMARY.md` when done (action + leaf + BracketView changes, the prod-build verification result, commits). This closes milestone v1.0.
</output>
